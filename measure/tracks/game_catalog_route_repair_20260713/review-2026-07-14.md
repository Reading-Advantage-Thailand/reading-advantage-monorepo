# Review: Game Catalog Route Repair — 2026-07-14

**Commit reviewed:** `d1e9d034` — `fix(games): restore retained catalog routes (track_id: game_catalog_route_repair_20260713)`

**Note on paths:** The track does not live at the monorepo-root `measure/tracks/` path given in the review request. The spec, plan, and metadata were committed under `apps/advantage-games/measure/tracks/game_catalog_route_repair_20260713/`. This report is written to the requested root path; consider relocating it next to the track.

## 1. Summary of changes

- `apps/advantage-games/src/lib/gameCards.ts`: renamed `withdrawnApkGameIds` → `unroutableGameIds`, removed `dragon-flight`, `dungeon-liberator`, `magic-defense` from the withheld set, and added explicit `href`s for those three cards (`/student/games/vocabulary/dragon-flight`, `/student/games/sentence/dungeon-liberator`, `/student/games/vocabulary/magic-defense`). The coming-soon transform now only applies to titles with no implemented page route.
- `apps/advantage-games/src/lib/gameCards.test.ts`: replaced the "withholds 14 APK cartridges" test with a positive test asserting the four retained routes (incl. `labyrinth-goblin-king`) are exposed as `playable` with correct hrefs.
- Track scaffolding added: `spec.md`, `plan.md`, `metadata.json`, and a `[~] In Progress` entry in `apps/advantage-games/measure/tracks.md`.

## 2. Spec alignment

- **FR-1 (playable cards link to implemented routes): Met.** All 16 hrefs in `gameCards.ts` map 1:1 to the 16 `page.tsx` files under `src/app/[locale]/(student)/student/games/{vocabulary,sentence}/`.
- **FR-2 (locale-aware route tree): Met.** Hrefs are locale-agnostic (`/student/games/...`); existing test guards against `/en/` prefixes.
- **FR-3 (no stale withdrawal misclassification): Met.** The three retained games are no longer forced to `coming-soon`.
- **AC-1 (test verifies every playable link): Partial.** The new test checks only 4 specific cards; the "positive control" test checks href presence, not route existence. Route resolution was verified manually (dev-server HTTP 200 per plan), not in CI.
- **AC-2 (Labyrinth of the Goblin King playable): Met.**
- **AC-3 (tests + type checking pass): Met.** Re-ran `CI=true pnpm --filter vocabulary-games test -- src/lib/gameCards.test.ts --runInBand` — 3/3 pass. Plan records `check-types` passing; not re-run in this review.

## 3. Code quality observations

- Minimal, well-scoped diff; the rename from `withdrawnApkGameIds` to `unroutableGameIds` accurately reflects the new semantics, and the stale comment was updated.
- Test rewrite loses regression coverage for the 11 still-withheld titles — nothing now asserts they remain `coming-soon` with no href.
- The route-existence guarantee (the actual bug class: catalog href → 404) is not encoded in any automated test; a future route rename would silently reintroduce 404s.
- Pre-existing jest warning: duplicate manual mock `next/server` (src vs `.next/standalone` copy) — unrelated to this change but worth cleaning up.

## 4. Risks or blockers

- No blockers. Change is low-risk, data-only, and verified.
- Residual risk: catalog/route drift can recur undetected until a test asserts every playable href resolves to a real route file.

## 5. Recommended next actions

1. Mark the track complete: `tracks.md` still shows `[~] In Progress` and `metadata.json` has `status: in_progress` despite all plan tasks checked.
2. Add an automated test that derives the implemented route set from the app tree (or a static list) and asserts every playable card href resolves — closes the AC-1 gap.
3. Restore an assertion that the 11 unroutable titles stay `coming-soon` without hrefs.
4. Move this review file (and any future track artifacts) under `apps/advantage-games/measure/tracks/game_catalog_route_repair_20260713/` to match where the track actually lives.
