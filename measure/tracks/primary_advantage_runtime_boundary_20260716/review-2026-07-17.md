All verification is done — here is the review report.

# Track Review: `primary_advantage_runtime_boundary_20260716` (last 24h)

## 1. Summary of changes

**Committed (7 commits, all 2026-07-16, all documentation/Phase 1):** track creation, a spec correction adding the role-drift and CI-gap findings, two boundary-inventory commits (64-API disposition matrix, proxy→postgres chain), the runtime contract, and plan checkoffs.

**Uncommitted (working tree — this is where all Phase 2–4 implementation lives):**
- `packages/auth`: new `./browser` subpath export exposing only `SESSION_COOKIE_NAME` and `ROLES`/`Role` (no db imports); `dist` is rebuilt and newer than `src`.
- `proxy.ts`: rewritten from 105→40 lines. All `currentUser()`/database reachability removed; it now checks only opaque cookie presence for restricted paths and no longer blocks or redirects sign-in.
- New `lib/auth/route-policy.ts` (exact uppercase-role route policy, locale stripping, no hierarchy inference) and `lib/auth/server-route-guard.ts` (`validateSession(db, token)` before any authorization).
- All five protected layouts pass `routePath` into `AppLayout`, which now validates the cookie server-side and redirects to sign-in/unauthorized.
- New `lib/contracts/client.ts` (DTOs + `summarizeArticleReads` + `toStudentAssignmentDto`); the four client components (`user-reading-chart`, `student-assignment-table`, `edit-license-form`, `license-table`) plus `user-account-nav` no longer import `@reading-advantage/db` or Drizzle.
- `assignmentController.ts` maps the wire `title` field to the DTO `name` field.
- CI path filter adds `apps/primary-advantage/**`; 6 new test files.

## 2. Spec alignment

| Requirement | Status | Notes |
|---|---|---|
| FR-1 runtime-safe import boundaries | **Met** (in working tree) | Proxy and clients import only browser-safe modules |
| FR-2 server-authoritative auth | **Met** | Guard validates opaque token; sign-in always reachable; INTERN/SALES dispositions explicit (settings only) |
| FR-3 browser-safe client contracts | **Met** | DTOs preserve chart/assignment/license behavior; fixes a real `title`→`name` mismatch |
| FR-4 Node deps server-only | **Met** | No config change needed; no shims/fallbacks introduced |
| FR-5 regression detectable | **Met** | I re-ran the suites: 1 auth test + 19 Primary boundary tests pass, including a CI path-filter regression test |
| FR-6 Primary triggers root CI | **Partial** | Filter added, but hosted proof (AC 6–7) is deferred pending a PR |

Acceptance criteria 1–5 have recorded evidence (`phase2-phase3-evidence.md`: dependency-closed 19-task build passed, lint 0 errors, 72 tests pass, type-check backlog classified). Criteria 6–7 and manual verification are correctly deferred with `[b]` markers, consistent with the "no silent deferral" convention.

## 3. Code quality observations

- Quality is high: Google-style JSDoc on new exports per the NFR, null-safety added throughout the assignment table/dialog, and the leaderboard fetch now requires `schoolId`.
- The boundary tests mix source-text assertions (regex over `proxy.ts`/component files) with behavioral tests. Source-text checks catch direct imports but not a future *transitive* server import through, say, a helper added to `route-policy.ts` — acceptable for now, worth knowing.
- Two browser-visible behavior changes go slightly beyond "preserve current behavior": the Settings menu item is now shown to students (previously hidden — consistent with the policy that allows students into `/settings`, but a change), and an already-authenticated user visiting `/auth/signin` is no longer redirected to their role dashboard. The second is spec-mandated at the proxy level, but nothing shows the sign-in *page* handles logged-in users — possible UX regression.
- `spec.md` and `runtime-contract.md` were amended after their Phase 1 commits (adding the sign-in-recovery requirement) but sit uncommitted — legitimate corrections, currently invisible in history.

## 4. Risks or blockers

1. **Everything from Phase 2 onward is uncommitted.** ~19 modified + 9 untracked files, including the plan checkoffs themselves. The committed plan state doesn't match reality, and the work is one `git checkout` from loss.
2. **The mandated re-review hasn't happened.** The evidence file says "A revised independent review is required before commit," and `review-2026-07-17.md` exists but is empty. This is the explicit gate before commit.
3. **Hosted CI proof outstanding** (AC 6–7): the path filter is untested against an actual PR.
4. **Layout-based guarding doesn't cover route handlers/server actions** — the API matrix is deliberately deferred as separately-owned remediation, but 4 endpoints were classified "no evident auth" in Phase 1, so that follow-up track should stay prioritized.

## 5. Recommended next actions

1. Complete and record the independent re-review in `review-2026-07-17.md` (the process gate that's currently an empty file).
2. Commit the working tree in logical units — e.g., `packages/auth` browser entrypoint, Primary boundary implementation + tests, CI filter, track docs/evidence.
3. Open the Primary-only PR to prove the new CI path filter and run the hosted root build against the TS7 baseline (closes AC 6–7 and the `deferred:hosted-ci` items).
4. During manual verification, specifically check the signed-in-user-visits-`/auth/signin` flow and the student-visible Settings link, since both changed behavior.
5. File or confirm the separately-owned track for the 4 no-evident-auth API endpoints from the Phase 1 matrix.
