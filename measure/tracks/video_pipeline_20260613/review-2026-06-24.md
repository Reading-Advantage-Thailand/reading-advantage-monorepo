# Track Review — video_pipeline_20260613
**Review date:** 2026-06-24  
**Track status after 24h:** active  
**Commits in window:** `eb2b2cfe` (formalize track: spec.md, plan.md, metadata.json, review-2026-06-23.md)

## Summary of changes
No application code changed in the last 24 hours. The track was formalized retroactively:
- Created `measure/tracks/video_pipeline_20260613/spec.md`, `plan.md`, `metadata.json`, and `review-2026-06-23.md`.
- Updated `measure/tracks.md`.

The corresponding implementation in `apps/marketing` already exists on `HEAD`:
- `packages/db/src/schema/marketing.ts` defines `settings`, `pastTopics`, `videoProjects`, `videoAssets`, and `campaigns`.
- API routes exist for `video/research-topics`, `video/save-topics`, `video/generate-script`, and `video/projects`.
- `apps/marketing/app/lib/scene-editor.ts`, `script-generation.ts`, and `script-schema.ts` implement the scene editor and Thai script generation.
- `apps/marketing/app/campaigns/[id]/video/page.tsx` provides the UI.

## Spec alignment
**Partial.**
- Existing code matches FR-1 through FR-5 well: topics are deduplicated and persisted, scripts are generated with the required scene shape, scene editor utilities are pure and bounds-checked, projects are persisted, and the campaign video page exists.
- The plan claims a migration `0021_marketing_tables.sql` was generated, but the actual migration ledger has no such file; the marketing tables appear to have been introduced in earlier migrations. This should be corrected.
- Several plan tasks remain unchecked: schema-parity tests, full deduplication/route unit tests, replacing the hand-rolled script validator with Zod, scene-editor unit tests, page/component tests, and build/lint.

## Code quality observations
- The existing scene-editor utilities are clean, immutable, and correctly bounds-check indices.
- The generate-script route validates settings, returns a helpful 400 when the API key is missing, and validates script shape before persisting.
- `script-schema.ts` currently uses a custom validator instead of Zod (acknowledged in the plan). Error responses return generic 500 messages, which is acceptable but not ideal.
- Because the track was formalized after the code landed, acceptance criteria were implicit and some gaps were only captured when the plan was written.

## Risks or blockers
- The migration-name mismatch could confuse future auditors.
- Missing tests mean regressions in topic deduplication, script validation, or scene editing could go unnoticed.
- Build/lint/type-check status for `apps/marketing` is unknown because those gates are still unchecked.

## Recommended next actions
1. Correct the plan/spec to accurately describe how the marketing tables were added (no standalone `0021_marketing_tables.sql`).
2. Add the missing unit tests for `deduplicateTopics`, scene-editor utilities, and route behavior.
3. Replace the custom script validator with Zod and add edge-case tests.
4. Run `pnpm turbo run lint check-types --filter=marketing` and fix any issues.
5. Complete remaining UI/QA phases before scheduling closeout.
