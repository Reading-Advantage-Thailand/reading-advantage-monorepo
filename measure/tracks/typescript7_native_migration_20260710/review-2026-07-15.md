# Track Review: `typescript7_native_migration_20260710` — last 24 hours

## 1. Summary of changes

Eight commits landed on 2026-07-14, all confined to Measure paths (no product source, manifests, or lockfile changes were committed). They complete **Phase 1: Contract & Schema Definition**:

| Commits | What they did |
|---|---|
| `e322a520` → `87911235` | Authored `test-strategy.md` (680 lines), then revised it twice for review findings and Red→Green phase alignment |
| `88d71cf3` | Phase-1 red shape tests: `measure/tests/test_typescript7_native_migration_phase1.py` (20 unittest cases validating artifact schemas) |
| `f68897ac` | Seven Phase-1 artifacts: compiler baseline, surface inventory, dual-compiler contract, empty parity ledger, benchmark/rollout record schemas, pre-existing-failure classification |
| `b0d013c8` | Marked Phase 1 plan tasks `[x]` |
| `32346d6a` | **Remediation**: full 31-workspace TypeScript 5.9 baseline — `capture-phase1-baseline.py` (sequential, `TURBO_CONCURRENCY=1`, 300s timeout, resumable), 31 raw logs, 1,112-line baseline JSON |
| `47683590` | Added a read-only `--check-completeness` mode verifying baseline↔inventory workspace-set equality |

The remediation commits exist because an acceptance audit (untracked `phase-1-acceptance-result.json`, audited at `b0d013c8`) **failed Phase 1**: the plan claimed "all workspaces" baseline evidence while only `packages/types` had been measured. Commits `32346d6a`/`47683590` fixed exactly that gap.

## 2. Spec alignment

| Requirement | Status | Notes |
|---|---|---|
| FR-1 baseline capture | **Met (after remediation)** | Env, versions, CPU/mem, Turbo concurrency, graph.db freshness, 31-workspace diagnostics+timings, 4 failures classified pre-existing. Caveat: 15/31 workspaces contaminated by the dirty worktree — honestly recorded, not hidden. |
| FR-1 integration inventory | **Met** | 39 tsconfigs, 51 tsc scripts, 35 TS peers, tool ownership map. Correctly refused to copy the spec's stale counts (24/25/21). |
| FR-2 dual-compiler contract *definition* | **Met (Phase-1 scope)** | Exact pins (`7.0.2` / `@typescript/typescript6@6.0.2`), native/compat/parity/rollback commands, ownership matrix. Rows honestly labeled `"hypothesis"` pending Phase-2 proof. Nothing installed yet — correct for this phase. |
| Evidence schemas (parity/benchmark/rollout) | **Met** | All parse; the 20 shape tests pass live (I re-ran them: 20/20 OK, and `--check-completeness` passes). |
| FR-3 through FR-8 | **Not started** | Phases 2–4 remain `[~]` — consistent with the plan; no premature claims. |
| Phase-1 formal acceptance | **Missed (process)** | Last recorded audit is a **fail** at `b0d013c8`; no re-audit exists covering the remediation commits, the acceptance result and review artifacts are uncommitted, and plan checkboxes still cite only `f68897ac`. |

## 3. Code quality observations

- **Strong evidentiary discipline.** Failures recorded as evidence rather than laundered: reading-advantage's SIGABRT, science-advantage's 31 errors, and per-workspace contamination flags are all preserved with classifications. The superseded `packages/types`-only measurement was kept as historical rather than deleted.
- **Good self-correction loop.** The acceptance audit caught an untruthful `[x]` claim, and the follow-up commits fixed the evidence rather than the wording — the right response, and consistent with your "no deferring blockers" norm.
- The capture script is well-designed: sequential execution, bounded timeouts, resumability, and a zero-write verification mode.
- **Minor defect:** `surface-inventory.json` says `tsconfig_count: 39` but its `tsconfigs` list has 38 entries — an internal inconsistency the shape tests didn't catch.
- **Hygiene:** `__pycache__/` directories are untracked noise in `measure/` and the track dir.

## 4. Risks or blockers

1. **Phase 1 is formally red.** The only acceptance record on disk is a fail, audited before the remediation landed. Until a re-audit passes at `47683590`, Phase 2 shouldn't start under the track's own workflow.
2. **reading-advantage has no valid baseline** — `tsc` died with exit 134 (SIGABRT, likely OOM) and 0 diagnostics. This is the largest app (957 files) and the spec's headline benchmark target (FR-6 requires ≥3x speedup on the largest standalone app). Diagnostic parity (FR-4) also can't be established for it without a successful TS 5.9/6 run.
3. **The host can't satisfy FR-6.** 4 CPUs, 7.5 GB RAM, swap 71% used at capture time directly conflicts with the "otherwise idle machine" benchmark requirement. Benchmarks on this machine will likely be rejected by the track's own harness rules.
4. **Dirty-worktree contamination** (254 porcelain entries; 15/31 workspaces affected) means the baseline is a snapshot of an uncommitted state, which will complicate parity attribution in Phase 3 if those files change or land meanwhile.
5. **Spec drift:** FR-3 and acceptance criterion 3 say "all 24 TypeScript configurations," but the verified count is 39. The acceptance target needs a recorded amendment or the criteria will be unauditable.

## 5. Recommended next actions

1. **Re-run the Phase-1 acceptance audit at `47683590`**, commit the passing result, and update the plan's Phase-1 checkbox commit references to include `32346d6a`/`47683590` (the `[x]` claims currently point only at the pre-remediation commit).
2. **Resolve reading-advantage's OOM before Phase 2** — e.g., retry with bounded `NODE_OPTIONS=--max-old-space-size` on a quieter host, and record a valid TS 5.9 baseline. Without it, both parity and the primary benchmark are blocked.
3. **Decide the benchmark host now.** If this 4-CPU/7.5 GB machine is the only option, record that constraint and invoke the spec's documented-bottleneck escape hatch deliberately rather than discovering it in Phase 3e.
4. **Amend the spec (or add a reviewed deviation record)** updating 24→39 tsconfigs, 25→35 peers, 21→31 workspaces so acceptance criteria match reality.
5. Small cleanups: fix the `tsconfig_count` 39-vs-38 mismatch and gitignore the `__pycache__` directories.
