# Review — typescript7_native_migration_20260710 — 2026-07-17

*Authored by daily automation (Kimi) after the Claude reviewer hit a session limit; based on git history, spec, plan, and recorded evidence.*

## 1. Summary of Changes

Ten commits in the last 24 hours, all scoped to the CI rollout/parity-observation phase:

- `2f5be01b` observe full semantic parity — expanded the CI parity lane (+62 lines in `ci.yml`), added ~101 lines of phase-3 parity tests, and extended `capture-ts7-rollout-observation.mjs` (+74).
- `5b52e1db` reconcile hosted parity — fixed `apps/reading-advantage/lib/trpc.ts` (+5/-3, the only app-source change), added a 336-line `diagnostic-parity-ledger.json`, and extended parity tests.
- `fb1865bd` qualify CI ordering evidence — tightened rollout-record schema and observation capture for order-dependent diagnostics.
- `fe495a90` remove stale parity exceptions — pruned 16 lines of superseded ledger entries, with test coverage for the cleanup.
- `0e96e231`, `92d26f4d`, `17909e03` — recorded three CI observation runs (`ci-observation/*.json`) and marked the parity observation window complete in plan/metadata.
- `ff56c009`, `69b51ec2` — provisioned (`scripts/ci/install-repo-graph.sh`, +33) and pinned the graph scanner runtime in CI, each with test updates.

## 2. Spec Alignment: **partial (on track)**

- FR-7 (CI rollout): the "observe at least three representative CI runs" requirement is now evidenced by three observation JSONs and the completed observation window — met for the observation sub-item. The parity lane is still temporary/non-blocking; promotion to required gate remains open per plan.
- FR-4 (diagnostic parity): hosted parity reconciled with every intentional exception recorded in the ledger, including removal of stale entries — consistent with the spec's "reviewed intentional changes documented in a parity ledger".
- FR-6 (benchmarks): still open. `metadata.json` deviation notes record that the controlled benchmark was not accepted (TypeScript 6 baseline hit the 128 MiB swap stop-loss under the CPU-idle override). This is the largest remaining spec gap.
- Acceptance criteria 6 and 12 are now arguably satisfied; criteria 11 (speedup threshold) and the Phase 4 closeout tasks (full gates, docs, graph refresh, doctor) remain unchecked in the plan.

## 3. Code Quality Observations

- Every behavioral change carries matching test updates in `test_typescript7_native_migration_phase3.py` — the contract-test-first discipline of earlier phases is holding.
- Evidence is machine-readable (observation JSONs, parity ledger), matching the spec's non-functional evidence requirement.
- CI changes are small and single-purpose (provision, then pin) — easy to revert, consistent with the reversibility NFR.
- The single app-source edit (`lib/trpc.ts`) is minimal and parity-motivated; no blanket suppressions or escape hatches were added, per FR-4 constraints.

## 4. Risks or Blockers

- **Benchmark acceptance is blocked by hardware limits**: the TypeScript 6 baseline hit the swap stop-loss on `reading-advantage`. Without an accepted controlled benchmark, criterion 11 cannot close and the lane cannot be promoted on evidence alone.
- The parity lane is still non-blocking; until promotion, TypeScript 7 is not the required gate (criterion 1 unmet).
- Residual risk of order-dependent diagnostics was explicitly managed but only across three runs; more observations would raise confidence cheaply.

## 5. Recommended Next Actions

1. Decide the benchmark path: rerun the controlled matrix on an idle machine (or a larger runner), or formally document the bottleneck per FR-6's escape clause.
2. Bound TypeScript checker concurrency under Turbo (open plan item) before promoting the lane.
3. Promote the TypeScript 7 parity lane to required once benchmarks are resolved, then execute the Phase 4 closeout tasks (full gates, operator docs, graph refresh, doctor review).
