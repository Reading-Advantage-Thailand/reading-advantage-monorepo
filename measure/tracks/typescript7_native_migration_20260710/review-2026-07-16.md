I have everything I need. Here's the review.

# Track Review: `typescript7_native_migration_20260710` — last 24 hours (2026-07-15 05:13 → now)

## 1. Summary of changes

68 track commits landed on `codex/typescript7-native-migration`, executing essentially all of Phase 3 in one day. The arc:

- **Morning (06:27–09:03):** Remediated the phase-1 audit that the daily review had flagged red (inventory denominators, executable-contract corrections), recorded Phase 1/2 acceptance, then pinned the dual-compiler architecture: root `typescript@6.0.2` plus a `typescript7` alias (`npm:typescript@7.0.2`).
- **Mid-morning (09:28–10:53):** tsconfig 7-compatibility pass (`a66b461b`) and diagnostic parity proof across all configs (`7b827cb4`).
- **Midday (11:18–13:26):** Ordered `check-types` cutovers for every app and shared package (15 commits, each with per-workspace evidence JSON), followed by 20 per-package emit migrations with byte-diff evidence and a `declaration-emit-diff-ledger.json`.
- **Afternoon (13:48–17:45):** Tool-smoke matrix recorded, native checker concurrency bounded via `scripts/run-ts7-check-types.mjs` (`TS7_CHECKERS` limited to 1 or 2), a live benchmark harness with resource guards, the non-blocking TS7 CI parity lane with observation artifacts (`d383b326`), Turbo cache keys bound to compiler identity, and a live cache-proof harness with 16 turbo-summary evidence files.
- **Evening (18:24–21:33):** `measure/tech-stack.md` compiler-ownership documentation, and a final fix making the CI artifact upload include hidden directories plus an explicit tRPC client type in `apps/sales-advantage/lib/trpc.ts` for portable declaration emission.

**Untracked in the worktree:** a phase-3g benchmark run (`evidence/phase-3g/run-20260715T214000Z/`, executed 21:34–22:04, *after* the last commit) and an empty `review-2026-07-16.md` (0 bytes).

## 2. Spec alignment

| Requirement | Status | Notes |
|---|---|---|
| FR-1 Baseline | **Met** | Phase 1 accepted; morning commits fixed the red audit rather than papering over it. |
| FR-2 Dual-compiler contract | **Met** | Exact pins, unambiguous `tsc` routing through the wrapper, explicit `check-types` / `:compat` / `:parity` / `:rollback` scripts, contract tests. |
| FR-3 Config 7-compatibility | **Met** | Phase 3b/3c accepted; marketing `baseUrl` removed in the compat pass. |
| FR-4 Diagnostic parity | **Met** | Parity ledger with reviewed exceptions (4 science, 24 primary, 6 reading); harness is provenance-bound. |
| FR-5 Command migration | **Mostly met** | All cutovers + 20 emit migrations + cache-identity proof done. Checker-concurrency *bounding* exists (1–2), but final selection is explicitly open pending benchmarks. |
| FR-6 Benchmarks | **Missed (blocked)** | No accepted benchmark. The untracked phase-3g run is `status: rejected` (harness error `[Errno 3] No such process`); most reading-advantage TS6 samples were SIGTERM'd (`invalid_resource`) by the swap stop-loss; the full-graph target wasn't reached. |
| FR-7 CI rollout | **Partial** | Non-blocking parity lane + observation capture script landed; zero of the required three CI observation runs recorded yet. |
| FR-8 Docs/rollback | **Met** | tech-stack.md documents ownership, editor setup, Next.js boundary, fallback, and the TS7.1 follow-up; metadata honestly records the benchmark as not accepted. |

Acceptance criteria 11 (speedup threshold) and 12 (three CI runs) are the open items; everything else is either satisfied or has recorded evidence.

## 3. Code quality observations

- **Evidence discipline is excellent.** Every cutover and emit migration has a machine-readable result file; discarded benchmark attempts were recorded as rejected rather than cherry-picked — exactly what the spec demanded (a faster run with missing diagnostics counts as failure).
- **The native wrapper (`scripts/run-ts7-check-types.mjs`) remaps exit 1 → 2** to normalize TS7 diagnostic exits against TS6. It works, but the reasoning lives only in a commit message; a one-line comment in the wrapper would spare the next reader.
- **The two source edits are annotation-only** (`sales-advantage/lib/trpc.ts` explicit `CreateTRPCReact` type, `ui` test setup matcher augmentation), so the "no runtime behavior change" constraint holds.
- **Encouraging signal buried in the rejected run:** the single completed TS6 reading-advantage sample (145.6s, 2.69 GiB RSS) vs TS7 (29.6s, 2.01 GiB RSS) with identical diagnostic counts (107/107) suggests ~4.9x speedup with parity intact — but per the track's own rules it cannot be cited as evidence.
- **Commit hygiene:** small, ordered, well-noted commits with git notes explaining what/why/verification. Very reviewable despite the volume.

## 4. Risks or blockers

1. **Benchmark harness bug:** the phase-3g rejection reason is `ESRCH` ("No such process") — almost certainly a race polling a child that already exited, distinct from the resource problem. Until fixed, even a healthy host can't produce an accepted run.
2. **Host cannot complete the TS6 baseline:** TS6 on reading-advantage repeatedly trips the 128 MiB swap stop-loss (2.69 GiB RSS on the one sample that survived). Local hardware may simply be inadequate for the TS6 side of the matrix.
3. **CI observation window not started** — the migration must stay reversible until three clean runs; promotion of the TS7 lane is correctly gated but nothing has been observed yet.
4. **Dirty worktree:** the phase-3g evidence and the empty `review-2026-07-16.md` are untracked. Your Measure supervisor refuses dirty worktrees, so this will block the next automation pass. The 0-byte review file looks like an aborted artifact and should be deleted or populated.
5. Minor plan drift: the CI-rollout subtask "Add a temporary non-blocking parity lane" is still unchecked in plan.md even though `d383b326` implemented it.

## 5. Recommended next actions

1. **Fix the benchmark runner's ESRCH race** (guard the process-poll for already-exited children), then decide how to obtain the TS6 reading-advantage baseline — options: raise the documented stop-loss with justification, run that one baseline on a larger host, or invoke the spec's escape hatch ("document why end-to-end orchestration limits the gain") using the RSS evidence.
2. **Commit or discard the phase-3g run directory** — as a rejected run it's still required negative evidence per the harness's own rules — and remove the empty review file to restore a clean worktree.
3. **Trigger CI on this branch** to start collecting the three parity-lane observation runs (the artifact-upload fix in `d96909b3` was the last blocker there).
4. Check off the parity-lane subtask in plan.md and add the wrapper exit-code comment.
5. Once benchmarks are accepted, select and record the final `TS7_CHECKERS` value for local and CI, closing the last FR-5 box.

## 6. Superseding status addendum (2026-07-16)

This review was an accurate pre-hosted-benchmark snapshot. The following later
evidence supersedes its stale operational claims without changing its original
findings:

- The benchmark process-group race was fixed and covered by the Phase 3 test
  suite; local benchmark records remain rejected only for the independently
  observed resource stop-loss. The manual hosted benchmark from run
  `29459858785` is still in progress on an older source revision and cannot
  select checker concurrency. A complete benchmark must be rerun from the
  current branch after it finishes.
- CI observations are no longer zero. `29460878641` is a recorded semantic
  rejection (38/39; stale ledger pair), while `29461114234`, `29462009156`, and
  `29462559289` complete the required three consecutive clean semantic-parity
  observations (39/39, stable provenance, zero semantic compiler and ordering
  differences) at provisional checker count 1. This is not promotion because
  final checker selection and the independent Primary Advantage build baseline
  remain open.
- The Phase 3 evidence and this review are tracked. The current worktree must
  remain clean between evidence commits, and the next remaining gates are the
  accepted current-source benchmark, final checker selection, full-gate
  baseline reconciliation, and the Phase 4 closeout checks.
