{
  "schema_version": "dual-compiler-contract.v1",
  "status": "proposed",
  "captured_at": "2026-07-14T00:00:00Z",
  "note": "Candidate contract data only. No aliases are installed, no resolution is changed, and no commands are wired in Phase 1. Phase 3a installs the aliases; Phase 3d wires the commands.",
  "catalog_aliases": {
    "typescript": "6.0.2",
    "typescript7": "npm:typescript@7.0.2",
    "typescript6_alias_note": "The 'typescript' package is installed directly at exactly 6.0.2 so compiler-API consumers (typescript-eslint, ts-node, tsup, tsconfck) resolve the compatibility API without a wrapper's floating inner dependency. Phase 3a updates the root dependency and pnpm-workspace.yaml override to 6.0.2, NOT per-package, to avoid ambiguous resolution.",
    "typescript7_alias_note": "TypeScript 7.0.2 is installed under a separate 'typescript7' alias. The package retains its upstream bin name and does not create a typescript7-tsc executable, so workspace check-types scripts invoke node_modules/typescript7/bin/tsc explicitly.",
    "exact_versions": {
      "typescript7": "7.0.2",
      "typescript6": "6.0.2",
      "typescript5_9_current": "5.9.3"
    },
    "resolution_rule": "No floating major ranges. TypeScript 6 is the direct package at exactly 6.0.2 and TypeScript 7 is the separate typescript7 alias at exactly 7.0.2. pnpm-workspace.yaml overrides.typescript currently pins 5.9.3; Phase 3a replaces it with 6.0.2, adds the typescript7 alias, and rejects @typescript/typescript6 because its @typescript/old dependency floats as npm:typescript@^6.",
    "installed_layout_verification": {
      "phase": "3a",
      "requirements": [
        "pnpm install completes after the exact package changes",
        "node node_modules/typescript/bin/tsc --version prints Version 6.0.2",
        "node node_modules/typescript7/bin/tsc --version prints Version 7.0.2",
        "pnpm install --frozen-lockfile passes after the reviewed lockfile is committed"
      ]
    }
  },
  "commands": {
    "check-types:native": "node node_modules/typescript7/bin/tsc --noEmit",
    "check-types:compat": "node node_modules/typescript/bin/tsc --noEmit",
    "check-types:parity": "node node_modules/typescript/bin/tsc --noEmit && node node_modules/typescript7/bin/tsc --noEmit && <parity-harness-compare>",
    "check-types:rollback": "node node_modules/typescript/bin/tsc --noEmit",
    "command_notes": {
      "check-types:native": "Invokes the TypeScript 7 upstream tsc bin directly from the typescript7 alias path. Used by required check-types gates after Phase 3d cutover.",
      "check-types:compat": "Invokes the direct exact TypeScript 6 tsc bin from the 'typescript' package. Used as the compatibility/fallback path.",
      "check-types:parity": "Invokes the direct exact TypeScript 6 tsc bin, then the TypeScript 7 tsc bin, then compares normalized diagnostics modulo diagnostic-parity-ledger.json. Phase 2 implements the harness; Phase 3c reconciles.",
      "check-types:rollback": "One-command fallback that invokes the direct exact TypeScript 6 tsc bin. Switches required checks back to TS 6 without reverting unrelated configuration correctness fixes."
    },
    "turbo_cache_inputs": {
      "note": "Phase 3i accepted run-20260715T103600Z proves scoped local Turbo MISS/HIT behavior in a disposable worktree. Native check-types hashes its wrapper, checked compiler identity, root manifest/lock/workspace/Turbo configuration, shared config, local task inputs, and TS7_CHECKERS. Compatibility and rollback hash the same relevant compiler/config inputs without the native wrapper or checker value. Every changed input missed and every exact restoration hit.",
      "native_inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/scripts/run-ts7-check-types.mjs", "$TURBO_ROOT$/scripts/typescript-compiler-identity.json", "$TURBO_ROOT$/package.json", "$TURBO_ROOT$/pnpm-lock.yaml", "$TURBO_ROOT$/pnpm-workspace.yaml", "$TURBO_ROOT$/turbo.json", "$TURBO_ROOT$/packages/config/tsconfig/**"],
      "compatibility_inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/scripts/typescript-compiler-identity.json", "$TURBO_ROOT$/package.json", "$TURBO_ROOT$/pnpm-lock.yaml", "$TURBO_ROOT$/pnpm-workspace.yaml", "$TURBO_ROOT$/turbo.json", "$TURBO_ROOT$/packages/config/tsconfig/**"],
      "native_environment_inputs": ["TS7_CHECKERS"],
      "evidence": "measure/tracks/typescript7_native_migration_20260710/phase-3i-cache-invalidation-result.json"
    }
  },
  "ownership_matrix": [
    {"consumer": "Root + workspace check-types (post-cutover target)", "compiler": "TypeScript 7", "alias": "typescript7", "binding": "binding", "programmatic_api": false},
    {"consumer": "tsc emit builds (JS + .d.ts)", "compiler": "TypeScript 7", "alias": "typescript7", "binding": "binding", "programmatic_api": false},
    {"consumer": "typescript-eslint", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": true},
    {"consumer": "ts-node", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": true},
    {"consumer": "tsup", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": true},
    {"consumer": "tsconfck", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "commitlint (transitive peer)", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "vitest / @vitest/* runner", "compiler": "TypeScript 6 or TS 7", "alias": "same or TS 7 alias", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "jest / ts-jest runner", "compiler": "TypeScript 6 or TS 7", "alias": "same or TS 7 alias", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "@playwright/test runner", "compiler": "TypeScript 6 or TS 7", "alias": "same or TS 7 alias", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "Next.js next build / next typegen", "compiler": "Mixed", "alias": "documented per app", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "Vinext/Vite vite build", "compiler": "Mixed", "alias": "documented per app", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "Drizzle Kit / drizzle-kit CLI", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": false},
    {"consumer": "ESLint flat config loaders", "compiler": "TypeScript 6", "alias": "typescript@6.0.2 (direct)", "binding": "hypothesis", "programmatic_api": false}
  ],
  "tools_must_remain_on_ts6": [
    "typescript-eslint (programmatic API consumer)",
    "ts-node (programmatic API consumer)",
    "tsup (programmatic API consumer for declaration emit)",
    "Any consumer proven by Phase 2 smoke harness to embed the TypeScript programmatic API"
  ]
}
