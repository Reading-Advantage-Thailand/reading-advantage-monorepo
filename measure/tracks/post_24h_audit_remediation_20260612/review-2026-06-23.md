# Track Review — post_24h_audit_remediation_20260612
**Date:** 2026-06-23  
**Review base:** `bee70d76` (24h ago) → `HEAD`  
**Commits reviewed:** `019b9d83`, `a46e8c40`, `7f6274a2`, `eb8541e2`, `dab0dc09`, `cf8a5525`, `87d30729`, `3168b543`, `cc72b786`, `434627ed`

## Summary of changes

Ten commits in the last 24h closed Phase 2 (auth hardening) and Phase 3 (codecamp review closeout test cleanup) of the remediation track:

- **Auth/security (Phase 2):**
  - `packages/auth/src/session.ts`: session cap now counts only non-expired sessions, wrapped in a transaction; legacy `token` removed from `Session` interface; `CreateSessionResult` carries the raw token separately; `deleteSession` uses `.returning()` and lets real DB errors propagate.
  - `packages/api/src/routes/auth/reset-password.ts`: single `requireRole` gate, school-scoped target-user lookup for TEACHER actors, explicit 400 when target has no credential account, audit-event failure logged.
  - `packages/api/src/routes/auth/register.ts`: uses `instanceof AuthError` instead of string-name checks.
  - `packages/auth-client/src/context.ts`: minor type alignment.
  - `packages/auth/src/__tests__/session.test.ts`: updated for corrected cap contract.
  - `packages/api/src/__tests__/reset-password.test.ts`: school-scoping assertions pass.

- **Phase 3 closeout artifacts:**
  - Added `phase3-closeout-report.md` and `__tests__/phase3-closeout.test.mjs`.
  - Updated `plan.md` checkpoint markers and task `[x]` / SHA backfills.
  - Fixed `test-strategy.md` Phase 7 test command.

- **DB sentinel hygiene:** `packages/db/src/sentinels.ts` cleaned up.

Diff reviewed: ~580 insertions / ~80 deletions across 14 files.

## Spec alignment: partial

| FR | Status | Notes |
|----|--------|-------|
| FR-5 Session cap non-expired + race-safe | ✅ Met | Count + eviction + insert wrapped in `db.transaction`. |
| FR-6 Remove `token` from `Session` | ✅ Met | `Session` no longer has `token`; `CreateSessionResult` adds it for cookie wiring. |
| FR-8 Inline typed `createSession` values | ✅ Met | Elaborate cast removed. |
| FR-9 `deleteSession` error handling | ✅ Met | Uses `.returning()`; no broad catch. |
| FR-10 Audit event logging | ✅ Met | `.catch()` now logs with `console.error`. |
| FR-11 `handleResetPassword` hardening | ✅ Met | Single role gate, school scoping, credential-account check. |
| FR-12 `handleRegister` `instanceof AuthError` | ✅ Met | String-name check removed. |
| FR-13 Crypto test timeout | ✅ Met | Timeout increased to 15000ms. |
| FR-14/15 Webhooks closeout tests | ✅ Met | `phase-6-acceptance.test.ts` and `phase-7-closeout.test.ts` pass (78/78 webhooks tests). |
| FR-16 Warm dashboard production verification | ❌ Deferred | Still needs production deployment / network probe; owned by `codecamp_perf_warm_dashboard_20260608`. |
| FR-18 Resolve stashes | ⚠️ Open | Plan Phase 5 Task 25 still `[ ]`. |
| FR-25–27 Cross-cutting hygiene | ⚠️ Open | Stashes, generated-artifact ignores, and registry-status updates remain pending. |
| Final verification manual step | ⚠️ Open | Phase 6 manual verification task is unchecked. |

## Code quality observations

- **Strengths:**
  - Auth changes are precise and backed by passing tests: `reset-password.test.ts` 8/8, `packages/api` full suite 161/161, `packages/webhooks` 78/78.
  - `session.ts` is now much cleaner: the `Session` interface is honest, the cap is race-aware, and `deleteSession` no longer hides errors.
  - Reset-password route correctly distinguishes 401/403/404 and validates the credential account before updating.

- **Concerns:**
  - `metadata.json` declares `status: "complete"` while `tracks.md` lists the track as `[ ]` and the plan still has unchecked Phase 5/6 tasks. This inconsistency needs reconciliation.
  - The plan has duplicated Task 11/12/13 entries under Phase 2 (copy-paste artifacts) that could confuse future reviewers.
  - Audit events are still fire-and-forget (now logged). The spec preferred awaiting them where latency is acceptable; consider awaiting on the success path and adding a structured logger rather than `console.error`.

## Risks or blockers

1. **Status mismatch:** `metadata.json` says complete, but the track is not in `archive/` and the plan has open tasks. Premature archiving could lose the remaining hygiene work.
2. **Deferred warm-dashboard verification** is the only remaining acceptance-criteria item for FR-16; it depends on a separate track.
3. **Long-lived stashes and generated artifacts** (Phase 5) are still in the worktree; if left unresolved they may be lost or cause dirty-state confusion.
4. **Cross-package test flakes:** `packages/db` full suite has 4 pre-existing failures and `packages/auth` has 35 pre-existing integration/closeout failures unrelated to this track.

## Recommended next actions

1. Reconcile track status: either complete Phase 5/6 tasks and archive, or change `metadata.json` to `in-progress` and update `tracks.md` to `[~]`.
2. Resolve Phase 5 hygiene tasks: inspect/drop stashes, add `packages/db/scripts/*.js` + `*.d.ts*` to `.gitignore`, update registry status for `auth_security_hardening_20260611` and `db_migration_ledger_20260611`.
3. Remove the duplicate Task 11/12/13 entries from `plan.md` to avoid confusion.
4. Consider upgrading audit-event failure handling from `console.error` to a structured logger and awaiting on the success path.
5. Keep FR-16 warm-dashboard verification assigned to `codecamp_perf_warm_dashboard_20260608`; do not close this remediation track until the dependency is resolved or explicitly deferred in `tech-debt.md`.
