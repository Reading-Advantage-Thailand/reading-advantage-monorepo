# Review: `backend_capability_kernel_20260713` — last 24 hours

## 1. Summary of changes

Three commits landed under this track on 2026-07-18, all in `packages/backend` (~5,800 lines added):

| Commit | Scope | Plan tasks |
|---|---|---|
| `ff47431c` scaffold package | Package manifest, tsconfig, vitest, Turbo wiring | Task 1 ✅ |
| `00313b81` publish capability contracts | 13 contract modules (descriptors, context, policies, errors, audit, idempotency, catalog, route-bindings, projections, registry, invalid-combination matrix) + 3 contract test suites | Tasks 2–5 ✅ |
| `569f4f21` define kernel Red behavior | 2,468 lines of Red tests (62 registry + 45 executor counterexamples), runtime skeleton | Tasks 6–9 ✅ |

Each phase has an "Accepted" evidence doc with independent review (`scaffold-verification.md`, `phase-1-contract-verification.md`, `phase-2-red-verification.md`).

**However, the most consequential change to this track's code did not carry this track's ID.** Six hours after the Red checkpoint, SSO-track commit `2ec48b0f` ("ship company sso and sales lms", `company_identity_sso_20260715`) implemented the kernel itself: `runtime.ts` grew from 268 to 1,216 lines, and it added `durable-value.ts` (638 lines), `postgres-idempotency.ts` (233 lines), runtime-hardening tests, the Drizzle migration `0038_capability_idempotency_records.sql` + schema, and an entire `modules/company-identity` consumer (~3,600 lines). Five follow-up SSO commits hardened it. Result: this track's Phase 2 Red suites are now **Green** (I ran the gate: 156 passed, 7 skipped; `check-types` clean).

## 2. Spec alignment

| Requirement | Status | Notes |
|---|---|---|
| FR-1 descriptor contract | **Met** | Contracts + counterexample matrix committed and independently reviewed under this track. |
| FR-2 executor/context | **Partial** | Implemented and passing tests — but delivered under the SSO track with no Phase 3 evidence or acceptance gate recorded here; plan still marks Tasks 10–13 `[~]`. |
| FR-3 policy behavior / FR-4 error contracts | **Partial** | Same situation. The 7 skipped tests are the Postgres two-connection idempotency integration suite (`describe.skip` without `PG_TEST_URL`), which is exactly the "isolated two-connection atomic acquisition/settlement/rollback evidence" Task 13 requires — so that gate evidence hasn't been produced. |
| FR-5 deterministic catalog | **Missed** | No generator code, no `backend:generate` script, nothing under `measure/generated/` for capabilities. Phase 4 not started. |
| FR-6 route binding generation | **Missed** | Not started (schema contracts exist, generation doesn't). |
| FR-7 bounded pilot | **Off-plan** | `modules/company-identity` is a de facto pilot but violates the spec's shape: it's a full module (service, 869-line Postgres repository, rate limiter, protocol), not "one bounded low-risk operation" selected by documented criteria (Task 17 was never done). |

## 3. Code quality observations

- The committed work is genuinely high quality: every export has JSDoc, contracts are Zod-first with fail-closed defaults, and the Red suites assert handler non-execution per failed precondition — matching the acceptance criteria's letter.
- Evidence docs are specific (exact commands, counts, review verdicts) rather than boilerplate.
- `runtime.ts` at 1,216 lines is becoming a monolith; the plan's task split (builders / context / transaction / audit+idempotency) suggests it was meant to be decomposed.
- There are now **two** Postgres idempotency implementations: `kernel/postgres-idempotency.ts` and `modules/company-identity/postgres-idempotency.ts` (194 lines, added in `2b88a07d`). If the module one isn't using the kernel's durable idempotency port, that's duplication the kernel exists to prevent — worth an explicit look.

## 4. Risks / blockers

1. **Track bookkeeping no longer reflects reality.** Phase 3 (Tasks 10–13) is substantively done but attributed to the SSO track, with none of this track's Phase 3 acceptance gate run or recorded (≥80% coverage, `db`/`domain` test gates, architecture baselines, migration governance). `metadata.json` has an empty `deviation_notes`. Anyone auditing this track will conclude the kernel is unimplemented.
2. **Task 13's key safety evidence is unproven** — the two-connection atomic acquisition/settlement/rollback suite has never run (skipped without `PG_TEST_URL`).
3. **The kernel's public API was consumed before its acceptance gate.** The spec says the durable-job/consumer integration "depends on this track's accepted descriptor/executor API"; the SSO track built on it pre-acceptance, so any contract change now has a downstream consumer.
4. **A review run today appears to have failed:** `review-2026-07-19.md` and `.err` in the track directory are both empty and untracked — and per your measure workflow, a dirty worktree blocks the supervisor automation.

## 5. Recommended next actions

1. Reconcile the track: record the cross-track delivery in `deviation_notes`, write a Phase 3 evidence doc, and update Tasks 10–13 checkboxes honestly (done-via-`2ec48b0f`, gate pending).
2. Run the Phase 3 acceptance gate under this track: the kernel suite with `PG_TEST_URL` set (un-skipping the 7 integration tests), coverage ≥80%, `pnpm --filter @reading-advantage/db test`, `--filter @reading-advantage/domain test`, and `pnpm architecture:check`.
3. Audit `modules/company-identity/postgres-idempotency.ts` vs the kernel adapter and either unify them or document why two stores exist.
4. Decide the FR-7 pilot question explicitly: either retroactively document company-identity as the pilot with the required compatibility/tenant/rollback evidence (Task 17), or keep it out of scope and select a properly bounded pilot.
5. Delete or regenerate the empty `review-2026-07-19.*` files, then proceed to Phase 4 (catalog/route-binding generation), which is now the largest genuinely unstarted chunk of the spec.
