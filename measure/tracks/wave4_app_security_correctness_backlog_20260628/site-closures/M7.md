# Site-Closure Checklist — Primary M7 (Prisma artifact cleanup)

> **Track:** `wave4_app_security_correctness_backlog_20260628` / Phase 7
> **Source evidence:** `measure/audit-reports/primary-advantage-full_20260626/migration-tracks.md` Track M7
> **Resolves:** LR-001-001 (Prisma in Dockerfile + code); contradicts AGENTS.md migration claims
> **Status legend:** 🔴 open · 🟢 fixed · ⚪ NA · 🟡 deferred:<follow-up>

## Affected same-class sites (from source review artifacts + baseline grep)

| # | Site | Current state (baseline) | Required fix | Status |
|---|---|---|---|---|
| 1 | `apps/primary-advantage/Dockerfile` lines 12–13, 18 (`prisma:generate` + build) | runs `npm run prisma:generate && npm run build` | remove `prisma:generate` step; build with Drizzle | 🔴 open |
| 2 | `apps/primary-advantage/Dockerfile` lines 40–42 (copy `prisma/` + `.prisma`) | copies Prisma schema + generated client | remove Prisma copy steps | 🔴 open |
| 3 | `apps/primary-advantage/Dockerfile` npm install | uses `npm` in a pnpm monorepo | convert to pnpm workspace install | 🔴 open |
| 4 | `apps/primary-advantage/package.json` Prisma deps/scripts | (baseline: no `prisma` in package.json — already removed from deps; verify no dangling `prisma:generate` script) | remove any dangling prisma script | 🔴 open |
| 5 | Prisma-era patterns in `server/models/*` | tbd per grep (`@prisma`, `PrismaClient`) | clean up Prisma-era patterns; use Drizzle | 🔴 open |
| 6 | M7 guard test (new) | absent | guard test: `git grep -i prisma apps/primary-advantage/Dockerfile apps/primary-advantage/server apps/primary-advantage/package.json` returns nothing | 🔴 open |

## Closeout requirement
Rows 1–5 🟢. Row 6 🟢 — the guard test exists, runs in CI, exits 0 (defense A12: no dangling guard
ref). Primary does NOT depend on `@reading-advantage/domain` (verified baseline) — M7 needs no
domain migration. See `test-strategy.md` Phase 7.
