# Site-Closure Checklist — Primary M9 (Remove hardcoded secrets/credentials)

> **Track:** `wave4_app_security_correctness_backlog_20260628` / Phase 7
> **Source evidence:** `measure/audit-reports/primary-advantage-full_20260626/migration-tracks.md` Track M9
> **Resolves:** ~103 instances across config/utility/data files (LR findings scattered across utility/config batches)
> **Security-bearing:** explicitly out of Wave 1; owned here.
> **Status legend:** 🔴 open · 🟢 fixed · ⚪ NA · 🟡 deferred:<follow-up>

## Affected same-class sites (from source review artifacts + baseline scan)

> Baseline scan (`grep -rlnE "(password|passwd|secret|apiKey|api_key|token|project_id)\s*[:=]" apps/primary-advantage/ --include="*.ts" --include="*.tsx" --include="*.json"`) returned 19+ source files; the audit's ~103 figure counts instances within files. Full per-instance enumeration happens at implementation.

| # | Site class | Representative baseline files | Required fix | Status |
|---|---|---|---|---|
| 1 | Hardcoded API keys / project IDs / service-account emails | `utils/google.ts`, `utils/openai.ts`, `server/utils/genaretors/audio-generator.ts`, `proxy.ts` | move to env vars (Zod-validated `lib/env.ts`); use secret manager in deploy | 🔴 open |
| 2 | Hardcoded passwords / credentials in models/middleware | `server/models/{student,classroom,teacher,user}Model.ts`, `server/utils/middleware.ts`, `lib/session.ts` | env-read + production guard | 🔴 open |
| 3 | Seed/test credentials | seed/data files | env-guarded; non-production; `if (NODE_ENV === 'production' && !process.env.X) throw` | 🔴 open |
| 4 | Hardcoded emails | `lib/zod.ts`, admin/teacher pages | remove or env-read | 🔴 open |
| 5 | Remaining ~long tail (grep-verified at implementation) | tbd | same pattern | 🔴 open |
| 6 | M9 secret-scan guard (new) | absent | CI guard: labeled-integer hit count `Hardcoded-credential hits: <N>` parsed, asserts `=== 0` (defense A3); allowlist by path/marker not bare words (defense A7) | 🔴 open |
| 7 | M9 production-guard live test (new) | absent | `NODE_ENV=production` + unset seed credential env → seed path throws (defense A5: live, not artifact-only) | 🔴 open |

## Closeout requirement
Rows 1–5 🟢. Rows 6–7 🟢 — both the artifact secret-scan guard AND the live production-guard test
exist and pass. M9 does NOT close on the grep guard alone (the live production-guard test is
required). See `test-strategy.md` Phase 7.
