## Summary of changes
- Reviewed `318dcda` and `39a9a2b`: added defense suitability/descriptor records, source-bound candidate mechanics, a fail-closed Storm adapter, and quarantined Advantage Games QC UI/E2E surfaces.

## Spec alignment (met / partial / missed)
**partial.** The commits implement the QC-only Tasks 1–5 boundary and preserve Storm's absence/historical block, but no title has the required Reading/Primary, persistence, retirement, or final acceptance evidence.

## Code quality observations
- The code explicitly disables completion delivery and keeps selected unions resolver-issued; Storm does not synthesize a runnable historical mechanic.
- The interactive QC canvas is exposed as `role="img"`, which is weaker than appropriate control semantics for keyboard/pointer input. Tests were not rerun for this review.

## Risks or blockers
- QC-only result suppression is correct but leaves the domain-owned, tenant-safe, idempotent completion requirement unproven.
- All downstream host and exact-removal work is intentionally deferred.

## Recommended next actions
1. Improve the QC interaction semantics and verify accessibility behavior.
2. Do not promote any defense candidate until per-title host, persistence, and retirement evidence is accepted.
