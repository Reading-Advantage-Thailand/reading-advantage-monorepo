# Next Executable APK Phase Assessment — 2026-08-01

## Selected local phase

**Reading full-TypeScript-baseline attribution** is the only concrete local phase that can advance formal closure without treating the bounded Dragon Flight evidence as authority for a downstream title. Its work is diagnostic: reproduce and classify the broad `apps/reading-advantage` `tsc --noEmit` baseline, assign each diagnostic to an owning scope, and identify whether any diagnostic is host-proof-local.

The configured app project includes every `**/*.ts` and `**/*.tsx`; it has no host-proof-only TypeScript project. The measurement sequence is precise: the default-heap command exited 134 with no diagnostics; the pre-fix 4 GiB command exited 1 with 81 diagnostics; and the post-fix 4 GiB bounded retry exited 124 with no diagnostics. The two APK-local test-support sites were corrected, but the expected 79-non-local/0-local split is inference only, not a re-observed green full check. Therefore the remaining broad baseline is not a defined product behavior with a truthful Red test yet.

## No legitimate Red test

No broad-baseline Red test was written. A test that expects a particular current type error would encode an unreviewed baseline defect as required behavior; a test that expects the broad app type check to pass would claim closure before its existing errors are attributed. The narrow APK-local configuration test-support correction is separate and does not define expected behavior for the remaining inventory. The remaining phase first needs a dedicated, scoped TypeScript-baseline remediation authorization after its owning files and expected fixed behavior are established.

## Downstream phases not selected

- Existing Action, Legacy Defense, Legacy Traversal, and Legacy Puzzle host-proof, retirement, and cohort steps remain `[b]` behind the Dragon Flight reference-acceptance dependency.
- Existing Core retirement and cohort review remain `[b]`; the active corrective record is implementation/contract evidence only.
- Residual Cross-Host Closeout cannot start until all vertical cohort outcomes are accepted or explicitly blocked.

This assessment grants no production exposure, title adoption, host cutover, legacy retirement, deployment, cohort acceptance, or owner authority.
