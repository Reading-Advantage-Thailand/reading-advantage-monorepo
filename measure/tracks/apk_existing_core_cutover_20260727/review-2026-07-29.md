> **Superseded.** This 2026-07-29 review predates the current dirty-host-proof
> remediation and is retained only as historical audit input. The authoritative
> follow-up is `review-2026-07-31.md`.

## Historical Review — `apk_existing_core_cutover_20260727` (2026-07-29)

### 1. Summary of changes

Five commits carry this track's `track_id` (all 2026-07-28 22:04–22:10 +0700), ~4,500 added lines:

| Commit | Layer | Content |
|---|---|---|
| `e0a5fb2a5` | Advantage Games + measure | Task-4 QC cartridge surface (`ExistingCoreCartridgeQc.tsx`), QC e2e spec, 5 Python guard tests, track docs/receipts |
| `366df8ee6` | packages | Task-5A shared contract `game-contracts/src/host-proof-bindings.ts`, 5B parity test in `game-cartridges`, 5C adapter `domain/src/games/host-proof.ts` (+ `gameTypes` allowlist filter in `queries.ts`) |
| `65416f152` | primary-advantage | Task-5E: hidden route/page/client, Playwright config + auth setup + seed, unit tests |
| `88561a468` | reading-advantage | Task-5D: same surface mirrored, Playwright config, unit tests |
| `7e4bdf142` | packages/auth, packages/db | Added `"default"` export conditions to every subpath; lockfile |

Test status I ran locally: `game-contracts` 56/56 ✅, `game-cartridges` 74/74 ✅ (incl. parity guard), `domain` host-proof mock+PGlite 42/42 ✅, `primary-advantage` vitest 85/85 ✅.

### 2. Spec alignment

**Met**
- 5A/5B/5C are real and well-guarded: five frozen bindings pinned to both accepted receipt digests, fail-closed lookup, QC-registry↔contract parity test, and catalog/loader quarantine asserted (`cartridgeCatalog === []`).
- Server-authoritative XP, server-derived tenant, permission delegation, Zod-validated results, structured error codes — all present and covered by the PGlite live suite.
- Spec line "do not create a host-wide migration" is honored in the app layer (surfaces are flag-gated, `HOST_PROOF_ENABLED !== "true"` ⇒ 404).

**Partial**
- **5D/5E hosts ship code, not proof.** Both Playwright suites exist but are decoupled from `test` and unexecuted; Reading's suite `test.skip`s entirely unless `HOST_PROOF_TEST_SESSION_TOKEN` or username/password is set. "Playwright real-input proof" is asserted by file, not by result.
- **"Identical contract mirror" (plan E) is not identical.** Reading has replay + Previous/Next navigation and a stable per-attempt `completionAttemptId`; Primary has neither. Disabled-flag responses differ (Primary returns a JSON `HOST_PROOF_NOT_FOUND` body, Reading an empty 404), and `?limit=0` yields 400 on Reading but silently defaults to 50 on Primary. The parity guard only covers QC-registry↔contract, not host↔host.
- Task-5 was started while the plan's own gate (line 8, Asset Contract v2 + per-role suitability dossier) is still `[ ]`. The plan permits parallel infrastructure, so this is allowed — but per spec line 10 no title may *claim* host-proof completion until that gate lands, and no dossier exists yet.

**Missed / not started (expected at this point)**
- 5F evidence: no `task5-reading-primary-host-proof-evidence-v1.json`, no Task-5 measure guard test.
- Plan 16 (retirement of replaced legacy paths) and 17 (cohort acceptance) untouched; no asset-copy or full-pack-delivery removal yet, so spec acceptance criteria 2 and 3 remain open by design.
- **Track state is stale**: `plan.md` still shows 5B–5F as `[ ]` and `metadata.json` `updated_at` is `2026-07-28T04:21:02Z` — 18 hours before the commits that implemented 5B–5E.

### 3. Code quality observations

Generally strong: strict `.strict()` schemas, deep-frozen contract data, explicit fail-closed lookups, doc comments that state boundaries. Specific issues:

- **`packages/domain/src/games/host-proof.ts:100`** — `hostProofCompletionRequestSchema` is exported and documented as the request guard, but `recordHostProofGameCompletion` parses with the base `gameCompletionInputSchema` and checks the binding separately. Behavior is equivalent today (the base schema is `.strict()` and rejects client `xp`), but the exported schema is dead and free to drift.
- **`apps/primary-advantage/components/host-proof/HostProofGameClient.tsx:204`** — `idempotencyKey: makeIdempotencyKey()` is minted per click, so two "Complete" clicks are two distinct completions. Primary's own e2e tests (`host-proof-games.spec.ts:84` and `:97`) click Complete twice and expect "Duplicate completion" — those tests cannot pass as written. Reading fixed this via `state.completionAttemptId`; the fix wasn't mirrored.
- **`apps/reading-advantage/jest.config.ts`** — no `testPathIgnorePatterns` for `tests/e2e`, so Jest now loads the Playwright spec and `pnpm --filter reading-advantage test` fails on master (`Test Suites: 1 failed, 4 passed`). This is a new red introduced by `88561a468`.
- **New type errors** (both apps carry large pre-existing baselines — Reading 100, Primary 135 — but these are new to this track): Primary `host-proof-test-config.ts:21`, `lib/__tests__/host-proof-config.test.ts:15`, `lib/__tests__/host-proof-test-config.test.ts:12`, `tests/e2e/host-proof-games.spec.ts:21`; Reading `__tests__/host-proof-game-client.test.tsx:38`, `lib/host-proof-config.test.ts:11,17`.
- **`7e4bdf142`** is labelled "align host deps" but actually adds `"default"` export conditions to `packages/auth` and `packages/db` — both `"type": "module"`. That changes resolution for *all* consumers (a `require()` that previously failed to match now resolves to an ESM file), and it's a foundational-package edit under a title-scoped track.
- Primary's route uses `sessionUser as UserContext` where Reading uses an explicit `toUserContext` mapper; the cast type-checks against `validateSession`, so this is stylistic, not a defect.

### 4. Risks and blockers

1. **Hash-lineage break in the Task-3 acceptance chain (highest severity).** `measure/tests/test_apk_existing_core_cutover_task3_acceptance.py` fails (1 failed / 24 passed). Two subjects pinned by `task3-product-owner-acceptance-v1.json` no longer match the bytes committed in `e0a5fb2a5`:
   - `review-task3-semantic-candidates-remediation.md` — expected `9b7df1be…`, actual `e2ee8519…`
   - `review-task3-semantic-candidates-rereview.md` — expected `7c3675ac…`, actual `ac8eccd4…`
   
   The Task-4 chain verifies clean (5/5). Since the whole track's authorization model rests on these digests — and `plan.md:6` and the shared contract both cite `7c3675ac…` — Task-5 currently builds on a receipt whose own subjects don't validate.
2. Host proof is asserted but unverified: no e2e run, no evidence artifact, and Primary's duplicate/replay tests are contradicted by its client code.
3. Reading's `test` script is red on master, which will block any CI gate that runs it.
4. Export-map change in `auth`/`db` is the widest-blast-radius edit in the batch and is untested by anything in this track.

### 5. Recommended next actions

1. Resolve the Task-3 digest drift first — determine whether the two review docs were edited post-acceptance (re-pin under a new acceptance) or whether the receipt is correct and the committed bytes regressed (restore). Do not advance Task 5 acceptance until `test_apk_existing_core_cutover_task3_acceptance.py` is green.
2. Fix Primary's idempotency key (hoist to a per-attempt `completionAttemptId` like Reading) and port replay + navigation to Primary, or drop the Primary e2e tests that assert them.
3. Add `testPathIgnorePatterns: ["/tests/e2e/"]` to `apps/reading-advantage/jest.config.ts`; clear the seven new type errors.
4. Add a host↔host parity test so "identical contract mirror" is guarded, covering the 404 body, `limit` handling, and the presence of replay/navigation controls.
5. Actually execute both Playwright suites against a seeded DB and capture results into `task5-reading-primary-host-proof-evidence-v1.json` + a Task-5 measure guard test (plan 5F). Add Reading's missing bundle-isolation guard (Primary has one; Reading's QC import is type-only so the guard should pass immediately).
6. Update `plan.md` checkboxes for 5B–5E and `metadata.json` to reflect what actually landed — currently the track docs understate the committed work.
7. Move the `auth`/`db` export-map change to its own track, or justify it in this track's deviation notes.

`measure/tracks/apk_existing_core_cutover_20260727/review-2026-07-29.md` exists but is empty and untracked — say the word and I'll write this report into it.
