# Implementation Plan: Business Operations Graph Baseline Remediation

## Current Gate State

- Parent Review B verdict remains `FAIL`; RB-01, RB-02, and RB-03 remain
  represented by pinned parent evidence and keep the parent gate blocked.
- Phase R0 validator/contract remediation is accepted for its limited scope:
  the Green receipt and independent rereviews A and B report PASS with no
  remaining Critical/High findings. This does not accept R1+ evidence or
  authorize any parent/successor unblock.
- `small_company_admin_privileges_20260722` Phase S1 remains blocked.
- `customer_licensing_crm_20260722` remains blocked at its shared graph gate.
- Work occurs only in the shared `master` checkout. Never stage, revert, reset,
  stash, overwrite, or repair unrelated APK changes.
- This track must not edit `measure/automation-supervisor.py`.

## v2 Recapture Status

- [x] Task: Recapture the R1 source/graph candidate and regenerate R2 Tasks 1-2
  candidate evidence from the shared dirty `master` worktree at baseline
  `e78fe22bb405de732de14c18590b19af0ce5f0de`, after the accepted root
  `.pnpmfile.cjs` boundary correction and Company Identity security fixes. The
  new candidate used the accepted R1 producer, a canonical stable-window scan,
  the documented clean-audit procedure, and the strict R2 compensation
  validator. Its v2 graph-binding candidate is
  `r1-task3-graph-binding-v2-20260801.json`. Terra PASS and Sol bounded ACCEPT
  are recorded in `r1-r2-v2-terra-phase-review-20260801.md` and
  `r1-r2-v2-sol-independent-acceptance-20260801.json`; commit `772839f` binds
  the evidence. It is not R2 Task 3/4 acceptance and cannot unblock either
  successor.

## Superseded Historical Evidence

- The retained R1 v1 bundle/graph binding (`r1-task2-source-and-graph-20260731/`,
  `r1-task3-graph-binding-20260731.json`) and R2 v1 Task 1/2 evidence
  (`r2-clean-audit-attempt-20260731/`,
  `r2-task2-compensation-denominator-20260731.json`, and
  `r2-task2-scan-transaction-20260731/`) remain immutable historical evidence.
  They are superseded as current inputs by the forthcoming v2 candidate only;
  their previous bounded review results are not rewritten or broadened.
- R2 Task 3/4 evidence remains historical and does not become accepted through
  this recapture. Both task markers stay blocked until their separately scoped
  future work and review.

## Exact Next Red Task

- [x] Task: Keep `measure/tests/test_business_operations_graph_baseline_remediation.py` and its versioned adversarial fixtures executable, close every Critical/High finding from `r0-review-a-correctness-20260730.json` and `r0-review-b-safety-20260730.json`, and obtain a passing independent re-review. Evidence: `r0-critical-high-remediation-20260730.json` Green receipt, `r0-rereview-a-20260730.json`, `r0-rereview-b-20260730.json`, and the focused unittest command recorded below. No parent/successor gate was changed.

## Phase R0: Freeze contracts and make Red executable

- [x] Task: After the Red result is recorded, add the minimal versioned acceptance schemas, validator, source-snapshot denominator rules, and counterexample corpus needed to make only the frozen contract tests pass; keep this task in progress until independent re-review reports no Critical/High findings. Evidence: `red-run-receipt-20260730.md`, `r0-rereview-a-20260730.json`, and `r0-rereview-b-20260730.json`. Acceptance is limited to R0 validator/contract remediation; R1+ and all parent/successor gates remain blocked.
- [x] Follow-up: implement and test a genuinely non-writing `fixtures/v1/generate-fixtures.py --check` mode before using that command as fixture-integrity evidence. Evidence: `r0-rereview-a-20260730.json` finding `R0-REREVIEW-M1` (Medium, CLOSED), `r0-rereview-m1-follow-up-receipt-20260731.md`, and commit `fc2f3ea`. Scope was limited to the fixture generator, focused tests, this plan, and one follow-up receipt; no parent or successor gate changes.

## Phase R1: Bind the dirty-worktree source and graph

- [x] Task: Correct the deterministic snapshot producer so its concurrent-drift contract is scoped to the complete scanner-input denominator rather than unrelated repository documentation activity. It must capture and verify a pre/post HEAD interval, while still failing closed for every scanner-input, scanner-status, scanner-staged-diff, branch, worktree, or non-ancestor-history drift. Scanner symlink targets that traverse another symlink now fail closed before the scan window. Accepted by independent rereview v3 PASS: `r1-task1-scoped-concurrency-independent-rereview-v3-20260731.json`. (deferred:phase-r0-contract-green)

  Producer: `measure/business_operations_graph_baseline_snapshot.py` (`produce_snapshot`, `replay_archive`, `verify_snapshot`).
  Focused tests: `measure/tests/test_business_operations_graph_baseline_snapshot.py` (`37` tests, all `OK`) and `measure/tests/test_business_operations_graph_baseline_remediation.py` (`29` tests, all `OK`; `66` combined tests in `38.455s`). Adversaries cover untouched replay, already-dirty byte drift with unchanged status, nested workspace-export `extends`, staged deletion, non-master, alternate scanner root, second worktree, executable `lstat` mode, scanner-emitted Next declaration capture, produced-artifact R0 validation, scanner-scoped staged-diff drift, unrelated staged-documentation drift, changed-and-reverted scanner commits, changed-and-reverted intermediate-symlink chains, and non-ancestor history.
  Red receipt: `r1-task1-snapshot-producer-red-receipt-20260730.md`.
  Historical high-finding remediation receipt: `r1-task1-high-remediation-20260730.md`. Its independent rereview `r1-task1-independent-rereview-20260730.json` correctly reports `PASS` for the superseded global-Git-state contract at audited HEAD `96f9f81ba`; it does not accept this source-scoped correction. The new Red receipt is `r1-task1-scoped-concurrency-red-receipt-20260731.md`; the Green receipt is `r1-task1-scoped-concurrency-green-receipt-20260731.md`. `r1-task1-scoped-concurrency-rereview-remediation-receipt-20260731.md` records the H2 multi-hop fail-closed follow-up and 65 combined focused tests Green. The independent v3 rereview `r1-task1-scoped-concurrency-independent-rereview-v3-20260731.json` reports `PASS` with no findings, accepting Task 1's bounded producer scope. No parent/successor gate is changed.
  Integration check: a producer-written R0 v1 projection is accepted end to end by the unchanged validator, and the accepted R0 regression suite (`measure.tests.test_business_operations_graph_baseline_remediation`, `29` tests) passes (`8.227s`). The R0 validator is not modified.
  No parent or successor gate was changed; the master worktree dirty tree, the real Git index, and unrelated paths are preserved.
- [x] Task: Capture the candidate source snapshot in a coordinated stable window, prove archive replay equals its manifest, and prove pre-scan/post-scan denominator, status, and staged-diff identity. Abort rather than normalize or clean the dirty tree when drift occurs. The first current-head capture was discarded before commit when its graph contained 24 `.next/**/*.d.ts` file rows absent from the producer denominator; the focused producer test and denominator walk now cover those scanner-emitted declarations. Fresh accepted Task 2 evidence: `r1-task2-source-and-graph-20260731/` and `r1-task2-stable-window-receipt-20260731.md`; `r1-tasks2-3-independent-review-20260731.json` reports PASS. No parent/successor gate changed.

  The prior `r1-task2-source-snapshot-run-receipt-20260730.md` is invalidated: it did not execute the canonical scan in the producer transaction, kept the full bundle only outside the repository, and its verifier omitted rich/R0 state artifacts. `r1-task2-evidence-transaction-remediation-20260730.md` records the bounded correction and 50-test Green run. The retained fresh bundle was produced by the canonical in-transaction scan, replayed externally and durably, and then reconciled against the scan-bound graph. The first replacement bundle was discarded because quoted workspace globs were recorded incorrectly; `r1-task2-quoted-workspace-remediation-20260730.md` records the Red/Green correction.
- [x] Task: Run the canonical `repo-graph scan . ./graph.db` between equal source manifests; bind graph SHA/size/schema/tool/commands and every graph file-row hash to the snapshot; rerun exact Accounts, backend-kernel, company-identity, license, and www `search`/`inspect`/`callers` probes. Evidence: `r1-task3-graph-binding-20260731.json` and `r1-task2-stable-window-receipt-20260731.md`; all `3420` graph file rows bind to archived snapshot path/hash/size entries and all `28` read-only commands replayed with matching result hashes. `r1-tasks2-3-independent-review-20260731.json` reports PASS. No parent/successor gate changed.

## Phase R1 v3: Hermetic execution-closure recapture

- [~] Task: Red-first, define and recapture a v3 execution closure bound to the accepted R1 v2 scanner snapshot without changing its R0 projection, scanner denominator, or historical R1 graph acceptance. The closure must freeze every non-derived source/test fixture needed by the Accounts and backend FR4 suites, including current Cloud Build, SQL migration, immutable-audit, shell, standard-pack, and metadata inputs; bind pre/post closure bytes, status, and staged diff; materialize a fresh external root; run `pnpm install --offline --frozen-lockfile`; build the required DB, auth, and backend workspace exports inside that root; and record the unchanged four FR4 commands with exact raw argv/cwd/environment/exit/stdout/stderr receipts. It must reject shared-worktree source, `node_modules`, `dist`, or other live overlays and preserve nonzero build or gate exits as blocked. The required starting diagnosis is hash-bound to `r2-task3-v2-execution-closure-blocker-20260801.md` (`319fca3ecb81e75ba89c4d51444de736feda588f311d47bacdf8540c882fc482`) and `r2-task3-v2-execution-closure-blocker-clarification-20260801.md` (`c4c71bb83fbab0d8dab41667ef6e6889a6da6ae4ade6b096146fa7d9cce1520b`); this remains a candidate-only R1 remediation until independent acceptance. R2 Task 3 is `[b]` pending v3 acceptance; R2 Tasks 4-5, all R3 tasks, and parent/successor gates remain `[b]`. (deferred:phase-r1-v3-execution-closure-red)

  Execution boundary addendum: candidate materialization, install, prerequisite builds, FR4 gates, and graph scan run only beneath a route-proven `podman run --rm --network none` boundary. The candidate must pin the Node image digest, Podman version and architecture, exact wrapper argv, clean `/tmp` work-root realpath, allowlisted RO mounts, and a disposable COW-only pnpm-store overlay. It must record recursive pre/post inventories for the pnpm launcher, pnpm store, repo graph, and every other mounted runner tool; explicit `env -i` bootstrap PATH; and container-local route, DNS, and TCP negative raw proofs. The v3 archive is exactly the clean retained v2 entries plus the finite recorded supplements; its recomputed inventory must bind pre-build, post-build, and post-standard-pack-generation states. Every source supplement and raw stream is hash/size-bound and unique; the standard-pack generator follows a recorded stale-output clear. These requirements preserve candidate-only status and do not authorize any marker or successor change.

  Failed-attempt preservation addendum: `r1-v3-podman-execution-blocker-20260801/` is immutable historical evidence of the first producer defect and cannot be repaired or overwritten. Any later failed/partial retry must atomically reserve a fresh empty sibling directory named `r1-v3-podman-execution-attempt-YYYYMMDD-NNNN`, where `NNNN` is the next monotonically greater ordinal for that date; it must never reuse a deleted, existing, or symlinked path. Its failure record must retain the exact failed-stage id/reason, nonzero staged command exit, logical and Podman executor argv, and hash/size-bound stdout/stderr. In particular, an `inventory-pnpmLauncher-pre` parse/runner failure is not `initialization`, and the failed command must be present. Network negative proofs require executable CommonJS payloads, empty stdout, and structured negative-result stderr; SyntaxError/import/module failures are invalid. This preservation protocol remains blocked evidence only and cannot publish or replace the canonical candidate.


  Hermetic pnpm and interruption addendum: first diagnose package-manager behavior from the recorded local configuration and `pnpm install --help`, not from a presumed `minimumReleaseAge` setting. The frozen workspace configuration contains only `minimumReleaseAgeExclude`; pnpm `11.8.0` documents `--trust-lockfile` as skipping its reapplication of the lockfile supply-chain policy only when the lockfile is already trusted. Therefore a future candidate may use exactly `pnpm install --offline --frozen-lockfile --frozen-store --trust-lockfile` (with the existing exact container store-dir payload) only after recording a `hermetic-pnpm-install-contract` that binds the retained V2/V3 `pnpm-lock.yaml` and `pnpm-workspace.yaml` path/SHA-256/size entries and the pnpm `11.8.0` identity. It must record strategy `TRUST_HASH_BOUND_FROZEN_LOCKFILE`, the documented-control/effect/justification fields, `network.mode: none`, `registryRequestsMaximum: 0`, and `retryEventsMaximum: 0`. This changes no prerequisite-build or FR4 argv; those commands retain their exact existing intent and the same `podman run --rm --network none` executor boundary. Any registry request or retry marker (including `EAI_AGAIN`) is a deterministic gate failure, never an open-ended attestation wait; a missing offline artifact must instead fail promptly as a package-manager failure with its structured `ERR_PNPM_*` diagnostic and raw streams.

  The retained `r1-v3-podman-execution-attempt-20260801-0001/failed-attempt.json` (SHA-256 `88940b45cc5a8628514eb58a66dfc227e8096e1d6cc0f52ea7bc077484e67ffb`, 4,220 bytes) and its raw 879,208-byte stdout are immutable legacy evidence: it recorded exit `137`, empty stderr, and 6,708 `EAI_AGAIN` occurrences under the old contract, but lacks supervision provenance and so cannot be rewritten or retrospectively relabeled. Later attempts must reserve a fresh append-only sibling before execution and may classify `EXTERNAL_INTERRUPTION` only with explicit supervisor evidence (`EXTERNAL_SUPERVISOR_STOP`, signal, actor, reason) that matches the recorded exit; an exit alone is not proof of interruption. A nonzero exit with a real pnpm structured diagnostic and no external-stop evidence is `PACKAGE_MANAGER_FAILURE`. Both classifications remain `BLOCKED`, retain exact stage/argv/raw hash-size receipts and the immutable historical-blocker reference, and neither can create/replace the canonical v3 directory or change R1/R2/R3/successor markers.


  Published-attempt integration and run-day clarification: the pure hermetic classifier is not sufficient. For every later `offline-install` failure, the failed-attempt publisher itself must receive the hash-bound hermetic contract and optional explicit supervisor record, read the staged raw stdout/stderr before finalization, call `classify_hermetic_pnpm_install_outcome_v1`, and write its complete outcome into both the failure object and the final command’s matching zero-request/zero-retry attestation. The resulting append-only `BLOCKED` attempt must bind `hermeticPnpmInstallContract`, the historical blocker, exact raw references, and one of only: (a) `PACKAGE_MANAGER_FAILURE` with an unambiguous `ERR_PNPM_*` diagnostic and no external stop; or (b) `EXTERNAL_INTERRUPTION` with no package-manager diagnostic and a validated `EXTERNAL_SUPERVISOR_STOP` whose signal matches the command exit. The failed-attempt validator must accept these classified offline records and reject a generic `COMMAND_EXIT_NONZERO`, altered policy/attestation, missing diagnostic, or unproven signal. A retry-policy violation must also preserve a blocked append-only attempt with its raw evidence rather than bypassing publication merely because it is detected before the ordinary zero-exit gate.

  `20260801` is frozen only in the canonical v3/historical paths and retained 0001 record; it is not a documented future execution date. The literal `ATTEMPT_DATE = "20260801"` must not select a later attempt directory or constrain its validator. The execution entrypoint must capture one explicit, validated eight-digit run day at the beginning of each run and pass it through reservation, publication, and validation. Thus a run begun on `20260802` reserves `r1-v3-podman-execution-attempt-20260802-0001` (then `...-0002` for a same-day subsequent failure), never mutates or reuses 0801 evidence, while the canonical candidate remains absent and its frozen 0801 name unchanged.


  Scoped pnpm payload placement addendum: `r1-v3-podman-execution-attempt-20260802-0001/failed-attempt.json` (SHA-256 `5732224404afea4c59c17c4f24f27e9f2914dd47f9893c7a42f42da0cb26ee7c`, 4,178 bytes) is immutable blocked evidence of the next real gate: `build-db` exited `1` because the old payload appended `--store-dir=/root/.local/share/pnpm/store/v11` after `pnpm --filter @reading-advantage/db build`, which pnpm forwarded to `tsc` and produced TS5023. Do not alter, relabel, or reuse that 0802/0001 record. Before another candidate attempt, add one fail-closed payload builder for every scoped pnpm command: all three prerequisite builds, standard-pack generation, and every FR4 command must retain their existing logical argv exactly, but their container payload must be exactly `/usr/local/bin/node /opt/pnpm/bin/pnpm.mjs --store-dir=/root/.local/share/pnpm/store/v11 <logical argv after pnpm>`. The store option is consequently a pnpm global option before `--filter` and the selected script, never a trailing script argument. The hermetic install contract remains unchanged. The candidate profile/receipt validator must reject late store-dir placement for every affected command while preserving `--network none`, `env -i`, pinned toolchain/mount provenance, logical argv, raw stdout/stderr hash-size references, and candidate-only/blocked marker status.


  pnpm 11.8 config-store correction: `r1-v3-podman-execution-attempt-20260802-0002/failed-attempt.json` (SHA-256 `525f319d481f28bb97255a10f4e44d03377bb55e71e008523919f345b50fac5e`, 4,177 bytes) is immutable blocked evidence that merely moving the plain `--store-dir` flag before `--filter` is still invalid for non-install pnpm commands: the no-network pinned-image probe and retained raw stderr show pnpm 11.8 `run` rejects it as an unknown option. The prior payload-placement direction is superseded only for non-install commands; neither 0802 record is to be modified. The exact supported no-mutation payload for every prerequisite build, standard-pack generation, and FR4 pnpm command is `/usr/local/bin/node /opt/pnpm/bin/pnpm.mjs --config.store-dir=/root/.local/share/pnpm/store/v11 <unchanged logical argv after pnpm>`. This explicit command-local config override must precede `--filter`, not appear in logical argv, and must be the only store selection mechanism: no `pnpm config set`, workspace/user config writes, `PNPM_*`/`NPM_CONFIG_*` environment variables, HOME configuration, or inherited host environment. The exact receipt must retain `logicalArgv` as the source command, `environment: {CI: "true"}`, `effectiveEnvironment: {CI: "true", PATH: "/usr/local/bin:/usr/bin:/bin"}`, `inheritedEnv: []`, an `env -i` executor suffix containing only CI/PATH then the payload, and a `podman run --rm --network none` prefix with the declared disposable COW store mount. The hermetic install payload deliberately remains unchanged. Validators must reject plain `--store-dir` in every affected non-install payload, a config override after `--filter`, config/environment mutation, executor drift, or missing raw hash/size provenance; all failure outcomes remain append-only `BLOCKED` evidence and cannot create the canonical candidate or change markers.


  Nested pnpm runtime-shim addendum: `r1-v3-podman-execution-attempt-20260802-0003/failed-attempt.json` (SHA-256 `83e9d5ee6640ef8ebc1ec79eb236cc461fc7a43ca46caac31064cb3a5989326f`, 4,488 bytes) and its hash/size-bound raw streams are immutable blocked evidence. Its exact non-install payload has the corrected `--config.store-dir` placement, but the selected package script invokes `pnpm build` in a child shell and fails `sh: 1: pnpm: not found` / `spawn ENOENT`; do not relabel, repair, or reuse this 0802/0003 record. Before a fresh append-only attempt, materialize one stage-generated 63-byte executable shim whose exact bytes are `#!/bin/sh\nexec /usr/local/bin/node /opt/pnpm/bin/pnpm.mjs "$@"\n`, mode `100755`, SHA-256 `34b0b65d66551669ed031bf3c7f8a6f2808107b25a91fcbd3a8deee39b6631f9`. Bind-mount it read-only at `/usr/local/bin/pnpm` as `runnerTool:nested-pnpm-shim` with purpose `hash-bound-nested-pnpm-shim`; it must invoke only the already pinned `/usr/local/bin/node` and `/opt/pnpm/bin/pnpm.mjs`. This preserves the legacy clean `env -i` effective environment exactly (`CI=true`, `PATH=/usr/local/bin:/usr/bin:/bin`), the unchanged logical and config-store payload argv, `--network none`, the pinned launcher/image, and the disposable COW store; it may not use a host pnpm binary, `PATH` expansion, HOME/config mutation, inherited variables, workspace writes outside `/work`, or a writable shim mount. Future candidate profile and receipt must each carry the exact nested-pnpm-runtime-shim contract (logical mount identity/target/access/purpose; executable path/mode/size/SHA-256; node and pnpm launcher targets), the runtime mount must appear in the existing recursive pre/post inventory, and the candidate validator must fail closed on every contract, mount, toolchain, executor, or receipt drift. `test_nested_pnpm_runtime_shim_pins_attempt_0003_and_requires_hash_bound_receipt_provenance` is the Red acceptance gate; it does not authorize a candidate run, marker update, or attempt mutation.


  Frozen workspace build-DAG addendum: `r1-v3-podman-execution-attempt-20260802-0004/failed-attempt.json` (SHA-256 `a0a5e1652e11adb4b49a11593e71583ce2d9f84ec4574a242dd19baebeb54cdb`, 4,617 bytes) and its 835-byte stdout / 70-byte stderr are immutable blocked evidence. Its shim, no-network executor, clean `env -i`, and config-store payload are already correct; the APK generator’s own `pnpm build` instead fails TS2307 because frozen `@reading-advantage/advantage-play-kit` source imports its declared `workspace:*` dependency `@reading-advantage/game-contracts`, whose frozen root export/types fields point to `dist/index.js` and `dist/index.d.ts`, while V3 built only db/auth/backend. Do not alter, relabel, or reuse 0001–0004. Before another append-only attempt, derive a closure-only prerequisite build DAG from the frozen V2/V3 archive—not a package-name exception: locate the trigger package from the unchanged logical generator argv; bind its package manifest; discover static workspace import sites; require matching `dependencies`/`optionalDependencies` workspace declarations; resolve each target package manifest, build field, exports/types/main/module fields, tsconfig and all non-derived package source inventory; then recursively derive and topologically order each target’s own buildable workspace prerequisites. The contract must bind every edge’s consumer/provider, manifest references, source import-site path/SHA-256/size, package source inventory, logical prerequisite argv, and the expected post-install `node_modules` symlink path, target, and realpath. It must also bind the initially missing declared export targets, validate observed symlink/realpath/output state after the unchanged hermetic install, reject unresolved packages, undeclared imports, missing build fields, malformed exports/types, dynamic/ambiguous resolution, cycles, source/manifest/exports/tsconfig drift, non-COW or host-state paths, and any addition that changes the existing install/generator logical argv, no-network boundary, shim, toolchain, or environment contract.

  For current frozen inputs, the generic derivation yields `pnpm --filter @reading-advantage/game-contracts build` before the unchanged APK generator; this is a `PREREQUISITE_BUILD_OMISSION` only while the frozen link is valid and its declared `dist` targets are absent before the derived build. If any mechanically derived prerequisite itself exits nonzero after its inputs/link/output expectations validate, classify it instead as `UPSTREAM_PREREQUISITE_BUILD_FAILURE`, preserve a blocked append-only attempt with raw evidence, and do not disguise it by modifying package source, manifest, exports, tsconfig, build script, lockfile, or the closure. `test_workspace_build_dag_contract_derives_frozen_prerequisites_before_apk_generation` is the Red acceptance gate; it proves generic renamed-package derivation, rejects unresolved/cyclic and frozen-input drift, and authorizes neither a candidate retry nor any marker change.


  Direct-command runtime read-set and capacity addendum: `r1-v3-podman-execution-attempt-20260802-0005/failed-attempt.json` (SHA-256 `dc5c463ec240ce6254d0a766e61de7b1a0db79686b80300f64a6e67a19f72284`, 4,618 bytes) and its hash/size-bound raw streams are immutable blocked evidence. Game-contracts' derived build had already succeeded, but the unchanged APK generator next invoked Node and failed because `/work/packages/advantage-play-kit/scripts/generate-standard-pack-release.mjs` was absent. V2's manifest discovery admitted only `.ts`, `.tsx`, `.mts`, and `.cts`, so it captured neither the direct `.mjs` script nor any standard-pack asset entries. This omission is not authority to use the live worktree. The script itself is eligible only through its exact baseline-Git binding: baseline `e78fe22bb405de732de14c18590b19af0ce5f0de`, blob `fe2dad94aea145d30941675b029a46cbcb6b18f2`, SHA-256 `ea4e072430cdc26d6072950651b3b18fbc4a62bde8bfbd91d8a3dda6a35edbb6`, 5,081 bytes, mode `100644`. Do not alter, relabel, or reuse 0001–0005.

  Before any fresh append-only attempt, derive only safe direct `node <relative-path>` invocations from the target package's frozen script: reject absolute paths, traversal, variables/interpolation, shell escape/extra command segments, malformed manifests, and non-package paths. For every qualifying command, perform a separate bounded discovery stage against an exact baseline-Git materialization, never current-worktree source. The source sidecar must bind path, baseline commit/blob, SHA-256, size, and mode for the script; every source leaf read; and the three receipt reads. It must also bind canonical directory-listing inventories, including every discovered child, so an added or removed leaf cannot evade capture. For the current generator, recursive enumeration excludes leaves with `.md`, `.txt`, `.tsv`, and `.json` suffixes, but `IMPORT-RECEIPT.tsv`, `CURATED-RECEIPT.tsv`, and `LICENSE-RECEIPT.tsv` remain explicit required reads despite `.tsv`; all other recursively discovered nonignored leaves below the resolved `assets/standard` root are required source reads. The stale `standard-pack-release.json` and other ignored JSON/TXT leaves are not source inputs; the stale catalog is only a cleared, declared derived output/write. A finite initial script-only supplement is not acceptable.

  The generator's `../dist/assets/index.js` import is a separate runtime-open class: permit it only as a precise, hash/size-bound, receipt-bound derived build output produced by the preceding `pnpm build` segment, never as a baseline-Git supplement and never as an untraced workspace read. The discovery and final execution traces must each biject exactly with their bound baseline-read set and receipt-bound derived-read set; final writes are limited to declared derived outputs. Reject an omitted receipt, extra output/source read, unbound derived read, source write, added directory leaf without a matching read, removed leaf still claimed by the trace, source/manifest/trace drift, quota breach, or any untraced workspace access. A missing/unverifiable baseline blob remains `FROZEN_RUNTIME_ASSET_ABSENT`; every later mismatch remains blocked and receives a fresh append-only failure record with raw evidence, never a live fallback, candidate publication, or marker update.

  Capacity is a mandatory pre-execution fail-closed gate, not an optimization. Bind V2 archive `snapshot.archive.json` (SHA-256 `e5a638e11ed57cfe6750cbe60e5ab31cbdcb0fd4ff3000458bae7168f868332e`, 61,177,049 bytes), the standard-source discovery ceiling (43,138 regular files; 188,324,464 apparent bytes; 325,713,920 allocated bytes), and a fresh filesystem-free-space probe. Reserve three 325,713,920-byte copies (baseline-Git discovery materialization, candidate COW workspace, immutable archive supplement), 24,946,348 derived-output bytes, 16,777,216 metadata bytes, plus an immutable 1,073,741,824-byte post-run headroom: `requiredAvailableBytes=2,092,607,148`. The lower recorded 3,364,311,040-byte free-space probe passes that deterministic calculation; every later run must record and revalidate its own probe and block before staging if it falls below the requirement. `test_direct_command_runtime_read_set_discovery_binds_full_fixture_tree` is the Red phase acceptance gate: its renamed fixture proves finite recursive capture, the `.md/.txt/.tsv/.json` ignore rule with mandatory receipt TSV exceptions, directory-listing dynamics, derived-build read binding, output exclusion, exact final trace bijection, quota/headroom rejection, and failure preservation. `test_direct_command_runtime_attempt_preserves_missing_script_blocker` is immutable-attempt/bootstrap evidence only; it does not require superseded runtime APIs. Neither test authorizes a candidate run, marker update, or attempt mutation.


  Direct-runtime runner-transaction integration addendum: the accepted read-set slice is pure only; it has no production caller and cannot be treated as a materialization, trace, candidate, or R1 acceptance. Before any future attempt, freeze a detached attempt/profile/input digest and execute this monotonic transaction: baseline-Git source packet; discovered read set; capacity preflight; source-archive supplement/materialization and preinventory; container/network/mount attestation; offline install; prerequisite builds; explicit `build-advantage-play-kit-for-runtime` receipt and post-build `dist` identity; stale-output clear; unchanged standard-pack generator; nonce-bound raw runtime trace; then FR4/postinventory/closure. A failure records the reached stage and all later stages as `NOT_RUN`; no later work or candidate publication may occur.

  The source packet must derive every runtime source byte from one coherent Git object tree, never the live worktree or `.git` mount. It binds baseline commit/tree/blob IDs, Git mode, SHA-256, size, content bytes, source range/use site, and the complete read-set inventory. Its detached serialized packet is a hash-bound source-archive supplement and an exact `/runner/direct-runtime-source-packet.json` read-only mount; it is not a package-wide or host-worktree fallback. Capacity is checked before the archive copy, container context, or source-packet staging, using the accepted deterministic budget plus reserved raw trace/failure-evidence space; ENOSPC, quota, or event-log-cap failure blocks and preserves evidence rather than using host storage.

  Immediately before generation, prove all traced `dist` runtime reads resolve inside the clean `/work` tree and equal the preceding build receipt's command/environment/raw/exit and post-`dist` leaf manifest/root digest. The trace instrumenter is itself hash-bound, mounted/activated through an inherited nonce handshake, emits append-only raw events with path containment, and fails on disabled/truncated/duplicate/unresolved/missing/extra reads. Parse the raw trace and invoke `validate_direct_command_runtime_execution_trace_v1`; source reads and catalog writes stay distinct. Both candidate profile and receipt bind the integration, source packet, derived build identity, trace receipt, mount inventories, and preflight. The archive/profile/receipt/failed-attempt validators must reject drift. The failed-attempt publisher receives the integration and reached stage, atomically writes its detached receipt digest plus reached raw children, and never amends/splices earlier attempts.

  `test_direct_runtime_read_set_is_wired_into_v3_runner_transaction` is the next Red gate. It requires the runner integration builder, Git-object packet capture, integration validator, nonce/event trace parser, source-archive/RO mount wiring, ordered write-flow hooks, explicit build/dist receipt gate, profile/receipt/archive validation, and append-only `NOT_RUN` failure forwarding. It does not execute Podman, authorise a candidate retry, modify source assets, change markers, or accept R1.

  Packet-materialization and actual-trace correction: the current runner integration is not accepted. It mounts `/runner/direct-runtime-source-packet.json`, but `materialize.mjs` reads only `/runner/archive.json` entries; adding packet metadata cannot put the omitted generator or standard assets into `/work`. It also accepts arbitrary `direct_runtime_trace_events` after generation, so a nonce-shaped caller value is not evidence of the generator's filesystem reads. Before any candidate attempt, define and validate an exact detached-packet materialization contract: it selects precisely the source packet's baseline read-set identities (path, blob ID, SHA-256, size, and mode), hash-verifies and writes their bytes into an initially empty `/work`, rejects path escape, conflict, live-worktree fallback, and any extra/missing/drifted byte, and publishes an in-container hash/mode probe receipt. The archive, RO packet mount, profile, receipt, failure publisher, and validators must bind that same materialization contract and packet digest. The mounted packet is the detached input only; generator source reads must resolve from its copied `/work` entries, never from a live mount or host worktree.

  Replace caller-supplied traces with an isolated, hash-bound in-container tracer activated for the unchanged generator command through the inherited nonce handshake. It must append raw, bounded events to a declared artifact with the nonce, tracer identity/digest, generator command identity, containment-resolved path, access kind, and ordered sequence; disabled, truncated, duplicate, unbound, extra, or missing reads/writes block. The runner consumes that raw artifact/receipt directly after the traced generator exits, binds its artifact digest and provenance to the integration/profile/receipt/failed-attempt state, then parses and validates the exact runtime trace. A post-generation parser fed by a caller parameter is invalid. The mandatory materialization probe occurs after archive materialization and before install; tracer activation precedes generation and raw-artifact capture follows it. Failure at either new stage records the reached stage and all later stages as `NOT_RUN`, without candidate publication, Podman execution, marker change, source alteration, or attempt mutation.

  `test_direct_runtime_packet_materialization_and_trace_are_in_container_evidence` is the focused next Red gate. It includes a local detached-packet materializer smoke probe only; it does not run Podman, a candidate, or any marker/attempt operation. It requires exact `/work` packet-byte materialization and output evidence, archive/mount/profile/receipt validation, a real in-container materialization probe, and raw-trace provenance that cannot be passed in by a caller. It is phase-contract work only and does not accept R1.

  Generator-child tracer inheritance correction: the current `--import=/runner/direct-runtime-tracer.mjs` is attached to the Node process launching pnpm. The unchanged package script can spawn a separate Node generator process, so that parent-only preload does not prove the generator's `node:fs/promises` reads were traced. Before any candidate attempt, the exact generator command must receive the explicit clean-environment override `NODE_OPTIONS=--import=/runner/direct-runtime-tracer.mjs`; its explicit `env -i` receipt/profile validator must bind that one override only for the unchanged standard-pack generator, with no inherited host environment. The direct `--import` pnpm payload is not sufficient and must be rejected as the sole activation mechanism.

  The mounted tracer must remain inert for pnpm and every other process. It activates only when the resolved `process.argv[1]` equals the trace-policy `generatorResolvedPath` for the unchanged generator script, and it must otherwise neither register the filesystem loader nor create/append the raw artifact. The trace policy/config binds the relative generator script, resolved `/work` path, exact node-option string, activation policy `INHERITED_NODE_OPTIONS_EXACT_GENERATOR_SCRIPT_ONLY`, and `PNPM_PARENT_EXCLUDED`. Every raw event and the raw-artifact receipt must bind the inherited nonce, packet digest, tracer identity, generator PID, and resolved generator script; capture/profile/receipt validators reject absent, mixed, parent-PID, wrong-script, or unproven-child provenance before parsing the read-set bijection. This correction does not relax path, event-cap, output, materialization, or `NOT_RUN` failure rules.

  `test_direct_runtime_tracer_requires_exact_generator_child_inheritance` is the narrow next Red gate. It runs a local synthetic pnpm-parent-to-child-Node probe plus contract checks only; it does not run Podman or a candidate, and does not advance R1. The parent inherits `NODE_OPTIONS`, spawns the generator without reinjecting it, and the test requires exactly the child generator's canonical resolved script/PID/nonce in the raw artifact and receipt, with no parent-pnpm event. It also requires explicit generator-only `NODE_OPTIONS` executor provenance and a resolved-script/PID tracer guard excluding pnpm activity.

  Materialization failure-forwarding correction: prospectively extend the direct-runtime runner stages in this exact order: `direct-runtime-preflight`, `direct-runtime-discovery`, `materialize`, `direct-runtime-materialization-probe`, `build-advantage-play-kit-for-runtime`, `direct-runtime-dist-identity`, `generate-standard-pack-catalog`, and `direct-runtime-trace`. Do not relabel or mutate any immutable historical attempt. Immediately before each materialization command, set the direct-runtime reached stage to that command's exact receipt id. If either command exits nonzero, its append-only temporary/final failed-attempt receipt must retain the same `failure.stage`, `commandId`, command receipt id, `directRuntimeIntegration.reachedStage`, and nested integration attempt stage. Its `laterStages` must be precisely every subsequent stage in that ordering, each `{ "status": "NOT_RUN" }`; it may not report stale `direct-runtime-discovery`. `test_direct_runtime_materialization_failures_preserve_exact_stage_receipts` is a Red-only temporary-attempt contract: it does not run Podman, publish a candidate, change a marker, mutate a retained attempt, alter source assets, or accept R1.

  Pinned-image child-trace acceptance correction: `test_direct_runtime_tracer_child_only_pinned_podman_acceptance` is an opt-in, prospective Green gate run only as `RUN_R1_PODMAN_CHILD_TRACE_ACCEPTANCE=1 python3 -m unittest measure.tests.test_business_operations_graph_baseline_execution_closure.R1V3ExecutionClosureRedTests.test_direct_runtime_tracer_child_only_pinned_podman_acceptance`. It stages only a tiny synthetic parent-pnpm-to-child-generator ESM fixture under one `TemporaryDirectory`, derives the mounted runner scripts and `/runner/direct-runtime-trace-config.json` from the committed `_runner_scripts` integration path, and uses the existing digest-pinned image through exactly two `podman run --rm --pull=never --network none --userns=keep-id` invocations: generator trace then in-container raw receipt. The only mounts are temporary synthetic `/work:rw` and generated `/runner:ro`; it may not mount the repository, V2 inputs, a candidate, an attempts root, or product/Finance paths. Green requires both exits `0`, distinct parent/child PIDs, one raw `node:fs/promises` child-only baseline-read event with the config nonce/packet/tracer/resolved script, a matching in-container receipt/raw digest, deletion of the raw artifact, and temporary mount cleanup. A missing local image or either nonzero exit is explicit blocked test output with captured stdout/stderr; it must not pull or build an image, publish a candidate or attempt, alter product/V2/Finance inputs, change markers, or accept R1.

  Production input-preparation correction: `write_execution_closure_v1` must not accept a caller-supplied `direct_runtime_read_set`, source packet, or attempt because no production caller can safely construct evidence for the unchanged direct command. Before another full R1 transaction, introduce one internal `prepare_direct_command_runtime_execution_inputs_v1(run_day)` protocol and its internal finalizer. Preparation must derive—not accept—from the frozen V2 archive and unchanged standard-pack generator: one baseline Git commit/tree, recursive `git ls-tree` discovery of the generator's standard-asset root, exact Git-object packet bytes/identities, detached-packet materialization contract, finite entry/byte quota, and a pre-staging capacity budget. It must bind `GIT_OBJECT_DATABASE_ONLY`, live-worktree fallback rejection, and `SINGLE_UNINTERRUPTED_R1_V3_TRANSACTION`; it returns no read-set or invented `dist` digest. The same `write_execution_closure_v1` call must consume that preparation, run the existing clean-room materialization and recorded runtime prerequisite build, capture the actual post-build `dist` identity with its exact producer receipt in-container, and only then finalize the read set/integration immediately before the unchanged generator and child-only trace. No external preparation artifact, caller-injected runtime evidence, candidate, failed-attempt directory, marker, V2/history, Finance input, or Podman operation is authorized by this Red phase. `test_direct_runtime_input_preparation_protocol_is_self_derived_and_transaction_bound` is the focused Red contract: it requires the entrypoint/finalizer surface, removes the unsafe caller inputs from the writer, freezes the preparation/finalization order and protocol fields, and verifies that the preparer itself can only read immutable inputs; it neither executes a candidate nor accepts R1.

  Executable preparation-transaction correction: the fail-closed `INPUT_PREPARATION_FINALIZATION_REQUIRED` shortcut and a comment-only finalizer reference are not phase delivery. Before any full R1 attempt, `write_execution_closure_v1` must invoke a real in-process scheduler after preparation; no legacy caller-input variables may remain reachable. The scheduler must execute, in one call and exactly once for the runtime build: archive, context, detached packet materialization, recorded runtime build, in-container post-build identity, `finalize_direct_command_runtime_execution_inputs_v1`, archive/context integration binding, unchanged generator, then trace capture. Its finalizer output must be the identity bound into archive, context, generator, and trace; comments or bytecode-unreachable calls are invalid. Separately, preparation must call a pure `derive_direct_command_runtime_capacity_from_selected_tree_v1` over a canonical sorted `{ path, gitBlobSha1, sha256, size, mode }` selected Git-tree inventory, bind its canonical digest, and calculate regular-file count, apparent bytes, block allocation, and resource reservations from the inventory rather than static literals. `test_direct_runtime_preparation_transaction_is_executable_and_tree_capacity_is_derived` is a no-Podman synthetic Red gate: it uses a patched real finalizer and in-memory executor to prove the sequence and exact binding, rejects the early raise and unreachable legacy path from compiled writer bytecode, and mutates a synthetic Git-tree size to require changed inventory/capacity output. It does not call the full writer, Podman, candidate publisher, failed-attempt publisher, or markers, and does not accept R1.

  Scheduler-handoff correction: binding `execute_direct_command_runtime_prepared_transaction_v1` to a local and deleting or bypassing it is invalid. The writer must pass the exact object returned by `prepare_direct_command_runtime_execution_inputs_v1` into the scheduler before creating a temporary stage or calling archive, context, container, candidate, or failed-attempt functions. The writer-owned executor passed to the scheduler is the only route into those operations. `test_writer_hands_preparation_to_scheduler_before_execution_side_effects` patches preparation, scheduler, stage/context/container/publisher tripwires, and exits through a private `BaseException` sentinel so the ordinary failed-attempt publisher cannot run. Green requires only `prepare`, then scheduler receipt of the same object; all candidate, attempt, marker, V2/history, Finance, and Podman state remains unchanged. This is a no-Podman Red gate and does not accept R1.


  Frozen two-segment and production-ownership correction: the frozen `packages/advantage-play-kit/package.json` script is `pnpm build && node scripts/generate-standard-pack-release.mjs`; it is a decomposition rule, not a permitted later `pnpm generate:standard-pack-catalog` command. Derive `derive_direct_command_runtime_execution_segments_v1(trigger)` from the frozen trigger into exactly one `RUNTIME_BUILD` segment (`pnpm --filter @reading-advantage/advantage-play-kit build`) and one `DIRECT_NODE_GENERATOR` segment (`node packages/advantage-play-kit/scripts/generate-standard-pack-release.mjs`). The post-build identity and real finalizer must remain strictly between those two segments; the legacy pnpm generator command is prohibited after the runtime build because it silently invokes a second build.

  `write_execution_closure_v1` must construct a concrete `DirectCommandRuntimeProductionExecutorV1` without I/O and give it, with preparation, to the scheduler before creating a temporary directory or invoking archive/context/container/publisher work. The scheduler must own all subsequent side effects, first call its separately represented `probe_direct_command_runtime_production_capacity_v1(preparation, filesystem_roots)`, and retain the capacity probe and every transaction carrier rather than discarding them. That production probe must bind `os.statvfs` capacity and `st_dev` for each actual temporary-stage, archive, COW, and evidence filesystem; it must reject insufficient capacity or device mismatch. The pure selected-tree capacity helper remains usable for synthetic tests but cannot authorize production staging through a caller-supplied byte integer. `test_frozen_two_segment_execution_plan_requires_owned_executor_and_capacity_probe` is a source/synthetic-only no-Podman Red gate: it checks the exact two segments, executor construction/handoff/output retention, finalizer placement, and per-filesystem probe surface. It creates no candidate, attempt, marker, V2/history, Finance, or Podman state and does not accept R1.

  Immutable sealing/binding correction: finalization must be followed by a separate binding stage that returns a distinct sealed integration; it must never mutate already executed archive or context bytes. The scheduler result must retain both the finalizer output and `sealedIntegration`, while the production executor’s generator and trace stages consume only that sealed integration after finalizer → bind ordering. `test_direct_runtime_preparation_transaction_is_executable_and_tree_capacity_is_derived` is amended as a source/synthetic-only no-Podman gate: it snapshots archive/context canonical bytes, requires the separate binding result, and rejects generation or tracing given the unsealed integration. It performs no runtime probe, candidate, attempt, marker, V2/history, Finance, or Podman operation and does not accept R1.

  Adversarial production-executor correction: `probe_direct_command_runtime_production_capacity_v1` must sample every temporary-stage/archive/COW/evidence root and reject both one mismatched `st_dev` and the lowest-root `statvfs` shortfall before allocation. The real generator must receive only the distinct sealed integration’s trace context and run exactly the direct Node payload with the exact generator `NODE_OPTIONS`, never the legacy pnpm generator; after any first executor-stage error the scheduler invokes preservation and touches no later stage. `test_production_capacity_probe_rejects_device_mismatch_and_insufficient_space`, `test_production_executor_generation_uses_sealed_direct_node_payload_only`, and `test_scheduler_stops_after_first_executor_failure_without_later_stage_invocation` are monkeypatched/in-memory no-Podman gates. The existing materialization-failure gate remains the retained-attempt `NOT_RUN` evidence gate; this new first-failure probe does not publish any attempt.

  Post-build identity provenance correction: `direct-runtime-dist-identity.mjs` must emit for each derived `dist` read its logical path, resolved `/work` realpath, regular-file mode, SHA-256, and size. Its observation must be a staged receipt/reference from the same transaction (not `receipt=False`), and `finalize_direct_command_runtime_execution_inputs_v1` must bind that receipt to reject spoofed or cross-attempt identity. `test_post_build_identity_requires_resolved_mode_and_same_attempt_receipt` is a source-only Red gate; it performs no runtime, Podman, candidate, attempt, marker, V2/history, or Finance operation and does not accept R1.

  Scheduler/executor ownership correction: the five retained direct-runtime tests must inspect each concern at its owning boundary rather than demand pre-extraction implementation text in `write_execution_closure_v1`. The writer remains limited to preparation, no-I/O production-executor construction, and scheduler handoff. The scheduler owns ordered capacity-to-trace orchestration, and the production executor owns materialization, direct-Node generation, trace configuration, and exact failure-stage handoff. These tests retain every safety assertion; in particular, the materialization-probe test stays Red until the executor sets `direct-runtime-materialization-probe` immediately before that command, preserving its exact failed-attempt receipt and `NOT_RUN` successors. This alignment performs no runtime, Podman, candidate, attempt, marker, V2/history, or Finance operation and does not accept R1.


  Same-attempt post-build identity correction (H1): add one pure identity envelope with exactly `attemptNonceSha256`, `preparationSha256`, `archiveSha256`, `contextSha256`, `runtimeBuildReceiptSha256`, the full observer raw receipt reference/digest, `/work` root, and canonical derived-read-set digest. The in-container observer must echo the current attempt nonce hash and `/work` identity; finalization validates the preparation/build/observer links, binding validates the executed archive/context bytes without mutating either, and trace validates a fresh post-generator observer against the sealed envelope before raw trace capture. A byte-identical observer from another nonce and every mutated preparation, archive, context, build, observer-receipt, or post-generator derived byte must fail. `test_same_attempt_post_build_identity_envelope_rejects_stale_observer_and_toctou` is an in-memory, no-Podman Red gate only: it makes no candidate, attempt, marker, V2/history, or Finance change and does not accept R1.
  Same-attempt executor-protocol correction (H1a): every executor that can reach finalization must provide a callable `build_same_attempt_identity_envelope` capability. Missing, non-callable, or malformed builders must fail closed after post-build identity and before finalization, binding, generation, or trace; a generic executor without that capability is never a scheduler success path. The retained synthetic success test must declare and validate the capability explicitly, preserving its ordering assertions only for an H1-capable executor. `test_same_attempt_identity_scheduler_rejects_executor_without_callable_envelope_builder` is an in-memory, no-Podman Red gate only: it makes no candidate, attempt, marker, V2/history, or Finance change and does not accept H1 or R1.

  Same-attempt post-bind carrier correction (H1b): after `bind_finalization` returns and before `generate` can consume the base context, the scheduler must revalidate the same sealed envelope against the exact archive/context carriers. A post-bind mutation must fail closed before generation or trace; binding an initially valid context and then mutating it is not an allowed executor behavior. `test_same_attempt_identity_scheduler_revalidates_after_bind_before_generation` is an in-memory, no-Podman Red gate using the real H1 envelope and binding validator with generation/trace tripwires. It makes no candidate, attempt, marker, V2/history, Finance, or H2 change and does not accept H1 or R1.

  Frozen direct-Node split-semantics correction (H2): derive the runtime build/direct-generator split from the exact frozen `packages/advantage-play-kit/package.json` manifest bytes and the selected script expression `pnpm build && node scripts/generate-standard-pack-release.mjs`, never a hard-coded command list or current-worktree package file. The pure semantic contract must bind the manifest reference, package CWD `/work/packages/advantage-play-kit`, clean allowlisted/absent environment with no inheritance, exactly one `pnpm build` followed by exactly one direct `node scripts/generate-standard-pack-release.mjs`, and direct logical/resolved script provenance. Reject shell segments, extra commands, a second build, a pnpm generator/lifecycle path, and `prebuild`/`postbuild` hooks. `test_direct_node_split_semantics_are_derived_from_frozen_script_and_forbid_shell_hooks` is an in-memory, no-Podman Red gate only: it makes no candidate, attempt, marker, V2/history, Finance, or H2 execution change and does not accept R1.
  Frozen direct-Node production-consumption correction (H2a): the H2 semantic helper is not delivery while the production executor continues to derive root-level legacy segments. `DirectCommandRuntimeProductionExecutorV1.build_context` must select the manifest entry referenced by the resolved frozen trigger, derive and retain the exact H2 semantic contract, and reject the legacy `derive_direct_command_runtime_execution_segments_v1` path. The actual runtime build and direct-Node generation must consume those retained semantics exactly once and in order: both commands run with Podman workdir `/work/packages/advantage-play-kit`, the contract's allowlisted clean environment, absence predicates, and no inherited environment; the build uses `pnpm build` only; and generation uses only `node scripts/generate-standard-pack-release.mjs` with the H2 contract's manifest/hash, package-relative/logical/resolved script provenance and exact generator override. No root-level filtered pnpm build, absolute root-level logical generator path, pnpm generator/lifecycle command, hard-coded duplicate segment, or unbound environment/script context may bypass that carrier. `test_production_executor_consumes_frozen_direct_node_split_semantics_without_legacy_bypass` is an in-memory, no-Podman Red integration gate: it tripwires legacy derivation, patches every host/container boundary, and requires the real production `build_context`, `runtime_build`, and `generate` methods to select/store/execute the retained contract. It creates no candidate, attempt, marker, V2/history, Finance, H3, or runner/candidate operation and does not accept H2 or R1.

  Frozen direct-Node live-validator correction (H2b): H2/H2a are not delivered while the live clean-environment, finalization, integration, and retained prerequisite-receipt validators still require the legacy root-level command identities. The real `_container_executor` must validate the carried `directNodeSplit` semantics before accepting a command: only the two exact, non-duplicated package-cwd segments may run, with `pnpm build` plus no override and `node scripts/generate-standard-pack-release.mjs` plus only the exact generator `NODE_OPTIONS`; all root-level filtered-build/absolute-generator/pnpm-generator forms, wrong/missing generator override, inherited environment, and wrong or unbound CWD must fail closed. The real finalizer and `_validate_direct_runtime_apk_build_receipt_v1` must accept the H2 package-relative build receipt, bind it to the existing receipt-bound derived read, and reject its legacy root-filtered argv. `test_h2_package_relative_segments_are_accepted_by_live_override_finalizer_and_receipt_validators` is a Red compatibility gate: it derives the selected frozen manifest and H2 semantics, builds the real packet/read-set/post-build identity fixture from read-only pinned Git preparation, and then calls the unmocked override builder, finalizer, integration validator, and receipt validator under subprocess and Podman tripwires. It creates no candidate, attempt, marker, V2/history, Finance, H3, or runner/candidate operation and does not accept H2 or R1.

  Frozen direct-Node provenance and network-carrier correction (H2b follow-up): the H2b executor carrier must bind `frozenScript.manifest` to the exact selected frozen archive reference, not merely a syntactically valid digest, and must permit exactly one network declaration, `--network none`; a later `--network host`, `--network=host`, or duplicate `--network none` is an execution-context override and must fail before any Podman call. `test_h2b_executor_rejects_forged_frozen_manifest_and_network_override` is a separate in-memory Red test with subprocess and Podman tripwires; it preserves H2b's existing finalizer, receipt, CWD, clean-environment, and legacy-command coverage, makes no candidate, attempt, marker, V2/history, Finance, H3, or runner/candidate operation, and does not accept H2 or R1.
  Frozen direct-Node complete-prefix correction (H2b second follow-up): after the exact `podman run --rm --network none --workdir /work/packages/advantage-play-kit` H2 carrier prefix is established, no later Podman option may weaken or ambiguously restate it. In particular the short workdir alias `-w /work/packages/advantage-play-kit` and `--privileged` must fail before a Podman call, even when all V2-derived frozen-script, segment, and clean-environment fields otherwise agree. `test_h2b_executor_rejects_later_short_workdir_and_privileged_prefix` is a separate in-memory Red test with subprocess and Podman tripwires; it leaves both prior H2b gates frozen, makes no candidate, attempt, marker, V2/history, Finance, H3, or runner/candidate operation, and does not accept H2 or R1.
  Frozen direct-Node structured complete-prefix correction (H2b third follow-up): derive the complete direct-command Podman prefix from the ordered structured mount records, not an accepted prefix head plus arbitrary forwarded tail. It is exactly `podman run --rm --network none --workdir <root-or-package-cwd>` followed by the ordered `--volume source:target:mode` pairs (`O` only for the disposable COW store); `/work` to the frozen package CWD is the only allowed delta. The base `directNodeSplit` context, recorded executor argv, and profile container validator must rederive and agree on that full prefix. Reject insertion, replacement, duplication, omission, reordering, aliases, equals forms, separators, nonstring/empty tokens, dropped operands, and unknown flags across user/userns, capability/security/privilege, entrypoint/environment, mount/device, namespace, remove, network, and workdir controls before any Podman call. `test_h2b_structured_mount_prefix_is_exact_across_context_executor_and_profile_records` is an in-memory Red attack-matrix gate with subprocess and Podman tripwires; it preserves earlier H2b coverage and makes no candidate, attempt, marker, V2/history, Finance, H3, or runner/candidate operation, and does not accept H2 or R1.

  Bounded Green/acceptance (2026-08-02): Terra accepted only the H2b third-follow-up structured complete-prefix correction at working-tree runner SHA-256 `4680d3b3770591b560fd5c953b037968f1bab07a45d1135429bfd023687f1030`; the exact frozen test passed one test in `4.322s` and `git diff --check` was clean. No further H2b edits are authorized. Receipt: `h2b-structured-prefix-third-followup-green-acceptance-20260802.md`. The cumulative runner diff remains uncommitted shared R1-v3/H1-H2b work and is deliberately excluded from this documentation-only closeout. Phase R1 v3 remains `[~]`; no H2/H2a/H2b-wide, candidate, marker, parent/successor, Podman, or Finance acceptance is claimed.

  H3 terminality Red: a failed pre-seal direct-runtime stage through `direct-runtime-dist-identity` must retain a hash-bound `directRuntimePreSealAttempt` with the literal eight-stage plan, exact reached stage, and `NOT_RUN` suffix, matching preparation/attempt command metadata and no `directRuntimeIntegration`; the test rejects mismatched metadata, altered suffixes, carrier raw/extra keys, and simultaneous sealed integration. Generator and trace failures still require sealed integration. This test is temporary-evidence-only and does not run Podman, publish a candidate or attempt, change markers, or accept R1.

  H3 production-persistence Red: the real prepared-transaction scheduler and `DirectCommandRuntimeProductionExecutorV1` must retain one materialize failure through their real preservation/publisher/validator path, while temporary capacity/archive/context/container boundaries prevent Podman or permanent evidence writes. The emitted attempt must carry only the exact pre-seal terminality carrier, final raw streams, and causal materialize failure; no later stage or candidate publication may occur. This test is Red-only and changes no marker, V2/history, Finance input, or R1 acceptance.

  Bounded Green/acceptance (2026-08-02): Terra and Sol accepted only retention of one real nonzero production `materialize` failure before sealing, at runner SHA-256 `1e843616bb27cabb0677af2cd8098471f12408121dbcfed4935918b12f546cf2` and test-file SHA-256 `7bf0c72ae63f4b5a5bbec965d4010e1dbdd486d6b202947728859378e5938609`. Terra allowlist audit found no definition or module assignment outside the pre-seal builder/validator/publisher/executor handoff surface; the exact production test passed one test in `0.268s`. Receipt: `h3-materialize-preseal-retention-green-acceptance-20260802.md`. The validator and production-retention implementation are frozen, but the cumulative runner diff remains uncommitted shared R1-v3 work. Phase R1 v3 remains `[~]`; this does not accept other pre-seal stages, preservation-failure injection, candidate-publication durability, H3/H4/H5, R1-v3, a runner commit, or any Podman/candidate/Finance/marker/registry/successor action.

  H3 pre-seal failed-evidence self-atomicity Red: the real `preserve_failure` -> `_publish_failed_attempt` -> `_finalize_command` materialize path must write and validate a complete pre-seal record only under a private staging parent before its canonical final attempt path is exposed. The final validator receives a canonical `r1-v3-podman-execution-attempt-20260802-0001` leaf with matching nested `attempt.id`, complete hash-bound raw references using that eventual public path, the exact materialize carrier, and no sealed integration; the public path is absent during validation. A rejecting final validator must make `preserve_failure` fail closed as `CandidateExecutionBlocked`, retain the injected validator and original materialize error causal chain, and remove every public/staged JSON/raw artifact. Candidate publication, generic blocker routing, later stages, Podman, `os.replace`, V3 output, markers, V2/history, Finance, registry, and R1 acceptance remain out of scope. `h3-preseal-failure-evidence-self-atomicity-pre-green-baseline-20260803.md` freezes the allowed publication-ordering/cleanup tail bytes and pre-Green runner/test hashes; future Green may change only that tail plus one private pure helper if essential.

  Bounded Green/acceptance (2026-08-03): Terra and Sol accepted only private validation and cleanup for the retained pre-seal materialize failure at runner SHA-256 `9c70fcd2da2bd73a846bfb0fd9aca6fb7da4264f6e125e6632b96e78f5a3f6dc` and frozen test SHA-256 `e74124c9c64074af78d8d7b627b78f5b5bdf81637e7a8d06c80d7199c20b6a9c`. Terra and root independently passed the exact four focused no-Podman tests in `1.397s` and `1.437s`; the scoped runner diff check was clean. The committed pre-Green artifact reconstructs and hash-verifies the old authorized 27-line publisher tail at SHA-256 `d6b21b7f5868d2d2af9fc7d13725732add6e0ce2fe90ea2922d60e1ad00721c3`; current behavior derives logical identity without public reservation, stages canonical raw/JSON evidence privately, uses the real validator, then collision-reserves and renames once with nested cleanup. Receipt: `h3-preseal-failure-evidence-self-atomicity-green-acceptance-20260803.md`. The old full runner is digest-only at `1fc29d748045d0f9192ed0c631da03b61eafb939c7bd7012389d7013ed91a98d`, so this does not claim a byte-for-byte whole-file delta or cumulative runner attribution. The runner/test code remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`; no other pre-seal/generic command variant, collision/rename/raw/JSON fault injection, candidate path, H3/H4/H5/R1-v3 acceptance, runner commit, Podman/candidate/Finance/marker/registry/successor/V2/history action is accepted.

  H3 pre-seal rename-failure causality and cleanup Red: after the real validator accepts exactly one private canonical materialize attempt with public logical raw/JSON references and the public path absent, the publisher may create only its empty public reservation and make exactly one private-leaf to public-path `os.rename` call. An injected rename `OSError` must be rethrown through `CandidateExecutionBlocked` with the original materialize failure as the rename error's explicit cause, while both reservation and private staging/raw/JSON artifacts are removed. Candidate publication, generic blocker routing, `os.replace`, Podman, trace, runtime build, generation, V3 output, Finance, markers, registry, and R1 acceptance remain out of scope. `h3-preseal-materialize-rename-failure-pre-green-baseline-20260803.md` freezes the current narrow rename exception/cleanup block and context anchors; future Green may change only that captured block.

  Bounded Green/acceptance (2026-08-03): Terra and Sol accepted only one post-validation `os.rename` failure for retained pre-seal materialize evidence at runner SHA-256 `cecfe1fe4713454c1b856024d2ae9f79a9c4faca879086f9d97b6976ecd7c471` and frozen test SHA-256 `899d21a89a1dd5707db8f5a080277bbd1dc7bee5a91d3862bdc379967e63e089`. Terra and root independently passed the exact five focused no-Podman tests in `1.594s` and `1.576s`; the scoped runner diff check was clean. The committed artifact reconstructs and hash-verifies the old captured rename/reservation/cleanup block at SHA-256 `128f187a8ba8d86f0c8d414519a09ee3dfa89bfead41a8e35ee3381cfdcbf17f`; the bytewise change captures the same rename error, keeps cleanup intact, then rethrows it from the original materialize error. Receipt: `h3-preseal-materialize-rename-failure-green-acceptance-20260803.md`. The old full runner is digest-only at `9c70fcd2da2bd73a846bfb0fd9aca6fb7da4264f6e125e6632b96e78f5a3f6dc`, so this does not claim a byte-for-byte whole-file delta or cumulative runner attribution. The runner/test code remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`; no collision/raw-copy/JSON-write/validator/cleanup fault injection, other pre-seal/generic variant, candidate path, H3/H4/H5/R1-v3 acceptance, runner commit, Podman/candidate/Finance/marker/registry/successor/V2/history action is accepted.

  H3 pre-seal publication-collision causality and non-destructive cleanup Red: after real validation of one complete private canonical materialize attempt with no public path, the sole expected public `Path.mkdir` request is replaced by a native-created competing directory containing a unique hash-bound sentinel and then `FileExistsError`. The publisher must retain that competing directory byte-for-byte, add no failed-attempt JSON/raw child, remove its private staged leaf, avoid rename/candidate/generic/Podman/later paths, and fail through `CandidateExecutionBlocked` with the exact `V3_PODMAN_ATTEMPT_PUBLICATION_COLLISION` error whose explicit cause is the original materialize failure. `h3-preseal-materialize-collision-pre-green-baseline-20260803.md` freezes the public-reservation exception/cleanup block and anchors; future Green may change only the `FileExistsError` block to establish the causal chain.

  Bounded Green/acceptance (2026-08-03): Terra and Sol accepted only final-directory `FileExistsError` causality and non-destructive cleanup after successful private validation for retained pre-seal materialize evidence at runner SHA-256 `90d550c3e4c2871de6b15349fa50cfda647af25a08532ad3842f1eb36a730490` and frozen test SHA-256 `8f403418cfb729b0228928cd51315ed3b94dcd45efb608bf063eab097b5f881a`. Terra and root independently passed the exact six focused no-Podman tests in `1.351s` and `1.455s`; the scoped runner diff check was clean. The committed four-line collision block hash is `3d05a8465e98620956829c709a92dc703f99b28ca4854d1330d4887b23820383`, and replacing only the current hunk with it reconstructs the complete pre-Green runner at `cecfe1fe4713454c1b856024d2ae9f79a9c4faca879086f9d97b6976ecd7c471`. The sole runner delta catches the unchanged collision error and raises it from the original materialize error; the competing public directory and sentinel remain byte-identical while only private staging is removed. Receipt: `h3-preseal-materialize-collision-green-acceptance-20260803.md`. This does not reconstruct history before `cecfe1fe...` or accept the untracked shared helper historical identity; runner/test code remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`. No rename/raw-copy/JSON-write/validator/cleanup fault injection, successful publication, other pre-seal/generic variant, candidate path, H3/H4/H5/R1-v3 acceptance, runner commit, Podman/candidate/Finance/marker/registry/successor/V2/history action is accepted.

  H3-H5 candidate-publication durability Red: a trace-complete integration whose private candidate fails `validate_execution_closure_v1` must retain exactly one validator-accepted append-only operation failure, not a synthetic nonzero `candidate-publication` command. Its closed, mutually exclusive `candidatePublicationFailure` carrier has only `schemaVersion`, `kind: execution-closure-candidate-publication-failure`, completed-integration SHA-256, `direct-runtime-trace`, `validate-private-candidate`, canonical repo-relative V3 destination, and `published: false`; a trace-complete `directRuntimeIntegration` remains required, while pre-seal, hermetic, workspace, and command variants are absent. The failure is exactly post-trace `candidate-publication` / `CANDIDATE_VALIDATION_FAILURE` with that `operationId`, never `commandId`. The final destination remains absent, `os.replace` is never reached, preservation errors remain fail-closed, and successful preservation rethrows the original validation error. This temporary, no-Podman Red gate does not publish a candidate, amend V2/history, or change Finance, registry, marker, or R1 acceptance.

  Bounded Green/acceptance (2026-08-02): Terra and Sol accepted only durable retention of one post-trace private-candidate validation failure, at runner SHA-256 `766dc8a991dab722d2c54f16091ad08e5acd926715682d7b9ce3cb0bd47b7e8a` and test-file SHA-256 `c649f47d9dfaabc4acc13faafb514c58c56696743333c319537fec89a7371a48`. The hash-pinned delta is confined to the candidate failure carrier/validator/dedicated publisher and production publication-preservation handoff; the accepted pre-seal path and `_publish_failed_attempt` remain frozen. The exact candidate-validation retention test passed one test in `2.742s`. Receipt: `h5-candidate-validation-retention-green-acceptance-20260802.md`. The cumulative runner remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`; this does not accept atomic-replace failure, other publication failures, preservation-failure injection, broader pre-seal stages, candidate contents, H3/H4/H5, R1-v3, a runner commit, or any Podman/candidate/Finance/marker/registry/successor/V2/history action.

  H5 atomic-replace durability Red: after exactly one successful private validation, a deterministic `os.replace(private-candidate, final-destination)` failure must retain one validator-accepted, unpublished operation-only attempt and rethrow that same `OSError`. Validation precedes the sole replace call; final output stays absent and the private stage stays private. The existing closed seven-key `candidatePublicationFailure` carrier remains schema-identical but has `operationId: atomic-replace`, `published: false`, the trace-complete integration digest/reached trace, and canonical destination; the outer failure is exactly `candidate-publication` / `CANDIDATE_ATOMIC_REPLACE_FAILURE` / `atomic-replace`, never a command id. No raw stream or synthetic candidate-publication command is allowed, and preservation remains fail-closed. This temporary no-Podman Red gate changes no success semantics, V2/history, Finance, registry, marker, or R1 acceptance.

  Bounded Green/acceptance (2026-08-02): Terra and Sol accepted only durable retention of one post-trace atomic candidate-replace failure, at runner SHA-256 `6b71e745a9e8501ca608bc4255a69c2ab7088fd9224f474e4a146c37e2fee893` and test-file SHA-256 `50b2daaff8275aa90b568ab8b57e65216ba86aabbcef41b6472ef05e85bfe41d`. The hash-pinned delta additively extends the accepted candidate failure carrier, validator, publisher, preservation routing, and only the narrow validated-private-candidate to atomic-replace boundary; the pre-seal and generic command-failure paths remain frozen. Terra paired candidate-operation gates passed: private validation one test in `2.252s` and atomic replace one test in `2.249s`. Receipt: `h5-atomic-replace-retention-green-acceptance-20260802.md`. The cumulative runner remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`; this does not accept successful candidate publication, candidate contents, other publication failures, preservation-failure injection, broader pre-seal stages, H3/H4/H5, R1-v3, a runner commit, or any Podman/candidate/Finance/marker/registry/successor/V2/history action.

  H5 failed-evidence self-atomicity Red: for either retained candidate operation, the dedicated failure publisher must validate only a private unpublished staged record before atomically exposing its final append-only attempt. The validator must receive the canonical final-pattern leaf and matching record id under a private staging parent, while the corresponding public final path remains absent. If that final validator rejects after staging, `preserve_failure` must fail closed as `CandidateExecutionBlocked`, retain the injected validator as cause and the original candidate-operation error beneath it, and leave no final attempt, staging/tmp directory, partial JSON, raw stream, candidate output, replace call, generic publisher call, or command receipt. This test freezes the existing carrier, classification, trace binding, empty-command, and operation semantics; it does not alter candidate artifacts, private-validation/atomic-replace success behavior, pre-seal, generic publication, V2/history, Finance, marker, registry, or R1 acceptance.
  Bounded Green/acceptance (2026-08-02): Terra and Sol accepted only self-atomic publication of candidate-operation failure evidence at runner SHA-256 1fc29d748045d0f9192ed0c631da03b61eafb939c7bd7012389d7013ed91a98d and frozen test SHA-256 c5155f318fb735dba4d9bf07330570fc23bbb1bd237760e682a08c5848c72269. Terra and root independently passed the exact three focused tests in 8.055s and 5.616s; the scoped runner diff check was clean. The accepted behavior selects the logical final attempt identity without public reservation, stages a final-pattern leaf privately, validates it with the real validator, publishes only after validation through collision-safe reservation/rename, and removes unpublished staging/reservation artifacts on failure. Receipt: h5-candidate-evidence-self-atomicity-green-acceptance-20260802.md. The prior accepted runner SHA-256 6b71e745a9e8501ca608bc4255a69c2ab7088fd9224f474e4a146c37e2fee893 was uncommitted content with no exact byte snapshot or reachable Git object, so this is a current live-state semantic allowlist acceptance, not reconstructible byte-for-byte delta or cumulative runner attribution. The runner/test code remains uncommitted shared R1-v3 work and Phase R1 v3 remains [~]; no generic/pre-seal publisher atomicity, successful candidate publication, other failure path, H3/H4/H5/R1-v3 acceptance, runner commit, Podman/candidate/Finance/marker/registry/successor/V2/history action is accepted.

  H3 pre-seal materialize raw-copy causality and private cleanup Red: the real
  materialize preserve_failure to _publish_failed_attempt to _finalize_command
  path must copy only stdout into its private canonical leaf before a
  deterministic stderr shutil.copyfile OSError. It must neither write JSON nor
  call the validator; it may not expose a public attempt, rename or replace,
  use candidate/generic/Podman/trace/later-stage paths, or leave any private
  raw/staging residue. The exact outer failure is
  V3_PODMAN_FAILURE_EVIDENCE_UNPRESERVED: materialize:
  V3_TEST_PRESEAL_FAILURE_EVIDENCE_RAW_COPY, and the caught same raw-copy error
  must explicitly retain the original materialize error as its cause.
  h3-preseal-materialize-raw-copy-pre-green-baseline-20260803.md freezes the
  precise finalizer call and hashes; future Green may only wrap that call to
  rethrow the same caught OSError from the original materialize error.

  Bounded Green/acceptance (2026-08-03): accepted in-loop, single-authority per
  `AGENTS.md` Measure-acceptance ownership, for only the stderr raw-copy failure
  at runner SHA-256
  `9f5ad52728c4c3c01ec1d9ff210de35f11ec82a0da3ebd656ab92944ae763b97` and frozen
  test SHA-256 `54f81f425dbe65911907e8d7615e21657641b6dc926f498558906d69f9aa9cea`,
  which equals the Post-Red hash in the baseline, so the test surface is
  unchanged since commit `459697e3c`. The seven focused tests, enumerated by
  name in the receipt, passed three times: `2.932s`, `2.728s`, and `1.788s`
  under a guard wrapping all five `subprocess` entry points, which recorded zero
  podman invocations and so proves the no-Podman property rather than asserting
  it. The sole runner delta is one
  `+119`-byte hunk wrapping the frozen `_finalize_command` call in
  `try/except OSError as raw_copy_error: raise raw_copy_error from error`;
  replacing only that block with the committed five-line frozen artifact
  (SHA-256 `957aa86ddb9c6ae2dde192a043b21ac0eca8ec9af27c7cffb3b34aa8c6183d46`)
  reconstructs the complete pre-Green runner at
  `90d550c3e4c2871de6b15349fa50cfda647af25a08532ad3842f1eb36a730490` exactly,
  so this slice is bytewise attributable and all other runner bytes are proven
  unchanged. Receipt:
  `h3-preseal-materialize-raw-copy-green-evidence-20260803.md`. The receipt also
  fixes the previously count-only focused-suite definition to an explicit list
  and flags that continuity with the earlier three/four/five/six-test suites is
  inferred, since no committed artifact enumerates them. This
  does not reconstruct history before `90d550c3...` or accept the untracked
  shared helper historical identity. The runner/test code remains uncommitted
  shared R1-v3 work and Phase R1 v3 remains `[~]`; no collision/rename/
  JSON-write/validator/cleanup fault injection, successful publication, other
  pre-seal/generic variant, candidate path, H3/H4/H5/R1-v3 acceptance, runner
  commit, or Podman/candidate/Finance/marker/registry/successor/V2/history
  action is recorded.

  H3 pre-seal materialize JSON-write causality and private cleanup Red: the real
  materialize preserve_failure to _publish_failed_attempt path must complete both
  raw receipt copies into its private canonical leaf, then take a deterministic
  `_write_json` OSError against `failed-attempt.json` while that JSON is still
  absent. It must not call the validator; it may not expose a public attempt,
  reserve, rename or replace, use candidate/generic/Podman/trace/later-stage
  paths, or leave any private raw/staging residue. The exact outer failure is
  `V3_PODMAN_FAILURE_EVIDENCE_UNPRESERVED: materialize: V3_TEST_PRESEAL_FAILURE_EVIDENCE_JSON_WRITE`,
  and the caught same JSON-write error must explicitly retain the original
  materialize error as its cause.
  `h3-preseal-materialize-json-write-pre-green-baseline-20260803.md` freezes the
  authorized call and hashes; future Green may only wrap that call to rethrow the
  same caught OSError from the original materialize error. That baseline also
  records a reconstruction hazard absent from earlier slices: the authorized line
  is byte-identical to a frozen line at 6812 inside
  `_publish_candidate_publication_failure_attempt`, so Green acceptance must
  anchor on the unique two-line sequence at SHA-256
  `ca754ecf23d8838b24f517a0a3438a604b1462d57c055e9fb930a9d8e1d71070` and prove the
  6812 occurrence unchanged, never a content-only replacement.

  Bounded Green/acceptance (2026-08-03): accepted in-loop, single-authority per
  `AGENTS.md` Measure-acceptance ownership, for only the `failed-attempt.json`
  write failure at runner SHA-256
  `99e096bda4dc5748efef4a12ad9dd8e46394a55482598705d17bd79267d4ab94` and frozen
  test SHA-256 `f02b207eea0e737a15e397f521eaea29114bc7162bc418d8c7020629e91dbaa7`,
  which equals the Post-Red hash in the baseline, so the test surface is unchanged
  since commit `7675a6ea1`. The eight focused tests, enumerated by name in the
  receipt, passed three times: `2.470s` under a subprocess guard recording zero
  podman invocations, then `1.867s` and `2.075s`. The sole runner delta is one
  `+107`-byte hunk wrapping the authorized `_write_json` call in
  `try/except OSError as json_write_error: raise json_write_error from error`. The
  anchored reconstruction required by the baseline passed: the Green anchor occurs
  exactly once, replacing only it with the frozen two-line anchor reconstructs the
  complete pre-Green runner at `9f5ad527...` exactly, line 6812 and the whole
  candidate-publisher region 6800-6816 are byte-unchanged, and the frozen line
  still occurs exactly twice. Receipt:
  `h3-preseal-materialize-json-write-green-acceptance-20260803.md`. The Red test
  body was transcribed by a `reasonix` delegate whose sandboxed bash was refused;
  its diff was independently confirmed purely additive and every gate was re-run
  in-loop. The runner is untracked, so both states are verified by content hash
  only; this does not reconstruct history before `9f5ad527...`. The runner/test
  code remains uncommitted shared R1-v3 work and Phase R1 v3 remains `[~]`; no
  validator/cleanup fault injection, successful publication, the frozen 6812
  candidate-publisher call, other pre-seal/generic variant, candidate path,
  H3/H4/H5/R1-v3 acceptance, runner commit, or
  Podman/candidate/Finance/marker/registry/successor/V2/history action is
  accepted.

  H3 pre-seal staging cleanup masking Red: a private staging cleanup failure must
  never mask, replace, or reclassify the failure already in flight. Composing the
  accepted JSON-write fault with a deterministic non-`FileNotFoundError` `OSError`
  from `shutil.rmtree(staging_parent)` must still raise exactly
  `V3_PODMAN_FAILURE_EVIDENCE_UNPRESERVED: materialize: V3_TEST_PRESEAL_FAILURE_EVIDENCE_JSON_WRITE`,
  retain `json_write_error` as cause with `materialize_error` beneath it, and never
  surface the cleanup error as the raised exception or its cause. Cleanup is
  best-effort: exactly one cleanup attempt is made against the private
  `.failed-attempt-` parent, that residue may remain observable, and no public
  attempt may exist. This pins a real defect rather than adding coverage — the
  outer `finally` currently suppresses only `FileNotFoundError`, so any other
  `OSError` escapes it and destroys the original failure identity, with Python
  setting only `__context__` and not `__cause__`.
  `h3-preseal-staging-cleanup-masking-pre-green-baseline-20260803.md` freezes the
  authorized block at SHA-256
  `2a8c54e5c219359c53ad5282411916b2708c45c91e351c8c4d12e2966ed604dc` (unique in the
  runner); future Green may only widen that block's caught exception to `OSError`.
  Acceptance must prove the bare `except FileNotFoundError:` count falls 3 to 2 and
  both frozen `final_directory` cleanup blocks are byte-unchanged.

## Phase R2: Close or compensate graph coverage gaps

- [x] Task: Execute and record the documented clean-audit/configuration attempt, including `repo-graph config`, scan options, raw audit JSON, stdout/stderr, and exits; select the clean branch only for audit exit `0` with empty unaudited and integrity sets. The v2 candidate binds `r2-clean-audit-attempt-v2-20260801/attempt.json` to the fresh R1 bundle and graph binding; it retained the raw exit and selected the `COMPENSATION_REQUIRED` non-clean branch when unaudited symbols remained. Bounded technical acceptance is recorded by Terra and Sol in the v2 review receipts; commit `772839f` binds the evidence. This is not R2 phase acceptance or an unblock. (deferred:phase-r1-bound-graph)
- [x] Task: If the clean branch is unavailable, generate the exact unaudited route/field denominator, reconcile every node to frozen source anchors and digests, and prove two unchanged-input full scans have identical normalized file/route/field inventories. The v2 evidence preserves the actual audit exit and compensation label. Its producer input is `r1-task2-source-and-graph-v2-20260801/`; output is `r2-task2-compensation-denominator-v2-20260801.json` plus durable `r2-task2-scan-transaction-v2-20260801/` streams. `measure/business_operations_graph_baseline_compensation.py` did not read root `graph.db` or a machine-local database during validation. The strict validator exercised every mutation in `r2-task2-adversarial-fixtures-v1.json`, outside immutable R0 fixtures, through `measure/tests/test_business_operations_graph_baseline_r2_compensation.py`. Terra PASS and Sol bounded ACCEPT are recorded in the v2 review receipts; commit `772839f` binds the evidence. No R2 Task 3/4 acceptance or parent/successor gate change is claimed. (deferred:phase-r2-clean-decision)
- [b] Task: After independent v3 execution-closure acceptance, produce and test the Accounts unaudited-route security matrix, covering the complete discovered set and explicit authentication, permission ownership, validation, tenant/global scope, audit, and destructive-effect dispositions; run the Accounts and backend test/type gates from FR4. (deferred:r1-r2-v3-execution-closure-acceptance)
- [b] Task: After independent v3 execution-closure acceptance, compute the snapshot TypeScript-minus-graph denominator and resolve every excluded file by graph-safe inclusion or explicit source-anchor/type/test compensation; run the backend, Advantage Play Kit, and Advantage Games commands from FR5 without modifying failed unrelated APK code. (deferred:r1-r2-v3-execution-closure-acceptance)
- [b] Task: Apply the FR6 decision rule: create and record one minimal `bodangren/repo-graph` issue only if a verified tool limitation forces the compensation branch; otherwise record why no upstream issue is required. Test both decision outcomes. (deferred:phase-r2-clean-and-exclusion-decisions)

## Phase R3: Verify, review, and control unblocking

- [b] Task: Run the focused contract/counterexample suite, all required package gates, graph stats/audit/probes, and a final snapshot drift check; publish a candidate evidence manifest whose hashes cover every input and result without claiming acceptance. (deferred:phase-r2-complete)
- [b] Task: Perform a producer self-audit against RB-01/RB-02/RB-03 and anti-patterns A5/A6/A8/A10/A12/A15/A16; confirm only `[~]`, `[x]`, and `[b]` markers occur in this track and that successor blockers remain unchanged. (deferred:phase-r3-candidate)
- [b] Task: Obtain an independent acceptance review from a role that did not produce the candidate. The reviewer must independently rerun digest/denominator/decision checks, report severity-ranked findings, and emit a machine-readable `ACCEPT` or `REJECT`; any Critical/High finding keeps this task and both successor gates blocked. (deferred:phase-r3-candidate-self-audit)
- [b] Task: Complete Measure manual verification for the evidence replay and reviewer decision, with explicit owner confirmation; do not treat owner confirmation as a substitute for independent technical acceptance. (deferred:phase-r3-independent-acceptance)
- [b] Task: Only after independent `ACCEPT` and owner confirmation, publish a hash-bound handoff and update the parent Admin Phase 0 and CRM dependency markers in their owning plans/registry. If either prerequisite is absent, make no unblock edit and record the blocked disposition. (deferred:phase-r3-independent-acceptance-and-owner-confirmation)

## Completion Rule

The R0 validator/contract remediation is complete only within the acceptance
boundary recorded above. R1 Tasks 1 through 3 are complete only within their
bounded producer, stable-window, and scan-binding evidence scopes. The v2
recapture and R2 Tasks 1-2 are `[x]` only in their bounded technical evidence
scope: `772839f` and the Terra/Sol receipts bind that conclusion. R2 Tasks 3-5
and all R3 tasks remain `[b]` because no later-phase evidence or authorization
is recorded here.
No Admin S1 or CRM work may begin from producer or R0 evidence alone, and all
parent/successor gates remain blocked until the later hash-bound handoff,
independent R3 acceptance, and owner confirmation.
