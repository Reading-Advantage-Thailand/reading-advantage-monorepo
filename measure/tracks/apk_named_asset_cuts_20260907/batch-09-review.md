# Batch 9 corrected review

The owner approved this corrected batch. The owner requested six sheets per review.

- S209–S210 and their seasonal copies now have separate water boundaries.
- S330 contains 15 cliff components. S331 contains 14 components. The other seasons use the same boundaries.
- S332 now has separate terrain borders, fill tiles, and corner tiles. The source appears to contain six styles.
- I128 now contains the complete pale trunk.
- I129a shows the separate canopy tile. I129b shows the canopy tile with the pale trunk below it.
- I329 contains the complete small snow-colored leaf. Its source shape is isolated.
- F174–F178 now contain separate tiles. The bridge cuts retain their lower supports.

All changed boxes stay within their source sheets. The proposals cover every visible pixel.
Direct pixel comparisons confirm each reuse. Original review numbers remain stable. Split parts use letter suffixes.

The corrected preview is `packages/advantage-play-kit/assets/review/mixed-sheets-batch-09.html?changes=1`.
All 1,534 approved crops are exported. Exact source pixel checks and both library tests passed.
The receipt contains 47,974 assets. The native queue contains 75 sheets that await review or completion.

The builders in `/tmp/build-apk-batch09.py` and `/tmp/finalize-apk-batch09.py` predate these corrections.
Use the current JSON files for approval and cropping.
