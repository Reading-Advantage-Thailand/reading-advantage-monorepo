# Daily Review: wave1_high_risk_product_failures_20260628

**Review date:** 2026-07-02
**Commits reviewed (last 24h):**
- `5014c505` chore(measure): wave1 p3 acceptance
- `c84ca30d` chore(measure): fix p3 webhook test count
- `5d1ec165` fix(codecamp): wave1 phase3 review-b security
- `f83da8f5` fix(codecamp): wave1 phase3 tenantdb/webhook-idempotency/ack/streaming green
- `0c3df9ca` test(codecamp): fix phase3 webhook red tests
- `a5f8799f` test(codecamp): wave1 phase3 red tests
- `7ff3a8f8` chore(measure): wave1 phase2 acceptance
- `fd44af53` fix(reading-advantage): wave1 p2 security
- `d40a189e` fix(reading-advantage): wave1 phase2 green
- `9f49dbc1` test(reading-advantage): phase2 red tests
- `591b1cc1` fix(primary-advantage): session crash fixes
- `ef98f37a` fix(reading-advantage): lint fixes

## 1. Summary of changes

Wave 1 advanced substantially in the last 24 hours:

- **Phase 2 (Reading Advantage) completed and accepted.** Added destructive-operation auth/tenant checks, audit-event wiring, XP idempotency, level-test assessment JSON validation, and AI content-level gating. Targeted gate 29/29 tests pass.
- **Phase 3 (CodeCamp Runtime Reliability) completed and accepted.** Eliminated `TenantScopeError` on REFERENTIAL tables via greppable `unscoped("reason")` calls, implemented delivery-ID webhook idempotency, decoupled webhook ACK from LLM review, and fixed chat streaming `text/event-stream` framing. Review B found and fixed one High-severity cache-poisoning issue.
- **Primary Advantage session crash fixes** landed under Wave 1, aligning `useSession()` destructuring in game-completion components.
- **Reading Advantage lint fixes** resolved `no-var` errors introduced by new Jest-scaffolded tests via a scoped ESLint override.

Phase 4 (Sales Security/Privacy/Contract Hardening) remains untouched.

## 2. Spec alignment

**Partial / on track.**

- **Met:** Reading Critical security/audit/XP idempotency/AI validation (Wave 1 Reading scope) and CodeCamp TenantDB/webhook/streaming reliability (Wave 1 CodeCamp scope) are proven with behavior tests.
- **Partial:** Primary session crash fixes address M1, but the broader Primary stabilization slice (admin CRUD, flashcard schema, dashboard truth) was completed earlier and not touched today.
- **Missed / not started:** Sales Advantage Phase 4 is entirely unopened — role/auth context, IDOR, audio validation, privacy/consent, XSS, draft leakage, and `audioStorageKey` contracts still await Red tests and implementation.

## 3. Code quality observations

- **Test-first discipline held.** Every completed phase has Red tests committed before Green implementation.
- **Review-B hardening was constructive.** The invalid-timestamp delivery-ID cache-poisoning finding (`5d1ec165`) was fixed with an additional test, not by weakening assertions.
- **Scoped lint override is pragmatic.** The `__tests__/**/*.test.{ts,tsx}` `no-var: off` override in `apps/reading-advantage/eslint.config.mjs` matches the legacy `__test__/` pattern and avoids Jest hoisting TDZ issues.
- **Deferred items are explicit.** Postgres-backed webhook retry/DLQ and broader Primary/Reading propagation are honestly deferred to Wave 4 / Wave 6 with cross-references to the coverage matrix.

## 4. Risks or blockers

- **Sales Phase 4 is the next critical blocker.** Wave 1 cannot claim success without Sales role/auth, IDOR, audio validation, and privacy hardening.
- **Aggregate test gates remain red for pre-existing reasons.** `reading-advantage` full Jest run still has 29 failed suites (next-intl ESM issues) and `reading-advantage#check-types` OOMs; `codecamp-advantage` prod-smoke tests fail on live network. These are documented as pre-existing, but they block a clean aggregate closeout.
- **Process-local webhook dedup Sets are accepted technical debt.** Review B documented two Low-severity limitations (unbounded Set growth, 500 failures suppress retries) deferred to Wave 4.

## 5. Recommended next actions

1. **Open Wave 1 Phase 4 (Sales).** Write Red tests for `SALES_REP`/`SALES_ADMIN` auth context, IDOR on roleplay evaluation/reporting, audio size/MIME/duration validation, privacy/consent checks, and `audioStorageKey` nullability contracts.
2. **Re-run Wave 1 Phase 5 integrated acceptance** only after Phase 4 is green.
3. **Keep pre-existing aggregate failures documented;** do not treat them as Wave 1 regressions, but ensure they are owned by Wave 2/Wave 4 infrastructure tracks.
