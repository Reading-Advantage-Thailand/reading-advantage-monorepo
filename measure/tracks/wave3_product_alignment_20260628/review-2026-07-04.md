# Wave 3 — Product-Facing Truth and Reusable Surfaces: Daily Review 2026-07-04

**Track:** `wave3_product_alignment_20260628`  
**Review period:** last 24 hours (2026-07-03 → 2026-07-04)  
**Reviewer:** automated git review

---

## 1. Summary of changes

Fourteen commits in the last 24 hours carry `track_id: wave3_product_alignment_20260628`. All product work is **Phase 2 — Marketing App Public Workflow Security** closeout; the remaining two commits are orchestrator/docs hygiene.

Key commits:

| SHA | Message | What changed |
|-----|---------|--------------|
| `6a315351` | docs(measure): wave3 p2 marketing security strategy | Added 371-line Phase 2 test strategy. |
| `81d104e2` | test(marketing): add Red security tests | Added 5 new Red test files for auth, Zod validation, and AI-adapter boundaries. |
| `4a6bb1d8` | fix(marketing): add auth/Zod guards to public routes | Guarded 8 marketing routes; added 5 Zod schemas; added secret masking and AI-adapter routing. |
| `4e552658` | test(marketing): wire auth mocks; pin test isolation | Wired `authedRequest` helper into existing tests; disabled Vitest `fileParallelism`. |
| `cc120ede` | test(marketing): drop unused ts-expect-error | Removed stale test suppressions. |
| `11c81905` | docs(measure): mark wave3 phase 2 green tasks complete | Updated plan markers. |
| `2a65554b` | fix(marketing): close V1/V4/V5 security gaps in Phase 2 | Added auth to `test-connection`, `.strict()` schemas, generic 500 messages. |
| `2cab576d` | fix(marketing): redirect to login on 401 in workflow UI | Frontend 401 handling for campaigns/settings pages. |
| `595fefd9` | test(marketing): stabilize suite for Phase 2 gates | Stabilized adversarial and video-page tests. |
| `289bc349` | chore(marketing): remove unused catch bindings | Minor lint-related cleanup. |
| `3d123fc6` | docs(measure): add Phase 2 Review-B audit result | Committed `review-b-result.json`. |
| `542e8605` | chore(measure): wave3 phase 2 acceptance pass | Added `audit/phase-2-acceptance.json`; updated `review-a-result.json` and plan. |
| `29b2f8d8` | chore(measure): orchestrator audit + marker cleanup | Cross-track marker hygiene; updated this track's plan. |
| `1daadf0b` | chore(measure): merge orchestrator run 2026-07-03 | Large merge commit; no new Wave 3 product scope. |

**Phases 0, 1, 3, 4, 5, and 6 saw no committed work.**

---

## 2. Spec alignment

### Met

- **Phase 2: Marketing App Public Workflow Security**
  - Auth guards on settings, campaigns, and video routes before DB/AI side effects.
  - Zod validation on settings, campaigns, topics, and script-generation inputs.
  - AI calls routed through the shared `@reading-advantage/ai` adapter (`createAIClient`).
  - API key/token leakage prevented via masking in `GET /api/settings` and `redactSecrets()` in AI error paths.
  - Acceptance evidence: `audit/phase-2-acceptance.json` reports **status: pass**; marketing suite **202/202**; lint **0 errors**; type-check **0 errors**; build **exit 0**; Reviews A/B/C all pass.

### Partial / Not started

- **Phase 0: Product Decision Intake** — still `[~]`; PO decisions for website claims and games import policy are unresolved.
- **Phase 1: Website Claims Correction** — no committed changes.
- **Phase 3: Advantage Games Completion/Scoring Contract** — no committed changes.
- **Phase 4: Tenant-Safe Persistence and Leaderboards** — no committed changes.
- **Phase 5: Embeddable Runtime, i18n, Shared Package** — no committed changes.
- **Phase 6: Product Acceptance and Closeout** — not reachable until earlier phases are done.

**Overall alignment verdict: PARTIAL** — Phase 2 is complete and accepted; the rest of the wave is untouched.

---

## 3. Code quality observations

- **Positive:** Route handlers are now thin orchestration (`requireMarketingSession` → schema validation → persistence/query). Auth is delegated to the shared `@reading-advantage/auth` package rather than reinvented.
- **Positive:** New Zod schemas use `.strict()` and size bounds (e.g., generate-script topic capped at ~50 KB), preventing unknown-field injection and oversized inputs.
- **Positive:** Secret handling is defensible: `GET /api/settings` never decrypts and masks secret-shaped keys; AI error paths call `redactSecrets()`.
- **Positive:** Tests include positive controls (authed requests return 200) and side-effect-absence assertions (DB/AI not called on 401/400 paths), guarding against vacuous passes.
- **Residual:** 18 pre-existing `@typescript-eslint/no-unused-vars` lint warnings remain (exit 0, non-blocking).
- **Residual:** Review-C noted intermittent mock-isolation flakiness on two tests; acceptance re-run passed 202/202, but the root cause (shared dbMock singleton coverage) is not fully eliminated.
- **Resolved:** Review-A's deferred `test-connection` auth gap was closed in Review-B (`2a65554b`).

---

## 4. Risks or blockers

- **Product-owner blockers for the rest of the wave:**
  - Website claims evidence matrix (Phase 0/1).
  - Approved AI provider/model claims.
  - Advantage Games import policy (standalone-only, pilot, or full import).
  - Approved, evidence-backed efficacy stats and case studies.
- **Tenant scoping limitation:** Marketing DB tables are classified as `REFERENTIAL` and have no `schoolId`/`ownerId` columns. Multi-school scoping cannot be enforced beyond authentication until the PO approves schema changes.
- **Untracked review artifacts:** `review-a-result.json` and `measure/audit-reports/wave3-phase2-ux-api-review/result.json` are in the working tree but not committed, risking loss of audit evidence.
- **Cosmetic record drift:** Review-C records the 401 body as `{message:'Unauthorized'}` while the code returns `{message:'Authentication required'}`. Tests assert status only, so no contract break.
- **Scope creep risk:** The `1daadf0b` merge commit touched 490 files across many tracks; any future Wave 3 work should stay scoped to avoid mixing unrelated changes.

---

## 5. Recommended next actions

1. **Resolve `[NEEDS-PO]` items** and begin Phase 0/1 (website-claims correction).
2. **Commit untracked review artifacts** (`review-a-result.json`, `wave3-phase2-ux-api-review/result.json`) for durable audit evidence.
3. **Stabilize mock isolation:** extend the shared `dbMock` singleton pattern to `phase-w3-video-auth` to eliminate residual flakiness.
4. **Schedule Phase 3** (Advantage Games completion/scoring contract) only after Wave 0 tenant/contracts and Games contract work are green.
5. **Re-run the full marketing suite** if any further Phase 2 touch-ups occur, to keep the 202/202 gate green.

---

*Report generated from git history and track artifacts on 2026-07-04.*
