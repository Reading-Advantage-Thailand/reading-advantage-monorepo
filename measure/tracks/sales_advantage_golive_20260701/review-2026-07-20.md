I have everything needed. Here's the review report.

---

# Track Review: `sales_advantage_golive_20260701` — last 24 hours

## 1. Summary of changes

17 commits in the window (2026-07-19 05:11 → 22:35 local) belong to this track. They fall into four threads:

**App fixes surfaced by go-live verification**
- `6153894d` — managed-PostgreSQL role provisioning SQL + docs (Cloud SQL runtime role).
- `0526acd1` — lesson content now renders GFM tables via `remark-gfm`; still `skipHtml` (safe rendering).
- `d7769b35` — new `admin/layout.tsx` server-side gate: non-`SALES_ADMIN` principals are redirected off every admin screen (with a 73-line test).
- `78c188cf` — real bug fix in the tRPC middleware: `productScope` was assigned `parsedScope.data`, but `requireCompatibleSalesScope` already returns the parsed data, so downstream consumers were getting `undefined`. Corrected with a regression test.
- `36cd5369` — dashboard now shows actual completed/total lesson progress instead of the hardcoded "no progress" string.

**Release staging & verification tooling** (`273ce197`, `54709e7c`, `84dc5f69`, `36cd5369`, `58166639`) — `/api/health` and `/api/ready` endpoints, expanded `cloudbuild.yaml`, `.env.example` completion, release-source manifest generation and verification (`release-source-manifest.mjs`, `verify-sales-release-inputs.mjs`), legacy-rollback verification scripts, curriculum reconciliation logic in `static-seed.ts` (+DB-backed tests), and an 828-line pinned `predecessor-production.json`.

**Curriculum approval chain** (`6b67755e`, `dda49d85`, `597241de`) — owner-approved deterministic graph digest, archive script, approval records.

**Production continuation** (`2a24a654`, `2c96be94`, `ec043069`, `50e9bcc5`, `c91f32ff`) — a one-use, manifest-bound Cloud Build config (`cloudbuild.continue-f5063222.yaml`) that resumed failed release `f5063222` without replaying completed mutations. First attempt safely stopped on a Secret Manager metadata-permission issue; the fix removed only the redundant metadata lookup. Final build `342cdc52` completed all 15 steps and promoted revision `sales-advantage-00005-yas` to 100% traffic on `https://sales.reading-advantage.com`, with verified rollback `00004-jed` retained.

## 2. Spec alignment

| Acceptance criterion | Status |
|---|---|
| Wave 1 security merged & verified at HEAD | **Met** (Phase 0 done; the one open XSS finding is now effectively closed — lesson rendering uses `react-markdown` with `skipHtml`, replacing the unsanitized custom renderer) |
| MVP-track reconciliation | **Met** |
| Curriculum generated, reviewed, approved | **Met via a documented supersession** — OpenRouter generation was abandoned for an owner-approved deterministic graph (digest `ccba5498…`, external trust anchor). Plan records the supersession explicitly. |
| Dockerfile builds from monorepo root | **Met** (local docker smoke skipped — no Docker locally — but production Cloud Build succeeded, which is stronger evidence) |
| cloudbuild.yaml: build → migrate → doctor → deploy → secrets | **Met** (builds `b45acc2f`, `342cdc52`) |
| Cloud SQL `sales_advantage` DB + migrations | **Met** (verified by production migration and runtime probes) |
| `.env.example` full runtime surface | **Met** |
| Live on Cloud Run, smoke passes | **Met for unauthenticated + admin-SSO smoke; authenticated rep smoke deferred (human-gated)** |
| End-to-end QA pass | **Missed (open)** — rep journey, roleplay record→submit→eval, quiz threshold, Thai chat streaming, admin flows, i18n toggle, rate-limit 429 all unchecked |
| `deployment-status.md` updated | **Met** |
| `tech-debt.md` records go-live shortcuts | **Missed (open, [~])** |

Notable scope deviations, both defensible: (a) `d7769b35` adds admin-route gating, which brushes against the "no security remediation in this track" non-goal — but it closes a runtime gap found during go-live rather than reassigning a wave-owned finding; (b) the release architecture (deterministic graph, fail-closed seed gates, backup-bound continuation) grew far beyond the original "mirror codecamp" plan. The plan was amended with dated checkpoints each time, so the paper trail is intact.

## 3. Code quality observations

- **Strong test discipline.** Every code commit ships with tests — even 2-line cloudbuild tweaks update `production-readiness.test.ts`. The continuation alone has a 251-line suite covering validator mismatches, forbidden replay steps, and rollback behavior; 27/27 reported passing plus lint/typecheck (reported in evidence, not independently re-run here).
- **The `productScope` fix (`78c188cf`) was a genuine latent bug** — the double-`.data` access would have silently made the validated scope `undefined` for every sales procedure. Good catch, correctly fixed.
- **Fail-closed release engineering is consistently applied**: digest pinning, one-use continuation, drift rejection, trap-protected promotion with automatic rollback restore. The first continuation attempt failing safely (queued mutations untouched, traffic unchanged) is evidence the design works.
- **Churn smell**: three consecutive commits (`54709e7c` → `84dc5f69` → `36cd5369`) iterating on the same cloudbuild dependency-build issue — debugging through Cloud Build submissions. Hard to avoid, but noisy history.
- **One-use artifacts now live in the app directory**: `cloudbuild.continue-f5063222.yaml`, `release-continuation-f5063222.json`, continuation SQL/scripts. These are spent single-purpose files sitting next to evergreen code.

## 4. Risks or blockers

1. **Production is live to users without the full E2E QA pass.** The entire rep-facing critical path (audio record→submit→evaluation, quiz, streaming chat, rate limiting) has never been exercised against prod. This is the main outstanding risk.
2. **Audio-storage privacy verification is open** ([~]): signed-URL-only access and no orphaned keys on failure — a spec acceptance item with privacy implications.
3. **Rollback constraint**: `sales-advantage-00003-v4d` must never receive traffic again (predates the split credential contract). This is documented in the evidence file but is operational knowledge that could be lost; only `00004-jed` is a valid rollback target.
4. **Legacy-auth retirement and the observation window** are flagged open in the continuation evidence — until done, two auth paths coexist.
5. Free-tier eval-model reliability and absent CI/CD trigger are accepted shortcuts but not yet recorded in `tech-debt.md`, so they currently exist only inside this track's documents.

## 5. Recommended next actions

1. **Run the remaining E2E QA** against prod (rep journey, roleplay eval, quiz threshold, Thai chat streaming, admin flows, i18n, rate-limit 429) — it gates everything else.
2. **Verify audio storage privacy** (signed URLs only, orphan-key cleanup on failure).
3. **Write the `tech-debt.md` entries** (audio retention, no auto CI/CD trigger, free-tier model reliability) and any `lessons-learned.md` items — the safe-failure continuation pattern is a strong candidate lesson.
4. **Retire the one-use continuation artifacts** from `apps/sales-advantage/` into the track's evidence directory now that build `342cdc52` consumed them, so nobody re-runs a spent release path.
5. **Record the rollback constraint** (only `00004-jed`; never `00003-v4d`) somewhere operational, e.g. `deployment-status.md`, not just the track evidence file.
6. After QA and the observation window: complete legacy-auth retirement, then **archive this track and the reconciled MVP track** and flip `measure/tracks.md`.
