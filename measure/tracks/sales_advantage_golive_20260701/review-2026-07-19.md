All checks are done — spec and plan read, 12 track commits reviewed, key diffs inspected, and the app's test suite run (green). Here is the report.

# Track review: `sales_advantage_golive_20260701` — last 24 hours

## 1. Summary of changes

12 of the repo's 130 commits in the window belong to this track, in three streams (heavily interleaved with `company_identity_sso_20260715` commits touching the same app):

**Curriculum release pipeline (7 commits: `1d88f040` → `5a87e55c`).** Replaced the original "OpenRouter-generate → admin flips draft to approved" plan with a fail-closed deterministic release pipeline: an immutable curriculum graph in `static-seed.ts`, a manifest (`curriculum/release-candidate.json`) pinning the source corpus at `advantage-pr@8dd78171` with SHA-256 hashes for 11 canonical documents, an independent verification script, a review-artifact generator, and a seed gate that requires graph-bound human-approval JSON plus a reviewer-controlled Secret Manager trust anchor (`SALES_CURRICULUM_APPROVAL_SHA256`). Production seeding currently refuses to run with `SALES_CURRICULUM_HUMAN_APPROVAL_REQUIRED` — by design.

**Database identity/role hardening (`eba902fe`, `867cdec5`).** Hardened migration 0042 (company product principal uniqueness), expanded the runtime role-provisioning/grant/probe SQL for the split `sales_runtime` / `sales_legacy_runtime` credential model, tightened cloudbuild recovery boundaries, and added substantial real-DB test coverage.

**Earlier targeted fixes (`f87e12ba`, `22a4b651`, `e2c0ca55`).** Cloudbuild secret isolation with a guard test; an atomic `ON CONFLICT` upsert for quiz progression; a tRPC type-portability fix.

There are also **uncommitted working-tree changes**: the role-provisioning SQL was adapted for managed PostgreSQL (admins can't alter `SUPERUSER`/`REPLICATION`/`BYPASSRLS`, so the script now verifies those flags are false and fails closed instead), with matching test and runbook updates.

## 2. Spec alignment

| Acceptance criterion | Status |
|---|---|
| Wave 1 security merged & verified | **Met** (one exception: lesson-markdown XSS deferred to wave owner) |
| MVP-track reconciliation | **Met** |
| Dockerfile, cloudbuild, `.env.example`, smoke script | **Met** |
| Cloud SQL DB + migrations, service live on Cloud Run | **Met** — `sales.reading-advantage.com`, revision `sales-advantage-00003-v4d`, smoke + SSO verified per the 2026-07-18 rollout evidence |
| Curriculum approved for launch cohort | **Partial** — pipeline complete but human review of graph `ccba5498…` and the trust anchor are still pending; seed fails closed |
| End-to-end QA pass | **Missed** — the entire Phase 3 QA checklist (roleplay record→submit, quiz, Thai chat, admin flows, i18n, rate-limit 429) is unchecked; audio-privacy verification also open |
| deployment-status / tech-debt updates | **Partial** — deployment-status done; tech-debt, lessons-learned, and archive still open |

Note the spec's curriculum criterion was deliberately superseded (deterministic pinned graph instead of AI generation). The plan documents this honestly, but the spec itself was never amended — the acceptance criterion as written will never be checked off.

## 3. Code quality observations

- **Test discipline is strong.** Every commit pairs code with tests; the app suite passes right now: 23 files / 131 tests passed, 3 files / 8 tests skipped (real-DB opt-ins), ~83s.
- The quiz upsert (`22a4b651`) is a real correctness win: it also fixed a latent bug where a *failing* quiz still marked the lesson `completed`, and the monotonic `CASE`/`GREATEST`/`COALESCE` merge is safe (`score` is a `numeric` column, so `GREATEST` compares numerically).
- Consistent fail-closed philosophy across the seed gate, role provisioning, and the OpenRouter generator (pins the source commit, reads only `git show` bytes, requires exact provider provenance).
- **Churn concern:** `curriculum-release.ts` and its test were rewritten five times between 00:04 and 02:52 — the design was converging in production commits rather than in review. Phase 1 of the plan is now a chain of `superseded` markers plus two long checkpoint blocks; readable but bloated.
- `production-readiness.test.ts` asserts on SQL files via regex/`toContain` on raw text — effective as tripwires but brittle; a wording change breaks tests without a behavior change.
- The daily review artifact `review-2026-07-19.md` is **empty** (with an empty `.err` alongside) — the review automation appears to have failed silently this morning.

## 4. Risks or blockers

1. **Human curriculum approval is the hard blocker.** Nothing rep-facing ships until the reviewer signs graph `ccba5498…` and provisions the Secret Manager anchor.
2. **The live revision is stale relative to the candidate.** The deployed build verified graph `f8b13913…` (26 lessons / 13 quiz questions); the current candidate is `ccba5498…` (27 / 14). What's live does not match what's under review, and the live revision also predates the source-role repair work.
3. **Narrow rollback path.** The rollout doc explicitly invalidates `sales-advantage-00002-7ch` as a post-repair rollback target; the next build must first create a no-traffic `legacy-school` compatibility revision. That gate is defined but has never been exercised — the next deploy is the riskiest step remaining.
4. **Zero production E2E QA** on the core product loops (audio upload/evaluation, streaming chat, rate limiting).
5. **XSS gap still open**: lesson markdown rendered via `dangerouslySetInnerHTML` without sanitization, deferred to the wave owner — mitigated only by content being admin-sourced, which matters more once curriculum goes live.
6. **Uncommitted working-tree changes** (managed-PG role provisioning) must land before the next Cloud Build, or the build will run the old `ALTER ROLE` form that managed Postgres rejects.

## 5. Recommended next actions

1. Commit the working-tree role-provisioning fix — tests are already green against it.
2. Complete the human review of graph `ccba5498…`, fill `curriculum-approval.json`/`.md`, and store the SHA-256 anchor in Secret Manager (this is the single item unblocking everything downstream).
3. Run the next Cloud Build following the documented compatibility-revision gate (tagged no-traffic `legacy-school` revision → source-role repair → `company`-mode deploy), bringing the live service up to the approved graph.
4. Execute the Phase 3 E2E QA checklist against production, including audio signed-URL privacy and orphaned-key verification.
5. Investigate why `review-2026-07-19.md` came out empty and re-run the daily review.
6. Close out Phase 4 (tech-debt entries for audio retention / no CI trigger / free-tier model reliability, lessons-learned, archive both tracks), and either amend the spec's curriculum criterion to match the superseded approach or annotate it as superseded so the acceptance list can actually be satisfied.
