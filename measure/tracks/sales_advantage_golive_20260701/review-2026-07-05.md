# Daily Review — sales_advantage_golive_20260701 — 2026-07-05

## 1. Summary of changes (last 24h)

- **Phase 0 (Preconditions & MVP-track reconciliation)** — Completed.
  - Verified Wave 1 sales security fixes at HEAD: IDOR (`F-SALES-B05-001`), ungated routes (`F-SALES-B00-027`/`-023`), tRPC role-enum (`F-SALES-B00-030`), audio input hardening, privacy notices, and schema-drift/`audioStorageKey` nullability parity.
  - Reconciled `sales_advantage_mvp_20260622`: flipped Phases 3–7 to `[x]` with SHAs, annotated Phase 8 as superseded by this track, and updated `metadata.json`.
  - One finding **not** fully closed at HEAD: lesson markdown sanitization (`F-SALES-B00-011`) — documented as `[b] deferred:wave-owner` because `renderMarkdown` uses `dangerouslySetInnerHTML` without `sanitize-html`/`dompurify`.
- **Phase 1 (Production curriculum seed + human approval)** — Remains `[~]`. No new commits in the period.
- **Phase 2 (Deploy infrastructure)** — Mostly completed.
  - `apps/sales-advantage/Dockerfile` added (Next.js standalone multi-stage, mirrors codecamp).
  - `apps/sales-advantage/cloudbuild.yaml` added with build → push → migrate → doctor → deploy → allow-public-invoker.
  - `apps/sales-advantage/.env.example` expanded to full runtime surface.
  - `apps/sales-advantage/scripts/sales-smoke.sh` added.
  - Cloud SQL `sales_advantage` DB creation and Secret Manager provisioning marked `[b] deferred:human-gated`; Docker build skipped in this environment.
- **Phases 3–4 (Deploy + end-to-end QA + closeout)** — Remain `[~]`.

## 2. Spec alignment

- **Phase 0** — **Partial / Met with exception**. Wave 1 security preconditions are verified and the MVP track is reconciled, but the XSS/markdown-sanitization gap is acknowledged and deferred.
- **Phase 1** — **Missed**. No curriculum seed run (mock or real OpenRouter) and no human approval activity.
- **Phase 2** — **Partial**. All deploy artifacts written, but provisioning and local Docker build are not verified.
- **Phases 3–4** — **Missed**. No deploy or QA activity.

Overall: **Partial** — strong progress on preconditions and deploy infra; curriculum generation and live deploy remain blocked on human/environmental gates.

## 3. Code quality observations

- **Reconciliation is thorough**: `sales_advantage_mvp_20260622/plan.md` now explicitly maps every Phase 8 task to the go-live track, preventing duplicate work.
- **Security verification is honest**: the plan calls out the unresolved markdown-sanitization issue rather than pretending Phase 0 is fully green.
- **Deploy artifacts mirror proven codecamp pattern**: reduces operational risk and review burden.
- **Risk**: the lesson-page XSS gap means deploy is still somewhat irresponsible for content that may later include user input. Mitigation noted: current content is admin-generated curriculum seed.
- **Risk**: curriculum seed has not been run even with `AI_PROVIDER=mock`; idempotency and draft-status assertions are untested outside unit tests.

## 4. Risks or blockers

- **Markdown sanitization (F-SALES-B00-011)** — open XSS surface on lesson pages. Should be fixed in a Wave 4/6 security track before broad rep access.
- **Toolchain gap**: no `node`/`pnpm`/Docker in this environment, so build/test/seed verification could not be re-run.
- **Human-gated provisioning**: Cloud SQL database and Secret Manager entries must be created before deploy.
- **Curriculum content risk**: real OpenRouter generation has not run; cost/quality/time for 6 modules / ~26 lessons is unvalidated.
- **No auto CI/CD trigger**: consistent with codecamp, but deploy remains manual.

## 5. Recommended next actions

1. Fix or schedule the lesson markdown sanitization gap (`F-SALES-B00-011`) before end-to-end QA.
2. Run dry-run curriculum seed with `AI_PROVIDER=mock` against a local DB; confirm rows land as `reviewStatus: 'draft'` and upsert is idempotent.
3. Provision Cloud SQL `sales_advantage` database and Secret Manager entries; mark Phase 2 provisioning `[x]`.
4. Build and boot the sales-advantage Docker image on a networked machine.
5. Run `gcloud builds submit --config apps/sales-advantage/cloudbuild.yaml` and execute `sales-smoke.sh` against the live URL.
6. Execute the full Phase 3 QA checklist (admin create rep → roleplay record/submit/eval → quiz threshold → Thai chat streaming → admin cohort/curriculum approval → i18n toggle → rate-limit 429).
