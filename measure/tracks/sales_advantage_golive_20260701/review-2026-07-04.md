# Track Review: sales_advantage_golive_20260701

**Review date:** 2026-07-03  
**Commits reviewed (last 24 h):** `bd32fba5`  
**Track directory:** `measure/tracks/sales_advantage_golive_20260701`

---

## 1. Summary of changes

A single commit landed in the last 24 hours for this track:

- `bd32fba5` — `chore(sales): add deploy artifacts + reconcile MVP track (track_id: sales_advantage_golive_20260701)`

It delivered:

- **Phase 0 completion:**
  - Verified Wave 1 sales security at HEAD (5/6 items confirmed; lesson XSS sanitization deferred).
  - Reconciled `sales_advantage_mvp_20260622` plan/metadata: Phases 3–7 marked implemented with SHAs and Phase 8 annotated as superseded by this track.
  - Confirmed Wave 2 seed-safety (T6) and adapter-boundary (T4) preconditions.
- **Phase 2 deploy artifacts:**
  - `apps/sales-advantage/Dockerfile` — multi-stage Next.js standalone image (node:22-alpine, pnpm 11.8.0, turbo build, runner on port 8080).
  - `apps/sales-advantage/cloudbuild.yaml` — build → push → migrate → doctor → deploy → allow-public-invoker, with Artifact Registry and Secret Manager wiring.
  - `apps/sales-advantage/.env.example` — full runtime surface documented with comments.
  - `apps/sales-advantage/scripts/sales-smoke.sh` — post-deploy unauthenticated smoke test.
- **Collateral:** created/updated this track's `spec.md`, `plan.md`, `metadata.json`; created `measure/deployment-status.md`; also created the unrelated `marketing_golive_20260701` track files in the same commit.

---

## 2. Spec alignment

**Verdict: partial**

| Spec acceptance item | Status | Notes |
|---|---|---|
| Wave 1 security merged & verified (IDOR, route gating, tRPC role enum) | ✅ Met | Documented with SHAs; one exception below. |
| Lesson markdown XSS sanitization (`F-SALES-B00-011`) | ⚠️ Deferred | Explicitly deferred to wave owner; acknowledged risk. |
| MVP track reconciliation | ✅ Met | Phases 3–7 flipped, Phase 8 superseded. |
| Production curriculum generated & approved | ❌ Not started | Phase 1 tasks remain open. |
| Dockerfile builds production image | ✅ Artifact written | Local docker build skipped (Docker unavailable). |
| cloudbuild.yaml with migrate + doctor + deploy | ✅ Artifact written | Mirrors codecamp pattern. |
| Cloud SQL `sales_advantage` DB provisioned | ⚠️ Deferred | Human-gated provisioning. |
| `.env.example` complete | ✅ Met | Full runtime surface documented. |
| Service live + smoke script passes | ❌ Not started | Phase 3 not started. |
| End-to-end QA pass | ❌ Not started | Phase 3 not started. |
| `deployment-status.md` / `tech-debt.md` updated | ⚠️ Partial | `deployment-status.md` created but still lists sales-advantage as "Not deployed at all"; `tech-debt.md` go-live shortcuts not yet recorded. |

Phase 0 and Phase 2 artifact work are substantially complete. Phase 1, Phase 3, and Phase 4 closeout tasks remain pending on human-gated cloud provisioning and QA.

---

## 3. Code quality observations

- **Good:** Dockerfile follows the established `apps/codecamp-advantage` pattern, uses a non-root `nextjs` user, pins pnpm, disables telemetry, and emits a standalone Next.js image.
- **Good:** `cloudbuild.yaml` is explicit about stages, uses Secret Manager for all runtime secrets, attaches the Cloud SQL instance, and sets `--min-instances=1`.
- **Good:** `.env.example` is comprehensive and corrects the storage-key naming (`STORAGE_ACCESS_KEY` / `STORAGE_SECRET_KEY`).
- **Good:** `sales-smoke.sh` is guarded (`set -euo pipefail`) and clearly separates unauthenticated smoke from deferred authenticated checks.
- **Concern:** `cloudbuild.yaml` uses `node:20-slim` for the migrate/doctor steps while the Dockerfile builds on `node:22-alpine`. This Node version skew is minor but unnecessary; aligning to node:22 would reduce surprise.
- **Concern:** The commit bundles unrelated work (creation of the `marketing_golive_20260701` track and `measure/deployment-status.md`) with the sales track changes. This scope bleed makes the commit harder to review and revert atomically.
- **Concern:** The commit message claims "all test suites pass (sales-advantage 24/24, domain 374/379, api 235/235)". The domain figure (374/379) implies 5 tests did not pass; they may be skipped rather than failing, but the message does not distinguish. Verify independently before treating the gate as green.

---

## 4. Risks or blockers

1. **Security blocker (deferred):** Lesson markdown is rendered with `dangerouslySetInnerHTML` and no sanitization. The risk is partially mitigated because lesson content is admin-generated, but it remains a live XSS surface that must be fixed before full trust in the go-live.
2. **Infrastructure not provisioned:** Cloud SQL database, Secret Manager entries, and the Cloud Run service itself are still human-gated. No actual deployment has occurred.
3. **Curriculum not seeded:** Real OpenRouter generation, spot-check, and approval are unstarted. Reps will see no content until Phase 1 runs.
4. **Unverified build:** The Dockerfile was not built locally (Docker unavailable in the authoring environment) and the cloudbuild pipeline has not been exercised end-to-end.
5. **Test-count ambiguity:** The domain suite's 374/379 result needs clarification (skipped vs. failed) to confirm the pre-deploy gate is truly green.

---

## 5. Recommended next actions

1. **Human sign-off:** Decide whether the deferred lesson-XSS risk is acceptable for a staged deploy or must be fixed first.
2. **Clarify test result:** Confirm whether the 5 domain tests not counted in "374/379" are skipped or failing; fix if failing.
3. **Provision cloud infra:** Create the `sales_advantage` Cloud SQL database and populate Secret Manager with the values documented in `.env.example`.
4. **Run a mock seed dry-run:** Execute `AI_PROVIDER=mock pnpm --filter sales-advantage seed:curriculum` to validate idempotency and draft-state insertion.
5. **Build verification:** Run `docker build -f apps/sales-advantage/Dockerfile .` in an environment with Docker, then `gcloud builds submit --config apps/sales-advantage/cloudbuild.yaml`.
6. **Execute Phase 3:** Deploy to Cloud Run, run `sales-smoke.sh`, and perform the end-to-end QA checklist from the MVP plan Phase 8.
7. **Complete closeout:** Update `measure/deployment-status.md` to show sales-advantage as deployed and append go-live shortcuts to `measure/tech-debt.md`.
