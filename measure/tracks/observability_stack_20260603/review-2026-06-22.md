# Daily Review — observability_stack_20260603 (2026-06-22)

## Summary of Changes

Last 24h completed Phase 9 live-path instrumentation and Phase 10 closeout artifacts:

- **Phase 9 live-path init:** added `apps/science-advantage/instrumentation.ts` at the Next.js-loaded app root; wired `NodeSDK` with `serviceName` and span processors; wrapped `next.config.ts` with `withSentryConfig` so Sentry client/server configs load on the live path; added `Sentry.captureException(error)` in the recommendations route catch block.
- **OTel global cleanup:** simplified `instrumentation.ts` to start `NodeSDK` without mutating OTel globals; updated the live-OTel acceptance test to unwrap `ProxyTracerProvider` and assert the real delegate.
- **Phase 10 closeout:** marked tasks 4–6 `[x]` and produced closeout Red contract tests asserting F-902..F-906 resolution.

## Spec Alignment

**Partial / met.**

- FR-1 (Sentry installation + config): `sentry.client.config.ts` and `sentry.server.config.ts` exist; `withSentryConfig` now ensures they are loaded on the live runtime path. `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` in `.env.example`.
- FR-2 (OpenTelemetry installation): `instrumentation.ts` + `instrumentation.node.ts` register `NodeSDK`; console-exporter fallback when `OTEL_EXPORTER_OTLP_ENDPOINT` is unset.
- FR-3/FR-4 (AsyncLocalStorage + context-aware logger): `lib/observability/context.ts` and `logger.ts` implemented and tested.
- FR-5 (wrap `generateObject` in OTel spans): implemented in recommendation service.
- FR-6 (migrate 5 largest routes): completed; routes wrap handlers in `runWithRequestContext` and use `logger.error`.
- FR-7/FR-8 (ESLint `no-console` + remaining sites): Phase 7 rule added; Phase 8 remaining `console.*` sites were the focus of Phase 10 closeout.
- Track remains `reopened` because the completion audit previously rejected the mock-only closeout; the live-path evidence now exists but the track has not been formally re-closed.

## Code Quality Observations

- Positive: moving `instrumentation.ts` from `lib/` to the app root fixes the Next.js auto-loading issue that caused the original closeout rejection.
- Positive: the 2026-06-22 fix removed the OTel global-mutation hack, replacing it with a cleaner `NodeSDK.start()` path.
- Positive: live-path acceptance tests (3 sentry + 3 otel) pass, and Phase 1–8 regression shows 123 tests green.
- Watch: `instrumentation.ts` contains version-specific compatibility notes (`sdk-node@0.57.2` vs `resources@1.30.1` / `2.x`); if dependencies are upgraded by `dependency_upgrade_hardening_20260607`, this may need re-verification.
- Watch: Phase 7 eslint family was noted as blocked by the pnpm11 migration in earlier attempts; verify `pnpm turbo run lint --filter=science-advantage` now passes after pnpm11 landed.

## Risks or Blockers

- **Closeout status:** `metadata.json` status is still `reopened`. The live-path work is done, but formal closeout (archive, tracks.md reconciliation, final acceptance audit) is pending.
- **ESLint `no-console` enforcement:** the rule exists but the full-app lint gate needs to be re-run to confirm zero `console.log`/`console.info` regressions.
- **Cross-package OTel span wrapping:** `generateObject` spans are wrapped inside `apps/science-advantage/lib/ai/recommendation-service.ts`; ensure this is consistent with any future `packages/ai` provider changes.

## Recommended Next Actions

1. Run `pnpm turbo run lint --filter=science-advantage` and resolve any remaining `no-console` regressions.
2. Update `metadata.json` status from `reopened` → `done` and set `completed_at` once the live-path evidence is accepted.
3. Reconcile `measure/tracks.md` entry to `[x]` and move the track directory to `measure/archive/`.
4. Consider adding a lightweight CI smoke that asserts `instrumentation.ts` is at the app root and Sentry configs are importable, to prevent future regressions.
