# Measure Track Review — observability_stack_20260603
**Review date:** 2026-06-20  
**Review window:** commits since 2026-06-18 attributed to `track_id: observability_stack_20260603`  
**Spec / plan source:** `measure/tracks/observability_stack_20260603/spec.md` and `plan.md`

## Summary of Changes

47 commits landed in the review window, moving the track from Phase 0 setup through Phase 8 (replace remaining `console.*` sites). Key deliverables:

- **Phase 1 (Sentry):** `apps/science-advantage/sentry.client.config.ts`, `sentry.server.config.ts`, `@sentry/nextjs` dependency, `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` in `.env.example`.
- **Phase 2 (OpenTelemetry):** `lib/instrumentation.ts`, `lib/instrumentation.node.ts`, OTel SDK deps, env vars in `.env.example`.
- **Phase 3 (AsyncLocalStorage context):** `lib/observability/context.ts` with `runWithRequestContext` and `setRequestContextUserId`.
- **Phase 4 (logger auto-attaches context):** `lib/observability/logger.ts` emits JSON lines with `requestId`/`userId`/`route`/`method`/`latencyMs`.
- **Phase 5 (5 largest route.ts files):** Wrapped handlers in `runWithRequestContext`; replaced `console.*` catch blocks with `logger.error` in `update-mastery`, `quiz`, `analytics`, `recommendations`, `assignments`.
- **Phase 6 (generateObject spans):** `lib/ai/recommendation-service.ts` wrapped in `tracer.startActiveSpan`; `traceId` now uses active OTel span context.
- **Phase 7 (ESLint no-console):** `apps/science-advantage/eslint.config.mjs` rule + adversarial tests.
- **Phase 8 (remaining console sites):** `components/client-logger.ts`, bulk migration of `console.log/info/warn/error` across `app/`, `components/`, `lib/`, `proxy.ts`; grep gate tests.

Supporting test files were added for every phase and adversarial coverage was added for Phases 4, 6, 7, and 8.

## Spec Alignment

| Spec Requirement | Status | Evidence |
|---|---|---|
| FR-1 Sentry installed & configured | **Met** | Config files exist; DSN env wired; contract tests pass. |
| FR-2 OTel installed & configured | **Met** | `instrumentation.ts` / `instrumentation.node.ts`; SDK deps added. |
| FR-3 AsyncLocalStorage request context | **Met** | `context.ts` + `runWithRequestContext` + `setRequestContextUserId`. |
| FR-4 Logger auto-attaches context | **Met** | JSON log lines carry context; tests pass. |
| FR-5 Wrap `generateObject` in OTel spans | **Met** | `recommendation-service.ts` spans + traceId swap. |
| FR-6 Migrate 5 largest route.ts files | **Met** | All 5 routes wrapped; `logger.error` in catch blocks. |
| FR-7 ESLint no-console rule | **Met** | Rule added with `allow: ['error','warn']`; excludes sink/tests. |
| FR-8 Replace remaining 42 console sites | **Met** | Grep gate returns 0 production `console.log/info`; `clientLogger` created. |
| AC-11 `pnpm turbo run test --filter=science-advantage` | **Partial** | Phase-specific targeted suites pass; full aggregate deferred to Phase 9. |
| AC-12 `pnpm turbo run build --filter=science-advantage` | **Partial** | Pre-existing `child_process` browser-bundle failure noted; not a Sentry/OTel regression. |

**Overall alignment:** **Partial**. Phases 1–8 implementation is materially complete and well-tested, but Phase 9 final acceptance (full test/build/lint gates, Sentry throw-in-route test, OTel route-span test) and Phase 10 closeout (tech-debt update, archive) remain open.

## Code Quality Observations

- **Strong TDD discipline:** Every phase includes intentionally-red contract tests before the implementation commit, with SHA-recorded evidence in `plan.md`.
- **Adversarial coverage is good:** Additional tests close fixture-override and severity-regression gaps for the ESLint rule and logger shape.
- **Host-environment workarounds are documented:** MID handoffs explicitly note `bun`/`vitest.unit.config.ts` substitutions due to missing `pnpm`/Postgres; canonical commands are preserved for CI re-run.
- **Plan verbosity is high but traceable:** Each phase records Red command, fail count, per-task coverage map, and worktree hygiene.
- **Minor issue:** Phase 8 acceptance audit (`46fc963b`) had to add `no-console: off` for `scripts/**` and `vitest.integration.global-setup.ts` after the fact — this is correctly scoped outside FR-8, but it shows the initial lint gate was not fully anticipatory.

## Risks or Blockers

1. **Phase 9 live gates are outstanding.** The Sentry throw-in-route test and OTel route-span test are not yet committed/verified.
2. **Full aggregate gates are unproven locally.** `pnpm turbo run lint --filter=science-advantage` is green after the Phase 8 audit fix, but `pnpm turbo run test|build --filter=science-advantage` are noted as environment-blocked or pre-existing-failure blocked.
3. **`pnpm-lock.yaml` merge-conflict history.** Early Phase 1 MID handoffs had to restore the lockfile from a pre-MID snapshot; this is resolved but flagged as a known stale state in plan notes.
4. **Build failure is pre-existing but still unresolved.** The `child_process` browser bundle error blocks the build gate and is unrelated to this track; unless fixed elsewhere it will block Phase 10 closeout.

## Recommended Next Actions

1. Complete Phase 9 final acceptance:
   - Sentry throw-in-route test asserting `captureException` is called.
   - OTel route-span test asserting `generateObject` span attributes from a route handler.
   - Run full `pnpm turbo run test --filter=science-advantage` and record exit code.
   - Confirm `pnpm turbo run lint --filter=science-advantage` remains green.
   - Re-assess build gate and decide whether to fix the pre-existing `child_process` bundle error or document it as an exclusion.
2. Close out Phase 10:
   - Update `measure/tech-debt.md` to mark F-902–F-906 Resolved.
   - Add lessons-learned entry.
   - Move track to `measure/archive/observability_stack_20260603/` and update `measure/tracks.md`.
3. Re-run canonical commands with `pnpm` (not `bun`) in the dev environment to close host-environment substitutions.
