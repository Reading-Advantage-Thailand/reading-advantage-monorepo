## Review: `reading_remote_font_removal_20260728`

### 1. Summary of changes

Single commit in the window: **`fd7d01dd5` — `chore(reading): remove remote fonts`** (2026-07-28 22:09).

| File | Change |
|---|---|
| `apps/reading-advantage/styles/globals.css` | −4 lines: removed `/* Book Fonts */` comment + two `@import url("https://fonts.googleapis.com/css?family=…")` lines (Indie Flower, Amatic SC) |
| `measure/tracks/reading_remote_font_removal_20260728/{index,spec,plan}.md`, `metadata.json` | +25 lines: track scaffolding, created in the same commit |

`metadata.json` records this as a **recovery track**: *"created to assign a previously uncommitted stylesheet change"* — the CSS edit existed before the track did.

### 2. Spec alignment

**Spec requirement — MET.** `apps/reading-advantage/styles/globals.css` no longer contains any `fonts.googleapis.com` request. Verified downstream safety:

- No source reference to `Indie Flower` / `Amatic SC` anywhere in `apps/reading-advantage` (excluding `.next` build artifacts) — no dangling `font-family` left pointing at a now-unloaded family.
- The only `font-family` declarations remaining in `styles/` are `.font-heading → var(--font-cabin-sketch-bold)` and `.TabsTrigger → inherit`.
- Typography is otherwise served by `next/font` in `app/[locale]/layout.tsx:5-32` (Inter via `next/font/google`, self-hosted at build time) and local `CabinSketch-Regular/Bold.ttf` — so the app has no remaining runtime remote-font dependency in its global path.

**Plan alignment — PARTIAL.** Phase 1 (`[x]` remove imports) is done. Phase 2 — *"Verify the application's intended typography after deployment-preview review"* — is still `[ ]`, and `review-2026-07-29.md` in the track directory is **0 bytes**.

### 3. Code quality observations

- Diff is minimal and surgical; the section comment was removed with the imports, leaving no orphan.
- Correct fix direction: `@import url()` of a remote stylesheet inside a Tailwind v4 entry is a render-blocking runtime fetch; removing it is strictly better than replacing it, given nothing consumed those families.
- **Commit message omits the track trailer.** Every other commit in the 24h window carries `(track_id: …)`; this one does not. Since the measure supervisor scans by track association, this commit is not machine-traceable to its track.
- Mixing the app change and the track scaffolding in one commit is acceptable for a recovery track, but it means the spec/plan were authored *after* the code — the spec reads as a description of the diff rather than a driver of it.

### 4. Risks or blockers

| Severity | Issue |
|---|---|
| Medium | **Track is not closed out.** `metadata.json` still `"status": "in_progress"`, `"actual_tasks": null`, `updated_at` stale at `2026-07-28`. |
| Medium | **Phase 2 is an unactionable gate.** "Verify … after deployment-preview review" names no owner, no preview URL, and no pass criteria — this is the pattern your *no-deferring-blockers* rule exists to prevent. As written it can sit unchecked indefinitely. |
| Low | **Traceability gap** from the missing `track_id:` trailer (see above). |
| Low | **Adjacent remote-font surfaces remain in the same app** — `apps/reading-advantage/data/workbook-template/workbook_template.html:9` still imports Merriweather/Open Sans/Caveat from Google. Strictly out of this spec's scope ("global stylesheet"), but if the motivation was CSP/privacy/offline rather than just dead code, the track under-covers it. Same applies to `apps/primary-advantage/styles/globals.css:1`. |
| Low | Empty untracked `review-2026-07-29.md` alongside three sibling track review files — either fill or remove before it gets committed blank. |

Typography-regression risk itself is **low**: no code path referenced the removed families, so visual output should be unchanged.

### 5. Recommended next actions

1. **Rewrite plan phase 2 as an actionable check**, e.g. *"Load `/[locale]/` and a book-reader route in the deployment preview; confirm headings render Cabin Sketch Bold and body renders Inter, and DevTools Network shows zero `fonts.googleapis.com` requests."* Then execute and tick it.
2. **Close out metadata** — `status: completed`, `actual_tasks: 2`, bump `updated_at`, once phase 2 passes.
3. **Restore traceability** for `fd7d01dd5` — either amend/note the `track_id` (if the commit is unpushed) or record the mapping in `deviation_notes`, so the supervisor's scan doesn't show an orphan commit.
4. **Decide scope on the remaining Google-font fetches** (`workbook_template.html`, `primary-advantage/globals.css`). If the driver was policy rather than cleanup, open a follow-up track; if it was dead-code removal, add a one-line note to the spec saying those are intentionally retained.
5. Fill or delete the empty `review-2026-07-29.md`.
