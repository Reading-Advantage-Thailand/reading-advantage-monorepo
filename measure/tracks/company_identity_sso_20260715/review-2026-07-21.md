# Review: `company_identity_sso_20260715` — last 24 hours

## 1. Summary of changes

All 16 track commits in the window (2026-07-20, 06:57–08:14 +0700) are documentation and ledger changes under `measure/` — **zero product code** was committed for this track in the last 24 hours (nothing in `apps/accounts`, `packages/domain/company-sso`, or any app). The Codecamp cutover itself was executed operationally (Cloud Build `38c3f0c0`, revision `codecamp-advantage-00020-hay` at 100% traffic) from previously committed code. The commits fall into four groups:

- **Plan backfill** (`c30dc59c`…`88796415`): five commits rewriting S2–S7 checkboxes in `plan.md` from unchecked to checked, with prose evidence pointers, under a new "Execution reconciliation" table admitting the checklist had drifted from reality.
- **S7 evidence publications** (`99e7c08d`, `09194c15`, `8be2e445`, `122d925c`, `17d72dd4`, `abb64fc5`): production topology, secret inventory, migration evidence, OIDC client registry, and observation contract documents, then closing Task 49's publication sub-items.
- **Closure audit** (`39609f1a`): a 294-line `closure-state-20260719.md` scoring the track at ~82% complete, listing external-operator blockers and a 12-item "cannot archive yet" checklist.
- **Cutover closeout** (`0ba05278`, `10035369`, `2cba03ff`): Codecamp postflight record (Task 47 marked done except observation), metadata flipped to `archive-pending` with `actual_tasks: 47`, and the `tracks.md` registry row flipped to `[x]`.

Related: `235cc24f` records a Sales live-QA blocker (`SESSION_INVALID`, missing QA credentials) in the `sales_advantage_golive` track.

## 2. Spec alignment

| Story | Verdict | Notes |
|---|---|---|
| S1 identity boundary | Partial | Runtime complete; Task 6 (adapter boundary/allowlists/ratchet tests) still `[~]`, Task 7 doc/doctor/graph unchecked. |
| S2 employee SSO | Met (runtime) | Deployed and production-verified; Task 14 protocol docs unchecked. |
| S3 employee/role admin | Met (runtime) | Production-verified incl. last-admin protection; Task 20 docs unchecked. |
| S4 Marketing | Met | `marketing-00013-jil` live, role isolation verified; Task 25 docs unchecked. |
| S5 Sales | Partial | SSO live at 100%, but feature QA is **blocked** (SESSION_INVALID / no QA credentials). |
| S6 Codecamp migration | Met | 5 accounts migrated, product ownership preserved (155 progress rows, 24 reviews, 3 chats), idempotency and preservation tested against real PostgreSQL. |
| S7 cutover & verify | Partial | Codecamp deployed at 100% with public-domain checks — but the observation window has not run, legacy-auth retirement (Task 48) is unstarted, and the independent security/migration review is pending. |

Spec-level gaps: the spec's "agreed observation window → explicit approval → legacy retirement" sequence is correctly *not* short-circuited in code, but the track's own status markers get ahead of it (see below). All seven manual-verification tasks remain unchecked.

## 3. Code quality observations

- **The closure-state document is genuinely good auditing.** It catches real defects in the cutover runbook: `curl -sf` incompatible with expected 401/403/409 checks, a non-deterministic revision selector, and wrong table names (`company_user` vs. actual `company_accounts`) in the verification SQL.
- **Status inflation vs. the track's own evidence.** `tracks.md` marks the track `[x]` and metadata says `archive-pending`, while the closure-state written an hour earlier has 12 unchecked archive-gate items (observation window, retirement approval, Sales QA, doc/graph/doctor gates, independent review). The Codecamp deploy resolved only 3–4 of those.
- **Mis-scoped commit `ca423fbb`.** Its message says "record Codecamp SSO cutover postflight" but the diff only touches APK-corpus-audit evidence receipts; the actual postflight doc landed in `0ba05278` under a "close task ledger" message. This corrupts traceability for both tracks.
- **Bulk checkbox backfill weakens auditability.** The five backfill commits flip ~150 task boxes with narrative claims ("covered by scoped tests") rather than per-task commit hashes, despite the plan's own rule that evidence be commit-level. The reconciliation table honestly discloses this, but the boxes now look like primary evidence.
- **Metadata still unreconciled**: `actual_tasks: 47` vs. the plan's 49 numbered tasks vs. the stale estimate of 56 — the exact inconsistency closure-state told the owner to fix.

## 4. Risks or blockers

1. **Observation window never ran.** Codecamp went to 100% traffic with no recorded 30-minute four-app observation or operator signoff — the track's own acceptance contract for cutover.
2. **Rollback-target contradiction.** Closure-state (item 8) says after source-role repair the old anchor `codecamp-advantage-00019-682` "must not be used" and only a post-repair `legacy-rollback` revision is valid; the postflight's documented rollback command targets `00019-682` anyway. An incident rollback following the postflight doc could restore a broken auth state.
3. **Sales feature QA blocked** (SESSION_INVALID; QA credentials unprovisioned) — the S5 acceptance surface is unverified for authenticated journeys.
4. **Independent security/migration/change-quality review not done** — a material gap for a new identity provider handling credentials and sessions.
5. **Archive-pending status risks orphaning open work** — Tasks 6, 7, 14, 20, 25, 32, 48, part of 49, and all manual verifications are open; your Measure convention is to encode these as actionable phases, not defer them.

## 5. Recommended next actions

1. **Reconcile status honestly**: either revert `tracks.md` to `[~]`, or split the ~12 outstanding items into a follow-up closeout track before archiving — per the "no deferring blockers" rule.
2. **Run the observation window now** (Codecamp is already at 100%; every hour without it is unobserved exposure) and record operator signoff, then proceed to Task 48 retirement only on explicit approval.
3. **Fix the postflight rollback command** to reference the post-repair rollback policy from closure-state item 8, so an on-call operator can't restore an invalid revision.
4. **Provision Sales QA credentials** and complete the blocked S5 feature probes.
5. **Schedule the independent security/migration review** (Task 49's last sub-item) — highest-value remaining verification for an auth cutover.
6. **Clean the ledger**: reconcile `metadata.json` task counts (47/49/56), and add a corrective note for the mis-scoped `ca423fbb` commit so both tracks' histories are traceable.
