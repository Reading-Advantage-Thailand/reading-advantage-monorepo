Review complete. I examined all 14 commits from the last 24 hours matching this track, the spec, the plan (including its execution-reconciliation ledger), and the key diffs (migration engine, cutover switch, proxy, fix commits).

# Track review: `company_identity_sso_20260715` — last 24 hours

## 1. Summary of changes

Twelve commits belong to this track (two others, `fcdb967c`/`dd958148`, only reference it while closing out other tracks' plans). All work targets **Phase S6 (Codecamp migration)** and **Phase S7 (production cutover)**:

- **`507ca16f` feat(codecamp): migrate accounts into company SSO** — the centerpiece (~16k lines incl. a generated Drizzle snapshot). Adds the Codecamp identity-migration engine: a pure planning module (deterministic mappings, credential-algorithm classification, collision detection, secret-safe summaries) plus a PostgreSQL adapter with dry-run/apply modes, source fingerprinting, an apply-confirmation phrase, serializable isolation, an advisory lock, and a pinned SHA-256 of migration `0043_codecamp_company_principal_sync`. Includes real-PostgreSQL tests. Production migration of the five legacy accounts is recorded against this commit.
- **`e9c4113d` / `73198890`** — two follow-up correctness fixes to the migration adapter: a missing `await` on the transaction inside a `try/finally` (the `finally` could close connections while the transaction was in flight) and a cleanup guard so a synchronous throw from `sql.end()` can't mask the real error. Both ship regression tests.
- **`6dc29e5f` / `5ac38c53` / `243f0b28`** — two `cloudbuild.yaml` secret-binding corrections for the Codecamp runtime, each ratcheted into `deploy-gate-contract` tests, plus a production-readiness test asserting the valid Codecamp secret binding.
- **`d414fcc6` feat(codecamp): bound SSO cutover** — the app-side cutover: `CODECAMP_AUTH_MODE` switch (defaults to `company`, fail-closed on invalid values), legacy login/logout/reset routes gated behind explicit legacy mode (company mode returns a 409 Accounts handoff), mode-aware proxy/tRPC/session routing, an `auth-entry` component, a rollback runbook, and a no-traffic `sso-candidate` Cloud Build step. ~1,170 lines, roughly half tests.
- **`4b9fff88` feat(accounts): add release observability gates** — structured telemetry module for Accounts, health/readiness endpoints distinguishing process vs. identity-database readiness, and expanded capability tests.
- **`174ca67a`** — classifies principal-mapping conflicts by SQLSTATE instead of message matching in the domain package.
- **`748a8931` / `c5de9d63` / `514e8924`** — Measure ledger updates: Task 47 sub-boxes backfilled, rollout checkpoint evidence, deployment-status refresh (Sales now at company-SSO revision `00005-yas` at 100%).

## 2. Spec alignment

| Area | Status | Notes |
|---|---|---|
| S6 identity migration (preflight, dry-run, collisions fail closed, idempotency, real-PG tests) | **Met** | Engine verifies source DB identity, ledger, tables, fingerprint; refuses apply on fingerprint drift; unsupported hash algorithms fail closed; 5 accounts migrated with unchanged product ownership (155 progress rows, 24 reviews, 3 chats). |
| S6 Codecamp SSO integration (sign-in via company identity) | **Partial** | Cutover code is built and tested but not deployed — blocked on an IAM grant (see risks). |
| S7 staged cutover (Marketing → Sales → Codecamp) | **Partial** | Marketing and Sales live and verified; Sales advanced to 100% today. Codecamp deploy, observation window, and legacy retirement (Task 48) remain open. |
| NFR: legacy-auth recoverable, no dual credential writers | **Met** | `CODECAMP_AUTH_MODE` defaults to company; legacy credential paths only activate on explicit operator opt-in; rollback anchor `codecamp-advantage-00019-682` pinned. |
| NFR: observability (health vs. identity-DB readiness, structured logs) | **Met** | `4b9fff88` delivers exactly the readiness distinction and telemetry the spec requires. |
| NFR: secret-safe audit / no credential material in outputs | **Met** | Plan summaries aggregate algorithm counts only; audit rows immutable. |
| NFR: explicit database-identifying secrets | **Met** | Secret-binding mistakes were caught and converted into deploy-gate contract tests. |

## 3. Code quality observations

- **Strong test discipline.** Every fix commit, however small, carries a regression test, and both infrastructure mistakes (secret bindings) were ratcheted into permanent deploy-gate tests rather than just patched.
- **The migration engine is defensively built**: safe-identifier regex, pinned migration checksum, advisory lock, serializable isolation, explicit confirmation phrase, and fingerprint-match requirement between dry-run and apply.
- **One notable timeline wrinkle**: the production migration is recorded at `507ca16f` (15:31), but the unawaited-transaction race was fixed at 16:00 (`e9c4113d`). The production run executed on code containing that race. The recorded count/fingerprint verification passed, so exposure is likely nil, but it's worth acknowledging.
- The legacy proxy branch imports `@reading-advantage/auth` and the product `db` — that's the product database, not the identity database, so the spec's boundary (product apps never import the identity DB client) holds; Codecamp talks to Accounts only through the OIDC adapter.
- Minor: the cloudbuild secret binding took two attempts in 25 minutes — suggests the deploy-gate tests came after the fact rather than before; the ratcheting mitigates this going forward.

## 4. Risks or blockers

1. **Hard blocker (operator action):** Codecamp SSO deployment needs `roles/secretmanager.secretAccessor` on `projects/1090865515742/secrets/CODECAMP_COMPANY_AUTH_OIDC_CLIENT_SECRET` for `codecamp-cloud-run@codecamp-advantage.iam.gserviceaccount.com`, and the same grant for the Codecamp Cloud Build SA so the cross-project `--set-secrets` reference resolves at deploy time.
2. **Migration ran pre-fix:** as above — recommend re-verifying counts/fingerprints with post-fix code before flipping Codecamp traffic.
3. **Plan-checklist drift:** the reconciliation table honestly flags that most S1–S6 task boxes are unchecked despite deployed work (S1 Tasks 6–7 explicitly open). This is audit debt, not runtime risk, but it grows harder to backfill over time.
4. **Open QA:** Sales full feature QA and the S7 observation window remain open; legacy-auth retirement (Task 48) correctly awaits explicit approval.

## 5. Recommended next actions

1. Obtain the two IAM grants, then deploy the pinned `sso-candidate` Codecamp revision and run the Task 47 smoke tests (login, roles, progress, GitHub mapping, product ownership) while keeping rollback anchor `00019-682` warm.
2. Before shifting traffic, rerun the migration verification (dry-run fingerprint + aggregate checks) on the current, post-fix code to close the pre-fix-migration question.
3. Run the agreed observation window, then execute Task 48 (legacy-auth retirement) only on explicit operator approval.
4. Backfill the remaining plan checkboxes with commit evidence — starting with S1 Tasks 6–7 and the S2–S6 phases the reconciliation table already declares complete — and close out Task 49 documentation.
5. Complete the open Sales browser feature QA noted in the execution ledger.
