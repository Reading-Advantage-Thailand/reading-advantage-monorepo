All checks are done — targeted tests pass and the evidence docs are read. Here's the report.

# Track Review: `company_identity_sso_20260715` — last 24 hours

## 1. Summary of changes

27 commits carried this track's ID (2026-07-18 13:08 → 2026-07-19 04:04), totaling a large share of the ~36k lines added across the track's files. The work falls into four clusters:

- **Production go-live** (`2ec48b0f` "ship company sso and sales lms", evidence in `production-rollout-20260718.md`): Accounts, Marketing, and Sales deployed to Cloud Run at 100% traffic on public domains, with immutable image digests, rollback anchors, and browser/API verification of login, role isolation, suspension, credential reset, and session revocation.
- **Accounts hardening**: identity claims loaded during token exchange (`d561cfff`), a PostgreSQL-backed identity idempotency store with a two-connection production-schema test (`2b88a07d`, `00e5cf52`), persisted capability audits (`9150ea97`), Cloud SQL socket support (`093d25d4`), `bigint` auth-version normalization (`b4f7e869`), OIDC discovery fixes (`cb401ddd`), and runtime isolation/readiness probes (`168e860b`, `dec78b9e`).
- **Sales integration**: product-principal isolation via migration 0042 unique constraints and a `sync_sales_company_principal` database function (`ebf818fc`, `756fe57b`), company-organization scoping replacing `schoolId` authority (`2d685277`, `d24e0e3c`), and a legacy-auth rollback path — auth-mode switch, dual-mode login form, mapped rollback resolution, logout (`d3bb638c`, `756fe57b`, `ad505bde`).
- **Marketing**: named app-permission enforcement (`19d7f4cd`) and retirement of local-auth runtime database access (`ce169c7c`).

## 2. Spec alignment

| Story | Status | Evidence |
|---|---|---|
| S1 Identity boundary | **Met** (pre-window; Task 6 formally `[~]`) | Separate identity DB, migrations, boundary guards in place |
| S2 Employee SSO | **Met in behavior** | PKCE OIDC, HttpOnly sessions, SSO reuse across Sales/Marketing, logout and revocation verified against production domains |
| S3 Employee/role management | **Substantially met** | Production QA: employee create 201, ordinary-employee admin denied 403, independent company/app role changes, suspend/restore, revocation flipping 403→401 |
| S4 Connect Marketing | **Met** | Named permissions, company-admin-without-role denied (403), product DB untouched |
| S5 Connect Sales | **Partial** | Roles preserved, `schoolId` replaced by company org context; final company-admin-denial proof and the reviewed-revision deploy remain open |
| S6 Codecamp migration | **Not started** | No commits; correctly sequenced last per spec |
| S7 Cutover & verify | **Partial (by design)** | Marketing + Sales cut over with evidence; observation window, legacy retirement, Codecamp cutover open |

**Major process gap:** `plan.md` does not reflect any of this. Tasks 8–49 are unchecked and Task 6 is still `[~]`, while S2/S3/S4/S5/S7 work is deployed to production. Only a prose checkpoint note under Phase S7 records reality. Under Measure's "plan is source of truth" rule, the plan has drifted badly from the actual state.

## 3. Code quality observations

- **Test discipline is strong.** Nearly every commit pairs implementation with substantial tests, including real-PostgreSQL suites (`*.real-db.test.ts`) as the spec requires. I ran the reworked modules directly: `packages/domain` principal tests 15/15 and Sales auth/OIDC/auth-mode tests 21/21 pass.
- **Brittle error detection introduced in `756fe57b`.** `errorChainIncludes` in `packages/domain/src/company-identity-principal.ts` now matches database errors by *message fragment* ("Sales organization change requires an explicit mapping manifest") instead of the previous SQLSTATE `23505` code check. If the SQL function's message text is ever edited, manifest-required failures degrade silently into generic errors.
- **Business logic moved into SQL.** Principal synchronization now lives in `sync_sales_company_principal` inside migration `0042`, and that migration file was amended repeatedly across the window (`ebf818fc`, `eba902fe`). Editing an already-shipped migration is exactly the snapshot-drift failure mode that bit the Codecamp deploy; the ledger doctor was extended (+171 lines) apparently to compensate, but this pattern deserves scrutiny before S6.
- **Good commit hygiene otherwise**: small, scoped, consistently track-tagged. Note the plan's "Red commits contain tests only" rule was not followed — commits are mixed test+implementation `fix` commits — though the resulting coverage is real.

## 4. Risks or blockers

1. **Plan/reality drift** — automation and future sessions read `plan.md` as truth; its current state materially misrepresents the track.
2. **Today's automated review failed silently**: `review-2026-07-19.md` and its `.err` are both zero bytes.
3. **Dirty worktree**: 4 uncommitted sales files (`production-readiness.test.ts`, `sales-runtime-role-provision.sql`, provisioning doc, `next-env.d.ts`) — the Measure supervisor refuses to run against a dirty tree.
4. **Sales rollback gap**: the historical Sales revision (`sales-advantage-00002-7ch`) is explicitly *not* a valid rollback target post source-role repair; the required no-traffic `legacy-school` compatibility-revision gate is documented but not yet built.
5. **Codecamp (S6, XL)** is the largest remaining scope and hasn't begun; the misleading Codecamp DB secret flagged in Task 34 is still unresolved.
6. Open verification: Sales audio/roleplay/chat journeys, Marketing campaign/Thai-script journey, observation window, legacy-auth retirement.

## 5. Recommended next actions

1. **Reconcile `plan.md`**: mark completed tasks with their commit SHAs, record deviations (e.g., mixed Red/implement commits), and keep the S7 checkpoint note as the narrative anchor.
2. **Commit or drop the 4 dirty files** so supervisor automation can run again, and investigate why the 2026-07-19 review produced empty output.
3. **Build the Sales compatibility-revision Cloud Build gate** (tagged no-traffic `legacy-school` revision before source-role repair) before the next Sales deploy — it's currently only prose.
4. **Replace message-fragment error matching** with SQLSTATE/errcode-based detection in `company-identity-principal.ts`.
5. **Start Phase S6** (Tasks 33–34: migration contract + source-database preflight), including resolving the misleading Codecamp database secret.
6. Schedule the remaining manual journeys and the observation window so legacy-auth retirement has a defined evidence path.
