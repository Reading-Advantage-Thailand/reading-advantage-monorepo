# Track Review: `apk_corpus_audit_action_defense_20260712` (last 24h)

## 1. Summary of changes

~42 of the last 24 hours' commits belong to this track (all evidence artifacts under `measure/tracks/apk_corpus_audit_action_defense_20260712/`; no product code touched). They cover two batch lifecycles:

- **Batch A (Castle Defense, Magic Defense, Wizard vs Zombie)** — full role chain: strategy → collectors → mapper → truth tests (`31dac90b`). The truth tests **caught 12 fabricated/anchor-drifted claims** (fake SHA-256s, placeholder hex, line-drift) plus a receipt-immutability breach where unrelated SSO commit `ca423fbb` rewrote the wizard-vs-zombie receipt in place. Stop-loss cycle 1 fired (`32bfec71`), v2 re-collection/mapper/truth-tests/adversarial-review remediated it (47/47 pass), and batch A closed as **ACCEPTED-WITH-DISCLOSURE** (`62ff2a4d`, DISC-001: five claims whose text overstates the cited range).
- **Batch B (Village Guardian, Archer's Revenge, Storm the Castle Tower)** — strategy → discovery → per-game collector and mapper packages → truth gates (v1–v4) → asset audit → two browser-audit retries → two adversarial reviews → **STOP-LOSS recorded** (`25e48967`), then three independent reviews (correctness/security/UX-API) at `a49ebcc4` confirming the batch stays fail-closed.
- `measure/tracks.md` registry updated twice to reflect both statuses. (`review-*.md`/`.err` artifacts excluded per your instruction; the other recent commits belong to the traversal cohort, SSO, and pilot tracks.)

## 2. Spec alignment

| Spec / plan item | Status | Notes |
|---|---|---|
| Batches ≤3 games, distinct collectors/reviewers | **Met** | A/B/C partition matches plan; per-game collector receipts, `fork_turns=none` reviewers |
| Game-specific (non-generic) evidence, exact mechanics | **Met** for 5 games committed so far | Claim ledgers enumerate exact wave/spawn/targeting/terminal behavior; adversarial reviews re-derived claims byte-for-byte |
| Eight packages reconcile to denominator | **Partial** | 5 of 8 games have packages; **Castle Defense is in blueprint-v2 but absent from the batch A candidate/accepted manifests** (only magic-defense + wizard-vs-zombie, 187 claims); Batch C (Paladin's Twin-Soul, Gryphon Patrol) not started |
| Browser evidence for every runnable game | **Missed (Batch B)** | `agent-browser` exit 127, then CDP attachment failure; v3 WebBridge run used synthetic input (`isTrusted=false`, hidden tab, rAF scheduler replaced) — reviewers correctly refused it as real-input proof |
| Zero unresolved Critical/High/Medium | **Missed (Batch B)** | 1 Critical (87 unavailable provenance fields), 1 High (browser evidence), 1 Medium (v3 gate doesn't select active artifacts); Batch A carries DISC-001 as disclosed-only |
| No cross-game ontology decisions | **Met** | Nothing in the artifacts crosses game boundaries |
| Phase 4 reconciliation | **Not started** | Correctly sequenced after batches |

## 3. Code quality observations

- **The process worked as designed**: the truth-test layer detected real evidence fabrication (empty-string SHA-256 passed off as blob hashes) and an in-place receipt mutation, and both stop-losses halted acceptance rather than papering over. Append-only versioning (v1 preserved, v2/v3/v4 additive) is consistently honored.
- **Truth-test suite shape is uneven**: batch B v1 was 978 lines, v2/v3 dropped to ~200, v4 back to 332. The Medium finding (B-TEST-002) confirms the v3/v4 selectors don't pick up all active artifacts — the machine gate lags the artifact set it's supposed to verify.
- **33 `__pycache__` `.pyc` files are tracked in git** under `measure/` (plus more untracked, per git status). Generated bytecode in a track that hinges on byte-level hash provenance is asking for noise — worth a `.gitignore` + `git rm --cached` cleanup.
- Commit `ca423fbb` mixing SSO postflight with a mutation of this track's receipt shows cross-track commit hygiene slipped once; everything since has been cleanly scoped.
- `plan.md` checkboxes are all still `[~]` even though Phase 1 is accepted-with-disclosure — the plan file lags the registry.

## 4. Risks / blockers

1. **Batch B stop-loss is hard-blocked on product-owner direction** (2/2 remediation cycles used). No further dispatch is permitted without you.
2. **Castle Defense's exclusion from the batch A accepted manifest is undocumented.** Its evidence package exists (`040802b3`) and it appears in blueprint-v2, but the acceptance decision_basis never explains why the manifest lists only two games. Either it was deliberately deferred (IMP-CD-1 receipt impurity) and the disclosure is missing, or the manifest is incomplete — this threatens the "eight packages" criterion either way.
3. **Browser tooling is environmentally broken** for this repo's games (two independent runners failed); the same blocker was deferred in the T3 pilot, so it will recur in Batch C and every other cohort track.
4. **Provenance schema vs. reality gap**: 87 receipt fields are truthfully "unavailable" under the current harness — no amount of re-running fixes that without either harness changes or a product-owner ruling on what provenance is acceptable.

## 5. Recommended next actions

1. **Issue product-owner direction on Batch B** — the concrete decision is: (a) accept synthetic-input browser evidence with disclosure, (b) fix a real browser path first, or (c) mark the three games' browser dispositions formally deferred like the T3 pilot did. Option (b) is the only one that satisfies the spec as written.
2. **Resolve the Castle Defense manifest question** before Batch C: either add a documented disclosure for its exclusion or complete its remediation and re-issue the batch A manifest.
3. **Fix the browser harness once, centrally** (agent-browser install / CDP attachment), since it blocks all remaining cohorts — cheaper than per-track retries.
4. **Close B-TEST-002** by making the truth-test selector enumerate active artifacts (v-latest browser audit, review, receipts) instead of hardcoded lists.
5. Housekeeping: ignore/untrack `__pycache__`, and sync `plan.md` checkboxes with actual phase status.
