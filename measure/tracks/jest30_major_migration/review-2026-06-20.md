# Measure Track Review — jest30_major_migration
**Review date:** 2026-06-20  
**Review window:** commits since 2026-06-18 attributed to `jest30_major_migration*` variants  
**Spec / plan source:** `measure/tracks/jest30_major_migration/spec.md` and `plan.md`

## Summary of Changes

15 commits (across all `jest30_major_migration*` variants) completed the Jest 30 major migration for `apps/reading-advantage` and verified `apps/advantage-games` (already on Jest 30.3.0):

- **Phase 1 — Contract & schema:** `apps/reading-advantage/__test__/jest30-config.contract.test.ts` + `jest30-audit.md` audit (`ee707dfd`).
- **Phase 2 — Runtime Red proof:** `apps/reading-advantage/__test__/jest30-red.test.ts` asserting installed Jest major >= 30 (`f7dea19d`).
- **Phase 3 — Implementation:** Bumped `apps/reading-advantage/package.json` to `jest@^30.2.0`, `jest-environment-jsdom@^30.2.0`, `@types/jest@^30.0.0`, removed `ts-jest`; regenerated `pnpm-lock.yaml` (`dc246e79`). Canary files for `jest.requireActual` and `useFakeTimers` were re-run.
- **Phase 4 — Validate & close:** Targeted aggregate gate passed (RA `__test__` 208 tests, vocabulary-games smoke with pre-existing failures), `pnpm outdated` confirms Jest 30.x for in-scope apps, `pnpm audit` completed with no Jest-related advisories, `measure/tech-stack.md` updated with selected versions (`36c227e1`).

Track `metadata.json` marks status `done` as of 2026-06-19.

## Spec Alignment

| Spec Acceptance Criterion | Status | Evidence |
|---|---|---|
| AC-1: Jest upgraded from 29.x to 30.x in reading-advantage and advantage-games | **Met** | `package.json` pins ^30.2.0; advantage-games already at 30.3.0. |
| AC-2: Jest config files updated for new schema | **Met** | `jest.config.ts` removes `preset: "ts-jest"`, uses `"jsdom"` env, activates `coverageProvider: "v8"`; contract test 6/6. |
| AC-3: Snapshot tests updated | **Met** | Vacuously satisfied — no `toMatchSnapshot` callers; documented in plan. |
| AC-4: All existing test suites pass under Jest 30 | **Partial** | RA `__test__` 208/208 pass; advantage-games smoke 1720/1745 pass (25 pre-existing failures unrelated to Jest 30). |
| AC-5: Module resolution compatible | **Met** | No module-resolution failures reported in targeted gates. |
| AC-6: `pnpm outdated -r` shows Jest at target major | **Partial** | In-scope apps are 30.x; `@reading-advantage/scripts` still pins `jest@^29.7.0` and is documented as out-of-scope exclusion. |
| AC-7: Documentation updated in `measure/tech-stack.md` | **Met** | Doc contract test 4/4; records jest@^30.2.0, jest-environment-jsdom@^30.2.0, @types/jest@^30.0.0. |

**Overall alignment:** **Partial / Met with documented exclusions.** The substantive migration is complete; the only open tension is AC-6 because the legacy `@reading-advantage/scripts` package remains on Jest 29, which is explicitly excluded per `jest30-audit.md` §1.

## Code Quality Observations

- **Clear phase separation:** Red-phase tests were committed before Green implementation; supervisor corrections (e.g., reverting premature `package.json` bumps) are recorded transparently.
- **Bounded gates:** Phase 4 deliberately chose targeted gates over a full-monorepo aggregate because the full `pnpm turbo run lint|test|check-types|build` exceeded the 120s shell timeout locally. This decision is documented per `test-strategy.md` §3.3.
- **Permanent regression guard retained:** `jest30-red.test.ts` is kept as a 4-test guard that the installed Jest runtime stays on major 30.
- **Scope boundary is explicit:** The `@reading-advantage/scripts` Jest 29 pin is documented as an out-of-scope exclusion in both `plan.md` and `tech-stack.md`.
- **Minor concern:** `pnpm audit` timed out during the mid pass due to network constraints; the closeout claims it completed successfully but a reviewer should verify the audit artifact.

## Risks or Blockers

1. **Full-monorepo aggregate gate deferred to CI.** The local timeout means the closeout relied on targeted gates; CI may surface issues not seen locally.
2. **`@reading-advantage/scripts` remains on Jest 29.** While documented as out-of-scope, this creates a literal AC-6 deviation. A follow-up micro-track could bump it trivially.
3. **Pre-existing advantage-games failures.** 25 failures in performance-benchmark/griffinSkyJoust are unrelated to Jest 30 but could mask future regressions if not monitored.
4. **`pnpm-lock.yaml` conflict risk.** The migration regenerated the lockfile; any parallel dependency work should rebase carefully.

## Recommended Next Actions

1. Verify the `pnpm audit` artifact referenced in Phase 4 closeout exists and contains no Jest-30-related advisories.
2. Run the full `pnpm turbo run lint test check-types build` in CI and record the result.
3. Decide whether to open a follow-up micro-track to migrate `@reading-advantage/scripts` from Jest 29 → 30, making AC-6 literally true across the monorepo.
4. Update `measure/tracks.md` to include `jest30_major_migration` if it is missing (it was not found in the current `tracks.md`).
5. Move track to `measure/archive/jest30_major_migration/` if no further work is planned.
