# Review: apk_source_denominator_inventory_20260712 — 2026-07-18

Authored by Kimi (fallback) — Claude session limit hit, resets 10am Asia/Bangkok.

## 1. Summary of changes

36 commits touched this track in the last 24h (07-17 08:25 → 23:45). Unlike yesterday's status-hygiene window, this one contained real engineering, though roughly a third of the volume is attestation churn:

**Substantive work:**
- `47c856e5` — refreshed the hard T2 reviews `review-b-security.json` (2 High) and `review-c-contracts.json` (3 High + 1 Medium); both `status: fail`.
- `502d37a8` — added hard-review RED contracts (+290 lines across phase1–4 test files), including new Phase-4 counterexamples for inherited-role-context, output-ownership, and task-id-substitution attacks.
- `0e124135` → `4f196193` — RED test exposed a stale T2 authority map; fixed in `phase0-input-freeze.json` / `phase0-role-ownership-manifest.json`.
- `5de23429`, `d3cf7e92` — repaired and role-isolated the Phase-1 generator (`generate_phase1_denominators.py`).
- `8a090c04`, `228c6c8f`, `9d9dd442` — hardened Phase-2 human discovery (`generate_phase2_human_discovery.py`), plus evidence refresh commits `c21b0ac8`, `178fa8b8`, `c3d970da`, `4f4e4596`.
- `2a1d2892`, `d53678b4`, `5734b28d`, `96b7039f`, `47f99667` — hardened `measure/evidence_integrity_gates/opencode_provenance.py` (shell provenance, exact attestation mode, commit subject/workdir binding) with paired tests.
- `9f821ced` → `61f11520` — repaired the Phase-3 generator and regenerated `phase3-reconciliation.json` (+13k lines; `status: reconciliation-complete`, `unresolved_sources: []`).
- `cea7b5d4` — corrected the Phase-3 freshness check: mapper-owned outputs (e.g. `denominator-discrepancies.json`) are no longer compared against HEAD bytes but remain pinned in immutable input provenance. `97c48b77` regenerated `denominator-contract-test-report.json` documenting the corrected semantics and a 54/54 admission result.
- Receipt binding: `7eef6396` (evidence-collector), `a9c35fbb` (requirements-mapper), `8be2f8ac` (truth-test-author). `role-receipts/` now holds 4 of 5 frozen roles — only `adversarial-reviewer` is absent, by design.

**Attestation churn:** 9 of the 36 commits are empty, attestation-only (`c79d7692`, `eee9cdac`, `622aec55`, `0e83fe84`, `4f5dde0a`, `990dd9c0`, `dc0990db`, `edb6606d`, `7f42ed7d`) — no diff, just a receipt message binding a parent commit or path.

## 2. Spec alignment (met / partial / missed)

**Overall verdict: partial — Phases 0–3 met and re-verified; Phase 4 honestly blocked, now with additional legitimately-red contracts.**

- **Phase 0 (met):** freeze manifests corrected for the T2 authority-map gap (`4f196193`); phase0 suite 9/9.
- **Phase 1 (met):** generator repaired and role-isolated; denominators regenerated; phase1 suite 12/12.
- **Phase 2 (met):** discovery hardened; all four human-discovery artifacts refreshed; phase2 suite 18/18.
- **Phase 3 (met):** reconciliation regenerated and `reconciliation-complete` with zero unresolved sources; freshness semantics corrected; phase3 suite 15/15. Verified admission gate at HEAD: **54/54 passed** (up from 43 — the new hard-review contracts added 11 tests).
- **Phase 4 (missed — blocked by design):** `independent-review.json` still does not exist; `validate_phase4_inventory_acceptance` (last touched 07-15 at `21729ea5`) was not hardened this window. Focused suite is now **44 tests / 16 failures** (verified): 10 from the missing independent review, plus 6 new counterexamples failing because production acceptance permits inherited predecessor context and task/output-ownership substitution — exactly review-b finding RB2-001.
- Acceptance criteria "zero unresolved Critical/High/Medium findings" is currently **not satisfiable**: committed reviews B and C carry 5 High + 1 Medium blocking findings (some partially remediated later in the day, see §3).

## 3. Code quality observations

- The RED-first discipline held all day: every fix commit was preceded by or paired with a failing contract (`0e124135`→`4f196193`, `502d37a8`→provenance fixes). Commit messages carry exact commands, exit codes, and track_id trailers.
- The `cea7b5d4` freshness correction is well-reasoned and documented in `denominator-contract-test-report.json` (`corrected_contract_semantics`): it narrows the HEAD-byte comparison to the five current-stable Phase-1 artifacts while keeping the full set pinned in provenance — a genuine semantics fix, not a test weakening.
- **Reviews B/C are stale relative to the day's own remediation.** RB2-002 / APK-DEN-C-201 flagged missing requirements-mapper and truth-test-author receipts; those receipts now exist (`a9c35fbb`, `8be2f8ac`, committed 21:23/22:39). The review JSONs were not re-run, so the committed record still shows the 08:17 state. The RB2-001 validator-ownership finding, however, remains fully open and is now encoded as 6 failing counterexamples.
- **The stale plan evidence block flagged yesterday persists and is now staler.** `plan.md:44-51` still claims 32/32 admission, an absent validator, and 35 failures with `LEGACY_PHASE4_FAIL_OPEN`. Reality: 54/54 admission, validator present since 07-15, 44 tests / 16 failures. APK-DEN-C-204 (Medium) already flagged this at 08:23 when reality was 43/43 — it was never remediated.
- **Attestation churn is real process overhead.** 9 empty commits plus several single-file receipt-binding chores means ~half the day's commit volume binds evidence without advancing the denominator toward acceptance. The receipts are presumably required by the frozen evidence-integrity gate, but the one-commit-per-artifact-per-role pattern inflates history and, as noted yesterday, each interleaving commit risks invalidating baselines.
- `review-2026-07-16.md` is 0 bytes in the track dir — an abandoned placeholder from a prior automation attempt, same class of debris as yesterday's empty 07-17 file.

## 4. Risks or blockers

- **Primary blocker (by design, unchanged):** Phase 4 requires the frozen `adversarial-reviewer` role (`fork_turns="none"`, root agent forbidden) to produce `independent-review.json`; 10 of 16 Phase-4 failures are its absence.
- **New open High finding with failing contracts:** RB2-001 — production acceptance does not enforce frozen fresh-context/task-ownership. The 6 new RED counterexamples will not pass until `validate_phase4_inventory_acceptance` is hardened; no commit this window touched it.
- **Stale plan evidence** (`plan.md:44-51`) could mislead the adversarial reviewer or an orchestrator pass about the validator state and failure inventory.
- **Stale committed reviews:** B/C findings partially remediated but not re-attested; the next reviewer must distinguish already-fixed findings from live ones.
- **Worktree hygiene persists:** untracked/modified files remain across the repo (`.pyc` caches, other tracks' artifacts, this review file) — the supervisor's clean-worktree gate will still block the next automation run.

## 5. Recommended next actions

1. Refresh `plan.md:44-51` to current reality (54/54 admission, validator present, 44 tests / 16 failures categorized as 10 missing-review + 6 RB2-001 counterexamples) — this is now a known Medium finding (APK-DEN-C-204), not just hygiene.
2. Harden `validate_phase4_inventory_acceptance` against the RB2-001 attack classes (inherited role context, output-ownership swaps, task-id substitution) so the 6 counterexamples go Green before the adversarial reviewer dispatches.
3. Re-run / re-attest reviews B and C so the committed record reflects the receipt remediation (`a9c35fbb`, `8be2f8ac`) and only live findings remain.
4. Dispatch the adversarial reviewer once the worktree is clean; then the product-owner exact-hash acceptance gate (human, non-substitutable).
5. Populate or delete the 0-byte `review-2026-07-16.md`, and consider batching attestation commits to reduce churn.
