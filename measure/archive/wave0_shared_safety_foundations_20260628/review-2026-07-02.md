# Daily Review: wave0_shared_safety_foundations_20260628

**Review date:** 2026-07-02
**Status:** Archived (final acceptance pass on 2026-07-01)
**Commits reviewed (last 24h):**
- `b8e3b5d5` chore(measure): archive wave0 foundations
- `50569fb0` docs(measure): flip wave0/wave1 tasks to done
- `2823b767` fix(auth): skip DB suites when env absent
- `21a6e40e` fix(sales): audioStorageKey nullable
- `30d2749a` fix(db): migration surface and test sync
- `710d179e` fix(science-advantage): lint fixes

## 1. Summary of changes

Wave 0 was archived in the last 24 hours after final acceptance passed. The commits are closeout/gate-hygiene work rather than new feature implementation:

- `30d2749a` reconciled the `packages/db` vitest config, schema/migration counts, role enum guards, migration allowlist/journal, and marketing-schema expectations so the required `PROJECT_TESTS` gate exits 0 (638 passed / 6 skipped).
- `21a6e40e` made `audioStorageKey` nullable in `packages/domain/src/sales/schema.ts` and `packages/types/src/contracts/sales.ts`, matching the nullable DB column and unblocking `PROJECT_CHECKS`.
- `2823b767` made auth audit-retention integration suites gracefully skip when `DIRECT_DATABASE_URL` is absent, removing env-gated failures from the required gate.
- `710d179e` fixed science-advantage lint errors introduced by the shared ESLint base config unification.
- `50569fb0` flipped plan tasks to done and `b8e3b5d5` moved the track directory to `measure/archive/`.

## 2. Spec alignment

**Met.**

All Wave 0 acceptance criteria are now proven:

- Tenant registry coverage and fail-closed `TenantDB` for null tenants.
- Shared role schemas include every active app role.
- Auth rate limiting is Postgres-backed in production, dev-only in memory.
- Shared response/error/sales contracts have behavioral tests and API adoption proof.
- `packages/api` reports router no longer contains inline Drizzle business logic.
- Typed error mapping replaces string-based error matching in reviewed routers.
- Required verification commands pass under isolated/serialized runs.

## 3. Code quality observations

- **Closeout was evidence-driven.** The track was archived only after `final-acceptance-result.json` recorded green gates, not on documentation alone.
- **Default-parallel flake is documented.** Subprocess-spawn tests in `env-guards.test.ts` can time out under contested CPU; the acceptance record honestly labels this as non-blocking and inherited from `db_migration_ledger_20260611`.
- **Unrelated worktree pollution preserved.** The acceptance record correctly notes that an uncommitted `measure/tech-debt.md` change belongs to a separate deployment-audit workstream and was not folded into Wave 0.

## 4. Risks or blockers

- **Default-parallel CI may still flake.** If the monorepo CI runs the required `PROJECT_TESTS` command at full parallelism, the db subprocess-spawn timeouts could reappear. Mitigation is documented: run with `--concurrency=1` or per-package isolation.
- **No new risks introduced today.** All changes are closeout hygiene; the shared safety primitives themselves were not modified.

## 5. Recommended next actions

1. **None for Wave 0.** The track is archived and accepted.
2. **CI maintainer:** consider raising `env-guards.test.ts` `testTimeout` to 20s or gating those subprocess tests behind a serial Vitest project so default-parallel runs are deterministic.
3. **Wave 1 consumers** should rely on the Wave 0 primitives (tenant registry, shared roles, typed envelopes, fail-closed `TenantDB`) as stable foundations.
