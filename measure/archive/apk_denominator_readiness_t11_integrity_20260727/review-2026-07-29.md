Write is blocked in this session, so I've left `review-2026-07-29.md` empty — here's the report inline.

---

# Review: apk_denominator_readiness_t11_integrity_20260727 (2026-07-29)

Scope: commits in the last 24h referencing this track — exactly one, `8403d4b14` `chore(measure): accept APK readiness` (2026-07-28 21:47 +0700), 28 files, +2408 / −0.

## 1. Summary of changes

Single commit, entirely under `measure/`. No production code, no app/package files.

| Group | Files |
| --- | --- |
| Track artifacts | `spec.md`, `plan.md`, `index.md`, `metadata.json`, crosswalk, 2 independent-review reports, Phase-2 candidate receipt, owner acceptance, accepted readiness receipt |
| Validators | `validate_phase1_crosswalk.py` (494 L), `validate_phase2_readiness.py` (175 L) |
| Tests | `measure/tests/test_apk_denominator_readiness_phase1.py` (150 L), `..._phase2.py` (108 L) |
| Fixtures | 3 Phase-1 + 10 Phase-2 counterexample JSONs |

Substance: a 27-source-identity → 29-partition-assignment crosswalk bound to archived T2 manifests; a fail-closed, planning-only readiness validator pinned to T10/T11/standard-pack `2026.07.23` bytes; and an acceptance overlay receipt authorizing six named child cohorts to *begin* scoped work while asserting no readiness, cutover, or completeness.

Verified locally: both suites run green — **7 tests, OK**.

## 2. Spec alignment

| Requirement | Status | Evidence |
| --- | --- | --- |
| Source-to-assignment crosswalk; classify each surplus assignment | **Met** | Independently recomputed: 29 assignment rows, 27 distinct `source_identity_id`, 2 `historical_label`; labels, indices and locators all unique; every row carries a `source_locator`. |
| Independent review + owner acceptance before publishing a denominator | **Met** | Phase-1 review `681367b8…a8ad`, Phase-2 review `591c1c04…f802b`, owner acceptance `aa9b842f…39a12` — all recomputed and matching. |
| Verify T10/T11/`2026.07.23` archive hashes; preserve T10 zero-adoption and T11 disclosures | **Met** | `validate_phase2_readiness.py:12-29` pins 9 bindings and re-hashes each from disk; asserts `accepted_runtime_contracts == 0`, `approved_asset_mappings == 0`, `legacy_asset_adoption_approved is False`, `cartridge_cutover_authorized is False`, exact five-gap set. All 5 Phase-1 T2 source artifacts also re-hash clean. |
| Define Ready as cohort-specific proof (semantic adoption, selected output, three hosts, exact retirement) | **Partial** | The six evidence classes are structurally required (`validate_phase2_readiness.py:132-152`), but `validate_readiness` has **no success path** — an accepted crosswalk falls through to `_reject("PUBLICATION_NOT_IMPLEMENTED")` (line 173). No cohort can ever be evaluated as ready by this validator. |
| Publish planning/evidence only; acceptance unblocks listed cohorts without conferring readiness | **Met** | Every readiness/gameplay/browser/performance/host/asset/migration/retirement claim in `claims{}` is `false`; `excluded_child_tracks` names the two withheld cohorts. |
| **AC** 27/29 explained without duplicate or unclassified rows | **Met** | Above, plus `validate_phase1_crosswalk.py` re-resolves each JSON pointer into the archived partition manifest and matches label↔slug. |
| **AC** Machine-checkable record rejects missing/stale/revoked/waived input | **Met** | 10 fixtures cover `ARCHIVE_HASH_MISSING`, `ARCHIVE_HASH_STALE`, `T10_REVOKED`, `DISCLOSURE_WAIVED`, and the six evidence codes; each rejects with its declared code. |
| **AC** No success asserted beyond accepted evidence | **Met** | Phase-1 scans for forbidden success-claim keys (`_contains_forbidden_claim_key`); Phase-2 does not — self-disclosed as LOW. |

## 3. Code quality observations

**Positives.** Evidence binding is unusually rigorous: Phase-1 doesn't merely compare hashes, it resolves JSON pointers into the archived manifests, reads git blobs at a pinned revision (`_git_blob`), and verifies both whole-blob and line-range SHA-256. Rejection codes are stable and machine-readable, and fixtures assert the *exact* code rather than mere failure, so the contract can't silently reclassify. Both suites are genuine red-first artifacts, and the Phase-2 control test asserts the *blocked* outcome rather than a pass. Self-disclosure is honest — the receipt's own `phase2_low_findings` names the placeholder evidence and both validator gaps.

**Weaknesses.**
- **Validator asymmetry.** Phase-2 (175 L) is much thinner than Phase-1 (494 L): no forbidden-claim scan, and no check that `crosswalk_product_owner_acceptance` agrees with the Phase-1 artifact — it trusts a free-text field on the receipt it is validating.
- **Dead success branch.** `validate_readiness` returns `ok: True` only on the acceptance-*absent* path; the accepted path always rejects.
- **Hard-coded release pin.** `EXPECTED_BINDINGS` and the `2026.07.23` check live in code, so any standard-pack release needs a validator edit plus fixture rehash. Fail-closed by intent, but a chokepoint for six cohorts.
- Candidate evidence is placeholder strings (`"required-advantage-games-proof"`) — labelled and disclosed, but the "structurally complete" control proves shape only.

## 4. Risks and blockers

1. **`uncommitted_acceptance_state` is now stale, and the validator forbids closing it.** The archived T11 gap's `required_follow_up` is "commit the reviewed remediation, then refresh the immutable acceptance references." Commits `fd42aeb3c` and `8403d4b14` did the commit step. But `REQUIRED_DISCLOSURES` demands the gap be present *and* `status == "open"` (`validate_phase2_readiness.py:126-129`) — a successor receipt that honestly records it as resolved gets rejected with `DISCLOSURE_MISSING`/`DISCLOSURE_WAIVED`. The contract is fail-closed against remediation, not just against waiver.
2. **Receipt claims contradicted by the act of committing.** The receipt asserts `"commit_created_for_this_acceptance": false`, echoed in `plan.md:13` and `metadata.json`. True at publication (07-27), false as of `8403d4b14` (07-28). The bytes are hash-governed, so this can't be corrected in place — it needs the successor receipt its own `successor_receipt_required_after_any_bound_change` anticipates.
3. **None of this is enforced by CI.** `ci.yml` references `measure/doctor.sh` but never runs `measure/tests`. The downstream pin at `measure/tests/test_apk_existing_core_cutover_readiness.py:18` is what stops a child cohort consuming a mutated authorization — and it only fires if run by hand.
4. **No positive readiness path** for the six authorized cohorts; each will hand-roll its own "ready" determination, which is the divergence this track exists to prevent.
5. Authorization-boundary drift is low risk — the boundary text and `excluded_child_tracks` are hash-pinned and asserted downstream.

Every hash referenced in `plan.md` was recomputed and matches, including the receipt's own `d371fc5d…f1720`. No drift between committed artifacts and their bindings.

## 5. Recommended next actions

1. Publish a **successor receipt v2** recording `uncommitted_acceptance_state` as resolved (citing `fd42aeb3c` / `8403d4b14`), correcting `commit_created_for_this_acceptance`, and superseding v1 by hash. Extend `REQUIRED_DISCLOSURES` to accept `resolved` **with a bound remediation reference** while still rejecting bare `waived`. Re-pin `test_apk_existing_core_cutover_readiness.py:18`.
2. Add `python3 -m unittest discover -s measure/tests` to `ci.yml`. Without it the hash governance is advisory.
3. Give `validate_readiness` a real success path gated on accepted crosswalk plus non-placeholder per-game evidence, so the six cohorts share one gate. Add a positive fixture from a real receipt.
4. Port `_contains_forbidden_claim_key` into Phase-2 and verify `crosswalk_product_owner_acceptance` against the Phase-1 bytes rather than trusting the field — both already logged as LOW.
5. Leave the crosswalk alone. It verifies clean and is the strongest artifact here.

---

If you want this saved to `review-2026-07-29.md`, re-run with write permission and I'll drop it in.
