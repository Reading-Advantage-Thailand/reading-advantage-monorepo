# Implementation Plan: Denominator, Readiness, and T11 Release Integrity

## Phase 1: Establish the denominator

- [x] Read the archived T2 accepted denominator and partition manifests and create a 27-to-29 crosswalk with source locators. Evidence: `phase1-denominator-crosswalk.json` binds the exact accepted T2 manifest, ledger, and historical-source hashes and resolves all 29 assignments.
- [x] Write failing validation fixtures for duplicate, missing, and unexplained assignment rows. Evidence: the focused suite first failed on the absent validator, then passed 5/5 after `validate_phase1_crosswalk.py`; fixtures are under `measure/tests/fixtures/apk_denominator_readiness_phase1/`.
- [x] Classify the two-assignment difference and obtain independent review plus product-owner acceptance; otherwise retain the portfolio as blocked. Accepted evidence: immutable crosswalk SHA-256 `eb395d3d…c5f57`, Phase-1 review `681367b8…a8ad`, exact owner message `approved` in `product-owner-acceptance-v1.json` (`aa9b842f…39a12`), and successor governance in `accepted-readiness-receipt-v1.json` (`d371fc5d…f1720`). The Abyssal Well and Babel Architect are accepted as historical labels, not additional source identities. The Phase-1 LOW deleted-page revision disclosure remains binding.

## Phase 2: Publish fail-closed readiness

- [x] Verify T10, T11, and standard-pack archive hashes and record every disclosed limitation as a readiness gate. Evidence: `phase2-readiness-candidate-receipt.json` binds the exact archived T10 accepted manifest, successor hash set, owner acceptance, independent review; T11 delegated/extension acceptance, extension authorization, canonical bindings; and standard-pack release `2026.07.23`. `validate_phase2_readiness.py` recomputes every bound SHA-256 and preserves T10 zero runtime/legacy-asset approvals, all five T11 open disclosures, and `cartridge_cutover_authorized: false`.
- [x] Implement and test the planning-only readiness schema requiring per-game semantic adoption, selected output, Advantage Games, Reading, Primary, and retirement evidence. Evidence: `measure.tests.test_apk_denominator_readiness_phase2` first failed because `validate_phase2_readiness.py` was absent, then passed 2/2. Its fixtures reject stale, revoked, and missing hashes; waived disclosures; missing per-game semantic adoption, selected output, each host proof, and exact retirement evidence. A structurally complete candidate remains `ready: false` and `consumable: false` while Phase 1 crosswalk product-owner acceptance is absent.
- [x] Publish the accepted crosswalk/readiness receipt and notify cohort tracks; do not claim corpus completeness before acceptance. `accepted-readiness-receipt-v1.json` (`d371fc5d…f1720`) preserves the exact reviewed candidate hashes, all Phase-1/Phase-2 LOW findings, T10 zero-adoption restrictions, T11 no-cutover restrictions, and all five open T11 gaps. It authorizes only the five listed implementation cohorts plus the historical disposition cohort to begin scoped child work; no cohort is made ready, no cutover/deletion is authorized, and new-game intake/cross-host closeout are excluded. Candidate/review artifacts remain byte-immutable, no commit was created, and no production code was modified for acceptance.
