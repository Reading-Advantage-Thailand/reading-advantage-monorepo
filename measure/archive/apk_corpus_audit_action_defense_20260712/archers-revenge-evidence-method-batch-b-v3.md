# Archer's Revenge — Batch B evidence method v3

Collector: `evidence-collector-archers-revenge-batch-b-v3`
Phase base: `ff01cee9cc973dee89fdc0ba22102dcea0c50542`
Role base: `6be9da09ef11579f3d3a5e0285b8fdc9f8e6a4c4`

## Remediation boundary

This additive package supersedes only the Archer's Revenge V2 collector package.
V2 remains immutable failure evidence. V3 adds the missing `collector_id` to every
factual claim, retains one atomic proposition per exact source envelope, and
corrects the stale V2 receipt binding after whitespace correction. No mapper,
browser audit, review, truth-test, candidate, acceptance, or manifest work was
performed.

## Source and provenance method

The frozen baseline is `23bb5ad578c01fb29f9e8bb76a7d934d24a4b286`; historical
source is read only from reachable revision
`cd1936387d136ffb12e77a647f36cbce2d1fdd4e`. Text records identify the exact
inclusive Git-blob line range, whole-blob SHA-256, cited-range SHA-256, and
temporal boundary. Binary records use whole-file envelopes. Every factual record
is owned by the V3 collector ID. The WebBridge direction is bound as local-
verifiable process context only; this role performed no browser or route attempt.

Provider prompt, ancestry, fork, raw-isolation, event, timestamp, and final-
response telemetry were unavailable. The superseding receipt records those
fields as unavailable rather than treating nulls as attestations.

## Semantic safeguards and fixture policy

- Historical copy is copy evidence only, never currentness, learning correctness,
  runnability, or live-input evidence.
- Binary hashes prove presence only; they do not prove call sites or usage.
- Transition records are source inventories, not observed terminal behavior.
- Source-surface line counts are not person-hour or complexity estimates.
- All four negative fixtures remain separate, explicitly rejected, and excluded
  from factual claim totals.

## Accounting and verification

The frozen ceiling remains 52,428,800 bytes, 120 files, 80 commands, 45 minutes,
and 300 records. V3 actuals remain 4,300,000 bytes, 28 files, 22 commands, 38
minutes, and 92 records. JSON parsing, exact Git-object envelope re-derivation,
fixture rejection checks, receipt output hashes, and `git diff --check` are
required. Acceptance remains unclaimed pending authorized downstream gates.
