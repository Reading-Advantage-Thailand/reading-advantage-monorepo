# Code Review — review_findings_followup_20260626 (2026-06-27)

Compared spec.md + plan.md against the 13 commits landed in the last 24h on `reading-advantage-monorepo`. (Note: 14 commits listed in the report include also `shared_foundation_review_20260626` closeout commits — see separate review.)

## 1. Summary of changes
Phase 0 (baseline) → Phase 5 (closeout):
- **a9cd1029, e2dd2e90, 86968693** — Phase 0: strategy + track creation.
- **2b598492** — Phase 0: record baseline test counts.
- **1ffba8f7, 5c674118** — Phase 1 (later reverted as part of follow-up): FR-1 chat route authz (now superseded by Phase 2).
- **167beac4, a9fa178c, 18bb394a, ccd6be65, 1ef15034** — Phase 2: FR-2 PGlite harness + FR-1 distinct-student pagination in `getStudents`. `studentModel.fr2.test.ts` (the mock-based prior test) deleted.
- **4a490730** — Phase 7 (numbered differently in plan.md): test alignment for new tests.
- **5683836d, cea2b69b, 1fd1e3c8, d63c1831, 0b19795f, dc88f898, 3ccf25f9** — Phase 7 (alt track scope): sales/auth/primary fixes from the broader review follow-up scope.
- **53c30517, a567b785, 5077ad32** — Phase 8 closeout: plan SHAs + closeout + final manifest.

## 2. Spec alignment — **MET**
All four FRs implemented against their acceptance criteria:
- **FR-1** ✓ — `getStudents` now paginates over distinct students; behavioral test confirms RED on prior code (returned only `['s5','s4','s3','s2']` = 4 of 6 distinct), GREEN after fix.
- **FR-2** ✓ — PGlite harness reusable; behavioral tests for `classroomModel`, `teacherModel`, `assignmentModel` assert real returned data (not shape).
- **FR-3** ✓ — `new-generator.caller.test.ts` verified to FAIL when `db.transaction(...)` `await` is temporarily removed (production code untouched).
- **FR-4** ✓ — `extractCanonicalSourceExcerpts` and `getRoleplayEvaluationContext` unit tests pin real behavior. Spec draft guess ("returns `scenario: undefined`" on missing scenario) was wrong — the real impl throws `ScenarioNotFoundError`; test pins real behavior (correct deviation).

All five Acceptance Criteria verified in plan.md Phase 5 + Deviations section.

## 3. Code quality observations
- **TDD discipline is rigorous** — every FR has a Red proof, then Green evidence. `studentModel.fr2.test.ts` (which pinned the old single-query + JS-dedup structure) was correctly deleted as superseded.
- **Harness is reusable** — `__tests__/helpers/testDb.ts` provides PGlite + focused DDL + reset + close, bound to drizzle. Sibling model tests reuse it.
- **Commit grouping is honest** — plan.md explicitly notes that the harness + FR-1 fix landed together in `96563834` because prior failed-commit attempts left files staged. Not hidden behind a force-amend.
- **Spec deviations are documented** — `getRoleplayEvaluationContext` throwing instead of returning `undefined` for missing scenario is recorded as a deliberate test-pin of real behavior.

## 4. Risks or blockers
- **Track scope is broader than spec** — the commits include Phase 7 sales/auth/primary fixes (chat Zod, rate-limit decision banner, roleplay excerpts, role-marker injection, FR-10 eviction strengthening, FR-11 db mock, FR-12 import timeouts) that aren't in this spec's FR list. These belong to a sibling track (the broader review-findings follow-up). Plan numbering drift suggests two related tracks got interleaved.
- **Harness timing** — PGlite hook timeouts set to 60s for full-suite parallelism; if a developer doesn't set this env, the suite can hang.
- **Pre-existing TS2769 baseline in `studentModel.ts`** — 6 errors noted as out-of-scope. They sit adjacent to the FR-1 fix code, increasing the risk of accidental edits widening them.

## 5. Recommended next actions
- Archive this track to `measure/archive/` — all phases + closeout commit landed.
- Verify the Phase 7 sales/auth fixes belong to the broader `review_findings_followup` umbrella (or split into a separate track registry entry). The interleaved plan numbering suggests scope drift that the registry should resolve.
- Reaffirm PGlite harness timeout env in CONTRIBUTING.md so fresh clones don't hang.
- No human action required for the current track.