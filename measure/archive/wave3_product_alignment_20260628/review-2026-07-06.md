# Daily Review — wave3_product_alignment_20260628 — 2026-07-06

## 1. Summary of changes (last 24h)

43 unpushed commits on `master` complete and archive `wave3_product_alignment_20260628` (Phases 3–6). Key committed work:

- **Phase 3 — shared completion contract** (`895279ef`)
  - New `packages/domain/src/games/` module: `GameCompletionInputSchema` (`.strict()`, rejects client `xp`), `calculateGameXP`, `recordGameCompletion`, permissions.
  - `apps/advantage-games/src/lib/games/api/completeRoute.ts` rewritten to validate the contract and compute XP server-side.
  - Representative `haunted-library` game migrated to the new payload shape.
- **Phase 4 — tenant-safe persistence** (`bc792b68`)
  - New `gameCompletions` FLAT table with migration `0026_game_completions.sql`; `leaderboards.school_id` tightened to `NOT NULL`; unique index on `xp_logs(user_id, activity_id)`.
  - `recordGameCompletion` dual-writes `gameCompletions` + `xpLogs` in a transaction; `getSchoolLeaderboard` reads through TenantDB.
  - Host-mutation schemas (`recordActivityInputSchema`, `updateLessonProgressInputSchema`) hardened with `.strict()` and bounds.
- **Phase 5 — embeddable runtime / i18n** (`7e95f56b`)
  - Canonical `apps/advantage-games/src/lib/games-runtime/` module (`VirtualDPad`, `withBasePath`, `calculateClientXP`); 6 legacy duplicate files converted to re-export shims.
  - `GamesLocaleContext` + `generateStaticParams(['en','th','zh'])`; locale-agnostic hrefs in `gameCards.ts` (29 `/en/` prefixes removed); `onNavigate` contract wired in `haunted-library` and `dragon-rider`.
  - Import harness proves `haunted-library` is pilot-import-ready (9/9 tests).
- **Phase 6 — acceptance and closeout** (`be6dfacf`, `d5181776`, `35cc01db`, `60d7b5c4`, `34586251`, `14e96800`)
  - All 19 targeted gates re-ran green; `phase-6-final-acceptance.json` status `pass`; track moved to `measure/archive/`; closeout manifest and `metadata.json` updated.

Verification relied on committed artifacts and audit JSONs; `pnpm`/`node` are not available in this environment.

## 2. Spec alignment

| Area | Verdict | Notes |
|------|---------|-------|
| Public website claims (Phase 1) | Met | `phase-w3-claims` 20/20 pass; stale counts/dates/case-studies/model claims removed. |
| Marketing public API security (Phase 2) | Met | `marketing test phase-w3` 44/44 pass; auth/Zod/AI-adapter/secrets fixed. |
| Shared game contract + server-side XP (Phase 3) | Met | Domain `games` 524 pass; strict Zod rejects client `xp`; idempotency via UUID. |
| Tenant-safe leaderboard/persistence (Phase 4) | Met | `games-live` 524 pass; `tenant-coverage` exit 0; FLAT table + TenantDB scoping. |
| Embeddable runtime / i18n (Phase 5) | Met | Import harness 9/9; locale-agnostic hrefs; canonical runtime module. |
| Final acceptance / closeout (Phase 6) | Met | `phase-6-final-acceptance.json` pass; `doctor.sh` exit 0; track archived. |
| `babel-architect` status consistency | **Missed** | `gameCards.ts` lists `babel-architect` as `status: 'playable'`, but no page/component exists, it is excluded from `gameTypeEnum`, and both the source audit matrix and `phase-6-game-readiness-closure.md` label it `PLACEHOLDER`/`coming-soon`. This contradicts Wave 3's product-claim-alignment goal. |

**Overall: Partial** — all implementation phases met their acceptance gates, but one committed product-status claim is inconsistent with the track's own closure documentation.

## 3. Code quality observations

- **TDD discipline is strong**: red→green→review-a/b/c→acceptance→adversarial for every phase; SHA evidence recorded in `plan.md`.
- **TenantDB usage is correct**: `recordGameCompletion` writes the FLAT `gameCompletions` table through the scoped proxy and uses `tx.unscoped(...)` with a documented reason for the REFERENTIAL `xpLogs` insert.
- **Phase 5 adversarial audit** (`ab1de4d3`) added 50 tests and surfaced 8 nonblocking findings (path-traversal passthrough in `withBasePath`, unsanitized locale carrier, no runtime type-guard on `onNavigate`, vacuous positive control in import-harness 5B, etc.) — all documented for the successor track.
- **Minor schema drift**: `GameCompletionInputSchema` requires `clientTimestamp`, but the migration defines `client_timestamp bigint` without `NOT NULL`. The app always supplies it, so it is not a runtime bug, but the column should be tightened.

## 4. Risks or blockers

- **Low-severity spec inconsistency** (not a track blocker): `babel-architect` as `playable` could expose a broken link/404 in the games catalog and weakens the claim-correction narrative.
- **Deferred work is well-scoped**: 24 remaining games + `dragon-rider` sample remain NOT-READY/AT-RISK; Tier 2 PO decisions stay `[b] deferred:po`; all deferrals are precise `[b] deferred:<owner>` markers.
- **No severe blockers** for the archived track.

## 5. Recommended next actions

1. Fix `apps/advantage-games/src/lib/gameCards.ts` so `babel-architect` is `status: 'coming-soon'` (or remove the card) to match `phase-6-game-readiness-closure.md` and the source audit matrix.
2. Add a regression assertion (in `gameCards.test.ts` or a small catalog linter) that every `status: 'playable'` card has both a `gameTypeEnum` entry and an existing page/component directory.
3. In the successor games-migration track, address the Phase 5 adversarial nonblocking findings: `encodeURIComponent` locale values in fetch URLs, `typeof onNavigate === 'function'` guard, and replacement of the vacuous import-harness 5B length check with an exact-catalog assertion.
4. Schedule product-owner review for Tier 2 `[NEEDS-PO]` decisions (roadmap dates, AI provider names, efficacy stats + consent) before expanding public claims beyond provider-neutral/illustrative copy.
