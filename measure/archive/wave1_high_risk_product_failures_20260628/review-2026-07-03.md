# Daily Review: wave1_high_risk_product_failures_20260628

**Review date:** 2026-07-03  
**Commits reviewed (last 24h):**
- `8b0f422e` docs(measure): daily review reports 2026-07-02
- `b6d1d9f8` test(sales): wave1 p4 red tests
- `d83db701` fix(sales): wave1 p4 authz/audio/privacy/contracts green
- `ba483a4e` fix(sales): wave1 p4 review-b security
- `a60ffebc` chore(measure): wave1 p4 review-b result
- `01a2aecc` fix(sales): wave1 p4 review-c ux-api
- `0baab62d` chore(measure): wave1 p4 acceptance
- `fd349064` docs(measure): wave1 phase5 integrated verify
- `0137ed72` fix(primary-advantage): wave1 p1 flashcard tsc
- `500a81e6` chore(measure): wave1 final acceptance
- `992d6e72` chore(measure): archive wave1 closeout
- `af631ec9` chore(measure): finalize wave1 closeout

## 1. Summary of changes

Wave 1 was completed, accepted, and archived in the last 24 hours.

- **Sales Advantage Phase 4 completed and accepted.** Added Red tests and Green implementation for `SALES_REP`/`SALES_ADMIN` auth context, IDOR protection on roleplay evaluation and cohort reporting, audio size/MIME/duration validation before provider/storage calls, consent/retention metadata enforcement, UI consent wiring, and `audioStorageKey` nullability contracts. Review B added a security hardening test; Review C tightened UX/API assertions.
- **Primary Advantage type-regression follow-up fixed.** Commit `0137ed72` resolved the Wave-1-introduced `tsc --noEmit` errors on touched surfaces (`actions/flashcard.ts`, `flashcard-game.tsx`, `article-content.tsx`, and the two session-destructure game components). The touched-surface diagnostic grep now returns zero lines while all 56 vitest tests still pass.
- **Phase 5 integrated acceptance passed** at `500a81e6` with `may_proceed_to_closeout=true`.
- **Track archived** to `measure/archive/wave1_high_risk_product_failures_20260628/` and finalized at `af631ec9`; `tracks.md` and `metadata.json` updated to `status: done`.

## 2. Spec alignment

**Met.**

- All four app slices have Red → Green behavior tests for their highest-risk workflows.
- Primary: game-completion/session crash fix, flashcard schema contract, and dashboard truth verified (56/56 vitest tests; touched-surface type-clean).
- Reading: classroom destructive auth/tenant checks, audit events, XP idempotency, sensitive system/auth boundaries, level-test assessment JSON contract, and AI content quality gate verified (29/29 targeted tests).
- CodeCamp: `TenantScopeError` eliminated on REFERENTIAL tables via greppable `unscoped("reason")` calls, delivery-ID webhook idempotency implemented, webhook ACK decoupled from LLM review, and chat streaming `text/event-stream` framing fixed (7 targeted tests).
- Sales: role/auth context, ownership/tenant IDOR rejection, audio validation before provider/storage, consent/retention UX, and `audioStorageKey`/nullability contracts verified (30/30 targeted tests).
- Scope completeness: the named Critical/High migration tracks are closed or explicitly deferred with owners (Primary M1–M6/M8/M11, Reading M-RA-SEC-1..5/PB-1..3, CodeCamp MT-1..6, Sales T1/T2/T3/T7). Medium+ remainders are mapped to Wave 4 or Wave 6 in `medium-plus-coverage-matrix.md`; no silent drops.

## 3. Code quality observations

- **Test-first discipline held.** Sales Phase 4 Red tests (`b6d1d9f8`) preceded Green implementation (`d83db701`).
- **Review hardening was constructive.** Review B (`ba483a4e`) and Review C (`01a2aecc`) added tests and tightened assertions rather than weakening them.
- **Type regression was caught and fixed.** The Primary touched-surface `tsc --noEmit` regression introduced earlier in Wave 1 was remediated without reintroducing `as any` casts and without breaking the vitest suite.
- **Deferred scope is explicit.** Five tasks are marked `[b]` with `deferred:<owner>` (Wave 4, Wave 6, or Phikul) and cross-referenced in the coverage matrix.
- **Measure hygiene clean.** `doctor.sh` passes; no `.skip`/`passWithNoTests` added; no archived `measure/tracks/<id>/plan.md` references in `tests/*.sh`.

## 4. Risks or blockers

- **No Wave-1 blockers remain.** The track is archived.
- **Pre-existing aggregate failures persist** and are documented as nonblocking:
  - `reading-advantage` full Jest run: 29 failed suites / 32 failed tests (next-intl ESM parse, zustand v4/v5 mock mismatch, jest30 canary tests).
  - `codecamp-advantage` prod-smoke tests: 12 failed files / 42 failed tests (live-network probes, filesystem detectors, UI artifact test).
  - `sales-advantage#check-types`: 2 pre-existing TS2742 errors in `lib/trpc.ts` from scaffold commit `025f8fc9`.
  - `codecamp-advantage#check-types`: 2 pre-existing TS2307 errors importing `@reading-advantage/ai`.
  - `primary-advantage` and `reading-advantage` full type-check remain red on non-Wave-1-touched files.
- **Accepted technical debt carried to Wave 4:** process-local `processedDeliveryIds` Set for webhook dedup, in-process XP idempotency lock map, durable Postgres webhook retry/DLQ, and Sales curriculum sanitization/leakage/math.

## 5. Recommended next actions

1. **Wave 1 is closed.** No further commits or changes are needed on this track.
2. **Ensure Wave 4 / Wave 6 owners have picked up** the deferred `[b]` items from the closeout manifest and coverage matrix.
3. **Continue tracking pre-existing aggregate failures** in their owning workstreams (`codecamp_qa_prod_20260517`, `reading-advantage` legacy test debt, `sales_advantage_golive_20260701`) rather than as Wave 1 regressions.
