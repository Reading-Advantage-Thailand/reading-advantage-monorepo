# Post-Closeout Review — 2026-07-12

The track was incorrectly marked complete. Structural tests passed, but they did not prove the source-grounded analysis required by the specification.

## High findings

1. `generate-corpus.mjs` assigned every game one synthetic `main` scene, so all downstream scene mappings were incomplete.
2. `generate-blueprints.mjs` hardcoded mechanic summaries and attached directory-level evidence rather than exact claim evidence.
3. `generate-responsive.mjs` selected profiles through keyword matching and reused identical fixtures/requirements rather than auditing each game.
4. `generate-asset-usage.mjs` generated desired ontology roles from templates and slug allowlists rather than extracting concrete usages and variants.
5. `existing-asset-audit.md` summarized candidates by cohort instead of recording the required fields for every file.

All accepted dependent hashes are revoked. Phase 8 must replace the synthetic inputs, add behavior/evidence tests, regenerate downstream artifacts, and pass another independent review before completion can be claimed.
