# Track Review: rate_limiter_v2_20260603

**Project:** reading-advantage-monorepo  
**Track:** rate_limiter_v2_20260603  
**Review date:** 2026-07-04  
**Review period:** last 24 hours (commits 2026-07-03 → 2026-07-04)  
**Track status:** archived

---

## 1. Summary of changes

The track moved from active implementation to closeout and archive within the review window. Commits touching this track (all authored 2026-07-03):

| Commit | Message | Key changes |
|--------|---------|-------------|
| `abfd4505` | feat(auth): postgres rate-limit cleanup job + captcha trigger | Added `packages/auth/src/rate-limit-cleanup.ts` (1-hour interval, 24h stale-row cleanup, 1000-row batches, advisory-lock guarded); added `captchaRequired` trigger after 3 failures; surfaced flag in `login.ts` 401/429 responses; expanded `packages/auth/README.md`. |
| `3e48f386` | fix(auth): rate-limiter review fixes | Added `packages/api/src/routes/auth/client-ip.ts` with `TRUST_PROXY_COUNT`-aware `getClientIp`; replaced naive XFF parsing in `login.ts`; added atomic `store.increment` in `rate-limit-store.ts`; added `checkRateLimitByIp` wrapper; fixed `Retry-After` header on 429; added unit tests. |
| `0516c427` | test(auth): adversarial rate-limiter probes | Added 4 adversarial test files (client-ip, rate-limit, cleanup, fastpath) — 66+ probes covering IP spoofing, bucket independence, lock races, batching, dual-gate fast-path. |
| `ee3b409e` | fix(auth): rate-limit cleanup adversarial test tsc/build break | Replaced a synthetic `?lock-miss` dynamic-import cache-busting hack with canonical `vi.resetModules()` pattern; restored `tsc --noEmit` and `@reading-advantage/auth` build. |
| `12ee1d94` | docs(measure): rate-limiter v2 final acceptance + phase 8 close | Updated plan markers, lessons-learned, follow-up track placeholder. |
| `48806a5f` | chore(measure): archive rate_limiter_v2 | Moved track directory from `measure/tracks/` to `measure/archive/`. |

Overall: production code, tests, docs, and acceptance artifacts were all completed and the track was archived.

---

## 2. Spec alignment

**Verdict: partial — all functional requirements are implemented and verified, but the DB-backed integration test suite is legitimately deferred due to an unrelated infrastructure blocker.**

| Spec item | Status | Evidence / notes |
|-----------|--------|------------------|
| FR-1 `login_attempts` schema | Met | `packages/db/src/schema/auth.ts:11-25` defines table + unique index + window_start index; barrel re-exported. |
| FR-2 Drizzle migration | Met | `packages/db/drizzle/0024_futuristic_vulture.sql` creates table + indexes. |
| FR-3 `RateLimitStore` Postgres impl | Met | `packages/auth/src/rate-limit-store.ts` exports `createPostgresRateLimitStore`; uses `SELECT ... FOR UPDATE` and atomic `increment`. |
| FR-4 Refactor `rate-limit.ts` | Met | In-memory `Map` reduced to dev-only fast-path; `configurePostgresRateLimiter` is production default; `checkRateLimitByIp` exported. |
| FR-5 Cleanup job | Met | `packages/auth/src/rate-limit-cleanup.ts` runs hourly, deletes `windowStart < now() - 24h` in 1000-row batches, uses advisory lock distinct from audit-retention lock. |
| FR-6 Wire into login flow | Met | `packages/api/src/routes/auth/login.ts:66-85` extracts IP, checks both buckets, returns 429 with `Retry-After`, records/reset failures on all paths. |
| FR-7 Captcha trigger (hook) | Met | `CAPTCHA_THRESHOLD=3`; `captchaRequired` returned/surfaced on 401/429; reset clears state. |
| NFR: durable across replicas | Met | Postgres-backed store; no shared in-memory state in production. |
| NFR: independent buckets | Met | Username and IP buckets use separate composite keys; adversarial tests verify independence. |
| NFR: cleanup non-blocking | Met | Async job, batch-limited, advisory-locked. |
| NFR: lint/type-check/build green | Met | Final acceptance: auth/api/db typecheck and auth/api builds pass; lint passes. |
| AC-9 integration tests | Partial / deferred | Test file authored (`apps/science-advantage/src/__tests__/rate-limit-login.integration.test.ts`) but cannot run due to `drizzle-kit migrate` global-setup failure against `science_advantage_test` (INFRA-2). Unit + adversarial coverage is green. |

The only material divergence from the original spec text is the public API shape: the spec proposed `checkRateLimit(identifier, kind, config)`, but the shipped contract is `checkRateLimit(username, ip?)` plus the added `checkRateLimitByIp(ip, config?)` wrapper. This divergence is documented as CR-1 in the plan and is functionally equivalent.

---

## 3. Code quality observations

- **Positive:** The review fixes (`3e48f386`) addressed real security/UX gaps that the spec flagged as risks:
  - `TRUST_PROXY_COUNT`-aware IP extraction prevents left-side XFF spoofing.
  - Atomic `increment` statement closes the race-condition under-count risk in `recordFailure`.
  - `Retry-After` header is now returned on 429 responses.
- **Positive:** Adversarial test coverage is thorough (4 files, 66+ probes) and caught edge cases such as lock contention, batch boundaries, and fast-path gate bypass attempts.
- **Positive:** `ee3b409e` is a clean, minimal fix that replaced a brittle test-only hack (`?lock-miss` dynamic import) with the canonical Vitest pattern. Final acceptance correctly identified and remediated the build regression before closeout.
- **Positive:** Code is well-documented (JSDoc in `rate-limit.ts`, `client-ip.ts`, expanded `README.md`) and uses consistent naming with surrounding packages.
- **Minor:** The `cleanupOldAttempts` function in `rate-limit-cleanup.ts` ends without a trailing newline; purely cosmetic.
- **Minor:** `login.ts` repeats the same `captchaRequired` spread object on four failure-path returns — a small helper would reduce duplication, but it is not a defect.

---

## 4. Risks or blockers

| Risk / blocker | Severity | Detail |
|----------------|----------|--------|
| INFRA-2: `drizzle-kit migrate` global-setup failure | Medium | Blocks the authored DB-backed integration test from running. This is the only remaining gap against AC-9. Ownership is `infra`, not this track. |
| IR-1: `phase-7-closeout.test.ts` failure in `audit_log_retention_dsar_20260605` | Low | Causes the aggregate `pnpm turbo run test --filter=@reading-advantage/auth` to be red, but it is unrelated to rate-limiting and explicitly excluded from this track’s scope. |
| INFRA-1: `packages/utils` `child_process` in client bundle | Low | Blocks `science-advantage` build, but the plan and final acceptance correctly treat this as out-of-scope for the auth/db/api track. |
| Captcha trigger without verification | Low (accepted) | As designed, the track emits `captchaRequired: true` but does not verify any captcha token. Follow-up track placeholder created. |
| `TRUST_PROXY_COUNT` default is legacy leftmost-XFF | Low | Backward-compatible but requires production operators to set the value explicitly for multi-proxy deployments; documented in `README.md`. |

No new risks were introduced by the commits in the last 24 hours; the known blockers are pre-existing infrastructure issues outside the track’s scope.

---

## 5. Recommended next actions

1. **Resolve INFRA-2** (`drizzle-kit migrate` global-setup failure against `science_advantage_test`) so the authored integration test `apps/science-advantage/src/__tests__/rate-limit-login.integration.test.ts` can be executed and AC-9 fully closed.
2. **Run the 6-app smoke test** (Phase 7) once INFRA-2 is fixed; currently `[b] deferred:infra`.
3. **Mark F-403 / F-407 resolved in `measure/tech-debt.md`** only after the integration tests pass, per the A6 guard in the plan.
4. **Pick up the Captcha Verification follow-up track** to consume the `captchaToken` emitted by the frontend and verify it against a provider (reCAPTCHA / hCaptcha / Turnstile).
5. **Align `reset-password.ts` XFF handling** with `getClientIp` (SEC-4 from review-b was accepted as a follow-up, not fixed here).

---

*Report generated from git review of commits `48806a5f`, `12ee1d94`, `ee3b409e`, `45a01266`, `0516c427`, `0d426d85`, `3e48f386`, `aeb3cb7c`, `abfd4505`, `f269c007`, and `776b8b21`.*
