# Track Review: wave2_confidence_restoration_20260628

**Project:** reading-advantage-monorepo  
**Track:** wave2_confidence_restoration_20260628  
**Review window:** 2026-07-03 (last 24 hours)  
**Review date:** 2026-07-04  
**Status:** Archived (track directory located in `measure/archive/`)

---

## 1. Summary of changes

Seven commits touched this track in the last 24 hours. All were Phase 4/5 closeout work and final archival:

| SHA | Message | Files touched |
|---|---|---|
| `2b12242f` | feat(test): wave2 p4 reusable harnesses | Added 5 reusable test helpers + test fix + plan update |
| `00b2accb` | fix(test): wave2 p4 review-b security | Hardened 3 A2 consent cases + guard utility regex + tenant harness structural assertion |
| `6d5afb10` | chore(measure): wave2 p4 review-a result | `phase4-review-a-result.json` only |
| `7a1d460a` | docs(test): wave2 p4 review-c result | `phase4-review-c-ux-api-result.json` only |
| `6f487ccb` | chore(measure): wave2 p4 acceptance | `phase4-acceptance-result.json` + plan markers |
| `49e662cf` | docs(measure): wave2 p5 aggregate verification | `test-strategy-roadmap.md`, `lessons-learned.md`, plan.md |
| `1f2d1795` | chore(measure): archive wave2 confidence | Moved track to `measure/archive/`, added final acceptance + closeout manifest |

The only production-code changes in this window are the five new **test-only harness utilities**:

- `packages/domain/src/testing/tenant-isolation-harness.ts`
- `packages/api/src/testing/api-contract-kit.ts`
- `packages/ai/src/testing/provider-guard-utility.ts`
- `packages/db/src/testing/migration-doctor-helper.ts`
- `apps/www-reading-advantage/src/testing/product-claim-helper.ts`

plus accompanying consumer tests and the Phase 2 provider-guard fix (`internal-sdk` quarantine already landed earlier).

---

## 2. Spec alignment

**Verdict: met**

The Wave 2 spec required:

1. Migration/seed governance gates — delivered in earlier commits (`029cc2fa`, `9934153a`, `e52b9346`, etc.) and verified in Phase 5.
2. Provider-adapter enforcement — delivered in earlier commits (`d10c836d`, `5f1c81c0`); the 24-hour window added the reusable `provider-guard-utility.ts` harness that generalizes the enforcement scanner.
3. False-green test signal fixes — delivered in earlier commits (`47fbcd78`, `251fec66`, `ac8d7ff6`).
4. **Reusable test harnesses** — this is what the 24-hour commits completed. All five harnesses are present, each has a consumer test, and each is documented with file-level JSDoc and plan.md evidence.

The final-acceptance artifact added by `1f2d1795` records `status: "pass"`, `may_proceed_to_closeout: true`, and lists no blocking findings. The four Wave 2 phase guard chains pass at the acceptance HEAD (`49e662cf`).

---

## 3. Code quality observations

- **Harness design is clean and safe.** All five helpers are pure (no DB/HTTP/file I/O), live under a `testing/` subpath that is intentionally excluded from package `exports`, and are documented with JSDoc explaining why they are not public API.
- **Consumer tests exist and pass** per the plan.md evidence block (Phase 5 re-runs).
- **Security review feedback was acted on.** `00b2accb` added adversarial cases for consent-without-anonymization, anonymization-without-consent, and truthy-string consent bypass (A2 strict equality).
- **Minor issues:**
  - `packages/ai/src/testing/provider-guard-utility.ts` builds an unused `bannedRe` regex and silences it with `void bannedRe`; the variable is reserved for future extensibility but is currently dead code.
  - The provider guard uses line-based regex parsing rather than an AST, so exotic import syntax or commented code could produce false negatives/positives. The docstring acknowledges this is a test utility, not a production lint rule.
  - `packages/db/src/testing/migration-doctor-helper.ts` relies on a positional zip between `ledgerRows` and `journal`; the docstring notes the invariant is guaranteed by the helper for built-in variants, but hand-built states could drift.
  - Several new files are missing a trailing newline.

---

## 4. Risks or blockers

- **No blockers for Wave 2 itself.** The track is archived and final acceptance passed.
- **Aggregate test suites remain red** for `@reading-advantage/ai` (19 pre-existing artifact/version failures), `www-reading-advantage` (next-intl module resolution), `vocabulary-games`, `science-advantage` (sandbox DB infra), `codecamp-advantage` (prod-smoke/thai-text-width), and `sales-advantage` (`check-types` TS2742). These are explicitly documented as pre-existing, owner-labeled, and out-of-scope.
- **Deferred technical debt is preserved in ratchets:**
  - `CONSOLE_ERROR_BASELINE = 621` will only tighten if Wave 6 actively migrates call sites.
  - `@reading-advantage/ai/internal-sdk.ts` quarantine remains until the AIClient contract is finalized.

---

## 5. Recommended next actions

1. **For Wave 2:** none — the track is closed and archived.
2. **For Wave 6 / observability owner:** lower `CONSOLE_ERROR_BASELINE` as structured-logger migration progresses; do not delete the guard.
3. **For Wave 6 / AI adapter owner:** remove the `internal-sdk` quarantine once `AIClient` covers the remaining raw call sites.
4. **For `ai_sdk_major_migration` owner:** resolve the 19 artifact/version-contract failures so the shared aggregate can return green.
5. **For `db-platform` owner:** investigate the `env-guards.test.ts` 5-second timeout flake under `turbo` parallelism.
6. **For app remediation waves:** address the pre-existing `www-reading-advantage`, `vocabulary-games`, `science-advantage`, `codecamp-advantage`, and `sales-advantage` failures documented in plan.md Phase 5.

---

*Report generated from git history since 24 hours ago, compared against `measure/archive/wave2_confidence_restoration_20260628/spec.md` and `plan.md`.*
