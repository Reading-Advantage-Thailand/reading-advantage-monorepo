# Daily Change Review — housekeeping_batch_20260603

**Date:** 2026-06-18  
**Track:** Audit Housekeeping Batch  
**Commits reviewed:** 14 commits in the last 24 hours referencing this track

---

## Summary of Changes

Today's work concentrated on **Phase 0–2** of the housekeeping batch:

- **Phase 0** — Completed the FR checklist, Track 11 coordination, and graph baseline.
- **Phase 1 (F-205)** — Relocated legacy `apps/science-advantage/prisma/` seed-data to `apps/science-advantage/scripts/seed-data/`. Moved 53 JSON files plus READMEs and the `update-seed-files.ts` script. Updated import paths in 6+ seed/validation scripts. Added adversarial closure tests (`housekeeping-phase1-relocate-prisma.test.ts`) and a pre-move SHA-256 snapshot (`pre-snapshot.sha`).
- **Review A** — Fixed two missed active-code references to `prisma/seed-data` (`scripts/migrate-seed-data.ts`, `scripts/convert-md-to-structured.ts`) and updated `lib/__tests__/audit-phase2-static-analysis.test.ts` §2.8 to assert `prisma/` is removed and no active script references legacy paths.
- **Phase 2 (F-705)** — Confirmed the 4 auth `route.ts` stubs are active delegates to `@reading-advantage/api/routes/auth` and added JSDoc comments referencing the shared handlers and their test coverage.

---

## Spec Alignment

| Spec AC / FR | Status | Notes |
|--------------|--------|-------|
| FR-1 Relocate `prisma/` seed-data | **Met** | 53/53 JSON files relocated with hash identity preserved; `prisma/` removed; path refs updated; regression test added. |
| FR-2 Verify/delete auth stubs | **Partial** | Stubs confirmed alive and documented; not deleted because references exist. |
| FR-3 Update `AGENTS.md` | **Not started** | Still references Prisma/npm per plan. |
| FR-4 `*.log` in `.gitignore` | **Not started** | |
| FR-5 Backfill orphan TODOs | **Not started** | |
| FR-6 Re-pin `^` deps | **Not started** | |
| FR-7 `git notes` on refactor commits | **Not started** | |
| FR-8 Add `docs/adr/` | **Not started** | |
| FR-9 `commitlint` config | **Not started** | |
| FR-10 App-local CI deletion | **Deferred to Track 11** | Already resolved by `ci_typecheck_alignment_20260603`. |
| Final acceptance (lint/test/seed/build) | **Deferred** | Phase 11 live gates deferred to dev environment with reachable Postgres. |

---

## Code Quality Observations

- **TDD discipline is strong:** Red-phase snapshot, Green implementation, adversarial closure tests, and Review A fixes are all present.
- **Data integrity is well guarded:** The `pre-snapshot.sha` fixture and the 53/53 SHA-256 diff ensure the relocation is behavior-preserving.
- **Commit hygiene is good:** Most commits include `track_id: housekeeping_batch_20260603`.
- **Honest deferrals:** The live `pnpm seed` gate is documented as environment-blocked rather than falsely claimed green.

---

## Risks / Blockers

- **Environment blocker:** The host cannot reach `127.0.0.1:5432` and lacks pnpm/node on PATH, so the end-to-end `pnpm seed` and `pnpm turbo run test --filter=science-advantage` gates are deferred to Phase 11.
- **Scope remaining:** 8 of 10 FRs are still open (AGENTS.md update, `.gitignore`, TODOs, dep pinning, git notes, ADRs, commitlint).
- **Stale TODOs:** 5 orphan TODOs remain in source; if left too long they will decay into noise.

---

## Recommended Next Actions

1. Complete Phase 3 (`AGENTS.md` cleanup) and Phase 4 (`*.log` in `.gitignore`).
2. File GH issues for the 5 orphan TODOs and update in-code comments with issue references.
3. Decide on strict vs. documented `^` dep deviation and document in `AGENTS.md`.
4. Backfill `git notes` for the 24 `refactor(science):` commits.
5. Author the 3 ADRs and add the SQL-ADR guard lint.
6. Add `commitlint` + husky config (coordinate monorepo-wide impact).
7. Run Phase 11 final acceptance from a dev environment where Postgres is reachable.
