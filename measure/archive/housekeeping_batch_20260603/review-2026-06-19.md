# Daily Change Review — housekeeping_batch_20260603

**Date:** 2026-06-19  
**Track:** Audit Housekeeping Batch  
**Commits reviewed:** 40 commits in the last 24 hours referencing this track

---

## Summary of Changes

The track was **completed and archived** during this 24-hour window. Work spanned Phases 3–12:

- **Phase 3 (F-1102)** — Cleaned `apps/science-advantage/AGENTS.md` of stale Prisma, NextAuth, `npm install`, `npx prisma`, and `npm run` references; added deviation-from-monorepo header.
- **Phase 4 (F-1202)** — Added `*.log` to `apps/science-advantage/.gitignore` and removed two stray untracked log files.
- **Phase 5 (F-1305)** — Removed two remaining orphan `TODO` comments (`lib/gamification/badges.ts`, `app/(teacher)/teacher/page.e2e.spec.ts`); the other three cited TODOs had already been removed by the app-domain migration.
- **Phase 6 (F-1201)** — Documented the `^`-range dependency deviation in `AGENTS.md` rather than re-pinning 51+ deps; `pnpm-lock.yaml` remains the source of truth.
- **Phase 7 (F-1207)** — Appended `Track: prisma_drizzle_science_controllers_20260505` git notes to 5 remaining `refactor(science):` commits; 52 refactor and 73 total commits now carry the track ref. Added a lessons-learned entry for git-notes backfills.
- **Phase 8 (F-503)** — Created `packages/db/docs/adr/` with three ADRs (Drizzle-over-Prisma, JWT-era column drops, INTERN role), annotated `0012_codecamp_intern_role.sql`, and added `scripts/ci/sql-adr-guard.sh` with `--allow` grandfathering.
- **Phase 9 (F-1301)** — Added root-level `commitlint.config.js` (with inline plugin), `.husky/commit-msg` hook, `@commitlint/cli`, `@commitlint/config-conventional`, and `husky` devDependencies; documented the new-commits-only scope in root `AGENTS.md`.
- **Phase 11 (Final Acceptance)** — Fixed FR-table staleness (F-705/F-1202 marked complete, malformed F-1301 placeholder removed); recorded live-gate results (lint passes; test/seed/build environment-blocked or pre-existing).
- **Phase 12 (Closeout)** — Marked `audit_20260603_housekeeping_batch` tech-debt row `Resolved`, added batched-housekeeping lessons-learned entry, moved the track directory from `measure/tracks/` to `measure/archive/`, and updated `measure/tracks.md`.

---

## Spec Alignment

| Spec AC / FR | Status | Notes |
|--------------|--------|-------|
| FR-1 Relocate `prisma/` seed-data | **Met** | 53 JSON files relocated with SHA-256 identity preserved; `prisma/` removed; seed-script paths updated; regression test added in earlier Phase 1 work. |
| FR-2 Verify/delete 4 auth stubs | **Met** | Stubs confirmed as active delegates to `@reading-advantage/api/routes/auth`; JSDoc comments added referencing shared handlers and tests. |
| FR-3 Update `AGENTS.md` | **Met** | Prisma/NextAuth/npm references removed; deviation header added; script refs validated against `package.json`. |
| FR-4 `*.log` in `.gitignore` | **Met** | Rule added to app-local gitignore; no tracked `*.log` files remain. |
| FR-5 Backfill orphan TODOs | **Met** | Remaining orphan TODOs removed; original 5 cited TODOs were partially resolved earlier by app-domain migration. |
| FR-6 Re-pin `^` deps | **Met via documented deviation** | Chose documentation over strict pinning; `AGENTS.md` now explains `^` ranges and `pnpm-lock.yaml` as source of truth. |
| FR-7 `git notes` on refactor commits | **Met** | 5 backlog commits annotated; aggregate counts satisfy contract; negative controls preserved. |
| FR-8 Add `docs/adr/` + SQL-ADR guard | **Met** | 3 ADRs created, migration annotated, executable guard script with `--allow` grandfathering added. |
| FR-9 `commitlint` config | **Met** | Config, hook, deps, and docs added; regex exempts `chore` and mandates `track_id` for other types. |
| FR-10 App-local CI deletion | **Deferred / Met** | Resolved by Track 11 (`ci_typecheck_alignment_20260603`) per coordination agreement. |
| Final acceptance | **Met (contract-level)** | FR table and cross-phase artifact tests pass; live `seed`/`build` gates are environment-blocked or pre-existing. |
| Closeout | **Met** | Tech-debt resolved, lessons-learned updated, track archived. |

**Overall alignment:** **Met** — all 10 findings are resolved or explicitly deferred, and the track is archived.

---

## Code Quality Observations

- **Strong TDD / Red-Green discipline:** Each phase has a dedicated vitest contract file, Red-phase recording in `plan.md`, and Green-phase verification notes.
- **Data integrity for Phase 1:** The `pre-snapshot.sha` fixture and 53/53 hash-match verification protect against data corruption during the `prisma/` relocation.
- **ADR guard is well-scoped:** `sql-adr-guard.sh` uses `grep -E` with `--allow` grandfathering, making it suitable for incremental adoption without blocking existing migrations.
- **Commitlint regex is tight:** Corrected the plan.md `20260\d{4}` typo to `2026\d{4}`; added inline plugin because commitlint v19 does not ship `subject-pattern`; exempted `chore` as specified.
- **Honest environment deferrals:** Live `pnpm seed` and `next build` failures are explicitly recorded as environment-bound or pre-existing rather than falsely claimed green.
- **Process note:** One post-commitlint commit (`96bd4d9b fix(db): ... (F-503)`) uses the old `(F-503)` suffix instead of `(track_id: ...)`. Since commitlint is new-commits-only and this was a Phase 8 follow-up fix made shortly after the config landed, it likely predated the hook being fully active; it is not a spec violation but is worth noting for future enforcement.

---

## Risks or Blockers

- **No severe blockers.** The track is closed and archived.
- **Minor process risk:** `96bd4d9b` slipped through without the new `track_id` suffix. Going forward, ensure the husky hook is active in all local environments (i.e., `pnpm install` has run so `.husky/commit-msg` is wired).
- **Live gates remain environment-bound:** `pnpm seed` and `next build` were not fully green in the codex sandbox. Seed is blocked by podman Postgres networking; build fails on a pre-existing `packages/utils` Node-only `child_process` import that is unrelated to housekeeping. These are accepted per the plan's environment-bound deferral policy, but the build failure should be tracked by a follow-up track for `packages/utils` browser bundling.
- **Stashed Phase 9 follow-up:** `commitlint.config.js` + `pnpm-lock.yaml` improvements were stashed during Phase 11 to satisfy the supervisor gate. The stash was not popped before closeout; if it contains uncommitted improvements, it should be applied or discarded explicitly to avoid losing the work.

---

## Recommended Next Actions

1. **Pop or discard `stash@{0}`** containing the Phase 9 follow-up (`commitlint.config.js` + `pnpm-lock.yaml`) so the commitlint improvements are not lost.
2. **Enforce the husky hook** by ensuring all contributors run `pnpm install` after pulling `ac9b50be`; verify that future commits use `(track_id: <name>_<YYYYMMDD>)`.
3. **Open a follow-up track** for the pre-existing `packages/utils` browser-bundling failure (`child_process` in `packages/utils/dist/index.js`) that blocks `next build`.
4. **Run live acceptance from a dev environment** with reachable Postgres to close the deferred `pnpm seed` gate formally.
