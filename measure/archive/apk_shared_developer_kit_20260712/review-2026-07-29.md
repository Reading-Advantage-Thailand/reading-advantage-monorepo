File writing is blocked in this mode, so here's the report (the empty `review-2026-07-29.md` placeholder in the track's archive dir is still empty — let me know if you want me to fill it and I'll ask for permission).

---

# Review: `apk_shared_developer_kit_20260712` — 2026-07-29

## 1. Summary of changes

One commit in the 24h window carries this track:

| | |
|---|---|
| Commit | `fd42aeb3c` `feat(apk): add shared developer kit` |
| Date | Tue 2026-07-28 21:58 +0700 |
| Size | 82 files, +7818 / −17 |

- **`packages/advantage-play-kit/src/`** — new `systems/` (7 accepted capabilities + `gameplay-primitives.ts`), `responsive/` (683-line composition engine), `presentation/`, `qc/`, `guards/`, `scaffolding/`, `compatibility/`, `diagnostics/`, `testing/` extensions; `runtime/` extended for responsive recompose and resize-without-canvas-recreation.
- **`apps/advantage-games/`** — `AdvantageGamesAuthoringQc.tsx` (304 lines), `/qc` page wiring, `StandardPackQc` updates, Playwright spec `tests/e2e/qc/authoring-qc.spec.ts`.
- **Docs/scripts** — `docs/developer-kit.md`, `docs/simplification-report.md`, `scripts/check-accepted-inputs.mjs`.

Note on framing: the track was already archived (`metadata.json` `status: archived`, `completed_at: 2026-07-26`), and spec/plan/acceptance JSONs landed separately in `48baed27d`. This commit is the **code landing of work accepted three days earlier**, not in-flight development.

**Gates I re-ran against HEAD:**

| Gate | Result |
|---|---|
| `check-accepted-inputs.mjs` | PASS — T10 hashes + pack release `2026.07.23` verified |
| `lint` / `check-types` | PASS |
| `test` | PASS — 238 tests / 41 files |
| `test:coverage` | 89.80% stmts, 79.11% branches, 95.12% funcs, 92.81% lines |

Coverage matches the accepted claim (89.69%); `presentation/` and `qc/` are 100% statements/lines as claimed.

## 2. Spec alignment

| Req | Status | Notes |
|---|---|---|
| FR1 package/host boundaries | **Met** | Guards enforce the split; edition-free module scan passes. |
| FR2 session & progression | **Met** | Four accepted systems + exactly-once completion, registry-traced. |
| FR3 input & control | **Met** | Keyboard/pointer-tap/drag, edge+held, gesture cancel, teardown. |
| FR4 reusable gameplay systems | **Partial** | Movement/AABB/pooling/spawning/projectiles shipped, but not registered in the capability manifest (finding 3). |
| FR5 responsive composition | **Partial** | Six reference viewports, safe areas, hysteresis, transforms, Thai overflow, unsupported sizes, state-preserving transitions all tested. Missing the spec's required fuzz/property sampling and a fullscreen transition test. |
| FR6 presentation components | **Met** | Full set with semantic DOM equivalents, 100% stmt/line coverage. |
| FR7 semantic assets | **Met** | Pinned version + catalog digest, typed resolver, selected-union, attribution, fail-closed. |
| FR8 test & QC kit | **Partial** | Fixtures/assertions/budget instrumentation/browser helpers present; no device FPS benchmark. |
| FR9 authoring workflow | **Met** | 10-file non-copying scaffold, public-API exemplar, docs, working `/qc`. |
| FR10 prove simplification | **Met** | `docs/simplification-report.md`. |

Acceptance criteria still unmet, carried as disclosed gaps: Reading/Primary import smokes (not run — no production consumers), representative-device 60 FPS (not run), manual owner browser acceptance (not claimed), full Advantage Games Jest (nondeterministic legacy `devourerSlime`/`hauntedLibrary` failures, unrelated to this surface).

## 3. Code quality observations

**Good:** Zod at every external/config boundary; JSDoc present on sampled exported APIs; `guards/blocked-scopes.ts` handles its own obsolescence honestly (historical fail-closed guards `@deprecated`, documented, no longer wired into any live path); injected clock/RNG make the simulation helpers genuinely reusable rather than test-only.

**Concerns:**
- `responsive-composition.ts` at 683 lines is the largest and lowest-covered new module (83.6% stmts / 77.69% branches), bundling schema, resolution, regions, transforms, text fit, and transitions.
- Branch coverage 79.11% sits just under the >80% NFR read as a global gate.
- Deprecated blocked-scope guards are still re-exported from the public barrel `src/guards/index.ts` — a cartridge can still import a hard-throwing responsive guard that no longer reflects policy.
- One squashed 82-file / +7818-line commit makes review and bisection hard.

## 4. Risks or blockers

1. **Evidence-hash drift (integrity).** Two of 23 files in `t11-owner-extension-acceptance-v1.json` were committed with content that never matched their accepted SHA-256:
   - `src/assets/semantic-product-bindings.ts` — accepted `96cf0cbd…`, committed and current `937ea020…`
   - `apps/advantage-games/src/app/qc/page.test.tsx` — accepted `21d695ec…`, committed and current `ec96dbca…`

   Both are touched only by `fd42aeb3c`, so this is not later-track drift — committed content diverged from what was hashed at acceptance. (`AdvantageGamesAuthoringQc.tsx` *did* match at `fd42aeb3c` and drifted afterwards via `e0a5fb2a5` in the cutover track — legitimate.) The other 20 verify clean.

2. **Self-declared follow-up only half done.** The artifact records `uncommitted_acceptance_state: requires_follow_up` — "commit the reviewed remediation and then refresh the immutable acceptance references." The commit landed; the refresh did not. The artifact still describes itself as uncommitted, and all six `bound_history`/`extension_inputs` paths point at `measure/tracks/…` while the track now lives under `measure/archive/…`. Contents match at the new paths, so this is path drift, not content drift. `check-accepted-inputs.mjs` *was* correctly updated to archive paths.

3. **Capability traceability gap.** `ACCEPTED_CAPABILITY_REGISTRY` still lists exactly seven capabilities, none covering movement/collision/pooling/spawning/projectiles, and `gameplay-primitives.ts` carries no corpus or evidence reference. The owner extension authorizes *implementing* these without a new scope gate, but the spec's "every implemented shared capability traces to accepted corpus evidence" now has no backing artifact for them.

4. **Responsive matrix under-verified against its own spec.** `measure/apk-responsive-game-composition-spec.md` calls the six reference viewports "minimum common gates, not permission to special-case only those dimensions" and requires fuzz/property tests sampling widths, heights, and aspect ratios between supported bounds. The suite covers exactly those six. Fullscreen is a config flag and a transition reason with no transition test; orientation has one.

5. **Stale graph evidence.** The repo-graph scan is recorded as `timed_out`; the zero undocumented-export count is carried over from the earlier bounded scan, not independently re-verified.

## 5. Recommended next actions

1. Resolve the two mismatched evidence hashes — re-hash the committed files and publish a corrected acceptance revision, or find what changed between hashing and commit and restore it. Highest priority: it undermines the hash-binding discipline the track is built on.
2. Publish the deferred reference refresh: repoint `bound_history`/`extension_inputs` to `measure/archive/…`, clear `uncommitted_acceptance_state`, re-derive `acceptance_artifact_sha256` in `metadata.json`.
3. Extend `ACCEPTED_CAPABILITY_REGISTRY` with gameplay-primitive entries classified explicitly as forward product decisions rather than corpus-traced capabilities.
4. Add fuzz/property viewport tests and a fullscreen transition test; lift `responsive/` branch coverage above 80% and consider splitting the module along schema / resolution / transforms / text seams.
5. Stop re-exporting the deprecated blocked-scope guards from the public barrel before cartridge tracks start consuming the kit.
6. Re-run repo-graph to confirm the undocumented-export count independently.
7. Leave the disclosed gaps (Reading/Primary smokes, device FPS, manual owner QC) assigned to consuming cartridge tracks, where real consumers exist to test against.
