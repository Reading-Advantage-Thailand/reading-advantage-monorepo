# Track Review — pnpm11_major_migration
**Date:** 2026-06-23  
**Review base:** `bee70d76` (24h ago) → `HEAD`  
**Commits reviewed:** `0a6f5342` (previous daily automation — metadata/registry update only)

## Summary of changes

No source-code work landed on this track in the last 24 hours. The only changes were produced by the 2026-06-22 daily automation commit (`0a6f5342`):

- Updated `measure/tracks/pnpm11_major_migration/metadata.json` (registry/status notes).
- Added `measure/tracks/pnpm11_major_migration/review-2026-06-22.md`.
- Updated `measure/tech-stack.md` pnpm version line.

## Spec alignment: partial

| Acceptance Criterion | Status | Notes |
|----------------------|--------|-------|
| pnpm upgraded to 11.x | ✅ Met | `package.json#packageManager` = `pnpm@11.8.0`. |
| `pnpm-workspace.yaml` updated | ✅ Met | Overrides, peerDependencyRules, allowBuilds, nodeLinker, resolvePeersFromWorkspaceRoot all present. |
| Lockfile regenerated | ✅ Met | `lockfileVersion: '9.0'`, pnpm 9 package-key format. |
| `pnpm install --frozen-lockfile` | ✅ Met | Exit 0 verified in prior session. |
| `pnpm dedupe --check` | ✅ Met | Exit 0 verified in prior session. |
| CI pipelines updated | ✅ Met | `pnpm/action-setup@v4` has no explicit `version:` key; SSOT = `packageManager`. |
| All apps build/test under pnpm 11 | ⚠️ Partial | Filtered aggregate gate (`@reading-advantage/ai`) passes; full monorepo gate fails on pre-existing auth/db/domain issues unrelated to pnpm. |
| Documentation updated | ✅ Met | `measure/tech-stack.md` documents `pnpm@11.8.0`. |

## Code quality observations

- **Strengths:**
  - Contract tests are comprehensive and pass: `node --test measure/tracks/pnpm11_major_migration/__tests__/*.test.mjs` → **24/24 pass**.
  - Closeout report honestly documents the environment-blocked full aggregate gate rather than faking a pass.

- **Concerns:**
  - The track has been `[~]` for several days while the pnpm-specific work is effectively done. The remaining red on the full monorepo aggregate gate is owned by other tracks (`auth` integration tests, `db` drizzle-orm layout assumptions, `domain` tenant-registry unclassified tables).
  - No new work in the last 24h means this track is at risk of becoming a "zombie" in-progress item.

## Risks or blockers

1. **Stalled in-progress state:** pnpm11-specific deliverables are green, but the track cannot be cleanly archived because the full monorepo gate still fails due to other tracks.
2. **Environment fragility:** `pnpm install`/`turbo` still tries to fetch optional native binaries for non-current platforms and emits `EPIPE`/`UND_ERR_SOCKET` warnings in restricted network environments. This may block CI or future automation runs.
3. **Scope bleed:** If the track stays open until every other monorepo failure is fixed, it will absorb unrelated work.

## Recommended next actions

1. Re-run the filtered pnpm11 aggregate gate (`pnpm turbo run lint test check-types build --filter=@reading-advantage/ai --no-daemon`) to confirm it still passes after the latest commits.
2. Document the pre-existing full-monorepo failures explicitly in the closeout report with owning tracks, then archive `pnpm11_major_migration` and update `tracks.md` to `[x]` — the pnpm-specific migration is complete.
3. Open or link a follow-up `dependency_upgrade_hardening_20260607` / `typescript6_major_migration` task for the remaining full-monorepo gate failures so they are not lost.
