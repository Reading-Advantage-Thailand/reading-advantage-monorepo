# Daily Review: shared_video_tutorial_runtime_20260710 (2026-07-12)

## Summary of changes
- **46 commits** touched approximately **80 files** across all five phases.
- S1: versioned activity contracts, state machine, authoring validator, schema/migration tests.
- S2: React player with YouTube and hosted adapters, controllers, checkpoints, replay, accessibility/mobile policy tests, and Kimi/Playwright browser evidence.
- S3: session/event storage, Drizzle persistence, idempotent batching, watched-range compression, Mastery evidence projection, and adversarial persistence tests.
- S4: tutorial repository protocol with allowlisted checks, short-lived credentials, replay protection, offline recovery, and secure report upload.
- S5: Codecamp vertical slice integration (`activity-runtime-demo` host, APK unit lesson flow, Vinext fixture, product-owner approval on 2026-07-11).

## Spec alignment
- **S1 — Met.** Strict Zod contracts cover activities, sources, segments, checkpoints, questions, remediation, tutorial steps, and events; version migration/rejection is deterministic.
- **S2 — Met.** React player supports YouTube/hosted media, cue points, watched ranges, questions, feedback, replay, transcripts, diagrams, keyboard/touch, reduced motion, captions, screen-reader paths, and Thai route propagation.
- **S3 — Met.** Durable, idempotent, authenticated activity sessions; engagement events stay contextual while assessed events become weighted evidence; duplicates, reordering, reconnects, and cross-tenant access are rejected.
- **S4 — Met.** Tutorial manifest validates steps/checks/hints/reveals; `pnpm tutorial:check` produces allowlisted output; server verifies signed submissions and prevents spoofed cross-user evidence.
- **S5 — Met.** One coherent I Do/We Do sequence runs inside Codecamp; resource IDs replaced ad-hoc fields without breaking unaffected lessons; Next and Vinext fixtures consume the same core/React exports.

## Code quality observations
- Clean package boundaries: `activity-runtime` has no React/Next/DB imports; `activity-react` has no Next/Vinext routing/server/database imports; `activity-tutorial` isolates CLI/checker logic.
- Strong adversarial coverage for the tutorial protocol: secrets, path traversal, undeclared commands, output injection, forged results, stale tokens, duplicates, and offline recovery.
- Browser evidence includes Kimi WebBridge screenshots and Playwright desktop/mobile scenarios for checkpoint, remediation, resume, reduced motion, and Thai localization.
- More than 80% shared-package coverage is reported; lint, type-check, build, graph update, generated docs, and doctor gates passed.

## Risks or blockers
- **Tutorial capture worker is a new production surface.** The Docker/cloudbuild deployment and resource limiter need runtime hardening before high-traffic use.
- **Offline client bundle security:** the browser-safe offline bundle must not leak secrets or allow undeclared command execution; adversarial tests are in place but production abuse patterns should be monitored.
- **Contract coupling:** Codecamp APK unit and other consumers depend on report and activity contracts; backward-incompatible changes must be versioned and migrated.
- **YouTube policy/CSP:** the hosted-media gating and content-security policy must be kept current with provider requirements.
- **No severe blockers** blocking acceptance; product owner approved S5 on 2026-07-11.

## Recommended next actions
1. Harden and monitor the tutorial capture worker deployment, including queue depth, credential rotation, and resource limits.
2. Keep the `activity-runtime-demo` host gated until mass legacy-lesson migration is approved.
3. Add production alerting for failed activity evidence projections, tutorial report verification failures, and cross-tenant access attempts.
4. Document the version-pinning policy for consumers such as `codecamp_knowledge_graph_apk_unit_20260710`.
