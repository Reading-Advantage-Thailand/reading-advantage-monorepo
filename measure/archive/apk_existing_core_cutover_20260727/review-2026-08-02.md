## Summary of changes
- Reviewed `a8e58a1`, `78f0b81`, `a507305`, `8f5cadf`, `76a9a25`, and `8fee4a9`: Dragon Flight-only signed attempts, tenant-scoped replay storage/migration, runtime and asset fixes, Reading/Primary attempt endpoints, and corrective evidence.

## Spec alignment (met / partial / missed)
**partial.** The work is title-scoped and adds asset- and tenant-aware host-proof foundations, but the five-title host, selected-output, and exact-retirement criteria remain incomplete. At `76a9a25`, neither committed host client calls the new attempt/action endpoints; the cartridge emits a diagnostic only, so the plan's end-to-end checkpoint requirement is not yet met.

## Code quality observations
- Domain inputs are strict Zod schemas; credentials/checkpoints are HMAC-verified; persistence is tenant-classified and the routes derive actor and school from sessions.
- Focused domain, route, runtime, and migration tests were added. They were not rerun for this review because the checkout has unrelated modifications.

## Risks or blockers
- No committed browser-to-endpoint bridge enforces the server-issued dwell/checkpoint chain in either host.
- The plan records the Reading full TypeScript baseline as still ungreen; retirement and owner/independent acceptance remain blocked.

## Recommended next actions
1. Wire both committed host clients to issue, attest, and complete the signed protocol; add an end-to-end same-frame rejection test.
2. Resolve or explicitly attribute the Reading typecheck baseline, then complete title-specific host proof before considering retirement.
