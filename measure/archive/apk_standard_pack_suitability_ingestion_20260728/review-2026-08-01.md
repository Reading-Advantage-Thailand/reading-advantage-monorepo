## Summary of changes
- Reviewed commit `9c4a4e1d234854e6a1ae3e63d9d767ddd3bbd162` from the last 24 hours, which adds the Existing Core Task 5 canonical-reuse package, its focused Vitest coverage, public asset exports, hash-bound evidence and acceptance records, related Measure guards, and restart-test timeout increases.
- The committed package derives 17 title/role dossiers for five titles and seven unique canonical semantic keys. It records the accepted release, source receipts, licensing, credit, measured descriptors, selected unions, and explicit non-production authority boundaries.
- The worktree is dirty with many unrelated changes. In particular, the current Existing Core plan has advanced Task 6 from `[~]` to `[x]` after the reviewed commit, so working-tree verification is not equivalent to commit `9c4a4e1d2`.

## Spec alignment (met / partial / missed)
- **Met:** The bounded canonical-reuse scope satisfies the dossier, closed-disposition, candidate-comparison, release-pin, provenance, license, credit, descriptor, and hash-bound evidence requirements. The behaviorally incompatible side-view hero is rejected, and no ingestion, migration, cutover, deployment, or production authority is granted.
- **Met:** The reviewed evidence and related owner acceptance bind the exact five-title/17-role package, selected unions, source bytes, prior lineage, and host-proof inputs. Focused guards preserve historical receipts and fail closed on stale release data.
- **Partial:** The full track goal is not complete. No real legacy asset, lawful source packet, concrete-title behavior review, or real additive ingestion has been accepted. The plan and metadata explicitly defer this work, so the ingested-asset discoverability acceptance criterion remains unmet by design.
- **Missed:** No additional miss was identified within the bounded Phase 6 canonical-reuse change. Full-track completion still depends on the deferred real-asset path above.

## Code quality observations
- The implementation is strongly fail-closed: release identity, catalog bytes, source receipts, descriptors, dossier digests, and selected candidate suitability are checked before the package is returned. The focused tests cover stale pins, rejected candidates, authority boundaries, and evidence integrity.
- `createSelectedUnionInputs` uses `localeCompare` for ordering (`packages/advantage-play-kit/src/assets/existing-core-suitability.ts:717-728`). That is weaker than a code-point comparator for a requirement described as deterministic across runtimes.
- The 17-role matrix and measured descriptor facts are hardcoded in the TypeScript package while parallel JSON evidence is maintained under `measure/tracks/`. The tests bind both surfaces independently but do not compare generated package output with the persisted dossier/matrix output, leaving a drift risk.
- `git diff --check` found trailing whitespace in the related committed review file `measure/archive/apk_existing_core_cutover_20260727/review-2026-07-29.md:54`; this is a hygiene issue outside the target package.

## Risks or blockers
- **Material blocker:** Real ingestion and production/title adoption remain unauthorized until a lawful real source packet, provenance, license, credit, behavior-suitability review, independent review, and accepted additive release exist for a concrete asset. The current evidence-only package must not be treated as satisfying that gate.
- **Medium risk:** The public `createExistingCoreTask5CanonicalReusePackage` function returns selected-union inputs while its embedded owner state is still `pending`. The related owner acceptance is an external record, so a consumer that ignores that record could use draft inputs without an API-level acceptance check.
- **Medium risk:** Visual contrast is deliberately represented by the conservative floor `minimumContrastRatio: 1`, and atlas clips/directions are intentionally unassigned. This correctly avoids fabricated claims but leaves presentation accessibility and runtime behavior unproven for any production use.
- **Verification blocker:** TypeScript tests could not run because `node` and `pnpm` are unavailable. The current dirty worktree also caused two Python lifecycle assertions to fail after Task 6 was advanced beyond the reviewed commit; the clean extracted commit snapshot passed the other 37 Python guard tests, with the remaining archive-only error caused by its lack of `.git` metadata.

## Recommended next actions
- Keep the current no-authority boundary and do not expose the canonical-reuse package as production adoption or real ingestion evidence.
- Add an acceptance-gated consumer or an accepted-manifest API so selected-union inputs cannot be used without the separate owner-acceptance binding.
- Add a deterministic ordering test and replace locale-sensitive sorting with a runtime-independent comparator.
- Generate or cross-check persisted dossier/matrix evidence from the TypeScript package to prevent divergence between hardcoded inputs and JSON records.
- Re-run the package Vitest suite, typecheck, lint, and build from a clean checkout of commit `9c4a4e1d2` in an environment with Node/pnpm before treating the review as fully verified.
- For full-track completion, provide the concrete lawful asset packet and complete the independent suitability and additive-release acceptance flow.
