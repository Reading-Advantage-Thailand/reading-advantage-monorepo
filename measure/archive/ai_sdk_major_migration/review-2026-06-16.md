I have a complete picture. Here is the review report.

---

# Track Review: `ai_sdk_major_migration` — last 24h

**Window reviewed:** commits dated 2026-06-15 07:17 → HEAD (2026-06-16 07:17). All ai_sdk activity in this window is **Phase 4 (Validate & Close)**. Phases 1–3 landed earlier (06-13/06-14).

## 1. Summary of changes

The 24h window is the Phase 4 closeout. Of ~11 commits, most are Measure docs/gate records; the substantive ones:

| Commit | What it did |
|---|---|
| `512f834f` | Recorded Phase 4 closeout artifacts (`gate-result.json`, `outdated.json`, `audit.json`) + Green-gate analysis. |
| `52e3c900` | Closed a pre-existing lint blocker in `@reading-advantage/ai`. |
| `ed6716ac` | Re-aligned `phase-12-closeout-artifacts.test.ts` + `gate-result.json` to spec AC #3/#4/#8/#9 (199-line test rewrite). |
| `725c293a` | **Adversarial audit**: fixed 2 production-blocker bugs, added 13 regression tests, filed 1 tech-debt entry. |

**The two prod-blocker fixes** (the only app-source changes in the window) — both added a missing `await` on `streamText(...)`:
- `apps/codecamp-advantage/app/api/chat/route.ts:101`
- `apps/reading-advantage/server/controllers/stories-assistant-controller.ts:276`

Without the `await`, `textStream` destructured to `undefined` (TypeError on first chunk) and `toDataStreamResponse()` ran on a Promise — i.e. broken streaming responses shipped past the P3 gate. These are real bugs the P3 contract harness missed; good catch.

## 2. Spec alignment

| AC | Status | Notes |
|---|---|---|
| 1. All `@ai-sdk/*` on target major | ✅ Met | `ai ^5`, `@ai-sdk/openai ^2`, `@ai-sdk/google ^2`, `google-vertex ^3`, `react ^2`, `provider-utils ^3`. |
| 2. Adapter layer updated | ✅ Met | `packages/ai` + `get-recommendation.ts` DI shape preserved. |
| 3. `check-types` clean | ⚠️ Partial | Migration-scope clean; aggregate gate has 1 failing package (`auth`, pre-existing/archived track). |
| 4. AI tests pass | ✅ Met | `@reading-advantage/ai`: 179 passed / 3 skipped / 0 failed. |
| 5. No direct `@ai-sdk` imports in apps | ✅ Met | Enforced by `phase-arch-no-direct-sdk.test.ts`. |
| 6. Streaming/tool-calling/structured output verified | ⚠️ Partial | Streaming + structured output covered; **tool calling NOT implemented** (now a tech-debt row). |
| 7. Generate/embed provider tests | ✅ Met | `embed` not in adapter surface — worth confirming it was in scope. |
| 8. `pnpm outdated` zero behind | ✅ Met | `outdated.json` shows all `@ai-sdk/*` on selected majors. |
| 9. `pnpm audit` no new advisories | ✅ Met (artifact present) | `audit.json` captured. |
| 10. `tech-stack.md` updated | ✅ Met | Line 41 documents selected versions + track reference. |

## 3. Code quality observations

- The `await streamText` fixes are correct and minimal.
- Strong adversarial test discipline: 13 new regression nets specifically targeting (a) unawaited `streamText` call sites, (b) arch-guard regex gaps (dynamic import/require/side-effect imports the `G1_REGEX` misses), and (c) fabricated/false-positive substring assertions in `gate-result.json`. This last one is notable — they hardened the closeout test against the closeout artifact lying.
- **Version drift in manifests:** `ai` is pinned at `^5.0.201` in some manifests and `^5.0.95` in others. Same major (contract satisfied), but inconsistent — worth normalizing.
- The `plan.md` is extraordinarily long (3700+ lines) and dominated by repeated MID-role Red-gate re-verification narratives and multi-stash worktree-churn records. This is process noise, not engineering signal, and makes the plan hard to use.

## 4. Risks / blockers

- **Tool calling is unimplemented (AC #6).** The plan claimed it was deferred per test-strategy §3 item 5, but no tech-debt entry existed until the adversarial audit filed one on 06-15 — a silent acceptance-criteria gap that nearly closed unnoticed. Track now carries it as Medium open debt.
- **Aggregate gate exits 1.** `pnpm turbo run lint test check-types build` fails on `@reading-advantage/auth#test` (10 tests). Rationale is documented and credible (9 need `DIRECT_DATABASE_URL`; 1 references an archived plan.md path) and all are owned by the archived `audit_log_retention_dsar_20260605` track — **not** introduced here. But the green-gate is being declared on a *scoped* suite, not the spec's literal "all apps compile / all tests pass." That's a defensible call but it's a deviation from AC #3/#4 as written.
- **`metadata.json` still says `status: in_progress`** despite all Phase 4 tasks marked `[x]` and adversarial audit "pass." State and plan disagree.
- **`embed` (AC #7)** is named in the spec/metadata but I see no embed path in the adapter surface — verify it was actually in scope or explicitly descoped.

## 5. Recommended next actions

1. **Decide on tool calling (AC #6):** either implement `tools`/`toolChoice` on `AIClient` now, or formally accept the tech-debt deferral in the spec so the track can close honestly against its acceptance criteria.
2. **Resolve the metadata/plan mismatch:** flip `metadata.json` to the correct terminal status (or revert the `[x]` markers) so closeout state is unambiguous before archiving.
3. **Confirm the aggregate-gate carve-out is acceptable** to the track owner — the spec says "all apps compile / all tests pass," and closure currently relies on a scoped suite. Document the deviation in the spec if accepted.
4. **Normalize the `ai` version pin** (`^5.0.201` vs `^5.0.95`) across manifests.
5. **Verify `embed` (AC #7)** scope status.

Want me to save the AC #6 tool-calling gap and the aggregate-gate carve-out decision as project memories, or open these as the track's closeout blockers?
