# Daily Review — marketing_golive_20260701 — 2026-07-05

## 1. Summary of changes (last 24h)

- **Phase 0 (Build verification & pin)** — Completed.
  - Confirmed `pnpm --filter marketing build` green; `vinext` pinned from `^0.2.0` to `0.2.0` (`8d4a3c1e`).
  - `measure/tech-debt.md` updated to record blocker resolved.
  - Marketing test suite at 202/202 passing (16 test files).
- **Phase 1 (Security gate / Wave 3 precondition)** — Completed.
  - Verified `GET /api/settings` no longer returns decrypted keys; all `/api/video/*` routes require `requireMarketingSession`; campaign routes are auth/owner-scoped.
  - Verified Zod boundaries on all external inputs and AI-adapter routing through `@reading-advantage/ai`.
  - SHAs recorded in `plan.md`.
- **Phase 2 (Deploy infrastructure)** — Mostly completed.
  - `apps/marketing/Dockerfile` added (vinext multi-stage, full `node_modules` runner).
  - `apps/marketing/cloudbuild.yaml` added with build → push → migrate → doctor → deploy → allow-public-invoker.
  - `apps/marketing/.env.example` expanded from 5 lines to 35 lines covering DB, auth, AI provider keys, and runtime flags.
  - `apps/marketing/scripts/marketing-smoke.sh` added covering unauthenticated health/session/campaigns probes.
  - Docker build verification deferred due to no network access to pull `node:22-alpine`.
- **Phases 3–4 (Deploy + QA + closeout)** — Remain `[~]`.

## 2. Spec alignment

- **Phase 0** — **Met**. Build green, version pinned, tech-debt row added.
- **Phase 1** — **Met**. Wave 3 security preconditions verified against HEAD.
- **Phase 2** — **Partial**. All artifacts written, but local Docker build was not executed; Cloud SQL / Secret Manager provisioning marked `[b] deferred:human-gated`.
- **Phases 3–4** — **Missed**. No deploy, smoke, manual QA, or closeout activity in the period.

Overall: **Partial** — deploy infra is code-complete but not exercised; runtime provisioning and QA remain open.

## 3. Code quality observations

- **Good separation of concerns**: security fixes are owned by Wave 3; this track verifies and consumes them, avoiding mixing deploy and auth remediation.
- **Vinext-specific Dockerfile is well documented**: comments explain why full `node_modules` is copied (no standalone output), addressing the spec's key technical note.
- **Cloudbuild mirrors proven pattern**: steps match `apps/codecamp-advantage/cloudbuild.yaml` with appropriate marketing-specific secrets.
- **Risk**: `cloudbuild.yaml` references `0021_sales_advantage.sql` for marketing tables. Verify this migration file indeed creates the marketing schema; a naming mismatch would break deploy.
- **Risk**: smoke script only exercises unauthenticated endpoints; authenticated campaign/video flow smoke is deferred.

## 4. Risks or blockers

- **Toolchain gap**: no `node`/`pnpm` in this environment, so build/test verification and Docker build could not be re-run.
- **Provisioning blockers**: Cloud SQL marketing DB and Secret Manager entries are human-gated; until provisioned, Phase 3 deploy cannot proceed.
- **Wave 3 dependency**: if any Wave 3 marketing security finding is reverted, deploy is irresponsible. Current HEAD verification looks clean, but this should be re-checked before `gcloud builds submit`.
- **No CI/CD trigger**: per spec non-goals, deploy remains manual `gcloud builds submit`; this is acceptable but increases operational toil.

## 5. Recommended next actions

1. Verify `apps/marketing/Dockerfile` builds and boots on a networked machine (`docker build -f apps/marketing/Dockerfile .`).
2. Provision Cloud SQL marketing database and Secret Manager entries, then update `plan.md` to mark Phase 2 provisioning `[x]`.
3. Run `gcloud builds submit --config apps/marketing/cloudbuild.yaml` in the `reading-advantage` project.
4. Execute `apps/marketing/scripts/marketing-smoke.sh` against the live Cloud Run URL and add authenticated campaign/video flow probes.
5. Complete Phikul's manual end-to-end QA pass (login → create campaign → research topics → generate Thai script → edit scenes → persist project).
