# Review: marketing_golive_20260701 — 2026-07-19 (Kimi fallback — Claude limit)

## Final resolution — 2026-07-19

The findings below are retained as point-in-time review evidence and are now
resolved. Exact-source regional Cloud Build
`08fd00a1-de86-4f8f-b65d-632832279fa2` at commit
`a7fc3fbb6476eb30f95c4f1bd5757d2d7708ba29` passed all 15 release steps,
including migrations through the current journal, database doctor and runtime
contract, no-traffic candidate verification, exact revision promotion, mapped
domain checks, custom-domain smoke, and rollback evidence. Revision
`marketing-00013-jil` serves 100% traffic at digest
`sha256:df12a3aa962cf861a2332ffab766588330456fc7b1a3e4e84e67e87a69e5b2d6`.
Shell, database health, and Accounts readiness return 200; 301/301 tests pass;
the production dummy-member campaign/script/project persistence journey and
administrator-settings denial passed; and the final revision has no ERROR/5xx
log entries in the audited window. The track is therefore complete and archived.

The original review sections below describe the pre-release state and must not
be interpreted as current deployment status.

## 1. Summary of changes

14 commits carry `track_id: marketing_golive_20260701` in the window (07-18 09:16 → 07-19 03:11 ICT), in three waves:

- **Morning deploy-infra fixes** — `01204e08` preserve configured API secrets in the settings
  flow (new `app/lib/settings-update.ts` + page/route changes + 161 test lines); `36157dcc`
  clean-database deployment reliability (new `app/lib/ai-credentials.ts`, auth hardening,
  cloudbuild/env fixes); `44e36126` `.gcloudignore` includes app sources in Cloud Build;
  `0f4fca2f` build workspace deps inside the image; `59ab8206` smoke script aligned with the
  auth shell; `b925b9f8` preserve module runtime metadata in the Dockerfile.
- **Go-live (recorded ~14:22–14:49)** — `4afa405d` + `c6f3564f`: build
  `2e1d5b73-4118-480f-aeea-fe8f50db14b2` SUCCESS, revision `marketing-00005-fzp` on 100%
  traffic, `https://marketing.reading-advantage.com` live with Accounts SSO, smoke 7/7,
  `measure/deployment-status.md` updated.
- **Evening hard-review remediation** (six code commits, all after the deploy record) —
  `444f4f3e` project edits persist across reloads (reworked `api/video/projects` route, editor
  page, 472 new test lines); `cea4be8a` content-workflow contracts (script/topic schemas,
  save-topics concurrency, **introduces migration 0041**); `ca93a74c` release-review gates
  (**reworks 0041** with immutable NFC normalization + predecessor-writer trigger, extracts
  `MarketingAppShell` from `layout.tsx`, forbidden/auth-client UI states, non-OK response
  handling); `24c39d4c` authorization gates (route-handler inventory test asserting auth on
  every route, settings-page admin gate); `58bf6a05` inventory recognizes re-exported handlers;
  `ec149656` cloudbuild doctor now requires migration `0042`.
- Cross-track `apps/marketing` touches from `company_identity_sso_20260715` (`19d7f4cd`,
  `ce169c7c`, `6c72639b`, `2ec48b0f`) also landed in the window.

## 2. Spec alignment (met / partial / missed)

- **Met:** build pinned (`vinext` `0.2.0`, tech-debt row resolved); Wave 3 security gate
  verified pre-deploy; vinext Dockerfile + cloudbuild.yaml with migrate → doctor → deploy →
  smoke gating; Cloud SQL DB + Secret Manager provisioning; `.env.example` completed;
  smoke script green — re-verified this review: live `/api/health/db` 200, `/api/settings`
  401 unauthenticated; `deployment-status.md` shows marketing deployed.
- **Partial:** manual end-to-end QA (Phikul) still `[~]` in plan; Phase 4 closeout items
  (tech-debt go-live shortcuts, lessons-learned, archive/tracks.md) still `[~]`.
- **Missed:** none outright — but see the plan-evidence anachronism below.

## 3. Code quality observations

- Every remediation commit ships tests; the evening wave added ~700+ lines across
  `project-reload`, `project-update*`, `workflow-correctness`, `release-remediation-ui`,
  `release-response-handling`, `migration-0041-live`, `route-handler-inventory`.
- The route-handler inventory test is a structural guard that every route requires auth —
  a genuinely useful regression net, extended to re-exports in `58bf6a05`.
- Migration 0041 is well-built: `IMMUTABLE`/`STRICT`/`PARALLEL SAFE` normalization function,
  trigger-derived `normalized_key`, non-destructive duplicate preflight, runtime probe.
- `layout.tsx` slimmed from inline sidebar JSX to `MarketingAppShell` — cleaner.
- `pnpm --filter marketing lint` (bounded run this review): **0 errors, 13 warnings**, all
  unused vars, several being unused `error` bindings in catch blocks
  (`api/campaigns/route.ts`, `api/settings/route.ts`).
- Full vitest suite intentionally not run (PGlite stall risk per task bounds). Targeted run:
  `route-handler-inventory.test.ts` 4/4 pass. Commit notes on `ca93a74c` claim 265/265 plus
  lint/type/build green — plausible but not independently re-verified here.

## 4. Risks or blockers

- **Production is behind master.** Recorded revision `marketing-00005-fzp` (build `2e1d5b73`,
  ~14:00–14:49 07-18) predates all six evening code commits — project-edit persistence,
  release/authorization gates, and migration 0041 are committed but **not live**.
- **Production DB is two migrations behind the HEAD gate.** At deploy time the cloudbuild
  doctor required only `0021_sales_advantage` (verified in `c6f3564f:cloudbuild.yaml`); 0041
  was created at 23:25 and reworked at 00:40, and HEAD (`ec149656`) requires
  `0042_company_product_principal_local_unique`. Next deploy must apply 0041 + 0042; 0041's
  duplicate preflight over live `past_topics` data is the step to watch.
- **Plan-evidence anachronism:** plan.md Phase 2 (edited in `ec149656`) now says the recorded
  builds migrated "through 0042" with doctor requiring "exact 0042" — neither daytime build
  could have done that. The record describes HEAD, not what actually ran; audit-trail smell.
- **QA target moved:** the evening rework changed exactly the pages Phikul's pending manual QA
  exercises (editor, campaigns, settings). Running QA against stale `00005-fzp` would
  partially test superseded code.
- **Cross-track coupling:** 0042 originates from the sales/company-identity tracks, so
  marketing's deploy gate now depends on another track's shared-journal migration.

## 5. Recommended next actions

1. Redeploy from HEAD: `gcloud builds submit --config apps/marketing/cloudbuild.yaml` —
   migrate applies 0041/0042, doctor enforces 0042, then re-run `marketing-smoke.sh`.
2. Watch migration 0041's non-destructive duplicate preflight on production `past_topics`
   during that migrate step; abort and dedupe if it trips.
3. Run Phikul's manual end-to-end QA against the **new** revision only after the redeploy.
4. Correct plan.md Phase 2 wording to state what builds `7a6597f5`/`2e1d5b73` actually ran
   (doctor required `0021` at deploy time).
5. Complete Phase 4 closeout (tech-debt shortcuts, lessons-learned on vinext→Cloud Run,
   archive) and optionally sweep the 13 lint warnings.
