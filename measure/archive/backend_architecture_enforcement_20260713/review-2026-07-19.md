# Review: backend_architecture_enforcement_20260713 — 2026-07-19 (Kimi fallback — Claude limit)

## 1. Summary of changes

Gate 1 was closed yesterday morning (2026-07-18, +07) in three commits, exactly
as yesterday's review prescribed:

- **`4ff2caec` checkpoint Gate 1 (+7,731 lines)** — commits the full acceptance
  set that was uncommitted at yesterday's review: the four ACCEPTED second-round
  review artifacts (`reconciliation-{correctness,adversarial,developer-api,security}-review.md`),
  `plan-review.md`, the 5,903-line reconciliation input config
  `analyzer-reconciliation.v1.json`, the accepted baselines
  (`database.v1.json` 464→467, `provider.v1.json` 27→93, `ownership-map.v1.json`
  →111 exact exceptions), the populated acceptance record in the strategy, and
  `verification.md`. Carries the verification record as a Git note.
- **`251f108d` checkpoint architecture acceptance** — Phase 4 checkpoint carrying
  the same Git note; records the accepted transaction (preview exit 1 / no
  writes, acknowledged exit 0 / exactly one write, pre/post hashes matching the
  accepted candidates).
- **`2989f27b` close architecture Gate 1** — metadata `status: completed`,
  plan.md checkpoint SHAs recorded on Phases 3–4, strategy + registry
  (`measure/tracks.md`) flipped to `[x]` with the archive link.
- **`69371a5d` harden APK gates** — belongs to
  `apk_source_denominator_inventory_20260712`, not this track (+8,471 lines of
  APK inventory gates/tests). Included in the window only for completeness; no
  interaction with the architecture baselines.

The track directory was moved `measure/tracks/` → `measure/archive/` today; the
rename is staged in the index but not yet committed at review time.

## 2. Spec alignment (met / partial / missed)

**Overall: met — Gate 1 is formally ACCEPTED and archived.**

- FR-1–FR-4: met (unchanged from yesterday; rules/contracts, analyzers,
  counterexample fixtures all green, 210/210 focused tests).
- FR-5: **now met.** The one-time reconciliation executed per the dual-anchor
  contract: final state 467 db / 93 provider entries, 111 exact exception pairs
  (9 added), 54 covered test-only findings, 69 production additions, 0
  removals/renames. Verified live: the on-disk baselines contain exactly 467 /
  93 / 111, matching `verification.md`. Rules at closure already included the
  `COMPANY_IDENTITY_*` pair — the SSO ruleset was part of the accepted policy,
  not smuggled in later.
- FR-6: met (root command, CI step, doctor.sh, docs; both invoke the same
  `pnpm architecture:check`).
- AC-9: met. Strategy lines 285/288 now record "ACCEPTED by all four required
  reconciliation reviewers" and "Gate 1 result: ACCEPTED"; user manual
  confirmation recorded; checkpoint SHAs in plan.md.

**Post-closure live re-verification (this morning):** `pnpm architecture:check`
currently **exits 1: `new-debt (additions=58, removals=0, renames=12)`**. This
is not a closure defect — at closure the checker was verified clean (exit 0,
byte-identical runs, isolated-copy increase/removal proofs). The 58 additions
are post-reconciliation debt landed after 08:00 Jul 18, dominated by
`COMPANY_IDENTITY_PRODUCT_ISOLATION` / `COMPANY_IDENTITY_DB_BOUNDARY` findings
from `company_identity_sso_20260715` (packages/backend company-identity module,
apps/accounts, sales/codecamp/marketing OIDC libs) plus a few
`DATABASE_BOUNDARY` hits from sales/marketing commits. The ratchet is behaving
exactly as designed: fail-closed on unreviewed new debt.

## 3. Code quality observations

- The closure commits are disciplined: one commit for the accepted evidence
  transaction, one per phase checkpoint, one for the registry flip; the
  verification record rides as Git notes rather than polluting the tree.
- Rename detection held up under real churn: the 12 `~` entries are SSO-edited
  files whose finding identities moved — tracked as renames, not masked
  add/remove pairs (FR-5 rename-evasion protection working in production).
- Yesterday's trip hazards cleared: the four ACCEPTED reviews and the
  reconciliation config are committed; plan checkboxes and metadata now match
  reality; doctor.sh blank-marker debt in this track's plan was resolved.
- **Unfixed:** the stray `undefined` line at the end of failing CLI output
  (flagged yesterday) still prints — observed again in today's run. Cosmetic,
  but it is now in CI logs repo-wide.

## 4. Risks or blockers

- **CI is red repo-wide.** The `architecture:check` step wired by this track now
  exits 1 on the 58 SSO-era additions, so every track's CI trips until the SSO
  debt is remediated or explicitly ratcheted. This is intended fail-closed
  behavior, but it is immediate friction.
- **Baseline-growth discipline is the live question.** Spec out-of-scope forbids
  routine baseline growth; the SSO track must either fix its 58 findings or run
  a reviewed acknowledged baseline update with per-entry owner/rationale —
  silently absorbing them via `architecture:baseline:update --acknowledge`
  would violate the spirit of Gate 1.
- Dependent tracks formally unblocked: kernel `[~]` (scaffold `ff47431c`
  already landed), durable-worker `[ ]`, SSO `[~]`. But the SSO track is
  actively adding debt against the gate it depended on.
- The tracks→archive rename is staged but uncommitted; an interrupted
  automation run could leave the registry pointing at a half-moved tree.

## 5. Recommended next actions

1. Commit the staged archive move (and this review) to complete the closeout.
2. Triage the 58 additions with the SSO track owner: remediate true violations
   (notably `packages/backend/src/modules/company-identity/*` direct table
   access vs. the adapter-root rule) or run a properly reviewed acknowledged
   baseline update; do not bulk-acknowledge.
3. Decide on the 12 renames: if the underlying violations are unchanged, fold
   them into the same reviewed update rather than leaving CI red.
4. Fix the stray `undefined` CLI output line in `architecture-check-cli.ts`
   before the next evidence capture.
5. Keep the "no second reconciliation" invariant: any SSO baseline change must
   go through the normal acknowledged update flow with review, not a repeat of
   the Task 12a transaction.
