# Tech Debt Registry

> This file is curated working memory, not an append-only log. Keep it at or below **50 lines**.
> Remove or summarize resolved items when they no longer need to influence near-term planning.
>
> **Severity:** `Critical` | `High` | `Medium` | `Low`
> **Status:** `Open` | `Resolved`

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-04-29 | monorepo-scaffold | advantage-games ESLint 6236 warnings | Low | Open | Pre-existing code has many `prefer-const`, `no-undef`, and `no-explicit-any` warnings. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage `ignoreBuildErrors: true` / `ignoreDuringBuilds: true` | Medium | Open | Temporarily enabled to pass build. Cannot verify removal due to build hanging on resource-constrained hardware. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage: 26 failed test suites (91 tests), 50 passing | Medium | Open | Pre-existing from original repo. Game component tests fail due to Zustand v4 store mocking patterns. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage: zustand v4 vs v5 in monorepo | Medium | Open | App uses zustand v4, advantage-games uses v5. Align when reading-advantage is ready for v5 migration. |
| 2026-05-01 | migrate-primary-advantage | primary-advantage: `ignoreBuildErrors: true` / `ignoreDuringBuilds: true` | Medium | Open | Temporarily enabled. |
| 2026-05-02 | shared_config | No visual regression tests for Tailwind v4 migration | Low | Open | Deferred from shared_config_consolidation. |
| 2026-05-02 | i18n_migration | No shared i18n types across apps | Low | Open | Each app defines its own Locale type. @reading-advantage/config should export shared Locale and message shape. |
| 2026-06-08 | codecamp_qa_prod_20260517 | **P0: Deployment pipeline does not gate application rollout on matching DB migrations** | **Critical** | **Open** | Confirmed production incident: the June 2 CodeCamp image expected `users.license_id`, `expired_date`, and `grade_level`, but the production migration job had not run since May 18; all logins failed with PostgreSQL `42703` until an emergency schema repair on June 8. **Fixed by** `db_migration_ledger_20260611` (journal re-stamp, integrity test, ledger doctor, codecamp cloudbuild deploy gate). Doctor available via `pnpm --filter @reading-advantage/db doctor --check` / `--repair`. Remaining open scope: run `doctor --repair` against each production DB; wire deploy gates for non-codecamp pipelines (reading-advantage, primary-advantage, science-advantage). |
| 2026-05-14 | codecamp_curriculum | vocabulary-games: 16 pre-existing test failures (4 suites) | Medium | Open | Outside curriculum track scope — vocabulary-games package issue, not codecamp-advantage. |
| 2026-05-14 | codecamp_advantage | Mock-DB tests don't catch real DB constraint violations | Medium | Open | Requires Testcontainers/Docker-based integration test infra. Deferred: significant infra work beyond any single track; separate test-infra track required. Phase 2 review confirmed no progress. |
| 2026-05-15 | codecamp_review | `contentJson` schema validation missing in LessonContent | Low | Open | Seed data is canonical source; defensive runtime guards in place (typeof check + fallback to {}). Not in exercise-repos scope — Zod schema addition deferred: requires defining full contentJson shape (sections, blocks) across 85+ lessons. |
| 2026-05-15 | codecamp_thai_i18n_20260515 | Type-safe next-intl translation keys not configured | Medium | Open | Phase 4 complete — all keys extracted. Type generation requires next-intl plugin config. Deferred: needs dedicated follow-up track for lesson-page localization + type-safety setup. |
| 2026-05-16 | codecamp_exercise_repos_20260515 | `getInstallationTokenForRepo` uses single `GITHUB_INSTALLATION_ID` | Low | Open | Works for single-org setup. Fix: accept `owner` param, call GitHub API to list installations dynamically. Deferred: not needed until multi-org support; requires API call changes + test updates across packages/webhooks. |
| 2026-05-18 | codecamp_deployment_20260516 | No automatic CI/CD deploy trigger for codecamp-advantage | Low | Open | Current deployment uses manual `gcloud builds submit` with `apps/codecamp-advantage/cloudbuild.yaml`. Add a Cloud Build or GitHub Actions deploy trigger after local/prod QA confirms the redeploy process. |
| 2026-05-23 | prisma_drizzle_schema_unification | Zod insert/select types not exported from packages/types/ (FR-2/FR-4) | Medium | Open | drizzle-zod never installed; no createInsertSchema/createSelectSchema exports. Not a blocker — consumers use `typeof table.$inferSelect`. Track 3 plan (§0.7 + Phase 4) will hand-write the 2 affected validation files instead. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | Legacy matview SQL not folded into Drizzle migrations | Medium | Open | 8 Prisma migrations creating `mv_alignment_metrics`, `mv_srs_health*`, `mv_genre_engagement*`, `mv_assignment_funnel*`, `mv_velocity*`, `mv_activity_heatmap` preserved at `apps/reading-advantage/db-migrations/legacy-matviews/`. Already applied in prod/staging. Fresh DBs require manual psql. Fold into a Drizzle migration in a follow-up. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | lib/session.ts defaults emailVerified=true/onborda=false (cols missing in Drizzle users) | Low | Open | Legacy Prisma `users.emailVerified` and `users.onborda` columns are absent from the Drizzle schema. lib/session.ts hardcodes safe defaults with inline comment. Add columns to packages/db schema if these flags become product-critical. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | reading-advantage jest verification deferred | Medium | Open | Targeted Jest (`--testPathPattern="__test__"`) 11 suites / 194 tests PASS (re-verified 2026-05-23 after reopened SQL fixes — includes new dashboard-summary-controller / assignment-prediction-service / query-optimizer SQL-rendering tests). Full app-wide `CI=true pnpm --filter reading-advantage test` hangs >10min on this hardware (pre-existing). Build (AC#4) PASSES on Next.js 16 Turbopack. Verify full Jest in CI or on faster hardware before merge. |
| 2026-05-25 | science-advantage-ui | `/assignments` page is a hardcoded stub | Low | Open | `app/(student)/assignments/page.tsx` returns static "No assignments available yet." with no data fetching. Dashboard `StudentAssignmentsCard` works correctly. Pre-existing, not a Track 3 regression. Wire the page to `GET /api/students/me/assignments` (same endpoint the card uses). Discovered during Track 3 manual QA Gate 5. |
| 2026-05-25 | codecamp_exercise_repos_20260515 | `CODECAMP_DATABASE_URL` Secret Manager entry points at `reading_advantage` DB, not `codecamp_advantage` | Low | Open | Discovered during PR review E2E. Secret value: `postgresql://...@localhost/reading_advantage?host=/cloudsql/...`. Live Cloud Run service must be overriding the DB name via env var (codecamp tables only exist in `codecamp_advantage`; queries against the secret's `reading_advantage` find nothing). Misleading for anyone reading secrets to understand the topology. Fix: update the secret's URL path segment to `/codecamp_advantage`, or document the Cloud Run env-var override explicitly in `apps/codecamp-advantage/docs/`. |
| 2026-05-26 | audit_20260526 | reading-advantage API routes bypass domain layer | Critical | Open | **209** `route.ts` files import `db` directly; **0** use `@reading-advantage/domain`, `assertCan`, or `TenantDB`. Violates monorepo architecture (Next.js → tRPC → domain → Drizzle). No centralized auth/tenancy enforcement; each route rolls its own. |
| 2026-06-07 | codecamp_qa_prod_20260517 | Prod QA: code-complete, deployed, P1 perf follow-ups filed | High / Medium | Open | All P0/P1 launch gates green on deployed revision (commit `e3ed0c01`). Three P1 findings filed as follow-up tracks: `codecamp_perf_warm_dashboard_20260608` (warm dashboard 1363ms vs 1000ms budget); `codecamp_asset_render_blocking_20260608` (1 render-blocking `<script>` in `<head>`); `codecamp_infra_cold_start_20260608` (cold start >5s). Alert-policy artifact documented in `measure/alerts.md` (informational). |
| 2026-06-03 | audit_20260603_domain_bypass | science-advantage: App → Domain Migration (F-305 umbrella) | **Critical** | **Resolved** | Resolved 2026-06-04 by `app_domain_migration_20260603`: 27 route.ts via domain, assertCan everywhere, ~5,800→~900 lines, 335 tests pass. |
| 2026-09-23 | reading verification | Thai read-page genre badges render raw `selectType.types.*` key paths | Low | Open | Several genre keys (Art, Biography, Essay, Report, Novel Excerpt, Article) are missing from `locales/th.ts` `selectType.types`, so Thai users see raw key paths on article cards. The th file also uses lowercase `fiction` while the DB stores `Fiction`. Add the missing keys or normalize the lookup. |
| 2026-09-23 | reading verification | Question cards poison the question store on articles without questions | Medium | Open | MCQuestionCard and siblings call `useQuestionStore.setState` with response bodies that lack the default shape when the article has no questions, leaving `mcQuestion`/`saQuestion` malformed. The chatbot hotfix tolerates it; the cards should not write malformed state. |
| 2026-09-23 | reading verification | Chatbot renders an empty bot bubble on non-OK API responses | Low | Open | `chatbot-floating-button.tsx` shows `Error: Could not fetch response.` only when fetch throws; a 400/500 parses as JSON and renders `data?.text ?? ''` — an empty bubble. Check `res.ok` and show the error message instead. Same pattern as the sales empty-stream finding. |
| 2026-09-22 | sales verification | AI stream failure surfaces as an empty assistant bubble, no error shown | Low | Open | When the provider errors after headers are sent (e.g. discontinued model → OpenRouter 404), `/api/chat` returns 200 with an empty body; chat-tutor only shows `[Error: chat unavailable]` when fetch throws or !res.ok. Fix: detect zero-length assistant responses and show the error message, or emit an error frame from the route's stream. |
| 2026-09-22 | accounts_console_write_correctness_20260919 | Same-tick concurrent toggles of two different role checkboxes in one app can still lose a write | Low | Open | Found in S1 owner verification. Client read-modify-write refetch narrowed the race but did not close it; 350ms-spaced human toggles pass, same-tick synthetic toggles lose. Fix: server-side role merge, or disable the whole role section while any role request is pending. |
| 2026-09-22 | accounts_broken_ux_fixes_20260919 | Dev-mode console error: React eval blocked by new CSP header | Low | Open | `next.config.ts` CSP has no `unsafe-eval`; React dev mode evals for debugging. Production unaffected (React never evals there). Options: allow `unsafe-eval` in development only, or accept the noise. |
| 2026-09-20 | sales_structural_alignment_20260919 | sales-advantage admin navigation performs two Accounts introspections (admin layout RSC guard + tRPC request auth) | Low | Open | Accepted per roadmap §5 plan 6: App Router layouts cannot pass props to pages, so the layout guard (`app/[locale]/admin/layout.tsx:22-24`) and the tRPC handler's `authenticateSalesRequest` run in separate HTTP requests that must each authenticate. Data access stays protected by the `salesAdminOnly` tRPC middleware (`packages/api/src/routers/sales.ts:149`). Removal path: convert admin pages to server components calling domain functions directly, or add a short-TTL introspection cache with an explicit revocation-latency decision. |
| 2026-05-26 | audit_20260526 | primary-advantage Prisma still fully active | Critical | Open | **56** files import `@prisma/client`; `prisma/schema.prisma`, `migrations/`, `lib/prisma.ts` still exist; `package.json` still depends on `@prisma/client`, `prisma`, `@prisma/adapter-pg`. **Owner:** `primary_advantage_drizzle_migration_20260526` (carved out from Track 4). Blocks any unified backend work for this app. |
| 2026-06-05 | agents_md_audit_science_advantage_20260603 | Audit-track tests have no monorepo-root vitest config + Phase 0 git-clean test blocked | Low | Open | (1) Phase 0 setup tests live at `apps/science-advantage/lib/__tests__/audit-phase0-setup.test.ts` (only science-advantage vitest config picks them up). Natural home `measure/tracks/.../__tests__/` has no config. Fix: add root-level `vitest.config.ts` covering `measure/**/__tests__/*.test.ts`. (2) `audit-phase0-setup.test.ts` §"Phase 0 Task 3" expects `git status --porcelain apps/science-advantage/` to be empty but 8 modified + 1 untracked files from `ai_adapter_package_20260603` remain uncommitted. Fix: commit/stash the AI-adapter changes on their owning track. |
| 2026-06-05 | agents_md_audit_science_advantage_20260603 | build-graph route count ≠ route.ts file count (34 vs 27) | Medium | Open | `build-graph query ... type='route'` counts HTTP method exports, not route.ts files (27 → 34 entries). Test expects 27. Fix: update test to `COUNT(DISTINCT file_path)` or change assertion to 34. |
| 2026-06-05 | agents_md_audit_science_advantage_20260603 | 5 Phase 1 inventory-drift tests hardcode expected values | Medium | Open | Tests hardcode `claimed = N` (20/88/330/48/21) and assert filesystem matches. Filesystem has drifted (22/92/335/52/19). Fix: read expected values from `readInventoryMetric()` instead of hardcoding. |
| 2026-06-05 | agents_md_audit_science_advantage_20260603 | Phase 5: 2 of 95 finding-ID-reference tests RED (Track 0 metadata.json omits F-1003; Track 1 spec.md omits 4 subsumed F-IDs) | Low | Open | Fix: add missing F-IDs to Track 0 metadata.json and Track 1 spec.md. 1-line + 4 F-ID edits. |
| 2026-06-15 | ai_sdk_major_migration | Tool calling not yet on `AIClient` (spec AC #6) | Medium | Open | Spec AC #6 says "tool calling verified" but `AIClient` exposes only `generateObject` / `generateImage` / `generateText` / `streamText` — no `tools` path. Plan claimed tool calling was deferred per test-strategy §3 item 5 but no entry was filed. Fix: add `tools` to input types and forward to v5 SDK's `toolChoice` / `tools`; extend `runAIClientContract` with a `tools` round-trip. |
| 2026-08-22 | apk_lint_gate_20260822 | Shadow Gate Dungeon renders hard-coded English, unlike its sibling games | Medium | Open | `ShadowGateDungeonGame.tsx` called `useScopedI18n('pages.student.gamesPage.shadowGateDungeon')` and then made zero `t(...)` calls, so strings such as "Dark Cell", "Forgotten Crypt", and "Abyssal Chamber" ship untranslated. Sibling games make 10-39 `t(...)` calls each. The dead hook call was removed to clear the lint gate; the missing translation keys and call sites remain. Fix: add the `shadowGateDungeon` scope to every locale catalog and replace the literal strings. |
| 2026-08-29 | sales_marketing_sso_parity_20260829 | Shared cross-app redirect helper package (public-url/origin-approval/callback handoff) | Medium | Open | Owner: daniebo. |
| 2026-08-29 | sales_marketing_sso_parity_20260829 | Stale Sales admin source-grep checks and Marketing audit-field test drift | Low | Open | Pre-existing remediation candidates flagged in `measure/tracks/sales_marketing_sso_parity_20260829/reviews/phase3-review-ab-2026-08-29.json`. |
| 2026-09-12 | structural_ux_alignment_20260911 | **Game/flashcard/question endpoints trust client-derived XP** (Reviews C F-C6, D-2/3/4) | **Critical** | **Open** | 9 game controllers (`magic-defense`, `rpg-battle`, `castle-defense`, `dragon-flight`, `wizard-zombie`, `rune-match`, `enchanted-library`, `potion-rush`, `dragon-rider`), flashcard result savers (`flashcard-controller.ts` cloze/sentence/word ordering), question/story LAQ rating XP — all derive XP from client `xp`/`totalScore`/`correctAnswers`/`rating`. Needs dedicated track: server-derived XP only (pattern: `xpForActivityType` from the F-B1 repair). See `docs/reading-advantage-deferred-items-plan.md`. |
| 2026-09-12 | structural_ux_alignment_20260911 | Activity-log fake-target XP farming (Review D F-D5) | High | Open | `postActivityLog`/`putActivityLog` ignore client XP amounts but accept arbitrary targets + completion flags; repeated fake targets farm fixed XP. Fix: server-side target existence verification. **2026-09-18 widened by `reading_qa_srs_persistence_20260918`:** `resolveActivityTarget` now accepts explicit top-level client `targetId` (previously 400-rejected), so arbitrary-target XP posts succeed; the flashcard XP flow requires it (review H1). Expedite this fix or restrict `targetId` acceptance to flashcard activity types. |
| 2026-09-12 | structural_ux_alignment_20260911 | License/user-list scope gaps (Reviews C F-C5, D F-D7) | High | Open | `activateLicense` (and latent `deactivateLicense`/`updateUserLicense`) permit any ADMIN/TEACHER to target any user cross-tenant; `getAllUsers` exposes all users to any authenticated caller. Owner: `reading_license_control_plane_migration_20260722`. |
| 2026-09-12 | structural_ux_alignment_20260911 | `parseActivityType` returns `LESSON_READ`/`LESSON_RATING` absent from `lib/enums`; circular test assertion | Medium | Open | Review B F-B8. Align `lib/enums` with `user-activity-log-model.ts`; remove `canonicalValues.has(expected)` circularity in `parse-activity-type.test.ts:42-51`. |
| 2026-09-12 | structural_ux_alignment_20260911 | `change-role.tsx` dev-settings role selection now 403s for non-staff (intentional) | Low | Open | Side effect of F-C1 repair. Decide: remove the dev UI or add a dev-only bypass. |
| 2026-09-12 | component_deduplication_20260911 | FSRS manage-table merge (`vocabulary/tab-manage.tsx` + `manage-tab.tsx`, ~400 duplicated lines) deferred | Medium | Open | Deferred at track planning; also games-catalog card `Link` a11y wrap deferred behind APK track files. |
| 2026-09-14 | primary_test_hygiene_upload_fixes_20260914 | Upload routes: school guard keys off stored row not session; temp file leaks on validation-error 400 | Low | Open | Pre-existing, found by Kimi review R1 L2/L3. csv/route.ts:97 and classes/route.ts:214 read `currentUser.schoolId` (stored row) for the fail-closed guard while writes stamp from the session; csv 400 path returns before temp-file `unlink`. Both documented in the track's session-school-policy.md. |
