# Tech Debt Registry

> This file is curated working memory, not an append-only log. Keep it at or below **50 lines**.
> Remove or summarize resolved items when they no longer need to influence near-term planning.
>
> **Severity:** `Critical` | `High` | `Medium` | `Low`
> **Status:** `Open` | `Resolved`

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-04-29 | monorepo-scaffold | advantage-games ESLint 6236 warnings | Low | Open | Pre-existing code has many `prefer-const`, `no-undef`, and `no-explicit-any` warnings. |
| 2026-04-29 | monorepo-scaffold | react-konva peer dependency warning (wants react 19.2.0, app has 19.1.0) | Low | Open | Doesn't block builds. Consider upgrading React to 19.2.x across the monorepo. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage `ignoreBuildErrors: true` / `ignoreDuringBuilds: true` | Medium | Open | Temporarily enabled to pass build. Cannot verify removal due to build hanging on resource-constrained hardware. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage: 26 failed test suites (91 tests), 50 passing | Medium | Open | Pre-existing from original repo. Game component tests fail due to Zustand v4 store mocking patterns. |
| 2026-05-01 | migrate-reading-advantage | reading-advantage: zustand v4 vs v5 in monorepo | Medium | Open | App uses zustand v4, advantage-games uses v5. Align when reading-advantage is ready for v5 migration. |
| 2026-05-01 | migrate-primary-advantage | primary-advantage: `ignoreBuildErrors: true` / `ignoreDuringBuilds: true` | Medium | Open | Temporarily enabled. |
| 2026-05-02 | shared_config | No visual regression tests for Tailwind v4 migration | Low | Open | Deferred from shared_config_consolidation. |
| 2026-05-02 | i18n_migration | No shared i18n types across apps | Low | Open | Each app defines its own Locale type. @reading-advantage/config should export shared Locale and message shape. |
| 2026-05-03 | auth_strategy_review | science-advantage: `ignoreBuildErrors: true` for pre-existing tsc errors | Medium | Open | Re-evaluated 2026-05-24 post-Prisma-removal. Baseline = **360 tsc errors / 386 lines, 4 lint errors / 6 warnings**. Blockers: (a) ~354 testing-library matcher narrowing in `*.test.tsx` — needs `@testing-library/jest-dom/vitest` types wired into `vitest.unit.setup.ts`; (b) 2 INTERN role widening in `lib/auth/session.ts`; (c) 2 missing-sibling-module errors `lib/auth/{password,rate-limit}.test.ts`; (d) 3 ProcessEnv narrowing; (e) ~4 next@16 duplicate-instance type identities; (f) 4 misc. Lint: 4 `react-hooks/immutability` errors in `components/features/teacher/analytics/`. `eslint.ignoreDuringBuilds` is NOT set in `next.config.ts`. Flag retained; comment in `next.config.ts` updated to enumerate real blockers. |
| 2026-05-14 | codecamp_curriculum | vocabulary-games: 16 pre-existing test failures (4 suites) | Medium | Open | Outside curriculum track scope — vocabulary-games package issue, not codecamp-advantage. |
| 2026-05-14 | codecamp_advantage | Mock-DB tests don't catch real DB constraint violations | Medium | Open | Requires Testcontainers/Docker-based integration test infra. Deferred: significant infra work beyond any single track; separate test-infra track required. Phase 2 review confirmed no progress. |
| 2026-05-15 | codecamp_review | `contentJson` schema validation missing in LessonContent | Low | Open | Seed data is canonical source; defensive runtime guards in place (typeof check + fallback to {}). Not in exercise-repos scope — Zod schema addition deferred: requires defining full contentJson shape (sections, blocks) across 85+ lessons. |
| 2026-05-15 | codecamp_review | Duplicate `generateReview` LLM implementation | Medium | Open | Two identical OpenRouter implementations: `webhooks/github.ts:41` and `api/routers/codecamp.ts:405`. Domain `reviewExercise` uses DI correctly. Fix: extract shared impl to a new `@reading-advantage/llm` or similar package. Deferred: requires coordinated package dependency changes across 3 packages. |
| 2026-05-15 | codecamp_review | No integration tests for webhook → LLM → comment → DB pipeline | Medium | Open | Fire-and-forget async pattern untested. Requires test DB infrastructure and Redis. Not in exercise-repos scope. Phase 2 review confirmed: no progress — deferred to infra track. |
| 2026-05-15 | codecamp_thai_i18n_20260515 | Type-safe next-intl translation keys not configured | Medium | Open | Phase 4 complete — all keys extracted. Type generation requires next-intl plugin config. Deferred: needs dedicated follow-up track for lesson-page localization + type-safety setup. |
| 2026-05-15 | codecamp_thai_i18n_20260515 | `proxy.ts` admin guard checks cookie existence only, not token validity or ADMIN role | High | Resolved | Closed 2026-05-26 by `proxy_admin_guard_hardening_20260526` for both codecamp and science apps (same vulnerability in both). Next.js 16 defaults proxy.ts to nodejs runtime; `requireRole(db, token, 'ADMIN')` from `@reading-advantage/auth` is now called per request. FORBIDDEN → redirect /dashboard?error=forbidden; UNAUTHORIZED → clear cookie + signin; DB error → fail-closed signin?error=session_check_failed. Hierarchical roles mean TEACHER/ADMIN transparently pass /student. 8+17 unit tests + 6 science integration tests (real DB). Builds pass. |
| 2026-05-16 | codecamp_exercise_repos_20260515 | Webhook async LLM review pipeline has no retry or dead-letter queue | Medium | Open | Error handling marks review as "reviewed" with error note, but transient failures are not retried. Requires BullMQ/Redis job queue. Deferred to infra track — Phase 2 review confirmed no progress. |
| 2026-05-16 | codecamp_exercise_repos_20260515 | `getInstallationTokenForRepo` uses single `GITHUB_INSTALLATION_ID` | Low | Open | Works for single-org setup. Fix: accept `owner` param, call GitHub API to list installations dynamically. Deferred: not needed until multi-org support; requires API call changes + test updates across packages/webhooks. |
| 2026-05-18 | codecamp_deployment_20260516 | No automatic CI/CD deploy trigger for codecamp-advantage | Low | Open | Current deployment uses manual `gcloud builds submit` with `apps/codecamp-advantage/cloudbuild.yaml`. Add a Cloud Build or GitHub Actions deploy trigger after local/prod QA confirms the redeploy process. |
| 2026-05-23 | prisma_drizzle_schema_unification | Zod insert/select types not exported from packages/types/ (FR-2/FR-4) | Medium | Open | drizzle-zod never installed; no createInsertSchema/createSelectSchema exports. Not a blocker — consumers use `typeof table.$inferSelect`. Track 3 plan (§0.7 + Phase 4) will hand-write the 2 affected validation files instead. |
| 2026-05-23 | prisma_drizzle_schema_unification | Fresh-DB apply (AC #4) not independently re-verified post-review | Low | Open | docker-compose down -v && pnpm db:migrate not run during review (destroys local volume). migration-sql.test.ts passes statically. Both Track 3 and Track 4 plans include `pnpm --filter @reading-advantage/db migrate` in Phase 0 setup — exercised before controller work begins. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | Legacy matview SQL not folded into Drizzle migrations | Medium | Open | 8 Prisma migrations creating `mv_alignment_metrics`, `mv_srs_health*`, `mv_genre_engagement*`, `mv_assignment_funnel*`, `mv_velocity*`, `mv_activity_heatmap` preserved at `apps/reading-advantage/db-migrations/legacy-matviews/`. Already applied in prod/staging. Fresh DBs require manual psql. Fold into a Drizzle migration in a follow-up. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | lib/session.ts defaults emailVerified=true/onborda=false (cols missing in Drizzle users) | Low | Open | Legacy Prisma `users.emailVerified` and `users.onborda` columns are absent from the Drizzle schema. lib/session.ts hardcodes safe defaults with inline comment. Add columns to packages/db schema if these flags become product-critical. |
| 2026-05-23 | prisma_drizzle_reading_controllers_20260505 | reading-advantage jest verification deferred | Medium | Open | Targeted Jest (`--testPathPattern="__test__"`) 11 suites / 194 tests PASS (re-verified 2026-05-23 after reopened SQL fixes — includes new dashboard-summary-controller / assignment-prediction-service / query-optimizer SQL-rendering tests). Full app-wide `CI=true pnpm --filter reading-advantage test` hangs >10min on this hardware (pre-existing). Build (AC#4) PASSES on Next.js 16 Turbopack. Verify full Jest in CI or on faster hardware before merge. |
| 2026-05-25 | science-advantage-ui | `/assignments` page is a hardcoded stub | Low | Open | `app/(student)/assignments/page.tsx` returns static "No assignments available yet." with no data fetching. Dashboard `StudentAssignmentsCard` works correctly. Pre-existing, not a Track 3 regression. Wire the page to `GET /api/students/me/assignments` (same endpoint the card uses). Discovered during Track 3 manual QA Gate 5. |
| 2026-05-25 | codecamp_exercise_repos_20260515 | `CODECAMP_DATABASE_URL` Secret Manager entry points at `reading_advantage` DB, not `codecamp_advantage` | Low | Open | Discovered during PR review E2E. Secret value: `postgresql://...@localhost/reading_advantage?host=/cloudsql/...`. Live Cloud Run service must be overriding the DB name via env var (codecamp tables only exist in `codecamp_advantage`; queries against the secret's `reading_advantage` find nothing). Misleading for anyone reading secrets to understand the topology. Fix: update the secret's URL path segment to `/codecamp_advantage`, or document the Cloud Run env-var override explicitly in `apps/codecamp-advantage/docs/`. |
| 2026-05-26 | audit_20260526 | reading-advantage API routes bypass domain layer | Critical | Open | **209** `route.ts` files import `db` directly; **0** use `@reading-advantage/domain`, `assertCan`, or `TenantDB`. Violates monorepo architecture (Next.js → tRPC → domain → Drizzle). No centralized auth/tenancy enforcement; each route rolls its own. |
| 2026-05-26 | audit_20260526 | science-advantage API routes bypass domain layer | Critical | Open | **27** `route.ts` files import `db` directly; **0** use `@reading-advantage/domain`, `assertCan`, or `TenantDB`. Same architectural breach as reading-advantage: all auth/tenancy is hand-rolled per route. |
| 2026-05-26 | audit_20260526 | primary-advantage Prisma still fully active | Critical | Open | **15** files import `@prisma/client`; `prisma/schema.prisma`, `migrations/`, `lib/prisma.ts` still exist; `package.json` still depends on `@prisma/client`, `prisma`, `@prisma/adapter-pg`. Tracks.md incorrectly lists migration as complete. Blocks Track 4 (slice cleanup) and any unified backend work for this app. |
