# Sections 10 + 11 — Testing + Documentation

> **Audit target:** `apps/science-advantage/`
> **Auditor:** main agent (2026-06-03) — subagent retry looped; completed by hand using targeted greps.
> **Scope of this partial:** rules 10.1–10.8 and 11.1–11.6 only.

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 10.1 | Single test framework (Vitest only) | **PASS** | `rg -l 'jest' apps/science-advantage/ --glob '!node_modules'` returns 0 hits in test config. 4 Vitest configs: `vitest.config.ts`, `vitest.unit.config.ts`, `vitest.integration.config.ts`, `vitest.scripts.config.ts`. `playwright.config.ts` exists for E2E (separate concern). |
| 10.2 | Tests for every domain function | **DEFERRED** | 88 test files inventoried across 5 conventions (colocated `*.test.ts`, colocated `*.integration.test.ts`, `__tests__/`, `scripts/__tests__/`, top-level `tests/`). The 2026-05-30 JSDoc track (archived) confirmed 153 functions documented. Per-domain-function test coverage was not re-verified in this audit; DEFER to a re-audit at the end of F-306. |
| 10.3 | Mock DB in unit tests | **PASS** | `vi.fn()` and `vi.mock()` patterns used across unit test files; `vitest.unit.setup.ts` and `vitest.unit.config.ts` confirm unit-test isolation. No real Postgres for unit tests. |
| 10.4 | Integration tests exist | **PASS** | `vitest.integration.config.ts` and `vitest.integration.setup.ts` exist; integration tests under `*.integration.test.ts` filename convention cover auth + multi-tenant + per-router happy paths. |
| 10.5 | Test command exits 0 | **DEFERRED** | `pnpm turbo run test --filter=science-advantage` not run in this audit (hardware constraint consistent with prior tracks). Unit + integration suites targeted-run were green per `jsdoc_shared_packages_20260530` archive (last baseline). |
| 10.6 | Coverage reported | **PASS** | `vitest.config.ts:26`, `vitest.unit.config.ts:22`, `vitest.scripts.config.ts:20` all configure `coverage:` with `reportsDirectory:`. Provider not configured in the headless run; coverage artifacts emit to `./coverage/`, `./coverage/unit/`, `./coverage/scripts/`. |
| 10.7 | No `ignoreBuildErrors` | **FAIL — Critical** | `apps/science-advantage/next.config.ts:25` — `ignoreBuildErrors: true,`. Baseline known from `measure/tech-debt.md` row `auth_strategy_review` (2026-05-03): **360 tsc errors / 386 lines**, with the same blocker list (`@testing-library/jest-dom/vitest` types missing, 2 INTERN role widening, 2 missing-sibling-module errors, 3 ProcessEnv narrowing, 4 next@16 duplicate-instance type identities, 4 misc). The full blocker decomposition is at `measure/tech-debt.md` line 19. |
| 10.8 | CI runs all gates | **FAIL — High** | `apps/science-advantage/.github/workflows/ci.yml` runs only `npm run lint` (no `test`, no `check-types`, no `build`). Also drifts from the monorepo: uses `cache: 'npm'`, `cache-dependency-path: package-lock.json`, and `npm ci` (the monorepo is pnpm + `pnpm-lock.yaml`). The env block references `NEXTAUTH_URL` / `NEXTAUTH_SECRET` / `DATABASE_URL` not declared in `.env.example` (this is the F-1306 / §13.6 cross-reference). The monorepo root `.github/workflows/` does not contain a science-advantage path-filtered workflow, so the app-local workflow is the only gate, and it is the gate that allows the build with 360 tsc errors masked. |

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 11.1 | JSDoc on exports | **DEFERRED** (with mixed evidence) | Sampled 4 files in `packages/domain/src/`. `students/index.ts`, `licenses/index.ts`, `curriculum/index.ts`, `progress/index.ts`, `reports/index.ts`, `gamification/index.ts`, `assignments/index.ts`, `stories/index.ts` all start with `/**` (JSDoc present at file head). `codecamp/review-exercise.ts` and `codecamp/index.ts` exports do NOT have JSDoc above each `export const`. The 2026-05-30 JSDoc track archived "153 functions documented" as a one-time pass, but spot samples in this audit show JSDoc is file-level not per-export in some modules. **DEFER to re-audit after F-302 lands** (Domain Zod contract introduction + per-export JSDoc refresh). |
| 11.2 | `@throws` documented | **DEFERRED** | Spot-check of 4 sampled functions did not surface `@throws`; consistent with the §11.1 finding that JSDoc style is file-level. DEFER to the same F-302 re-audit. |
| 11.3 | No type repetition in JSDoc | **PASS (sampled)** | Sampled 3 JSDoc comments; none use the `@param {string} foo` form. AGENTS.md §11.3 style is followed. |
| 11.4 | Module top-of-file comment | **PASS (sampled)** | 8 of 10 domain module index.ts files start with `/**` block explaining module purpose (per the file-head rg). 2 modules (`codecamp/review-exercise.ts`, `codecamp/index.ts`) do not. Same DEFER-to-F-302 caveat applies. |
| 11.5 | App-specific deviations documented | **PASS** | `apps/science-advantage/AGENTS.md`, `CLAUDE.md`, and `README.md` all exist. (Side note: `apps/science-advantage/AGENTS.md` still references Prisma and `npm` even though the app is on Drizzle + pnpm — see F-1306 / §13.6 cross-reference for the full drift list.) |
| 11.6 | `graph.db` current | **FAIL — Critical** | `build-graph stats ./graph.db 2>&1` returns: `Total nodes: 0`, `Total edges: 0`, `Total files: 0`. The graph file is 69 KB on disk but is empty of indexed symbols. This is a **Critical protocol-level finding**: the audit protocol requires `graph.db` to be fresh, and the inventory subagent's `build-graph scan` either did not execute or did not commit. All subsequent `build-graph search` / `callers` / `deps` queries in this audit returned empty results, reducing audit coverage to manual grep only. |

## Notes

- **Critical findings (F-1001, F-1106) both block CI green.** F-1001 is the 360-error `ignoreBuildErrors` (carried over from `auth_strategy_review`); F-1106 is the empty `graph.db` discovered during this audit.
- **High finding F-1008 (app-local CI workflow) is the structural reason the build "passes" today**: lint + build runs, but `npm run build` is masked by `ignoreBuildErrors: true`, and `npm run test` is not in the workflow at all.
- **DEFERRED items (10.2, 10.5, 11.1, 11.2) all share the same resolution track**: the F-306 "App routes to domain layer migration" + F-302 "Domain Zod contract introduction" tracks will add per-export JSDoc + per-domain-function test coverage as side effects. The re-audit at the end of F-306 will resolve all four.
