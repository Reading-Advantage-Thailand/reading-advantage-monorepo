Task: Phase 0 / Pilot — migrate app/api/lessons/[lessonSlug]/route.ts off Prisma

Pilot scope per plan §0.2 worked example: one route file, behavior-preserving
swap, characterization test stays green.

Migration mapping:
  prisma.lesson.findUnique({ where: { id }, include: { standards, curriculumUnits } })
    →
  3 explicit Drizzle queries (lesson, standards via junction, classes via two joins)
  + 1 narrower enrollment query (only when needed for access check)

This pilot uncovered two real issues that informed the broader plan:
  1. 4 M:N junction tables missing from Track 1 (fixed in f818827; new migration 0015).
  2. Integration-test runtime infra is incompatible with Drizzle-first DBs:
     vitest.setup.ts runs `prisma db push --force-reset` against an unprovisioned
     5433/science_advantage_test DB; pointing it at the 5432/reading_advantage DB
     where Track 1's 0013 migration has already run fails with
     `ERROR: type "StandardsAlignment" already exists` (Prisma's CREATE TYPE has no
     IF NOT EXISTS path here).

Static verification:
  - eslint route.ts route.integration.test.ts: clean
  - tsc (pilot-scoped): 0 errors from this change
    (2 pre-existing errors in lib/auth/session.ts are unrelated INTERN/UserRole drift)
  - DB has all 15 science_* tables incl. the 4 new junctions

Runtime verification deferred to whichever phase resolves the test-infra blocker
(candidate: Phase 6 Seeds/Scripts when the prisma push is replaced with drizzle migrate).

— PAUSE HERE per plan instruction: "confirm the loop with the reviewer before continuing."
