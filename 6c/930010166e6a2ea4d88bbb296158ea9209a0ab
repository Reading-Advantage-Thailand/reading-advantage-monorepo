Task: Phase 3i compiler-identity cache proof.\n\nChanges: add a checked compiler identity manifest as an explicit scoped Turbo input for native, compatibility, and rollback check types; mutate it only in the disposable proof worktree.\n\nVerification: root cache-routing and cache-proof contracts passed; Python bytecode compilation passed.\n\nWhy: changing package dependencies made pnpm auto-install before the task command, preventing an actual Turbo cache proof. The explicit identity input is deterministic and testable without changing the frozen installation.
