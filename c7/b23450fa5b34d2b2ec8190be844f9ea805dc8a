Red: 5 new regressions failed: structural forgery, hostile field-error copying, primary proxy logging, resolution proxy logging, and storage proxy logging. Green: focused route and storage suites passed 50 tests; the full focused Accounting Finance group passed 63 tests. Checks: Accounting type check, changed-file ESLint, Prettier, git diff --check, staged diff check, and build-graph update passed. Behavior: only real AccountingSubmissionError instances receive domain handling; invalid-input translation preserves the real instance and copies sanitized field errors; reconciliation labels use fixed values without inspecting thrown values; keyed retries preserve primary errors; and a committed replay with the same evidence reference performs zero deletion.
