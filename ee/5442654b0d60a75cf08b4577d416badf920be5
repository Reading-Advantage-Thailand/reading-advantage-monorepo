Track durable_job_worker_platform_20260713: reordered PostgreSQL enqueue handling so active rows retain the current lease while storing complete follow-up snapshots; retry and redelivery pending rows now promote as new generations; fresh pending conflicting payloads remain conflicts. PG16, jobs, type, build, lint, format, diff, and graph checks were run. Complete enqueue evidence records one committed Red-test queue mismatch.
