Track: prisma_drizzle_science_controllers_20260505
Task: app/api/auth/session/route.integration.test.ts → DELETED (catch-all drift)
Tests: N/A (deleted)
Notes: Test was pre-existing 100% broken — imported Prisma against a
Drizzle-managed test DB and called GET() with no NextRequest against
the new package-delegating route wrapper whose handler lives in
@reading-advantage/api/routes/auth. Session behavior already covered by
packages/api/src/__tests__/auth-routes.test.ts (handleSession) and by
lib/auth/session.integration.test.ts (validateSession). Behavior-preserving
(deletion of stale test).
