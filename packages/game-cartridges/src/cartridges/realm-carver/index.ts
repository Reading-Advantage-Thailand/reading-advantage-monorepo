import { arenaWaveBlueprints } from "../../arena-wave-blueprints";
import { createArenaCartridge } from "../../families/arena/cartridge";
/** Realm Carver ordered territory sentence cartridge. */
export const realmCarverCartridge = createArenaCartridge(arenaWaveBlueprints[4], "Realm Carver", "Capture sentence beacons in order to reclaim the realm.");
