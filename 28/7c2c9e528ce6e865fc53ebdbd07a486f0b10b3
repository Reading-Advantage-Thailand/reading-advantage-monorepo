Task: Phase 3g aggregate graph compatibility.\n\nChanges: extend TS6 deprecation compatibility only to the four tsup declaration-bundling tsconfigs and retain Turbo's run summary even when a matched pre-existing check-types failure gives Turbo a nonzero exit.\n\nVerification: 76 focused Phase 3 contracts passed; all four tsup declaration builds passed.\n\nWhy: the full graph revealed tsup's internally synthesized baseUrl deprecation in each declaration-bundling package. Comparable nonzero baseline exits still need their freshly emitted cache summary and diagnostic evidence.
