/** Returns the only title identifier admitted by this hidden Dragon Rider surface. */
export function getDragonRiderHostProofSelection(): Readonly<{ id: "dragon-rider"; inputMode: "vocabulary" }> { return Object.freeze({ id: "dragon-rider", inputMode: "vocabulary" }); }
