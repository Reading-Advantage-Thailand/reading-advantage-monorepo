/** Minimal Vinext layout proving the shared activity packages require no Next runtime adapter. */
export default function Layout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
