/**
 * Wave 4 Phase 1 test-harness mock for `server-only`.
 *
 * `server-only` is a Next.js marker that throws when imported on the client.
 * Unit tests run in Node/jsdom, so we replace it with a no-op.
 */
export {};
