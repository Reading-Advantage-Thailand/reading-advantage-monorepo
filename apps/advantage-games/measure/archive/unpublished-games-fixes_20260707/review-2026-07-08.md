# Track Review: unpublished-games-fixes_20260707

**Date:** 2026-07-08  
**Reviewed commits:** 17 commits in the last 24 hours, all tagged with this track.

## 1. Summary of changes

The track delivered fixes for every issue surfaced in the 2026-07-07 unpublished-games review (`apps/advantage-games/UNPUBLISHED-GAMES-REPORT.md`):

- **4 game-breaking bugs fixed and live-verified:**
  - `griffin-riders-escape`: main loop now re-arms `requestAnimationFrame`; mount measurement re-run fixed so the Konva stage attaches before metrics run.
  - `haunted-library`: tick moved outside the `setGameState` updater, eliminating the exponential rAF storm caused by StrictMode double-invocation.
  - `realm-carver`: page now reads `sentence.term` instead of the non-existent `sentence.text`.
  - `alchemists-synthesis` (and `wizard-vs-zombie`): game frame height changed from `min(85svh, 100%)` to `"85svh"`, making the start button clickable.
- **2 additional gameplay defects fixed:** abyssal-well and storm-castle-tower had spawn state mutated inside updaters; tick/bookkeeping hoisted out.
- **i18n:** `en.ts` entries added for alchemists-synthesis, archers-revenge, rune-forge-chamber; hardcoded Thai back buttons replaced with `t("backToGames")` in 8 pages.
- **Layout/cosmetic:** gryphon-patrol world centered for 390×844; spellweavers-run orbs clipped to play area; word labels widened with `wrap="none"` in several sentence games.
- **Infrastructure:** `create_room` WebSocket ack now includes `roomCode`; stale route/UI tests updated; ESLint jest/node globals added for `*.test.*` files, removing 1,858 config warnings.
- **Verification evidence:** the report documents per-game browser verification (native start click, FPS measurement, live state probe), `tsc --noEmit` clean, and full jest suite green except for the timing-sensitive `performance-benchmark.test.ts`.

## 2. Spec alignment

**Status: Met** (given track scope).

The track has no dedicated `spec.md`; the de-facto spec is the findings list in `UNPUBLISHED-GAMES-REPORT.md`. Every listed blocker and systemic item has a corresponding committed fix and verification note. `plan.md` marks all tasks `[x]`, and the report's "Fix log" section asserts all issues are addressed.

## 3. Code quality observations

- Fixes are narrow and game-specific rather than broad refactors, which is appropriate for a bug-fix track.
- The impure-updater anti-pattern was identified and corrected consistently across three games (haunted-library, abyssal-well, storm-castle-tower); the review comments correctly explain the StrictMode hazard.
- Test maintenance kept pace with code changes: 6+ stale test files were updated to match current contracts.
- Minor concern: the word-label fix uses `wrap="none"` with wider labels; the report acknowledges edge-clipping risk on boundary-hugging entities.
- The ESLint global additions are configuration-only and do not change runtime semantics; they correctly silence false-positive `no-undef` warnings.

## 4. Risks or blockers

- `performance-benchmark.test.ts` is noted as flaky under parallel load. It passes in isolation, but this could mask real regressions in CI.
- Several "playable" games still have raw i18n keys or hardcoded English headers (e.g., paladins-twin-soul); these were not blockers per the report but remain UX inconsistencies.
- The `babel-architect` card was set to `coming-soon` in a separate commit; the game itself is still unimplemented.
- `storm-castle-tower` had a difficulty mismatch (`medium` vs `normal`) that was resolved by aligning the option to the engine's vocabulary. This is correct but worth confirming with design that "normal" is the intended label.

## 5. Recommended next actions

1. **Archive this track** — it is marked complete in `tracks.md` and all plan tasks are checked.
2. Quarantine or harden `performance-benchmark.test.ts` so it is reliable under parallel CI runs.
3. Open a low-priority polish track for remaining i18n/header inconsistencies in playable unpublished games.
4. Ensure `babel-architect` implementation is tracked separately if it is intended to launch.
