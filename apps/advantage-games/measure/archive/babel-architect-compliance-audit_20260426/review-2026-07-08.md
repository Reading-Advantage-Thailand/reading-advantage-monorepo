# Track Review: babel-architect-compliance-audit_20260426

**Date:** 2026-07-08  
**Reviewed commits:** `e9f71eab` — the only commit in the last 24 hours tagged with this track.

## 1. Summary of changes

The recent commit is a small follow-up to the April 2026 audit:

- `src/lib/gameCards.ts`: changed `babel-architect` card from `status: 'playable'` with an `href` to `status: 'coming-soon'` with no `href`, removing the guaranteed 404 identified in the unpublished-games review.
- `src/lib/games/babelArchitectCompliance.test.ts`: removed the 8 architecture/game-system compliance assertions (they were all failing because no source files exist) and kept only the 2 passing checks (registry entry and cover image existence).

No source files for the actual game were added.

## 2. Spec alignment

**Status: Partial / Missed**.

The audit `spec.md` acceptance criteria require:

1. All 25 compliance items audited and marked pass/fail — **met** in `report.md`.
2. Any failing items have associated fix tasks created — **partially met**; the report documents failures but no fix tasks were created inside this track.
3. Fix tasks completed and re-verified — **missed**; the plan explicitly states "full game implementation is out of scope for audit track" and no fix code was written.
4. Final compliance report written — **met**.
5. Coverage remains ≥ 80% after any fixes — **N/A** because no fixes were applied.

In other words, the audit completed, but the game remains non-compliant (23/25 specs fail) and the track did not produce the implementation fixes its own acceptance criteria require. The recent commit only addresses the menu-card 404 surface symptom.

## 3. Code quality observations

- The compliance test reduction is honest — it no longer asserts things that cannot be true for a non-existent game — but it also lowers the visible signal that the game is missing.
- The `coming-soon` status change is the correct minimal fix for the 404 risk.
- Audit documentation (`report.md`, `plan.md`) is thorough and well-structured.

## 4. Risks or blockers

- **Platform consistency risk:** `babel-architect` is listed as an upcoming game but has zero implementation. If it is still planned, it is a large unscheduled body of work (page, component, logic, API, assets, tests).
- **Coverage/test signal risk:** trimming the compliance test to only 2 passing assertions removes the automated reminder that the game is non-compliant. Future readers may mistake the green test for readiness.
- **Spec status inconsistency:** `metadata.json` and `plan.md` mark the track `completed`, while `tracks.md` still lists it as `[ ]` / "New". This mismatch should be resolved during the sweep step.

## 5. Recommended next actions

1. **Close/archive the audit track** if the scope was strictly audit-only, but update `tracks.md` and `metadata.json` to reflect completion and move the directory to `measure/archive/`.
2. Create a separate **implementation track** for `babel-architect` if the game is still planned, with clear scope and TDD plan.
3. If the game is *not* planned, remove or consolidate the `coming-soon` card so it does not accumulate stale expectation.
