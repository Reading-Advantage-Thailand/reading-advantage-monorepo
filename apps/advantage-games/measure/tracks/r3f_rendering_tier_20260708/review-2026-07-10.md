# Track Review: r3f_rendering_tier_20260708

**Date reviewed:** 2026-07-10  
**Track directory:** `apps/advantage-games/measure/tracks/r3f_rendering_tier_20260708`  
**Status per metadata:** `in_progress`  
**Commits reviewed (last 24 h):** `d2426bbb` — only added the 2026-07-09 review report; no source commits.

---

## 1. Summary of changes

Since the 2026-07-09 review, all work on this track has remained **uncommitted**:

- **Metadata/plan/spec reconciliation:** `metadata.json` status flipped from `new` to `in_progress`, estimated tasks increased from 28 to 34, and Story S6 (KayKit dungeon-fantasy asset pass) was added. `plan.md` now marks S3/S4 implementation tasks complete and adds Phase S6. `spec.md` now includes Story S6 acceptance criteria. `index.md` links to the new candidates and asset-shopping-list docs.
- **S5 gameplay redesign (uncommitted source):** The Abyssal Well was rethemed from the old breach-damage/highlighted-target rules to the new cycling-word design:
  - `abyssalWell.ts` / `abyssalWellConfig.ts`: all words spawn at random angles and staggered depths, breaches wrap harmlessly to the deep end with per-lap speedup, wrong hits cost a life, correct hits collect in order, and rotation is continuous via `setRotation`.
  - `wellProjection.ts` / tests: angle-based positioning API.
  - `AbyssalWellScene.tsx` / tests: single enemy color (no target highlight), angle-based positions.
  - `AbyssalWellGame.tsx` / tests: hold-to-rotate via `keydown`/`keyup` and touch start/end, built-sentence HUD, rewritten start-screen instructions, and a Quit button.
- **Skeleton theme seeding:** Default creature changed to `skeleton-warrior`; start-screen labels now reference Skeleton Scout / Warrior / Mage in anticipation of S6.
- **Candidates/workflow docs (uncommitted):** `r3f-game-candidates.md` and `kaykit-asset-shopping-list.md` are present but untracked; `apps/advantage-games/measure/tracks.md` was updated to seed three follow-up R3F tracks (Griffin Rider's Escape R3F Rebuild, Storm the Castle Tower R3F Rebuild, Crystal Cavern Rail Runner). `apps/advantage-games/measure/lessons-learned.md` received the R3F track lessons.

---

## 2. Spec alignment

**Status: partial / substantially implemented.**

| Story | Spec acceptance criteria | Evidence | Status |
|---|---|---|---|
| S1 | R3F deps installed, build passes | `package.json`, `pnpm-lock.yaml` | Met |
| S1 | Tech-stack documents two-tier policy | `measure/tech-stack.md` | Met |
| S1 | R3F smoke test passes without GPU | `SmokeScene.test.tsx` | Met |
| S2 | R3F version replaces Konva at same route | `AbyssalWellGame.tsx`, `AbyssalWellScene.tsx` | Met |
| S2 | Logic module retyped to `SentenceItem` | `abyssalWell.ts`, `abyssalWellConfig.ts` | Met |
| S2 | 25-spec compliance, >80% coverage | `compliance-report.md` (25/25), ~94.7% stmts | Met |
| S2 | Mobile perf budget (DPR cap, low-poly) | `dpr={[1,2]}`, `meshBasicMaterial` | Met |
| S5 | Cycling words, no target highlight, breach wraps, mistake lives, continuous rotation | `abyssalWell.ts`, config, scene, game component | Met |
| S5 | Browser smoke test of new gameplay | HTTP route 200 only; hydrated gameplay **not** verified | **Partial** |
| S3 | Stack-selection gate in skill + lessons learned | `SKILL.md`, `lessons-learned.md` | Met |
| S4 | Candidates doc + seeded tracks | `r3f-game-candidates.md`, `tracks.md` | Met |
| S6 | KayKit retheme with shared loader | Shopping list only; no assets landed | **Blocked** |

Automated quality gates (`pnpm lint`, `pnpm check-types`, `CI=true pnpm test`) are reported passing with 190 suites / 1,824 tests and 94.71% scoped coverage.

---

## 3. Code quality observations

- The pure-logic + thin-render-layer boundary is preserved; R3F only presents state and forwards input.
- The S5 redesign directly addresses the user-verification finding that the previous rules leaked the answer.
- Hold-to-rotate implementation correctly pairs `keydown`/`keyup` and `touchstart`/`touchend`, avoiding lane-snapping.
- `AbyssalWellGame.tsx` now pre-binds `createInitialState` and centralizes start/quit handlers, reducing closure staleness.
- The new S6 scope is cleanly isolated in `plan.md`/`spec.md` and does not bleed into the S2/S5 code path.
- Bundle isolation remains intact: three.js loads only on the abyssal-well route via `next/dynamic`.

---

## 4. Risks or blockers

- **S5 hydrated browser smoke test incomplete:** The HTTP route returns 200, but stable hydrated gameplay automation was not achieved. This is the highest-priority verification gap before S5 can be called done.
- **Manual verification backlog:** S2, S3, S4, and S5 all have open "Measure - User Manual Verification" tasks per `plan.md`.
- **S6 blocked on asset procurement:** KayKit Dungeon Remastered + Skeletons + Adventurers + RPG Tools packs must land in `public/assets/3d/kaykit/` before implementation can start.
- **Uncommitted source/docs:** All S3–S5 implementation changes, the candidates doc, the asset-shopping list, and the `tracks.md`/`lessons-learned.md` updates are uncommitted. This risks loss of the seed-track links and the workflow-gate documentation.
- **Mid-sprint scope expansion:** S5 and S6 were added during the sprint. Ensure the sprint goal remains achievable or explicitly re-scope S6 to a follow-up track.

---

## 5. Recommended next actions

1. **Complete the S5 hydrated browser smoke test** once local browser automation is available, then mark the S5 browser-smoke task and manual-verification tasks done.
2. **Procure and commit KayKit assets** to `public/assets/3d/kaykit/` so S6 can proceed, or explicitly defer S6 to a separate follow-up track.
3. **Commit the uncommitted S3–S5 source and docs** (including `r3f-game-candidates.md`, `kaykit-asset-shopping-list.md`, `tracks.md`, `lessons-learned.md`, skill updates, and game source) after verification.
4. Keep the track `in_progress` until manual verification and the S5 browser smoke test are complete; do not mark it complete based on automated tests alone.
