# Track Review: babel_architect_phaser_exemplar_20260708

**Date reviewed:** 2026-07-09  
**Track directory:** `apps/advantage-games/measure/tracks/babel-architect-phaser-exemplar_20260708`  
**Status per metadata:** `in_progress`  
**Commits reviewed (last 24 h):** 13 commits authored 2026-07-08, all tagged with this track.

---

## 1. Summary of changes

The track went from a fresh plan to a fully implemented Phaser-based sentence game in a single day:

- **Track setup:** activated the track, linked it to the archived compliance audit, and documented Phaser as the 2D engine choice in `measure/tech-stack.md`.
- **Stack:** added `phaser` dependency via the workspace catalog pattern.
- **Game logic:** implemented `src/lib/games/babelArchitect.ts` with red/green tests covering sentence splitting, correct/incorrect placement, stability/errors, difficulty presets, and completion summary.
- **API routes:** added `/api/v1/games/babel-architect/sentences` and `/complete` using the existing route factories.
- **React/Next shell:** added `page.tsx` under `/[locale]/student/games/sentence/babel-architect` and `BabelArchitectGame.tsx`, loading Phaser client-side via `next/dynamic` with `ssr: false`.
- **Phaser adapter/scene:** added `babelArchitectAdapter.ts`, `babelArchitectScene.ts`, mount/destroy/event-bridge tests, touch and keyboard input, and a 390×844 portrait fit.
- **Assets:** created an in-repo placeholder asset manifest (`assetManifest.ts`) and a `.gitkeep` for the public asset directory, leaving replacement paths for an approved pixel-art pack.
- **Catalog & closeout:** promoted the `babel-architect` card from `coming-soon` to `playable` with a real href, added a compliance test, and recorded lessons in `measure/lessons-learned.md` and `measure/tech-debt.md`.

---

## 2. Spec alignment

**Status: Implemented, but not fully verified.**

| Spec requirement | Evidence | Status |
|---|---|---|
| Real route at `/[locale]/student/games/sentence/babel-architect` | `page.tsx` added and routed | Met |
| Catalog promoted to `playable` | `src/lib/gameCards.ts` updated | Met |
| Phaser documented in tech-stack/closeout notes | `measure/tech-stack.md`, `lessons-learned.md`, `tech-debt.md` | Met |
| Pure logic module for sentence ordering, scoring, stability, difficulty, completion | `src/lib/games/babelArchitect.ts` + tests | Met |
| Phaser scene renders tower/blocks, handles input, bridges typed events | `babelArchitectScene.ts`, `babelArchitectAdapter.ts` | Met |
| API routes use shared factories | `sentences/route.ts`, `complete/route.ts` using `createSentencesRoute` / `createCompleteRoute` | Met |
| 390×844 portrait viewport | Scene config and CSS reference the reference viewport | Met |
| Placeholder art + stable replacement manifest | `assetManifest.ts`, `.gitkeep` in public asset dir | Met |
| Automated tests pass | Jest tests added for logic, page, route, adapter, compliance | Met |
| Manual browser verification (game start, correct/incorrect, completion, XP, navigation) | **Not documented as completed** | Open |

The implementation plan marks every automated task `[x]`, but the **manual verification** sub-tasks (verify portrait gameplay, correct/incorrect placement, completion/XP/back navigation) remain unchecked. The track metadata explicitly notes it was "reopened … because the registry claimed completion before browser/e2e verification and playability regressions were fixed."

---

## 3. Code quality observations

- Architecture respects the spec boundary: learning rules and scoring live in the pure TypeScript module; Phaser only renders and emits intents.
- `next/dynamic` with `ssr: false` is used correctly, isolating the Phaser bundle from 2D routes.
- Test coverage is broad: logic, page route, API routes, adapter lifecycle, and a compliance test.
- Regressions were addressed explicitly (wrap/selectable block layout, removed already-placed blocks from click targets, canonical completion payload, Exit control, tower/feedback spacing).
- Asset manifest is placeholder-only; no licensed art was committed.
- JSDoc coverage on exported logic functions/types appears consistent with the plan, though spot-checking every export was not done.

---

## 4. Risks or blockers

- **Manual verification gap:** The game is marked complete in commit history but the plan and metadata agree manual browser/e2e verification is still open. This is the single most important remaining item.
- **No asset files yet:** The placeholder manifest is sound, but the first playable build currently has no real block/background/particle art committed. This is acceptable per spec ("do not depend on licensed art"), but the visual baseline is unverified.
- **Coverage number not cited in track docs:** While tests exist, no explicit coverage percentage is recorded for the new modules. The spec asks for ≥80% on new logic and shell code.
- **Completion-state mismatch:** The commit `chore(measure): mark babel architect track complete` conflicts with `metadata.json` status `in_progress` and the open manual-verification tasks. This should be reconciled before the track is considered closed.

---

## 5. Recommended next actions

1. **Complete manual browser verification** and document the results in this track directory or in `plan.md`.
2. **Capture Jest coverage** for `babelArchitect.ts`, the page, adapter, and scene files; ensure ≥80% before final closeout.
3. **Reconcile status:** either finish verification and update `metadata.json` to `complete`, or keep the track `in_progress` until verification is done.
4. Once verified, consider archiving this track and copying closeout notes to `measure/lessons-learned.md` permanently.
