# Track Review: babel-architect-phaser-exemplar_20260708

**Date reviewed:** 2026-07-10  
**Track directory:** `apps/advantage-games/measure/tracks/babel-architect-phaser-exemplar_20260708`  
**Status per metadata:** `in_progress`  
**Commits reviewed (last 24 h):** `d2426bbb` — only added the 2026-07-09 review report; no source commits.

---

## 1. Summary of changes

Since the 2026-07-09 review, all work on this track has remained **uncommitted**:

- **Metadata/plan reconciliation:** `metadata.json` status flipped from `planned` to `in_progress` and `plan.md` was updated to record the regression-repair work and mark the browser/e2e smoke task complete.
- **Responsive shell repairs:** `BabelArchitectGame.tsx` now uses a fluid viewport height (`h-[calc(100dvh-8rem)]`) with min/max bounds instead of a fixed 390×844 aspect ratio, so the game fits tablets and larger phones.
- **Active block layout:** new `babelArchitectLayout.ts` (+ tests) computes wrapped, touch-friendly layouts for remaining selectable blocks, excluding already-placed words; integrated as DOM overlay buttons in `BabelArchitectGame.tsx`.
- **Keyboard target fix:** number-key input now indexes only the remaining blocks (`prev.blocks.slice(prev.progressIndex)`), so players cannot accidentally re-select placed words.
- **Exit affordance:** added an explicit Exit button and `handleExit`/`handleRestart` cleanup that resets results and the game-state ref.
- **Countdown pressure:** HUD now shows remaining seconds derived from `BABEL_ARCHITECT_DIFFICULTY[difficulty].timeLimitMs - elapsedMs`.
- **Completion payload:** `BabelArchitectGameResult` now carries the canonical `BabelArchitectCompletionInput` (`completion`) so the page can submit a server-authoritative payload.
- **E2E coverage:** new untracked Playwright spec `apps/advantage-games/tests/e2e/games/sentence/babel-architect.spec.ts` mocks sentences/completion and asserts start → Phaser canvas → victory → canonical payload.
- **Scene simplification:** `babelArchitectScene.ts` offloads interactive block positioning to the DOM overlay and focuses on tower/blocks/stability rendering.

---

## 2. Spec alignment

**Status: partial / complete-but-unverified.**

| Spec requirement | Evidence | Status |
|---|---|---|
| Real route at `/[locale]/student/games/sentence/babel-architect` | `page.tsx` present and routed | Met |
| Catalog promoted to `playable` | `src/lib/gameCards.ts` | Met |
| Phaser documented in tech-stack/closeout notes | `measure/tech-stack.md`, `lessons-learned.md` | Met |
| Pure logic module for sentence ordering, scoring, stability, difficulty, completion | `src/lib/games/babelArchitect.ts` + tests | Met |
| Phaser scene renders tower/blocks, handles input, bridges typed events | `babelArchitectScene.ts`, `babelArchitectAdapter.ts` | Met |
| API routes use shared factories | `sentences/route.ts`, `complete/route.ts` | Met |
| 390×844 portrait reference preserved | `babelArchitectLayout.ts` viewport constants + CSS bounds | Met |
| Placeholder art + stable replacement manifest | `assetManifest.ts`, code-generated palette | Met |
| Automated tests pass | Jest + new Playwright spec | Met |
| Manual browser verification (game start, correct/incorrect, completion, XP, navigation) | Still not documented | **Missed** |

The implementation is functionally complete, but the track cannot close until manual verification is documented. The `metadata.json` correctly keeps status as `in_progress`.

---

## 3. Code quality observations

- The DOM-overlay block buttons keep accessibility and testability high while the Phaser scene stays thin.
- `babelArchitectLayout.ts` is deterministic, pure, and covered by its own unit test.
- `next/dynamic` with `ssr: false` continues to isolate Phaser from 2D routes.
- Restart/exit lifecycle now correctly clears refs and results, preventing stale state.
- The new E2E spec follows the established pattern of mocking API routes and asserting the canonical completion payload shape.
- No licensed art is introduced; placeholder palette remains code-only.

---

## 4. Risks or blockers

- **Manual verification still open:** This is the only remaining acceptance criterion. The E2E spec is a strong proxy, but it does not replace a real-browser check of incorrect placements, defeat path, and XP/leaderboard integration.
- **Uncommitted source changes:** All of the repair work above is uncommitted. If it sits much longer, the repo state will drift from the documented track status and the next developer may re-fix the same regressions.
- **Completion payload contract:** The page now receives the full `completion` object; confirm the `complete` route consumes exactly this shape and does not double-compute XP.
- **Responsive CSS vs. fixed viewport:** The move from fixed aspect ratio to `dvh` improves real-device fit but should be checked on the 390×844 reference device to ensure the Phaser canvas and overlay buttons still align.

---

## 5. Recommended next actions

1. **Run the new Playwright spec** (`apps/advantage-games/tests/e2e/games/sentence/babel-architect.spec.ts`) and document results.
2. **Complete manual browser verification** on a 390×844 portrait viewport for start, correct/incorrect placement, victory, defeat, Exit, and back navigation.
3. **Commit the source changes** (game component, layout module, scene, tests, tracks.md/lessons-learned updates) once verified, then flip `metadata.json` to `complete`.
4. After closeout, move this track to `measure/archive/` and update `apps/advantage-games/measure/tracks.md` accordingly.
