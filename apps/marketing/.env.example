# Marketing Production Platform — required environment variables

# 32-byte AES-256-GCM key as a 64-character hex string.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
