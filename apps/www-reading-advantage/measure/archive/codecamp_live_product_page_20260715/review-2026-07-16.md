# Track Review: `codecamp_live_product_page_20260715`

## 1. Summary of changes

Five commits over the last 24 hours belong to this track (plus one closeout commit for the predecessor `codecamp_production_release` track that fed it):

- **`5d79aac3`** — Created the track (spec, plan, metadata, tracks.md registration).
- **`a175f3ba`** — The main implementation: rewrote `page.tsx` (697 lines changed) as a production-aligned marketing page, rewrote the EN/TH/ZH locale module (857 lines), updated the `/products` B2C/B2B solution cards, replaced the page test with contract tests, and added two dated dashboard/lesson screenshots.
- **`bd30da22`** — One-line root `.gcloudignore` fix: the www app was excluded from the Cloud Build context, which had caused two pre-Docker build failures.
- **`e8c3643e`** — "Release contract" completion: added the missing `tech-stack` section (page + locale copy + test assertion) and fixed a `LocalizedLink` mock ref-forwarding issue in test setup.
- **`177b65d9`** — Closeout: deployment evidence doc, three production browser screenshots, plan/metadata finalization, archive move.

Commit hygiene follows the Measure convention (track_id in messages, atomic implementation vs. chore commits, plan updated before/after each task).

## 2. Spec alignment

| Requirement | Status | Evidence |
|---|---|---|
| Live-program hero + primary CTA to production app | ✅ Met | `codecamp-live-cta` → `https://codecamp.reading-advantage.com/{en\|th}`, pulsing "live" badge; asserted in tests |
| Secondary **inquiry** CTA | ⚠️ Partial | The hero's secondary CTA is "Explore the curriculum" (`#curriculum` anchor, target verified). There is no inquiry/contact CTA anywhere on the page — the bottom CTA also links to the live app |
| 20 modules / 106 lessons / honest existing-cohort note | ✅ Met | Tests assert `modules.value === "20"`, `lessons.value === "106"`, and "Existing cohorts keep their assigned sequence"; `PHASES` sums to 20 (6+4+3+7) |
| Measure module + APK unit in Phase D | ✅ Met | `measure-module` and `apk-unit` testids render; Phase D asserted to have 7 modules |
| Mastery evidence vs. tutoring vs. advisory review distinction | ✅ Met | Separate `mastery-evidence`, `targeted-tutor`, `advisory-pr-review` sections |
| No shadow-mode approval/blocking claims | ✅ Met | Tests assert `advisory` and `does not approve` copy, and ban "automated pass/fail" in EN and TH |
| Accurate tech stack (no MERN/Django tracks) | ✅ Met | `tech-stack` section (added in `e8c3643e`); grep confirms zero MERN/Django/cross-curricular/Coming-2027/15-week matches in codecamp, b2c, or b2b copy |
| EN/TH/ZH locale parity | ✅ Met | All three exports present; the denominator test iterates every locale export |
| No banned claims remain | ✅ Met | Verified by grep and by test regexes (EN + TH + ZH banned strings) |
| Tests, lint, type-check, build, deploy, browser verification | ✅ Met | Focused tests re-run just now: **4/4 pass**. Evidence doc records 1,508 tests, lint/type/i18n/build green, Cloud Build `e0798e5c` SUCCESS, revision `00201-dvj` at 100% traffic, EN/TH/ZH routes returning 200, desktop + 390×844 mobile browser checks |

## 3. Code quality observations

- **Strong contract testing.** The test file directly encodes the acceptance criteria (banned-claim regexes in three languages, denominators across all locales, cross-page consistency including the B2B/B2C cards). This is the best part of the change — the spec is now regression-guarded, not just satisfied once.
- **Clean server-component page**: async params, `getScopedI18n`, all prose in the locale module, testids for every contract section, JSDoc on exports.
- **Sensible locale fallback**: the CTA maps `zh → /en` for the app link since the Codecamp app only ships EN/TH, while the www page itself keeps full ZH copy.
- **Minor duplication**: phase module counts live both in the `PHASES` const in `page.tsx` and in locale copy; only the locale side is test-guarded, so they could drift.
- **Test-coverage nit**: the banned-claim regex covers Laravel/Django/Rails/FastAPI but not "MERN" itself (an explicit acceptance-criterion string). Currently clean by grep, but unguarded against reintroduction.
- Date-stamped screenshot filenames (`dashboard-20260715.png`) will need manual refresh as the product UI evolves — acceptable for marketing, just a known maintenance cost.

## 4. Risks or blockers

- **No blockers.** The track is deployed, verified, and archived.
- The `.gcloudignore` fix (`bd30da22`) widens the Cloud Build upload context at the repo root for *all* builds, not just www — harmless today, but worth remembering that each newly deployed app needs the same un-ignore entry (this is the second time this class of failure occurred; consistent with the known codecamp deploy-blocker pattern).
- The missing inquiry CTA is a content gap, not a technical risk — but the spec's functional requirements say it should exist, and no test guards it, so it was silently dropped rather than descoped. Per your Measure "no deferring blockers" convention, this should have been recorded as a phase or an explicit spec delta.

## 5. Recommended next actions

1. **Resolve the inquiry-CTA gap**: either add a secondary contact/inquiry CTA (hero or bottom band) with a test assertion, or amend the archived spec with a documented delta explaining why "Explore the curriculum" replaced it.
2. **Add `MERN` (and optionally `MongoDB|Express`) to the banned-claim regex** in `page.test.tsx:82` so the acceptance criterion is fully regression-guarded.
3. Optionally derive the hero stats and `PHASES` counts from a single source (or assert `PHASES` sums to the locale `modules.value`) to prevent denominator drift.
4. Consider a short lessons-learned entry on the root `.gcloudignore` allowlist requirement for newly deployed apps, since it has now bitten two deployments.
