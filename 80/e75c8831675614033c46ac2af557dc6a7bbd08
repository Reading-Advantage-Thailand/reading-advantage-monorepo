Task: Phase 3g full-graph benchmark compatibility.\n\nChanges: bind each Turbo sample to the unique .turbo/runs summary path emitted by that invocation; add a Phase 3 contract proving fresh-path provenance; declare ignoreDeprecations 6.0 only in packages/utils so its TS6-bound tsup declaration bundler can tolerate its internally synthesized deprecated baseUrl option.\n\nVerification: 75 focused Phase 3 contracts passed; packages/utils build, TS6 compatibility check, and TS7 native check passed.\n\nWhy: the first full-graph measurement correctly exposed a pre-existing tsup/TS6 deprecation incompatibility and a runner assumption about Turbo's summary location. Both are fixed before collecting final benchmark evidence.
