Red: focused Accounting tests failed with 3 expected failures for conflict key retention, legacy deletion, and double-failure logging. Green: focused Accounting tests passed with 3 files and 54 tests. Checks: Accounting type check, five-file ESLint, Prettier, diff check, and build-graph update passed. Behavior: reads generated and accepted legacy evidence references, rejects legacy deletion, preserves the original upload error, logs only safe reconciliation fields, and keeps the idempotency key after 409.
